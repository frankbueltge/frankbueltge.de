This fixture file exists only for verify.test.ts, to prove the honesty harness reads a real file
from disk (via node:fs) and not only the in-memory strings the rest of that test file uses.
The measured drift for this scene was 4.2 degrees above baseline, recorded on the night of the crossing.
Nothing below this line is read by the test.
