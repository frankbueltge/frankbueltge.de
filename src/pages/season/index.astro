---
import Page from '@/layouts/Page.astro'
import SeasonPage from '@/components/pages/SeasonPage.astro'
import { CLOSED, SEASON1 } from '@/data/season/season1'
---
<Page
  title={`Season ${SEASON1.number} | Frank Bültge`}
  description={`Season ${SEASON1.number}, closed ${CLOSED.date} with ${CLOSED.shippedAtClosure} of ${SEASON1.episodes} episodes shipped and the direction never set — a dated archive of the ecology's one season, deleted in research ecology v2.`}
>
  <SeasonPage />
</Page>
