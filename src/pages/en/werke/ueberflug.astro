---
import Page from '@/layouts/Page.astro'
import MethodenblattUeberflug from '@/components/pages/MethodenblattUeberflug.astro'
import { t } from '@/i18n/ui'
const locale = 'en' as const
---
<Page title={`${t(locale, 'prot.method')} — Überflug | Frank Bültge`} description={t(locale, 'uefl.sub')}>
  <MethodenblattUeberflug locale={locale} />
</Page>
