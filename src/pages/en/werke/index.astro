---
// "Works" has been folded into "The File of the Present" in the Lab — index redirects there.
// The method sheets at /en/werke/<id> remain in place and reachable.
import { getRelativeLocaleUrl } from 'astro:i18n'
return Astro.redirect(getRelativeLocaleUrl('en', '/lab'))
---
