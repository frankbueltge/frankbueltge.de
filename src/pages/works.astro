---
// /works (plural) — the ecology's complete works register. NOT /work (singular), which is the
// conductor's own projects index and stays untouched; both pages say so in their own kicker.
import Page from '@/layouts/Page.astro'
import WorksRegister from '@/components/pages/WorksRegister.astro'
import { NAMING } from '@/config/naming'
---
<Page
  title={`${NAMING.worksRegister.pageTitle} | Frank Bültge`}
  description={NAMING.worksRegister.metaDescription}
>
  <WorksRegister />
</Page>
