---
// Ökologie-Neustrukturierung (Phase 3e, Branch ecology-restructure, Frank-Mandat 2026-07-15
// "komplett neu strukturieren"): die Startseite ist jetzt der Eingang in die aktuelle Begegnung
// statt der bisherigen Bio/Experimente-Übersicht (docs/design/site-restructure-concept.md §1/
// §2). Git ist das Archiv — die vorige Fassung lebt in der Historie dieser Datei, nicht als
// auskommentierter Block; der alte Home.astro-Baustein bleibt unangetastet im Repo, nur
// unverlinkt. NICHT auf main mergen ohne Franks ausdrückliches Go (siehe Arbeitsauftrag §0).
import Page from '@/layouts/Page.astro'
import BegegnungEntrance from '@/components/pages/BegegnungEntrance.astro'
import { currentEntrance } from '@/lib/begegnungen'

const locale = 'en' as const
const entrance = currentEntrance
---
<Page title={`${entrance.headline[locale]} | Frank Bültge`} description={entrance.status.statusLine[locale]}>
  <BegegnungEntrance />
</Page>
