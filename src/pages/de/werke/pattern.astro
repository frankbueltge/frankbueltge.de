---
import Page from '@/layouts/Page.astro'
import MethodenblattPattern from '@/components/pages/MethodenblattPattern.astro'
import { t } from '@/i18n/ui'
const locale = 'de' as const
---
<Page title={`${t(locale, 'prot.method')} — ${t(locale, 'pt.title')} | Frank Bültge`} description={t(locale, 'pt.sub')}>
  <MethodenblattPattern {locale} />
</Page>
