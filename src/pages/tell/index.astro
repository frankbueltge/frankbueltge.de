---
import Page from '@/layouts/Page.astro'
import TellPage from '@/components/pages/TellPage.astro'
import { t } from '@/i18n/ui'
const locale = 'en' as const
---
<Page title={`${t(locale, 'tl.title')} | Frank Bültge`} description={t(locale, 'tl.sub')}>
  <TellPage {locale} />
</Page>
