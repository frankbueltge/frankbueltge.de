---
import Page from '@/layouts/Page.astro'
import MethodenblattTrending from '@/components/pages/MethodenblattTrending.astro'
import { t } from '@/i18n/ui'
const locale = 'en' as const
---
<Page title={`${t(locale, 'prot.method')} — ${t(locale, 'tr.title')} | Frank Bültge`} description={t(locale, 'tr.sub')}>
  <MethodenblattTrending />
</Page>
