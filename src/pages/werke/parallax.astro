---
import Page from '@/layouts/Page.astro'
import MethodenblattParallaxe from '@/components/pages/MethodenblattParallaxe.astro'
import { t } from '@/i18n/ui'
const locale = 'en' as const
---
<Page title={`${t(locale, 'prot.method')} — ${t(locale, 'px.title')} | Frank Bültge`} description={t(locale, 'px.sub')}>
  <MethodenblattParallaxe locale={locale} />
</Page>
