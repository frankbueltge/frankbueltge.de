---
import Page from '@/layouts/Page.astro'
import MethodenblattTell from '@/components/pages/MethodenblattTell.astro'
import { t } from '@/i18n/ui'
const locale = 'en' as const
---
<Page title={`${t(locale, 'prot.method')} — ${t(locale, 'tl.title')} | Frank Bültge`} description={t(locale, 'tl.sub')}>
  <MethodenblattTell {locale} />
</Page>
