---
import Page from '@/layouts/Page.astro'
import MethodenblattPraemie from '@/components/pages/MethodenblattPraemie.astro'
import { t } from '@/i18n/ui'
const locale = 'en' as const
---
<Page title={`${t(locale, 'prot.method')} — ${t(locale, 'px2.title')} | Frank Bültge`} description={t(locale, 'px2.sub')}>
  <MethodenblattPraemie locale={locale} />
</Page>
