---
// „Werke“ sind als Experimente ins Lab verschoben — Index leitet dorthin um.
// Die Methodenblätter unter /werke/<id> bleiben bestehen und erreichbar.
import { getRelativeLocaleUrl } from 'astro:i18n'
return Astro.redirect(getRelativeLocaleUrl('de', '/lab'))
---
