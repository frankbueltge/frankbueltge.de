---
import Page from '@/layouts/Page.astro'
import MethodenblattCorrection from '@/components/pages/MethodenblattCorrection.astro'
import { t } from '@/i18n/ui'
const locale = 'en' as const
---
<Page title={`${t(locale, 'prot.method')} — ${t(locale, 'cr.title')} | Frank Bültge`} description={t(locale, 'cr.sub')}>
  <MethodenblattCorrection {locale} />
</Page>
