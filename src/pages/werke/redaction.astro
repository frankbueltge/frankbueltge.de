---
import Page from '@/layouts/Page.astro'
import MethodenblattRedaction from '@/components/pages/MethodenblattRedaction.astro'
import { t } from '@/i18n/ui'
const locale = 'en' as const
---
<Page title={`${t(locale, 'prot.method')} — ${t(locale, 'rd.title')} | Frank Bültge`} description={t(locale, 'rd.sub')}>
  <MethodenblattRedaction {locale} />
</Page>
