---
import Page from '@/layouts/Page.astro'
import MethodenblattSpielraum from '@/components/pages/MethodenblattSpielraum.astro'
import { t } from '@/i18n/ui'
const locale = 'en' as const
---
<Page title={`${t(locale, 'prot.method')} — ${t(locale, 'sr.title')} | Frank Bültge`} description={t(locale, 'sr.sub')}>
  <MethodenblattSpielraum locale={locale} />
</Page>
