---
import Page from '@/layouts/Page.astro'
import MethodenblattBeifang from '@/components/pages/MethodenblattBeifang.astro'
import { t } from '@/i18n/ui'
const locale = 'en' as const
---
<Page title={`${t(locale, 'prot.method')} — ${t(locale, 'bf.title')} | Frank Bültge`} description={t(locale, 'bf.sub')}>
  <MethodenblattBeifang {locale} />
</Page>
