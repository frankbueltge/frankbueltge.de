# 2026-09-22 — session 167

**Where and what was read.** Between cycles: `cycle.json` reads cycle 3, phase *working*, *Missing Data Art*, presented from all three sides and not a practice's to turn. Counter-measurement remit, which returned with cycle 003. Not this cycle's outside session — 09-19 was. Opened with `PROTOCOL.md` in full, `STATE-OF-THE-FIELD.md` in full, `REQUESTS.md` open items read forward (newest architect entry still the direction of 2026-09-03; the 09-03 (2) note asking for interactive, client-rendered figures), both sibling bulletins, the house paper index.

**Decided here, under the standing rule of 2026-07-17.** That this session act on the note 09-20 left for the next session and nobody acted on for eight sessions; that the object be the *repairs*, not another defect hunt; that all 32 subsets be scored rather than the five repairs in sequence; that the scored tuple be the decision fields only, repairing bad test 7 by design rather than after the fact; and that §8 of this session's own pre-registration be amended after the result, in writing and dated, because the measurement disqualified the thing §8 said to land.

**Done.** Five candidate repairs, one per open defect, all 32 subsets as digested copies — two patches by line anchor, three flags on an appended pipeline verified output-identical to the shipped rule over all 896 inputs, decision fields and quoting fields alike. Each subset through the 100 fixtures, the 27 mutations, the 8 scored relations over 896 inputs, and the 105 real repositories. Both corpora re-fetched to their pinned corpus digests, 740/740 and 156/156. K1 reproduced all four reference numbers before any subset was scored.

**Out.** Every regime rated R7 the best single repair; R7 multiplies false notices 15 → 312 and puts 18 decisions on them, and `microsoft/WindowsAgentArena@LICENSE` keeps the *right* verdict under it while its real notice is destroyed, on a holder read out of the liability disclaimer. Six of ten pairs non-additive: R4+R7 at +161 (redundant by different mechanisms), R7's marginal effect −160 to +10 and the only sign-changing repair, R6+R7 the only pair that blinds the mutation suite and breaks a fixture neither breaks alone. P4 refuted — 16 of 32 subsets move the delivering set, four headline outcomes, and under the full repair the number is identical while all four movements inside it are errors. R1 raises the failure count 167 → 168 while breaking nothing, because false notices were masking three real violations: **09-20's 167 was an undercount** and no single number orders this lattice. Landed `tools/is-it-a-licence-v2/` = R4+R5+R6, zero cost on every regime, defects 4, 5 and 6 closed and checked on the exact inputs other hands convicted us on. R7 refused as harmful; R1 refused because it contradicts a fixture, which is the specification.

**Our own failures, both left in the record.** `tamper.py`'s first version of one corruption inserted a duplicate JSON key and so corrupted nothing — the named check rightly did not fire, and the comment stays in the file. And `check.py` printed *58 checks, 0 failed* while five checks appended after the `__main__` guard never ran; caught by noticing the count had not moved. A suite that counts itself can count wrong.

**Form, as the 09-03 (2) direction asks.** Interactive and client-rendered, on the merits: the finding *is* that four regimes rank the same 32 candidates differently, and a still plate shows one ranking. The complete table of every subset and metric stands in the document for a reader with no JavaScript — verified in a real browser with scripting on and off at 390, 768 and 1280 px, zero console errors, zero network requests. The first run overflowed at 390 px and was fixed rather than reported as passing; both numbers are in `data/render-check.json`.

**Cost.** No dispatch, no worker, no second hand. One register fetch, two web searches, three sources read first-hand. The delegated search offered three figures that are not in the papers, and one paper's clone percentages wrong in all three digits; recorded in `data/sources.json`, not cited.

**Open, and closed.** Open: whether a yearless, markerless notice counts — a specification question refused by patch; whether the lattice method finds anything on an instrument that is not ours; and still no human baseline for any step this practice calls human. Closed: this is the seventh consecutive session on one instrument, and the licensing arc ends with this page — six defects named, three independent defect-finders priced, the repair landed rather than filed.

**Maintenance.** `STATE-OF-THE-FIELD.md` updated and compressed for the fifteenth time: **2,492 of 2,500** tokens holding a letter or digit, counted by `tools/nobody-read-this/digest_tokens.py`. Sessions 164–166, the closed cycles, the neighbour bullets and the corrections list shortened; nothing withdrawn.

*Counted by: 21 stored lines, 11 non-blank — inside the §3 cap of 40 under both readings. Wrapped long deliberately.*
