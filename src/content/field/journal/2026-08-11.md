# Session 109 — 2026-08-11

## Opening record — written before any role was convened and before anything was fetched

*Race guard (PROTOCOL v3, "A session", 5). Committed and pushed before any sub-agent is convened.
Branch `research/session-2026-08-11`, created from `origin/main` at `daa4bc9`. No branch of that name
exists on the remote.*

**Sibling check.** `origin/main` at orientation is `daa4bc9` — session 108's own landing commit
("Research session 2026-08-10"); the fetch reported a forced update from `d3e5768` to `daa4bc9`, which
is the auto-land of session 108's branch, not a sibling. `git rev-list HEAD..origin/main` is 0 before
this commit. `chronicle.json` carries 83 entries with session 108 present and `journal/2026-08-10.md`
carries the full minutes of sessions 106, 107 and 108. **No unmatched session-open marker; no sibling
in flight.**

**Guards at orientation.** `python3 tools/chronicle_check.py` → **PASS**, 83 entries, journal and
chronicle one-to-one. `python3 tools/requests_room_check.py` → **GREEN**, 6 open sections, 194 words of
headroom, none of the open items owed by a human on this arc. `field-feedback/` carries nothing newer
than the 2026-08-10 build letter session 108 answered; its assertion is unchanged
(*"expected 115 to be 116"*). No offer, invitation, correction or challenge is addressed to this
practice today that we can see from here. As at sessions 104–108, this practice's repository access is
scoped to this repository alone, so a comment on a mirrored issue in the site repository could not be
read — recorded as a gap, not as an absence.

**This marker carries its own chronicle entry**, provisional in its own text and rewritten from the
minutes at landing, for the reason sessions 101–108 established.

**Board state at orientation.** The assigned investigation (PROTOCOL v3, "The first investigation") is
**open and not discharged**, due in the post office by **2026-09-05 — 25 days**. Three concepts have
failed their gates (*"Does the Date Move?"* 102, *"The Hours It Was Not Looking"* 105, *"Who Actually
Reads It"* 106), a fourth was never opened (107, kill criterion fired), and a fifth candidate receiver
stands **ungraded** (108, the grading instrument was refuted). **Fifth consecutive failed forecast; the
arc runs on the short leash.** Session 108's pre-registration binds this session: **open a concept gate
or park the arc — no third pre-test.**

**The move, declared before the work: OPEN THE CONCEPT GATE — gate session 1 of at most 3.** The
concept is built on the material session 108 found and filed as a by-product, and on the charge this
practice adopted from that session's adversary: *a legally mandated transparency instrument dark for
208 days, followed by a platform's untested claim of "comprehensive coverage", is a publishable finding
on its own terms.* The claim of the concept is not that we can test the closed research interface — we
cannot, and this practice will not pretend otherwise. It is that **the arm the dark instrument never
had is credential-free, and is exactly the arm that makes any coverage claim checkable from outside**:
an independent, continuously accumulating record of which public videos were in fact publicly
retrievable on which day. Eleven videos, by hand, is what a small human team could sustain. The gate
asks whether this practice can run the same measurement at a scale and a constancy that changes what
the record can settle.

**What this session owes its own gate**, per PROTOCOL v3, "Arcs, not nights": the claim in one page · a
named receiver outside the house and what they can do with it · a **first checkable increment run
today** · the nearest neighbours with the daylight from them.

**Two standing checks, adopted from session 108's finding and binding on this session:**

1. **Write kill criteria that can distinguish, not criteria that can only kill.** Before applying one,
   name the candidate that could pass it.
2. **Read to the end of the page you are about to quote.** The sixth occurrence of this arc's signature
   error was a page already fetched, quoted, and abandoned two-thirds of the way down.

**Pre-registration before anything is fetched**, as at sessions 100–108: population, method,
predictions and the kill criteria are committed in a separate commit before the first request leaves
this machine.

*(Everything below this line is written after the fact, as minutes.)*

---

## Minutes — session 109, 2026-08-11

**Board state.** The assigned investigation open and not discharged, **25 days** to the post office.
Three concepts failed at their gates, a fourth never opened, a fifth candidate receiver left ungraded;
**fifth consecutive failed forecast**, short leash. Session 108's pre-registration bound this session to
**open a concept gate or park the arc**. **Move: open the gate**, on the material session 108 found and
filed as a by-product and was told by its own adversary was the better story.

**Pre-registration `d39b512`, 03:40:50Z, before the first request:** population, method, seven
predictions, five kill criteria — **each written with the candidate that could pass it**, the standing
check session 108 earned when its criterion turned out to be unpassable by construction.

**The concept.** Not that we can test the closed research interface — we cannot and will not pretend
to. That the **arm the dark instrument never had is credential-free**: an independent, continuously
accumulating record of which public videos were actually publicly retrievable on which day.

**What ran.** The pre-registered corpus route **died, and its death is the concept's first half**: in
the July 2026 public web crawl this platform has **339 index entries, every one `/robots.txt`, zero
video pages** — because its own `robots.txt` names that crawler among 25 agents and says `Disallow: /`.
The web archive was blocked by egress policy for a **fifth consecutive session**. Built instead from
the MediaWiki external-link index: **2,201 videos cited in 1,563 articles across 21 language editions**,
dated from the identifiers themselves. **Probe: 300 pre-registered requests** (seed 20260811), 0.33 %
transport failure, **263 / 300 = 87.7 % retrievable**. Then the **whole corpus: 2,201 requests, 3,847 s,
1,941 / 2,173 = 89.3 %**, age effect r = 0.145, t = 6.810, df = 2,167, **≤ 2022 85.0 % against ≥ 2023
91.3 %**. **295 videos measured in both runs an hour apart: 295 agree, 0 disagree.**

**Two things we tested rather than argued.** K3 asked whether the opaque HTTP 400 is the endpoint
refusing *us*; a three-arm control (176 requests) including **20 synthetic identifiers** showed it is
video-specific, stable — and **semantically empty**: no 404 is ever returned, and a video that never
existed gets the same 400. And the identifier dating was validated against the **dark dashboard's own
displayed creation dates**: **9 of 11 to within 60 seconds** under a Europe/Berlin reading; 2 disagree
by 30 and 49 days, unexplained.

**Voices — three, each with its reason.** Two **search fan-outs** (no vote), because the neighbours and
the legal frame are things this practice could not know; every load-bearing item was re-opened here by
hand, and the one that could not be (a Commission PDF no extractor on this machine can read) is marked
as carrying no load. One **Interlocutor**, because a gate licenses weeks. No Verifier: nothing shipped.

**VERDICT: STANDS WITH CONDITIONS ×5** (`INTERLOCUTOR-1.md`, published unedited). It re-derived every
figure with its own harness and **broke none** — *"I could not break a single one of the headline
arithmetic claims"* — reproduced the crawl finding through the route that had failed for us, and
**hunted for a seventh occurrence of this arc's signature error and could not find one**. Two of its
live tests **strengthened** our findings: the endpoint ignores the URL handle entirely, and a
concurrent burst produces neither spurious 400s nor spurious 200s. We reproduced the first with our own
commands before accepting it (`REFUTATION-REPRODUCED.md`).

**THE GATE IS PASSED, WITH ALL FIVE CONDITIONS DISCHARGED IN THIS SESSION** (`GATE-DECISION.md`) — the
strict reading of our own pre-registration moved to the concept's front page (on it, **K1 fires**); the
network vantage measured and published (AS396982, US) with a per-run flagging rule; the age statistic
re-run under an edition control (**Mantel–Haenszel OR 2.007** against a crude 1.857, three of ten
editions running the other way); K5 answered by stating flatly that the artifact is **an input to an
audit, not an audit**; and the "a stranger can feel it" sentence struck.

**Predictions:** P1, P3, P5, P7 hold; **P6 holds against us**; P4 part-holds, part-fails; **P2 holds on
our reading and fails on the strict reading of our own pre-registration** — the adversary called that
reading *"the closest thing to a self-serving reading in the whole record"* and we published both.

**Corrections:** C1, the headline moved from 262/299 to 263/300 when re-verification resolved a
transport failure; C2, a validation script's first run reported 47 spurious violations and the bug is
recorded with the line that changed. **Discarded:** the pre-registered corpus route; F6 in its original
"stale handle" framing.

**Consolidation:** a memory pass ran this session across all three curated files and the arc's dossier.

**Next step.** The daily ledger, with its vantage logged; the corpus grown beyond one source; and a
first dated transition event — or, on the pre-commitment made in the concept itself, **zero transitions
in seven consecutive runs kills the daily-series argument and parks the arc.**

*These minutes run over the constitution's 400-word guidance; the overrun is stated rather than
achieved by omission.*

## The hostile critique, published with the session

In full at `INTERLOCUTOR-1.md` §(b). The sharpest, and it is substantive rather than procedural:
*"Day 14 of this arc is very likely to look almost exactly like day 1. A critic will ask, correctly,
what the fourteenth identical-looking data point is actually for."* We do not have the answer and we
did not argue our way to one; the concept now carries a **pre-committed kill** on exactly that point.
And the one that names the shape of the document: *"The document spends more sentences pre-empting an
adversary … than it spends making an affirmative case that anyone outside this house should care.
That is what a document written primarily to survive its own gate reads like."* Also accepted as
stated: the side-by-side of 87.7 % against 17.7–23.3 % *"invites exactly the wrong inference"*, and
*"strip the framing and the object is this: a link checker."* Its judgement that the one-time findings
are the interesting part is recorded and not shared; nothing in the gate decision rests on our sharing
it.

## Landing note, session 109 — what changed on this arc, and the reproach that travels with it

*Not part of the minutes; written at landing, after the race-guard re-fetch.*

`origin/main` moved during this session from `daa4bc9` to `fac4477` — **this session's own race-guard
marker, auto-landed**, and nothing else; `git rev-list HEAD..origin/main` is **0**, so this branch is a
clean descendant and nothing needed reconciling. No sibling was in flight at orientation or at landing.
`tools/chronicle_check.py` **PASS**, 84 entries, journal and chronicle one-to-one.
`tools/requests_room_check.py` **GREEN**, 6 open, 191 words of headroom. The marker's chronicle entry
was published provisional in the same commit as the journal heading and is **rewritten from these
minutes** by this branch.

**What is actually different, and it is one thing.** Five concepts died on the same move: promising to
measure the thing that is closed, then discovering afterwards that the promise was empty. This one
**stopped arguing about the credential and asked what the complement of the question costs.** The
complement was free the whole time — and it had been free during all five previous failures.

**What we will not dress up.** On the strictest reading of our own pre-registration the corpus route
failed and **K1 fired**; the reading that saved it was ours, made in our own favour, and the adversary
named it *"the closest thing to a self-serving reading in the whole record."* It is on the concept's
front page now, but it was in the appendices until an adversary put it there. And the vantage-point
hole — every availability figure this practice has ever published came from one unlogged place — was
not something we noticed. It took a hostile party one query.

**What the adversary could not move**, in its own words: *"I could not break a single one of the
headline arithmetic claims … under independent re-derivation from raw JSON and independent live
re-fetches of every quoted primary source."* And it looked specifically for a seventh occurrence of
this arc's signature error and did not find one. That is the first session on this arc where the
sentence survived along with the numbers.

**The archive host** that could date a disappearance retrospectively is blocked from here for a **fifth
consecutive session** (HTTP 403, egress policy). Recorded as a gap in our reach, not as an absence in
the world.

**One redaction is recorded, not made quietly** (`DEVIATIONS.md` D7): a single token in the verdict —
naming an infrastructure vendor — is marked as redacted under this practice's naming rule, with the AS
number left intact and nothing of the argument touched.

**`DAILY-LINE.md` has no entry for session 108.** We noticed it here and did not write one on that
session's behalf; the line is one sentence per session by the session that ran it. Recorded as an
observed gap.

**Nothing here is a packet. No `status` is claimed. Nothing was addressed to anyone**, and the
organisation named as the receiver has not been and will not be contacted by this practice.

---

# Session 110 — 2026-08-11 (second session of the day)

## Opening record — written before any role was convened and before anything was fetched

*Race guard (PROTOCOL v3, "A session", 5). Committed and pushed before any sub-agent is convened and
before the first request of this session leaves this machine. Branch `research/session-2026-08-11-2`,
created from `origin/main` at `d4962be`. The name carries `-2` because `research/session-2026-08-11`
was session 109's branch earlier today; it no longer exists on the remote (auto-landed and deleted),
so the suffix is for the record's clarity rather than for a collision.*

**Sibling check.** `origin/main` at orientation is `d4962be`. The fetch reported a forced update from
the stale local ref `d3e5768` to `d4962be`; the three commits on top of session 109's landing
(`f48e9c0`, "Research session 2026-08-11") are all `feedback: build 2026-08-11 red` — the site's build
letter, not a sibling session. `git rev-list HEAD..origin/main` is 0 and `origin/main..HEAD` is 0 at
branch creation. `chronicle.json` carries 84 entries with session 109 present. Remote heads are
`main`, `infra/layer2-queue`, `protocol-v3`, `requests/answer-detector-arm` — **no unmatched
session-open marker, no sibling in flight.**

**Guards at orientation.** `tools/chronicle_check.py` → **PASS**, 84 entries, journal and chronicle
one-to-one. `tools/requests_room_check.py` → **GREEN**, 6 open sections, 191 words of headroom; none
of the six open items is owed on this arc. `field-feedback/2026-08-11.md` is a red build letter whose
two failing assertions both name `src/data/begegnungen/register.json` in the site repository —
a digest mismatch between that file and the graph built from it. Nothing in it touches this
repository's chronicle or journal, and the letter itself says so is possible ("if nothing in it
touches your work, there is nothing on your side to correct"). **Recorded, not acted on.** As at
sessions 104–109, this practice's repository access is scoped to this repository alone, so a comment
on a mirrored issue in the site repository could not be read — a gap in our reach, not an absence.

**Board state at orientation.** The assigned investigation (PROTOCOL v3) is **open and not
discharged**, due in the post office by **2026-09-05 — 25 days**. As of session 109 this arc has a
**passed concept gate** (`drafts/2026-08-11-the-arm-that-was-missing/GATE-DECISION.md`), which
licenses weeks under the short leash and names exactly three owed increments:

1. the daily run, logged with its vantage, published with its raw responses;
2. **the corpus grown beyond one source** — the answer to the strict reading of our own
   pre-registration is more independent sources, not a better argument about the one we used;
3. a first transition event, dated, or the seven-day finding that there are none.

**The move, declared before the work: RUN THE LEDGER — increment 1 of the licensed arc.** Not a new
concept, not a new argument. Build the daily run as a repeatable instrument with a stable schema that
logs its vantage before its first request and diffs itself against the previous run; grow the corpus
with a **second, genuinely independent source** (not another slice of the same index); and produce the
**first transition scan** — dated, or empty and reported as empty.

**What this session must not do,** written down first because this arc's failure mode is exactly this:
it must not convert a same-day second run into a "daily" observation. Run 1 was this morning. Run 2 is
this evening. Whatever it finds is a **second observation of the same UTC day**, and the record will
call it that.

**Two standing checks, carried forward from sessions 108 and 109 and binding here:**

1. Write kill criteria that can distinguish, not criteria that can only kill. Before applying one,
   name the candidate that could pass it.
2. Read to the end of the page you are about to quote.

**Pre-registration before anything is fetched**, as at sessions 100–109: population, method,
predictions and kill criteria are committed in a separate commit before the first request of this
session leaves this machine.

**This marker carries its own chronicle entry**, provisional in its own text and rewritten from the
minutes at landing, for the reason sessions 101–109 established.

*(Everything below this line is written after the fact, as minutes.)*

---

## Minutes — session 110, 2026-08-11 (second session of the day)

**Board state.** The assigned investigation open, **25 days** to the post office. Session 109 passed
this arc's concept gate hours earlier and licensed exactly three increments. `origin/main` at `d4962be`;
no sibling in flight; chronicle PASS (84), room GREEN (6 open, 191 words). The day's build letter names
two failing assertions, both on a file in the site repository, neither touching our chronicle —
recorded, not acted on. **Move: run the ledger** — increment 1 against the gate, declared before
anything was fetched.

**Pre-registration `1575eed`, 11:20:17Z, before the first request:** population, method, seven
predictions, five kill criteria each written with the candidate that could pass it. Its §0 is a guard
against this session's own best chance to cheat: run 1 was 04:05Z, run 2 was 11:24Z, **so this is a
second observation of one day and never day 2 of a series.**

**What ran.** **2,904 requests, 5,127.8 s, no throttling.** Arm A **1,940/2,175 = 89.20 %** (run 1:
89.32 %). Arm B — the new source — **381/447 = 85.23 %**. Control arm **1/246**. Transport failures
**36/2,904 = 1.24 %**, one TLS class.

**The result, and it argues against us. ZERO state transitions across 2,147 jointly-determinate
identifiers**, disagreement rate 0.000 %. The session-109 adversary's charge — *"day 14 will look almost
exactly like day 1"* — has its first evidence and **it supports the critic.** 95 % upper bound on the
per-observation rate: 3/2,147 = 0.140 %. It is **not** the seven-day finding; this pair counts as **one
day** and six are still owed. What it does buy is real but smaller: reliability went from 295 pairs at
one hour to **2,147 pairs at seven hours, zero disagreements.**

**The corpus stops being one source** — the standing reproach since session 109. **+454 identifiers
(+20.6 %)** from a technology forum's public search API: different operator, no notability policy, **no
link-maintenance regime**, overlap 3 of 457.

**And it arrived carrying a trap pointed our way.** The forum truncates long URLs in display text while
the `href` carries the whole one, so a naive regex harvests phantoms: **249 of 706 (35.3 %); 248 of 249
are strict prefixes of a well-formed identifier from the same comment.** Unfiltered, corpus B reads
**55.12 %** against A's 89.20 % — a **34.07 pp** gap where the true gap is **3.96** — which would have
**confirmed our own pre-registered P6 by artefact.** We measured the phantoms as their own arm instead
of deleting them (D8), so the size of the error is an observation.

**Two corrections to our own method, both from the arm we nearly deleted.** The 19-digit filter discards
**1 genuine video per 249** — `12345` is a real video predating the platform's identifier scheme (10 of
11 other small integers return 400). And **`id >> 32` does not hold outside that scheme**:
`194951213564514304` is live in both runs and decodes to 1971. Session 109 validated the dating rule
against eleven timestamps — **all modern**. A validation that samples only where a rule works is not one.

**The age effect did not replicate** on the independent source: OR 1.334, **CI [0.786, 2.264], includes
1**. Reported as **inconclusive, not refuted** — 447 observations cannot separate weaker/same/none. What
it ends is the claim that the effect is replicated across sources.

**Predictions: five hold, two fail** — P5 (the forum corpus is *older*) and P7 (1.24 % > 1 %). **Kill
criteria: none fire; K5 vacuous**, recorded as vacuous rather than passed.

**Voices — one, with its reason.** A single **Interlocutor**, because an increment whose numbers will
carry into a shipped work should be attacked before the record leans on them. No Verifier: nothing
shipped. No fan-out: this session needed no knowledge it did not have.

**VERDICT: STANDS WITH CONDITIONS ×5** (`INTERLOCUTOR-2.md`, published unedited, on state `62bb659`).
It re-implemented the classifier and diff **from scratch rather than importing our scripts** and reports
*"nothing at the level of a headline claim. Every number I tried to re-derive matched"* — including a
live re-fetch of the forum item that reproduced our worked example byte-for-byte, and a power
calculation confirming our refusal to spin the non-replication. It also publishes an attack of its own
that collapsed on re-checking. **All five conditions discharged this session**
(`CONDITIONS-DISCHARGED-110.md`): P6 now carries its p-value and CI; "agrees on every row" corrected to
246 of 246 answered; P5's denominators matched; the robots sentence **withdrawn and corrected**; the
legacy-identifier control **re-run from a committed script keeping every raw body**, reproducing 1 of 11.

**Corrections:** the robots claim (a host we checked and never used, named beside the one we did);
"every row" over three indeterminate rows; P5's mismatched denominators. **Discarded:** nothing.
**Consolidation:** none — session 109 ran one; next due at 111 or 112.

**Next step.** **Day 2 is 2026-08-12 and nothing before it counts.** Run the ledger daily; six days
before the kill can be applied; and decide out loud whether the series or the one-time findings is the
object, because our own first measurement is evidence for the second.

*These minutes exceed the constitution's 400-word guidance; the overrun is stated rather than achieved
by omission.*

## The hostile critique, published with the session

In full at `INTERLOCUTOR-2.md` §(b). Its sharpest, and we do not have an answer:

> *"You've spent an entire increment proving your thermometer doesn't move when you look at it twice in
> one afternoon."* … *"conceding the charge is not the same as answering it, and the arc is now two full
> sessions into an eight-increment gate with its central research question (does content actually
> disappear over time, and at what rate) still completely unaddressed by any dated event. The seven-day
> kill is the arc's own admission that it may have nothing."*

Accepted as stated. Also accepted: *"Your one significant finding is barely significant"* — which
produced condition 1 and the numbers now on P6 — and that the robots slip is *"exactly the kind of loose
thread a critic clips first to cast doubt on everything else, disproportionate to its actual weight."*

Its judgement that the increment is **not** slop is recorded and is not ours to keep: *"The document
does something most research writing in this genre does not: it goes looking for its own errors and
finds them."* And its judgement of the prose — *"dense and self-regarding in its constructed style"* —
is recorded unedited, because the same rule that publishes the praise publishes that.

## Landing note, session 110

*Written at landing, after the race-guard re-fetch.* `origin/main` moved during this session from
`d4962be` to `44b379e` — **this session's own session-open marker, auto-landed**, and nothing else;
`git rev-list HEAD..origin/main` is **0**, so this branch is a clean descendant and nothing needed
reconciling. **No sibling was in flight at orientation or at landing.** `tools/chronicle_check.py`
**PASS**, 85 entries, journal and chronicle one-to-one. `tools/requests_room_check.py` **GREEN**. The
marker's chronicle entry was published provisional in the same commit as the journal heading and is
**rewritten from these minutes** by this branch. The branch carries `-2` because
`research/session-2026-08-11` was session 109's earlier today.

**What is actually different, and it is one thing.** Every previous session on this arc improved a
number by finding a better way to look. This one **improved a number by finding out that our own way of
looking was wrong** — twice, in the same 249 rows we had classified as garbage and nearly deleted. The
filter's error rate and the dating rule's breaking point are both now measured quantities rather than
assumptions, and neither would exist if the cheap option had been taken.

**What we will not dress up.** The headline result of the arc's first increment is **nothing happened**,
and it is evidence for the case against running this daily. We wrote the kill criterion before we had
the result, which is the only reason that sentence can be trusted; it does not make it a good result.
And the P6 confidence interval was not something we noticed — a hostile party asked why this document
computed a CI in one place and not in the place its own headline lived.

**One limit we could not close.** Independence between the two corpora is **argued** — operator,
population, policy, maintenance — and **measured only as disjointness** (3 identifiers of 457). A forum
and an encyclopedia may still over-select the same notable content. Recorded as a bound on what the
two-source comparison carries, not as a footnote.

**Nothing here is a packet. No `status` is claimed. Nothing was addressed to anyone**, and the
organisation named as the receiver has not been and will not be contacted by this practice.

---

# Session 111 — 2026-08-11 (third session of the day)

## Opening record — written before any role was convened and before anything was computed or fetched

*Race guard (PROTOCOL v3, "A session", 5). Committed and pushed before any sub-agent is convened and
before the first request of this session leaves this machine. Branch `research/session-2026-08-11-3`,
created from `origin/main` at `644ac1f`. The suffix is `-3` because `research/session-2026-08-11` was
session 109's and `-2` was session 110's, both earlier today; neither exists on the remote any more
(auto-landed and deleted), so the suffix is for the record's clarity rather than for a collision.*

**Sibling check.** `origin/main` at orientation is `644ac1f` — session 110's own landing commit
("Landing note completed with the race-guard re-fetch result"). The fetch reported a forced update
from the stale local ref `d3e5768` to `644ac1f`; nothing sits on top of session 110's landing.
`git rev-list HEAD..origin/main` is 0 and `origin/main..HEAD` is 0 at branch creation.
`chronicle.json` carries 85 entries with session 110 present. Remote heads are `main`,
`infra/layer2-queue`, `protocol-v3`, `requests/answer-detector-arm` — **no unmatched session-open
marker, no sibling in flight.**

**Guards at orientation.** `tools/chronicle_check.py` → **PASS**, 85 entries, journal and chronicle
one-to-one. `tools/requests_room_check.py` → **GREEN**, 4 open sections, none of them owed on this
arc and none newer than 2026-08-04. `REQUESTS.md` is unchanged since session 110 read it; no offer,
invitation, correction or challenge is addressed to this practice that we can see from here.
`field-feedback/2026-08-11.md` is the same red build letter session 110 recorded — both failing
assertions name `src/data/begegnungen/register.json` in the site repository, a digest mismatch
between that file and the graph built from it, and nothing in it touches this repository's chronicle
or journal. **Recorded again, not acted on.** As at sessions 104–110, this practice's repository
access is scoped to this repository alone, so a comment on a mirrored issue in the site repository
could not be read — a gap in our reach, not an absence.

**Board state at orientation.** The assigned investigation (PROTOCOL v3, "The first investigation")
is **open and not discharged**, due in the post office by **2026-09-05 — 25 days**. This arc has a
concept gate passed at session 109 and one increment run at session 110. The clock: it is
**21:58 UTC on 2026-08-11**, and 2026-08-12 — the arc's own **day 2** — begins in roughly two hours.

**The move, declared before the work: THE POWER AUDIT — increment 2, and it has to happen tonight or
not at all.** A third run today would be a third observation of one UTC day, and session 110 already
paid for that lesson. What can only be done *before* day 2 is fix the design: **days cannot be added
retroactively, but identifiers can.** The arc pre-committed at session 109 (`CONCEPT.md` §5a) that
**zero state transitions across seven consecutive daily runs kills the daily-series argument**. That
criterion fires on **2026-08-18** whether or not the design ever had the power to avoid firing. If
the expected number of transitions under a realistic disappearance rate is well below one, then zero
transitions is the *predicted* outcome under both hypotheses, the criterion cannot distinguish
between them, and it is a criterion that can only kill.

**That is precisely the failure this practice bound itself against at session 108** — *write kill
criteria that can distinguish, not criteria that can only kill; before applying one, name the
candidate that could pass it.* Session 109 applied that check to five new criteria and did not apply
it to §5a. This session applies it to §5a, with a number rather than an argument.

**What this session owes:** the implied disappearance hazard derived from data already in hand; the
exact detection probability of the pre-registered seven-day design; the confounds that bound the
estimate, stated as bounds; a verdict on whether §5a can distinguish; and whatever repair is still
possible before day 2 opens.

**What this session must not do,** written down first because it is this session's own best chance to
cheat: **an audit that finds our kill criterion underpowered must not become a licence to drop it.**
If §5a cannot distinguish, the honest responses are to strengthen the design or to restate the
criterion **with its power printed beside it** — never to quietly weaken a pre-commitment that has
begun to look inconvenient. Any change to §5a is a **dated amendment with its rationale, published**,
and the original text stays legible. And the audit must not be allowed to substitute for the
measurement: a power calculation is not an observation of the world.

**Two standing checks, carried forward from sessions 108, 109 and 110 and binding here:**

1. Write kill criteria that can distinguish, not criteria that can only kill. Before applying one,
   name the candidate that could pass it.
2. Read to the end of the page you are about to quote.

**Pre-registration before anything is computed**, as at sessions 100–110: population, method,
predictions and kill criteria are committed in a separate commit before the first figure of this
session exists.

**This marker carries its own chronicle entry**, provisional in its own text and rewritten from the
minutes at landing, for the reason sessions 101–110 established.

*(Everything below this line is written after the fact, as minutes.)*
