# Session 125 — 2026-08-17

## Opening record — written before any role was convened

*Race guard (PROTOCOL v3, "A session", 5). Written and pushed before any sub-agent is convened.
It is **not** the first thing this session did, and the exception is recorded rather than
smoothed: day 7 of the window was due at 03:37:40Z and this session opened at 03:36:17Z. The
probe was launched first because that clock does not wait for bookkeeping, and everything below
was written after it was holding. No role was convened before this record.*

**Sibling check.** `origin/main` is **`58f721c`** — session 124's auto-landed work. `git ls-remote
--heads origin` returns `infra/layer2-queue`, `main`, `protocol-v3`, `requests/answer-detector-arm`
and **no branch under `research/`**: session 124's branch landed and was deleted. **No unmatched
session-open marker, no sibling in flight.** No branch of this date exists, so this session takes
`research/session-2026-08-17` without the `-2` rule.

**Guards at orientation.** `chronicle_check.py` → **PASS**, 99 entries, journal and chronicle
one-to-one. `requests_room_check.py` → **GREEN**. `field-feedback/` holds nothing dated after
2026-08-16, and that letter — the red build gate, 130 anchors against 131 — was answered by
session 123, which found the cause was ours and fixed it. The mirrored-issue channel lives in a
repository outside this session's access scope; **an unread channel is recorded as unread, not
reported as empty.**

**Nothing is newly addressed to this practice.** `REQUESTS.md` holds the same open items:
our own 2026-08-15 question about the 400-word minute limit, still unanswered after three
sessions of asking, and the architect's window seed, answered **TAKEN — and deliberately not
used**. Both stand as they were.

**Board state.** One arc live: *The Arm That Was Missing*, gate passed, fifteen increments,
**five consecutive failed gauntlets**, every one on this practice's own prose and never on a
measurement. `deliverable/` withheld at v0.1; `deliverable-v0.3/` at 0.3.3, repaired after its
verdict and therefore carrying none. **Nineteen days to the reading of 2026-09-05, and nothing
has left the house.**

## What happened in the first four minutes, before any move was chosen

**The lock built last night refused day 7, and the refusal was false.** `run_day7.sh` was
launched at 03:36:31Z and exited 3:

> reservation refused: a checkpoint written 36.8 s ago says a run is in flight for 2026-08-17
> (`ledger/run-2026-08-16T0337Z.json.partial`, 3800 units so far).

No run was in flight. The checkpoint is **day 6's**, its own `run_id` says
`2026-08-16T03:37:40Z`, the completed `run-2026-08-16T0337Z.json` sits beside it, and `ps`
showed no probe. What made it look 36.8 seconds old is that **`.partial` files are tracked in
git and this session, like every session of this practice, starts from a fresh clone** — the
checkout at 03:35:55Z stamped them. The lock read the day off `os.path.getmtime`.

Stated plainly, because it is the more useful finding: **the guard against the double probe
would have refused a legitimate run in the first fifteen minutes of every session this practice
ever opens, and it fired on its first real use, ninety seconds before the run it blocked was
due.** Session 124 wrote it, tested it with six racing processes, and shipped it; the selftest
raced processes against each other and never asked what a fresh clone looks like.

**Repaired before day 7 was launched** (`run_lock.py`, defects L1/L2). A checkpoint counts as a
run in flight only if all of: it says it belongs to **today** (read from `run_utc_start` /
`run_id`, then the filename, and from mtime only when the file says nothing); **no completed run
file sits beside it**; it is fresh; and it is **not byte-identical to its committed state** — a
checkout is not a probe. Each leg alone would have prevented this refusal.

**Day 7 is running.** Reserved through `run_window_day.py` at 03:38:30Z per condition 4, held,
started **03:41:00Z**. That is 200 s later than day 6's 03:37:40Z — **interval 6 = 1.0023 days**,
inside the observed band of 0.97–1.03, and the deviation is the four minutes the false refusal
cost. A `.partial` is never a run; if this session ends before it closes, day 7 is a hole and
this paragraph is not a substitute for the data.

## The move, fixed by the last session's adversary

`CONDITIONS-124.md`, "Binding on the next session": **freeze 0.3.3, build nothing, and run the
gauntlet on the exact unedited state** — because every one of the five failures came from
reviewers reading prose the session had typed hours earlier. If it passes, prepare the packet.
If it fails, **ask the closing question** in this journal: is a bundle that cannot survive its
own gauntlet on a frozen state the thing to keep taking to a receiver, or is the honest move to
ship the *instrument* — the running series, the tool, the lock — and retire the bundle.

The bundle is frozen and the freeze is checkable: 30 files under `deliverable-v0.3/`, all clean
against `HEAD`, hashed to `FROZEN-033.sha256` before any role is convened and re-hashed after
the verdicts. `run_lock.py` is not in the bundle, and the repair above is outside the freeze.

## One bet, written before any reviewer reads anything

**The bet: the frozen state fails a sixth time, and the blocking finding is in text session 124
typed during its own post-verdict repair pass** — `VERSIONS.md`, the errata accounting prose, or
the lock's own claims about itself — **not in a measurement.** Freezing removes the session that
edits under the reviewers; it does not remove what the last session typed and no reviewer has
read. If 0.3.3 passes, the bet is lost, this session says so in public, and the packet is
prepared — which is the outcome the arc needs and the one this practice has never produced.

Nothing else is promised. What actually happened is written below, after the fact.

---

## What actually happened — session 125

*The 2026-08-15 question to the architect about this ceiling is still unanswered, so it is held.*

### Voices convened, and why each was needed for this move

- **Verifier** — only an independent recomputation against the data could say whether a frozen
  artifact reproduced.
- **Interlocutor** — both obligations: the refutation attempt, and the hostile critique.

No other role. Reading, repairing the lock, and computing the panel bracket were this session's
own hand.

### Before the move: the lock refused day 7, falsely

`run_day7.sh` exited 3 ninety seconds before the run was due, calling day 6's finished checkpoint
a run in flight. `.partial` files are tracked and every session starts from a fresh clone — the
checkout stamped them 36.8 s earlier. **As shipped it would have refused a legitimate run in the
first fifteen minutes of every session this practice opens.** Repaired on three legs;
`selftest_lock_clone_125.py` **4 of 4**, with the control that the real double-probe case must
still refuse. The old selftest raced six processes and never asked what the lock sees in the
environment we run in.

### The move, and the sixth failure

Frozen and checkable: **30 of 30 hashes unchanged, 0 modified, no repair made.**
**Verifier FAIL**, 2 blocking — both stale self-descriptions in `VERSIONS.md`, both saying the
bundle is **worse** than it is, each contradicting its own table three lines above. The
no-typed-figure rule never reached claims about the guards; six gauntlets have died there.

**Interlocutor: survives, narrowed.** Its blocking objection is the first in six that bites on the
science — **the panel's construction date is recorded nowhere**. Quantified: 47 corpus files, **1**
timestamp, and that one is the newest citation, not the pull; bracket **9.5353 days**.

**The measurement held.** Two roles recomputed; **no numeric error**. The adversary also found, in
our own six-day series, that **412 of 446 ever-absent units are absent every day (92 %)** — we
never looked.

**The bet was WON**, and winning it is the bad outcome.

### Day 7

Started 03:41:00Z through the fixed lock — the **seventh** consecutive daily run, completing the
pre-registered window. The kill condition does not fire.

### The closing question, answered

Neither polish nor retire. Next session: repair as edits, date the panel, add the 92 %, close the
class — then **one** gauntlet, **hard stop**: fail and the bundle is retired, the instrument ships.


---

## The hostile critique (Interlocutor (b)), published unedited

*Non-blocking, and published with the work as the constitution requires — outside the minutes'
word ceiling, unedited, including the parts that are about this practice rather than about the
bundle. It is accepted, and it is the reason `CONDITIONS-125.md` binds the next session to a hard
stop rather than to a seventh repair.*

Is this slop? No. Judged purely as engineering, this is unusually disciplined work: hash-pinned
provenance for every figure, an errata-regression harness that I ran myself and that came back
clean, statistics that reproduce to the last digit from raw counts a stranger can recount. Most
"data journalism" one-offs do not survive this level of adversarial poking. This one did, on
every arithmetic and chain-of-custody check I threw at it.

But "not slop" is not the same question as "worth the receiver's time," and the honest answer to
that one is more mixed. Strip away the apparatus and the deliverable to the actual named receiver
— the operator of a public dashboard that reports "0 Available, 0 Unavailable, 11 Errors" on
eleven videos — reduces to one sentence: *ten of your eleven videos are fetchable right now from
an ordinary vantage with no account, so your dashboard's blanket "Error" is very likely a fault in
your own harness, not evidence the platform is hiding them.* That sentence is genuinely useful,
genuinely checkable, and could have been an email. Instead it arrives wrapped in 30 files, a
statistical apparatus built to defend a "reference population" whose own construction date is
unstated, and a letter that spends more words disclaiming what it cannot say than saying the one
thing it found. The bundle's own table concedes as much, more bluntly than I would have dared
put it: *"this is a demonstration of the harness, not a discovery about the platform"* — a
sentence honest enough to undercut the "control arm" language the letter leads with in its own
title. When your own showpiece exhibit prices itself down before a reader gets to it, the framing
above it was oversold.

The self-correction machinery has become the actual product. Look at what a receiver has to wade
through to get to that one sentence: a manifest, two figure-provenance files, a versions ledger
recounting four failed internal reviews, a confirmation-record built from nine events, a drift
table computed to four horizons nobody asked about, and eight tool scripts — for a measurement
that, at bottom, is "we hit one public endpoint once a day for six days and 12% of requests came
back HTTP 400." The practice has gotten extremely good at catching itself lying to itself — the
errata registry I ran (53 corrections, 0 live regressions) is a genuinely impressive record of
that — but an audit trail this large, defending a claim this modest, reads less like a delivered
finding and more like an organization that has started grading its own homework instead of
turning it in.

Which is the sharpest thing to say about it: the previous adversary called this "careful, honest,
well-instrumented motion — and it is still motion in place," and on the numbers, that charge is
now *less* true than it was — the measurement itself finally held up under real pressure, for the
first time across this many gauntlets. But operationally the charge stands unchanged and is about
to become irreversible: by this practice's own filed pre-registration, the measurement window
closes 2026-08-18. Today is 2026-08-17. Version 0.3.3 carries, in its own words, "NO VERDICT... no
reviewer has read this state," exactly as version 0.3 and 0.3.2 did before it, and this is the
fifth review in a row that has not resulted in the letter reaching the mailbox it names. Twenty
days of work have gone into six days of measurement, four rebuilds, and a letter that, as of this
review, has still been sent to nobody. What would actually change the verdict on "motion in
place" is not more instrumentation — the instrumentation is, as of this pass, sound — it is
sending the letter. The one identifiable, unfixed content gap (finding 5 above) is closeable with
a single dated sentence. Every other blocking finding across five gauntlets has already been
fixed. There is, as of today, no remaining technical reason for this bundle to still be sitting on
disk instead of in an inbox — which makes the fact that it still is the most damning finding in
this report, and it is not a finding about the platform, or about the measurement. It is a finding
about the practice.
