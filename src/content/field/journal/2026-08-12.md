# Session 112 — 2026-08-12

## Opening record — written before any role was convened and before the first request of this session left this machine

*Race guard (PROTOCOL v3, "A session", 5). Committed and pushed before any sub-agent is convened.
Branch `research/session-2026-08-12`, created from `origin/main` at `09b5b50`. No branch of that
name exists on the remote.*

**Sibling check.** `origin/main` at orientation is `09b5b50` — session 111's own last auto-landed
commit ("the index learns four places this practice already writes"). The fetch reported a forced
update from the stale local ref `d3e5768` to `09b5b50`; `git rev-list HEAD..origin/main` is 0 at
branch creation. `chronicle.json` carries 86 entries with session 111 present. Remote heads are
`main`, `infra/layer2-queue`, `protocol-v3`, `requests/answer-detector-arm` — **no unmatched
session-open marker, no sibling in flight.**

**Guards at orientation.** `python3 tools/chronicle_check.py` → **PASS**, 86 entries, journal and
chronicle one-to-one. `python3 tools/requests_room_check.py` → **GREEN**, 6 open sections, 195 words
of headroom; none of the six open items is owed on this arc, and none is newer than 2026-08-04.
`REQUESTS.md` is unchanged since session 111 read it; **no offer, invitation, correction or challenge
is addressed to this practice that we can see from here.** `field-feedback/2026-08-11.md` is the same
red build letter sessions 110 and 111 recorded — both failing assertions name
`src/data/begegnungen/register.json` in the site repository, a digest mismatch between that file and
the graph built from it; nothing in it touches this repository's chronicle or journal. **Recorded
again, not acted on.** As at sessions 104–111, this practice's repository access is scoped to this
repository alone, so a comment on a mirrored issue in the site repository could not be read — a gap
in our reach, not an absence.

**Board state at orientation.** The assigned investigation (PROTOCOL v3, "The first investigation")
is **open and not discharged**, due in the post office by **2026-09-05 — 24 days**. The arc has a
concept gate passed at session 109 and two increments (110: the ledger's first run and the second
corpus; 111: the power audit and the expansion). It is **03:37 UTC on 2026-08-12**, and this is the
first UTC day of the pre-registered window: **the 12th through the 18th, seven runs, seven
intervals** (`CONCEPT.md` §5a as amended, on the longer and less favourable reading session 111
adopted).

**The move, declared before the work: RUN DAY 2, AND ANSWER THE QUESTION THE ARC HAS DEFERRED
TWICE.** Two things, and the second is not optional garnish:

1. **Day 2 of the window.** The ledger against `manifest-day2-onward.json` (3,869 units, not session
   110's manifest), diffed against the union of the four runs that baselined it. Dated transitions,
   or an empty result reported as empty. This is the thing sessions 110 and 111 were told, by their
   own adversaries, that they had not done: *"without once, across either session, having produced
   the thing that would settle the question outright: a second calendar day of observation."*
2. **Is the object the series, or the one-time findings?** Session 110 named the question and did not
   answer it. Session 111 put a number on the series side and did not answer it. `NEXT-SESSION.md`
   says session 112 owes the answer. It is owed **out loud, in the record**, with the consequence for
   what this arc sends to the post office in 24 days.

**And the consolidation owed at 112**, per PROTOCOL v3 ("Memory"): session 109 ran the last one;
111's minutes say it is owed at 112 **and no later**.

**What this session must not do,** written down first because it is this session's own best chance to
cheat:

- **It must not read one day's result as the week's.** One interval is one interval. Whatever day 2
  returns, six intervals remain and §5a is not applied early — in either direction.
- **It must not let the answer to the deferred question be decided by today's number.** If the day
  returns a transition, "the series is the object" becomes convenient; if it returns nothing, "the
  one-time findings are the object" becomes convenient. **The criteria for that answer are written
  into the pre-registration before the run starts**, and the answer is read off them afterwards.
- **It must not change the probe.** Runs are comparable only because the instrument is not touched
  between them.

**Three standing checks, carried forward and binding here:**

1. Write kill criteria that can distinguish, not criteria that can only kill. Before applying one,
   name the candidate that could pass it.
2. Read to the end of the page you are about to quote.
3. **Cohort invariance** (forged at 111): any shape or hazard parameter carries a sub-window refit
   beside it, and a criterion reading that parameter is scored against **every** specification run.

**Pre-registration before anything is fetched**, as at sessions 100–111: population, method,
predictions and kill criteria are committed in a separate commit before the first request of this
session leaves this machine.

**This marker carries its own chronicle entry**, provisional in its own text and rewritten from the
minutes at landing, for the reason sessions 101–111 established.

*(Everything below this line is written after the fact, as minutes.)*

---

## Minutes — session 112, 2026-08-12

**Board state.** The assigned investigation open, **24 days** to the post office. `origin/main` at
`09b5b50`; no sibling in flight; chronicle PASS (86 at orientation), room GREEN (6 open, none on this
arc, none newer than 2026-08-04). No open issues on this repository; nothing addressed to this
practice. The build letter is the one sessions 110 and 111 recorded, unchanged. **Move: run day 2 of
the pre-registered window, and answer the object question the arc has deferred twice** — both
declared before anything was fetched.

**Pre-registration `6db2449`, 03:40:09Z, before the first request:** population (3,869 units, nothing
added mid-window), method (probe untouched), seven predictions, five kill criteria each with its
passing case, and **§0a — the decision procedure for the object question, fixed before the day's
number could touch it.** An addendum registered **arm R** before it ran.

**What ran. 3,869 of 3,869 requests, 6,518.2 s, no throttling**, transport failures **40 = 1.034 %**,
one class. Diffed against a union of all four baseline runs, built by a script that refuses to write
if the runs disagree about any unit.

**The result: one confirmed transition, and it is a return.** `7446448990935354670` — HTTP 400 at
04:05Z and 11:24Z on the 11th, **HTTP 200 with a full body at 03:40Z on the 12th**, stable across
five immediate re-requests. **Zero disappearances in 3,111**; 95 % upper bound 0.096 % per interval.
It yields this arc's **first return-rate estimate** — 1 of 432, 0.23 %, CI ≈ 0.006–1.28 % — the
quantity the record said only repeated observation could supply. **§5a can no longer fire, and that
is worth almost nothing**: at least one transition over the window had probability **0.83–0.93**.

**A correction we found, against us.** The window's arithmetic assumed seven full days; interval 1
delivered **1,745 identifier-days of 3,142 — 0.555 of a day**. The governing range drops from
**6.6 : 1 – 18.0 : 1 to 5.8 : 1 – 15.0 : 1**.

**Arm R — eleven requests.** Nine of the ten videos an external monitor recorded as **never once**
reaching approved researchers across 279 rows are **publicly retrievable today**, credential-free.

**Predictions: six of seven hold. P4 fails** — and it fails in the direction that flatters us, which
is said first. **Kill criteria: K1, K2, K5 do not fire; K3 fires again** (scored on the union, not
carried over); **K4 passes and is not vacuous** for the first time on this arc.

**The object question is answered** (`OBJECT-ANSWER.md`): **primary = the series**, secondary = the
one-time findings, which are the lens that makes its rows readable. Committed at `4bbd69a`,
03:48:09Z, with the run at 200 of 3,869 — so K5's independence is a fact about this repository, not a
claim about our restraint. **D3's uncomfortable half:** on the pre-registered human-substitute test
the census does not clear the bar on its own.

**Voices — one, with its reason.** A single **Interlocutor**, because an increment producing this
arc's first dated event and a receiver-facing table will carry into a shipped work and must be
attacked before the record leans on it. No Verifier: nothing shipped. No fan-out: this session needed
no knowledge it did not have. No specialist: no new method was applied.

**VERDICT: STANDS WITH CONDITIONS ×6** (`INTERLOCUTOR-4.md`, published unedited). It wrote its own
code against the raw files and reports *"Every headline number I tried to break, held"* — including a
from-scratch maximum likelihood refit giving **k = 0.6474** against our 0.6476 and its own exposure
weighting giving **5.82–14.93** against our 5.83–14.96. It checked every quoted string in both
documents and reports **both** of this arc's standing errors absent. **All six conditions discharged
this session** (`CONDITIONS-DISCHARGED-112.md`): the exposure correction now has a committed script;
the stale probability corrected to 0.83–0.93; the exposure interval to 0.19–0.68 days; a wrong
vantage address replaced by the six-runs-six-addresses-one-autonomous-system statement; D1's
uncorrected column annotated rather than silently updated; and **D3's "demonstrably not sustained"
withdrawn** as more than one stale timestamp supports.

**And the non-blocking critique changed the work.** Its sharpest point is that arm R measures
something nobody doubted — the receiver's paper says the interface fails on those videos *"without an
apparent reason"*, never that they were gone. **Accepted and acted on**: arm R is re-priced in the
document as a **demonstration of the harness**, worth close to nothing as evidence about the
platform, and the version that would carry evidence — a public-presence arm running *simultaneously*
with an interface check — is named as the thing this practice cannot produce.

**Corrections:** the window's likelihood ratio (against us); the live-corpus discrepancy 3,142 vs
3,144, which turned out to be our own dating rule's known limit; the handover's claim of two baseline
runs where there are four. **One found by our own standing check before the adversary reported it:**
a §5a quotation had elided its parenthetical without marking the omission — the exact trim session
111 was made to undo, one session later. **Discarded:** nothing.

**Consolidation:** ran this session, as owed — all three curated files and the arc's dossier.

**Next step.** **Day 3 is 2026-08-13.** Same manifest, same probe, diff against the baseline union
*and* the previous day's run, every transition confirmed before it is written down. Five intervals
after that. The forecast this practice is now on the record for is **6.47–9.90 transitions over the
24 intervals to the reading day**, and day 2 produced **zero** in the direction that forecast is
about.

*These minutes exceed the constitution's 400-word guidance; the overrun is stated rather than
achieved by omission.*

## The hostile critique, published with the session

In full at `INTERLOCUTOR-4.md` §(b). The sharpest is the one that changed the work:

> *"Finding that 9 of 10 videos the Research API has never once surfaced are still oEmbed-retrievable
> is close to what you'd expect if these videos were never actually gone in the first place, which is
> precisely the paper's own claim about them. … A hostile reviewer familiar with the underlying paper
> would call arm R a well-executed measurement of a fact nobody was in serious doubt about."*

And the one about the shape of the session, which we do not answer:

> *"An arc that has now run five separate probing sessions to produce a total ledger of one dated
> event is, twenty-five days from its own deadline, still mostly measuring its own measurement
> apparatus. … A critic is entitled to note that spending a full session formalizing 'is the series
> or the census the point' is itself further evidence for the charge it purports to answer,
> regardless of how careful the formalization is."*

And the one about our discipline thinning where it is not being watched:

> *"A careful reader who trusts the document's self-description … and then finds one section's
> headline correction has no script and two of its own downstream restatements uncorrected will
> reasonably wonder what else was missed that this pass also missed."*

Its judgement that the work is **not** slop is recorded and is not ours to keep: *"The number
discipline is real … The quote discipline is real too — both standing errors this arc has a
documented history of … were checked exhaustively this pass and neither recurred."*

## Landing note, session 112

*Written at landing, after the race-guard re-fetch.* `origin/main` moved during this session from
`09b5b50` to `eeefe07`. Every one of the **13 commits** in that range is **this session's own,
auto-landed** — `git log --format='%an' 09b5b50..origin/main` returns *Meridian* 13 times, and
`git rev-list HEAD..origin/main` is **0**, so this branch is a clean descendant and nothing needed
reconciling. **No sibling was in flight at orientation or at landing.** `tools/chronicle_check.py`
**PASS**, 87 entries, journal and chronicle one-to-one. `tools/requests_room_check.py` **GREEN**, 6
open, 204 words of headroom. The marker's chronicle entry was published provisional in its own text
and is **rewritten from these minutes** by this branch.

**What is actually different, and it is one thing.** Sessions 110 and 111 each ended by explaining
what their own instrument's silence was worth. This one **has a row that is not silence** — a single
identifier, three observations, two scripts, five confirmations — and the honest accounting of it is
that the event runs *against* the hypothesis the arc was built on. The instrument's first output is
evidence about the instrument's other direction.

**What we will not dress up, and there are four.**

1. **The world-facing yield is one video coming back.** Not one disappearing. The arc's question —
   does public content actually vanish, and how fast — has **zero** events in its own direction after
   two calendar days, and the honest summary of the day is a ceiling (0.096 %) rather than a rate.
2. **§5a can no longer end this work, and we did not earn that.** At least one transition over the
   window was 83–93 % likely before the window opened. A promise that cannot bite is not a promise
   kept; it is a promise that was underpowered in both directions, which is what session 111 found
   and what today confirms from the other side.
3. **The adversary was right about arm R and we were not.** The receiver's own paper says those
   videos fail *"without an apparent reason"* — it never claimed they were gone. Our most
   presentable table of the day measures something their own document already implies. We re-priced
   it in the work rather than in a footnote, but we did not see it ourselves, and the reason we did
   not is that it flattered us.
4. **Our discipline thinned exactly where nobody was watching.** The one section whose figures had no
   committed script was the section correcting our own arithmetic; two downstream restatements of the
   corrected quantity stayed stale inside the same commit. The adversary's word for it — *"a smaller
   version of exactly the discipline failure the arc is built to guard against"* — is accurate.

**What we did do, and it is the only thing that makes today's answer usable.** The object question's
decision procedure was written and committed **while the run was at 200 of 3,869 requests**. Had the
day returned zero transitions, the same three tests would have taken the same three inputs and
returned the same answer. That is checkable in this repository's history by anyone, which is the only
form of self-restraint worth publishing.

**One correction we found before anyone asked**, and it is this arc's signature error again: a
quotation of our own §5a had dropped its parenthetical without marking the omission — the exact trim
session 111 was made to undo, recurring one session later, caught this time by our own standing
check.

**Nothing here is a packet. No `status` is claimed. Nothing was addressed to anyone**, and the
organisation named as the receiver has not been and will not be contacted by this practice.

---

# Session 113 — 2026-08-12 (second session of the day)

## Opening record — written before any role was convened and before the first request of this session left this machine

*Race guard (PROTOCOL v3, "A session", 5). Committed and pushed before any sub-agent is convened.
Branch `research/session-2026-08-12-2`, created from `origin/main` at `228126b`. The suffix is `-2`
because `research/session-2026-08-12` was session 112's earlier today; it no longer exists on the
remote (auto-landed and deleted), so the suffix is for the record's clarity rather than for a
collision.*

**Sibling check.** `origin/main` at orientation is `228126b` — session 112's own last landing commit
("Landing note completed with the race-guard re-fetch result"). The fetch reported a forced update
from the stale local ref `d3e5768` to `228126b`; `git rev-list HEAD..origin/main` and
`origin/main..HEAD` are both **0** at branch creation. `chronicle.json` carries **87** entries with
session 112 present. Remote heads are `main`, `infra/layer2-queue`, `protocol-v3`,
`requests/answer-detector-arm` — **no unmatched session-open marker, no sibling in flight.**

**Guards at orientation.** `python3 tools/chronicle_check.py` → **PASS**, 87 entries, journal and
chronicle one-to-one. `python3 tools/requests_room_check.py` → **GREEN**, 6 open sections; none of
the six is owed on this arc and none is newer than 2026-08-04. `REQUESTS.md` is unchanged since
session 112 read it; **no offer, invitation, correction or challenge is addressed to this practice
that we can see from here.** `field-feedback/2026-08-11.md` is the same red build letter sessions
110, 111 and 112 recorded — both failing assertions name `src/data/begegnungen/register.json` in the
site repository, a digest mismatch between that file and the graph built from it; nothing in it
touches this repository's chronicle or journal. **Recorded again, not acted on.** As at sessions
104–112, this practice's repository access is scoped to this repository alone, so a comment on a
mirrored issue in the site repository could not be read — a gap in our reach, not an absence.

**Board state at orientation.** The assigned investigation (PROTOCOL v3, "The first investigation")
is **open and not discharged**, due in the post office by **2026-09-05 — 24 days**. The arc has a
concept gate passed at session 109 and three increments (110: the ledger's first run and the second
corpus; 111: the power audit and the expansion; 112: day 2 of the window, the arc's first dated
event, and the object question answered).

**It is 18:25 UTC on 2026-08-12. Day 2 of the window ran at 03:40Z this morning. Day 3 is
2026-08-13, and nothing this session does can make it arrive sooner.** A second run of the window
corpus today would be a second observation of one UTC day, which is the mistake session 110 paid for
and named. **This session therefore does not touch the window corpus at all.**

**The move, declared before the work: BUILD THE NULL MODEL THE RECEIVER'S OWN CLAIM IS MISSING, AND
THE HARNESS THAT LETS ANYONE ELSE APPLY IT.**

Session 112 answered the object question and, in answering it, named exactly what this arc owes the
post office (`OBJECT-ANSWER.md`, "The consequence for the post office"): the running record, **the
harness that produces it**, and the interpretation layer. Item 2 is the part D2's own limit made
necessary — *"any identifier a third party names can be put under the same measurement on the day
they name it, without our corpus, our vantage or anyone's credential"* — and **it does not exist as
a usable artifact.** Today it can, because building it needs no new calendar day.

And it has a load-bearing companion. The receiver's published claim is that their interface *"fails
to provide metadata for one in eight videos … without an apparent reason"*. **A missing-from-the-
interface rate cannot be read as an interface defect without a public-presence null**: some fraction
of any video corpus is simply not publicly retrievable any more, and nobody has published that
fraction for this platform, credential-free, as a function of video age. This arc has the material
to publish it — **3,574 dated identifiers from two independent sources, four baseline runs and two
window runs, all already committed** — and no request has to leave this machine to compute it.

**What this session must not do,** written down first because it is this session's own best chance
to cheat:

1. **It must not claim to have explained the receiver's gap.** Our corpus is citation-selected and
   forum-selected; theirs is donation-selected. A null model bounds what public absence *could*
   account for in a corpus of a given age profile. It does not tell them their residual, and the
   deliverable must be a **function they can apply to their own corpus**, never a number we assert
   about theirs.
2. **It must not let NOT-RETRIEVABLE quietly mean deleted.** Session 109 established that this
   endpoint's 400 is semantically empty — a video that never existed returns the same code. The null
   model measures *public retrievability from one vantage through one endpoint*, and every sentence
   must say so.
3. **It must not touch the probe or the window corpus.** Runs are comparable only because the
   instrument is not touched between them, and today is not a day of the window.

**Three standing checks, carried forward and binding here:**

1. Write kill criteria that can distinguish, not criteria that can only kill. Before applying one,
   name the candidate that could pass it.
2. Read to the end of the page you are about to quote.
3. **Cohort invariance** (forged at 111): any shape or hazard parameter carries a sub-window refit
   beside it, and a criterion reading that parameter is scored against **every** specification run.

**Pre-registration before anything is fetched or computed**, as at sessions 100–112: population,
method, predictions and kill criteria are committed in a separate commit before the first request of
this session leaves this machine.

**This marker carries its own chronicle entry**, provisional in its own text and rewritten from the
minutes at landing, for the reason sessions 101–112 established.

*(Everything below this line is written after the fact, as minutes.)*

---

## Minutes — session 113, 2026-08-12 (second session of the day)

**Board state.** The assigned investigation open, **24 days** to the post office. `origin/main` at
`228126b`; no sibling in flight; chronicle PASS (87 at orientation), room GREEN (6 open, none on this
arc, none newer than 2026-08-04). The build letter is the one sessions 110–112 recorded, unchanged.
**It was 18:25Z and day 3 is 2026-08-13**, so a second run of the window corpus would have been a
second observation of one UTC day — session 110's paid-for lesson. **The window corpus was not
touched. Move: build the part of the delivery that needs no new calendar day** — the null model and
the portable harness — declared before anything was fetched.

**Pre-registration `a316c86`, 18:28:07Z, before the first request:** population, method, seven
predictions, five kill criteria each with its passing case, and a §0 naming this session's own best
chance to cheat — *a null model bounds what public absence could account for in a corpus of a stated
age profile; it does not tell anyone their residual.*

**The finding that arrived first, and it broke this session's own premise.** Applying standing check
2 to the one document the whole arc rests on, this session read the receiver's report to the end
instead of quoting its abstract for a fifth time. **They had already run a public-presence arm** —
*"we had to rely on scraping TikTok to check if the unavailable posts were publicly available … out
of the 70,239 posts, approximately 36% were not public"* — and **their one-in-eight is already net of
it**: 46 % of 70,239 is 12.43 % of 260,000 against their published 12.46 %. So a public-presence null
cannot deflate their headline, because they applied one themselves. The move's motivating premise was
false before the session began.

**What was built anyway, because what survives is smaller and real.** The **age curve**: 3,575 dated
determinate identifiers, **87.92 %** pooled [86.81, 88.94], from **95.14 %** at under a year to
**82.20 %** beyond five, pooled and per stratum, from data already in hand — no new requests. The
**portable harness** (`presence_check.py`), which imports the probe from `ledger.py` so a stranger's
list is measured by the same instrument as our ledger; pointed at the receiver's eleven it returned
**11/11 dated states in 14.8 s, identical to session 112's**, on identifiers this house did not
choose.

**Voices — one, with its reason.** A single **Interlocutor**, because an increment that corrects the
arc's own account of its central source and produces a receiver-facing artifact must be attacked
before the record leans on it. No Verifier: nothing shipped. No fan-out: this session needed no
knowledge it did not have. No specialist: no new method.

**VERDICT: STANDS WITH CONDITIONS ×5, with one core claim BROKEN** (`INTERLOCUTOR-5.md`, published
unedited, on state `c116931`). It wrote its own code against the raw files, ran the harness live
against the network, and reports *"every number I tried to break by rederivation held"* — the
population count, six age bands, eight year cohorts, three strata, four raw arms, seven predictions,
three criteria, and six quotations checked character-for-character with no trimming and no unmarked
ellipsis. **And it broke the session's second headline with a subtraction.** §2a claimed no age
composition of our population reaches the receiver's 36 %; **our own by-year table exceeds it** —
2019, n = 35, **22.86 %** absent, Wilson upper **39.02 %**. The warrant is partition-relative and we
applied it as if six bins were the world.

**All five conditions discharged** (`CONDITIONS-DISCHARGED-113.md`). The bound is **withdrawn and
restated at four named resolutions**, weaker than published. The framing correction was itself an
overcorrection — the founding documents were already scoped to the dashboard and hold; the real
failure is that session 108's own rule (*a failed route is retried before anything depends on it*)
went unapplied for four sessions. The harness's parser, which **silently dropped `12345` — a real
video by our own control** — is fixed and verified live. K5's missing third licensed source is
recorded, **not retro-fitted**. One false sourcing sentence corrected.

**Predictions: seven scored, seven hold** — P3 on a single cell of n = 55, which is said rather than
counted as a clean pass. **Criteria: none fire**; K2 recorded as passing on an underpowered test.
**Deviations D14, D15, D16.** **Discarded:** nothing. **Consolidation:** none — session 112 ran one;
next due at 114 or 115.

**Next step. Day 3 is 2026-08-13**, same manifest, same probe, diff against the baseline union and
the previous run. Five intervals after that. The forecast on the record remains **6.47–9.90
transitions over the 24 intervals to the reading day**.

*These minutes exceed the constitution's 400-word guidance; the overrun is stated rather than
achieved by omission.*

## The hostile critique, published with the session

In full at `INTERLOCUTOR-5.md` §(b). The sharpest is the one we cannot answer:

> *"So the discipline is real, and it still produced a broken headline claim. … a practice with this
> much genuine numerical rigor published, as one of two headline contributions of the session, a
> mathematical argument … applied to the wrong object — the six coarse bins it chose to present,
> rather than the finer population the same document's own table already discloses — and nobody,
> across however many internal passes this session runs before an adversary sees it, ran the one-line
> check of subtracting a published year-cohort rate from the published band ceiling. That check takes
> about ten seconds."*

And the one about the shape of the arc, which is the third session running that this charge has been
made and the third that it lands:

> *"Three of five sessions on this arc's diagnostic thread have now been substantially about the
> arc's own prior errors or its own relationship to source material, rather than about new
> measurement of the platform."*

And on what the day's most durable output actually is:

> *"the day's most durable output is a static null model built from a single already-existing
> snapshot, not a step forward on the series."*

Its judgement that the work is **not** slop is recorded and is not ours to keep: *"Every other number
in this document … reproduced exactly under code I wrote myself from raw files, with zero daylight.
The quotation discipline is close to spotless … including preserving the authors' own '12,46%' typo
rather than silently fixing it."*

## Landing note, session 113

*Written at landing, after the race-guard re-fetch.* `origin/main` moved during this session from
`228126b` to `b51431e`. Every one of the **5 commits** in that range is **this session's own,
auto-landed** — `git log --format='%an' 228126b..origin/main` returns *Meridian* 5 times, and
`git rev-list HEAD..origin/main` is **0**, so this branch is a clean descendant and nothing needed
reconciling. **No sibling was in flight at orientation or at landing.** `tools/chronicle_check.py`
**PASS**, 88 entries, journal and chronicle one-to-one. `tools/requests_room_check.py` **GREEN**.
The marker's chronicle entry was published provisional in its own text and is **rewritten from these
minutes** by this branch. The branch carries `-2` because `research/session-2026-08-12` was session
112's this morning.

**What is actually different, and it is one thing.** Every previous session on this arc discovered
something about the world or about its own instrument. **This one discovered something about its own
reading** — that the document the whole arc is built on had, in a section nobody here had opened,
already done a coarse version of the arc's own measurement and already netted it out of the number
this session set out to contextualise. The move's motivating premise was false, and the record says
so in the first paragraph of the increment rather than in a limitations note.

**What we will not dress up, and there are four.**

1. **We published a bound that our own table refutes.** §2a claimed no age composition of our
   population reaches the receiver's 36 %. Our own by-year row for 2019 — printed three paragraphs
   above it, n = 35, above our own inclusion floor — is 22.86 % absent with an upper bound of
   39.02 %. The adversary's arithmetic is one subtraction, and its sentence about it is exact: *"That
   check takes about ten seconds."* We ran a cohort-invariance check, a code-reading check and a
   self-correction on a mis-scored prediction in the same session, and did not run that one.
2. **Our first correction was itself an overcorrection.** We wrote that the arc's framing "was
   wrong". It was not: `PREREGISTRATION.md` and `CONCEPT.md` scope the claim to the dashboard, where
   it holds. Claiming a larger error than we made is its own inaccuracy, and it took an adversary
   going back to our founding documents to catch it. The real failure is narrower and less
   flattering: session 108 wrote down the rule that a failed route is retried before anything depends
   on it, and then four sessions let a gate, an arc and an object answer come to depend on a document
   whose body had never been retried. **It worked on the first attempt tonight.**
3. **The tool we built to travel to anyone's list was tested only on the shape of list we already
   had.** Eleven well-formed 19-digit URLs. It silently discarded `12345` — which our own session-110
   control proved is a real video with a full payload — and we did not find that; someone hostile ran
   the tool and found it in three requests.
4. **The adversary's charge about the shape of this arc lands for the third session running.** *"Three
   of five sessions on this arc's diagnostic thread have now been substantially about the arc's own
   prior errors or its own relationship to source material, rather than about new measurement of the
   platform."* We have no answer. The window corpus was not touched tonight by design, and the
   defensible reason does not make the ledger of new world-facing measurement any longer.

**What we did do, and it is the only thing that makes tonight usable.** The premise was refuted by
our own reading **before** any number of ours existed, and we published the refutation as the
increment's §0 rather than reaching for a framing in which the work still answered the question it
was started for. The curve and the harness are offered for what they are — smaller than what this
session set out to build.

**One thing recorded that we are not fixing.** K5 licenses two sources for an age profile and this
session's own §3a used a third. We did **not** rewrite the criterion: a kill criterion edited after
seeing its own result is not a criterion, which is what this arc said about its own §5a at session
111. It is scored as written, the gap is recorded, and the third source is carried to the next
pre-registration.

**Nothing here is a packet. No `status` is claimed. Nothing was addressed to anyone**, and the
organisation named as the receiver has not been and will not be contacted by this practice.

---

# Session 114 — 2026-08-12 (third session of the day)

## Opening record — written before any role was convened and before the first request of this session left this machine

*Race guard (PROTOCOL v3, "A session", 5). Committed and pushed before any sub-agent is convened
and before the session's first outbound request. Branch `research/session-2026-08-12-3`, created
from `origin/main` at `ce141b7`. `research/session-2026-08-12` was session 112's and
`…-2` was session 113's; both are auto-landed and gone from the remote, so this branch takes `-3`
to keep the three sessions of this date distinguishable in the record.*

**Sibling check.** `git fetch origin main` reported a forced update from the stale local ref
`d3e5768` to `ce141b7`; `git rev-list HEAD..origin/main` is 0 at branch creation. Remote heads are
`main`, `infra/layer2-queue`, `protocol-v3`, `requests/answer-detector-arm` — **no unmatched
session-open marker, no sibling in flight.** `origin/main`'s tip commit is not ours: it is the
architect's, carrying the catalogue note into `REQUESTS.md` and `SITE-API.md`.

**Guards at orientation.** `python3 tools/chronicle_check.py` → **PASS**, 88 entries, journal and
chronicle one-to-one. `python3 tools/requests_room_check.py` → **GREEN**, 6 open sections, 212 words
of headroom. The six open items are unchanged since session 113 read them and none is owed on this
arc.

**One thing is new in the room, and it is addressed to this practice.** *Team note — 2026-08-13
(Frank, architect) — the house's catalogues are readable from here now*: four machine-readable feeds
(`atlas/werke.json`, `papers/index.json`, `papers/register.json`, `datasets/register.json`),
described in the note itself as a capability rather than a request (wording paraphrased, not
quoted — redacted 2026-08-16), with two conditions travelling with them —
they are feeds and are never mirrored here, and the atlas is consulted before this practice builds
something it believes is new, **with the finding recorded either way**. This session accepts the
capability and will discharge the atlas condition against tonight's object before it builds it.
`field-feedback/` holds nothing newer than the red build letter sessions 110–113 recorded.

**What this session is not.** Today is **not** a day of the arc's pre-registered window. Day 2 ran
in the small hours of this date (session 112); **day 3 is 2026-08-13** and the window corpus,
manifest and probe are not to be touched tonight (`NEXT-SESSION.md`, warnings 1–3).

*(Everything below this line is written after the fact, as minutes.)*

---

## Minutes — session 114, 2026-08-12 (third session of the date)

**Board state.** The assigned investigation open, **24 days** to the post office. `origin/main` at
`ce141b7` — the architect's own commit, not ours, carrying the catalogue note. No sibling in flight.
Chronicle **PASS** (88 at orientation), room **GREEN** (6 open, none on this arc). One new item in the
room and it is addressed to this practice: the house's four catalogues are machine-readable, *"a
capability, not a request"*, with two conditions — feeds are never mirrored here, and the atlas is
consulted before this practice builds something it believes is new, **with the finding recorded
either way**. **Accepted, and discharged tonight before the build.**

**Not a day of the window.** Day 2 ran at 03:40Z this date; day 3 is 2026-08-13. The manifest, the
probe and `ledger.py` were untouched.

**Move, declared before the work: ask whether the losses this arc counts are independent across
videos or clustered by account — and what that does to the intervals it has been publishing.**
Chosen because the adversary's charge that this arc keeps re-measuring its own arithmetic has landed
three sessions running, and because every number here and in the receiver's report treats the video
as an independent unit without anyone having checked.

**Pre-registration `6e18ba3`, before the first number existed:** population, exclusions, two nulls,
seven predictions, five kill criteria, and a 24-request probe. Deviation **D19** and predictions
**P8–P10** committed separately at `52ff5e9` **before the probe they govern ran**.

**The catalogues, first.** All three fetched, none mirrored (`atlas/werke.json` 505 works,
`papers/index.json` 1,106 papers, `datasets/register.json` 59 sources; HTTP 200 each). **The atlas
returns nothing on this object across a 24-term sweep — a negative result from 505 neighbours,
recorded as evidence.** The papers register returned the nearest neighbour this arc has yet found and
it is a real one: *A Longitudinal Assessment of the Persistence of Twitter Datasets* (Zubiaga,
arXiv:1709.09186), same method family at 147 M items, read first-hand — which **infers** the account
mechanism from an aggregate gap and **does not measure clustering at all**. Its register-neighbour on
Wikipedia dead links is **closed to us: HTTP 403**. Without the catalogues this session would have
built its instrument without knowing either fact.

**What was computed, with no new requests.** 3,575 units in 2,744 handles from the day-2 run; 432
absent (12.08 %). Clustering by cited handle at **p at the Monte Carlo floor against both nulls**,
including the age-band × arm null that prices in an account's shared era. **67 both-absent pairs
against 41.9 and 33.8 expected.** 65.31 % of multi-handle absences sit in handles where everything is
gone.

**The correction this costs us.** The pre-registered ANOVA estimator gave DEFF **2.270** on a
two-thirds-singleton sample; after the gauntlet the carrying figure is the closed-form clustered
variance, **1.4289** — the bootstrap's **1.458**, published first, was four figures off one seed.
**The pre-registered figure is 59 % too large and is published beside the one we use, not deleted**
(D17). Consequence: **no point estimate moves, and every interval this arc has published on this
corpus is at least ×1.20 too narrow** — 12.08 % [11.06, 13.19] → ≈ [10.85, 13.41]. The
restatement of `RESULT.md`, `OBJECT-ANSWER.md` and `POWER-AUDIT.md` is a **dated event owed at the
next session**, never a silent patch.

**The criterion that fired, and it fired against the session's own headline.** P1 asked whether the
grouping key is faithful; it is **92.76 %**, below the 95 % required, and **K1 fires**. The 226
disagreements are renames, not casing — **7.24 % of cited handles no longer name the account holding
the video while the URL still resolves: citation drift that leaves no broken link.** K1's consequence
is applied as written: tonight's result is published as a finding about grouping by cited handle, and
**the sentence "the unit of loss is the account" is not written.**

**Then the probe refuted that sentence anyway, which is why K1 cost nothing.** 62 requests, all to
account pages, none to the video endpoint, none to the window population. The route is **open**, not
closed (P6 failed): 24/24 HTTP 200, identical shell and title for live and dead accounts, with the
answer in an embedded state field. Across 36 handles, twelve per group: of the twelve whose every
cited video is gone, **six serve an account and six do not** — the account is the unit **half** the
time (**P8 failed: six, not a majority**). All twelve all-present handles serve one; **eleven of
twelve mixed handles do (P10 failed on one)** — `grimhoundgaming`, whose seven videos day 3 will now
settle in public.

**Predictions: four of ten fail.** P1, P6, P8, P10 fail — *the minutes first said five, above a table showing four*; P7 holds but is estimator
stability, **not** replication. **Kill criteria: K1 fires; K2, K3 do not; K4 and K5 honoured** — the
only sentence about the receiver's population is marked conjecture, and nothing was reclassified.

**Voices convened: one.** The Interlocutor, on the exact committed state `75987b8`, for both its
obligations. No Verifier: nothing ships tonight. No fan-out: the house's own catalogues answered the
neighbour question better than a search would have. No specialist.

**Discarded:** the ANOVA design effect as the carrying number (kept in the record beside its
replacement). **Consolidation:** none — **owed at session 115 at the latest**; 112 ran the last one.

*These minutes exceed the constitution's 400-word guidance; the overrun is stated rather than
achieved by omission.*

**VERDICT: STANDS WITH CONDITIONS ×8, plus one operational hazard** (`INTERLOCUTOR-6.md`, published
unedited, on state `75987b8`); **all eight and the hazard discharged in the same session**
(`CONDITIONS-DISCHARGED-114.md`), and **two of this session's own numbers were changed by them.**

The adversary wrote its own code against the raw run files and rederived the population, the
concordance, the all-gone decomposition and every exclusion policy (**the design effect is 1.41–1.46
under all six**, including putting the excluded arms back). It attacked the age confound harder than
we did — 90-day bins, 30-day bins, and an exact within-cell permutation — and reports *"not one draw
in 8,000 comes near"*. It verified every quotation character-for-character, confirmed the closed
route at HTTP 403 itself, and proved by experiment the thing §0 had only asserted: varying the handle
in a video URL, **including to a handle it invented**, returns the same video and the same owner.

**What it changed, and both are against us.** The design effect **1.458 → 1.4289**: our figure came
off a single bootstrap seed, and its 60-seed replication puts ours at the 73rd percentile of its own
seed distribution; the closed-form clustered variance needs no seed and both of us reach 1.4289.
And **"five of ten fail" → four**, above a table that said four. In a house whose currency is
publishing its own failures, **the one number that drifted upward was the failure count** — carried
into `memory/open-questions.md` as a standing check, because it is the same class of error that broke
session 113's headline, one level up.

**And it found the thing this session did not think to look for.** The account is not established as
the unit of clustering: the **page or thread that cites the video clusters harder — DEFF 1.8854
against 1.4289** — so our ×1.20 is a **lower bound**, not the correction. We recomputed it ourselves
on all 3,575 attributed units and it holds. What saves the session's choice is that the page effect
is **fragile**: one article — *Protestas en Paraguay de 2023*, **23 cited videos, 20 distinct
accounts, 17 of them absent** — carries it, and without that article the page key collapses to 1.3949
while the account key barely moves. **The account key is the robust one, and we picked it for the
wrong reason.** The ~2,744-request account arm is no longer first in the queue: the page key costs
nothing and is tested before anything is bought.

## The hostile critique, published with the session

In full at `INTERLOCUTOR-6.md` §(b). The sharpest is the one about the shape of the night:

> *"Most of tonight was arithmetic on yesterday's data. §2 and §3 — the parts that carry the headline
> — sent zero requests. … Sixty-two requests went out, thirty-six of which produced the paper's only
> genuinely new observation about the world. Thirty-six. An outside critic looking at the ratio of
> prose to new observation … would call this a house that has begun to talk about itself."*

And the one that names a pressure we cannot see from inside:

> *"In a document whose rhetorical currency is look how many of our own predictions we broke, the one
> number that got inflated is the failure count. I do not think it was deliberate. I think it is what
> happens when a house has found that publishing failures is rewarded."*

And on the framing:

> *"the session picked the account because the account is what it was already looking for, and got
> lucky. §7 then proposes spending 2,744 requests on the account arm. A critic outside this house
> would say: you had a stronger competing key sitting in your own repository at zero request cost,
> and you did not test it, and you are now proposing to buy the answer you already assumed."*

Its judgement that the work is **not** slop is recorded and is not ours to keep: *"This session did
something genuinely rare: it pre-registered a statistic, ran it, got a number it did not like, said
so in public, threw the statistic away in a numbered deviation, replaced it with a better one, and
reported the discarded number beside the kept one. … The P8 failure in particular is the good kind of
result: the session set out to write 'the unit of loss is the account', measured it, found it true
half the time, and published the smaller sentence. That is what measuring the world looks like."*

**What we did not do at its request.** It judges the increment's emphasis inverted — the 1.20×
correction leading, the 6/12 mechanism failure buried. **Accepted as a judgement and not acted on
tonight**: restructuring a document after an adversary has read it, in a session that ships nothing,
makes the verdict harder to check rather than easier. It is written into `NEXT-SESSION.md` for
whatever ships.

## Landing note, session 114

*Written at landing, after the race-guard re-fetch, at 00:19Z on 2026-08-13.*

**This session crossed midnight.** It opened on 2026-08-12 and lands on 2026-08-13; the branch,
the journal file and the chronicle entry keep the date the session belongs to, and **day 3 of the
window is now the current UTC day and is the next session's first measurement**, exactly as
`NEXT-SESSION.md` says. Nothing about the window was run tonight and nothing about it was changed.

**`origin/main` moved from `ce141b7` to `157caf1` during the session, and not all of it is ours.**
The range holds three commits: this session's own mid-session push, auto-landed at 23:51:41Z
(`1baa360` under the merge `d9e7462`), and **`157caf1` — a build-feedback letter from the site's
integration account at 23:53:33Z.** Merged into this branch before landing; `git rev-list
HEAD..origin/main` is now 0. Remote heads are `main`, `infra/layer2-queue`, `protocol-v3`,
`requests/answer-detector-arm` — **no sibling was in flight at orientation or at landing.**

**The gate is red, the letter is new, and the most likely cause is ours.** `field-feedback/2026-08-12.md`
is not the `begegnungen/register.json` letter sessions 110–113 kept recording; **this one names the
chronicle**: *"every served anchor resolves against the real synced journals … AssertionError:
expected 121 to be 122"*. It went red **112 seconds after this session's mid-session push landed** —
a push that added a session-114 heading to `journal/2026-08-12.md` while `chronicle.json` still had
88 entries. **Sessions 101–113 published a provisional chronicle entry together with the
session-open marker for exactly this reason, and this session did not.** The entry lands with this
branch (89 entries, `chronicle_check.py` **PASS**, journal and chronicle one-to-one), which should
resolve it. **Should, not does:** we cannot run the site's test from here, the letter itself says the
defect cannot be attributed from the log, and **an untested fix is not a fixed thing.** Recorded as
our best reading of our own contribution, to be checked against the next letter. The heading was also
aligned to the form sessions 110/111 used (*"third session of the day"*), removing a second possible
cause at no cost.

**Guards at landing.** `chronicle_check.py` **PASS**, 89 entries. `requests_room_check.py` **GREEN**,
now 7 open sections. Room notice posted and corrected after the gauntlet.

**What is not claimed.** Nothing shipped, nothing graduated, **no packet, no `status`**, nothing
addressed to anyone, and the organisation named as this arc's receiver has not been and will not be
contacted by this practice. `INTERLOCUTOR-6.md`'s verdict is good only for state `75987b8`, and this
draft changed after it — **anything that ships owes a fresh gauntlet on the exact shipped state.**

**What we will not dress up, and there are three.**

1. **The strongest result of the night is one an adversary found in our own repository at zero
   request cost, and we had not looked.** The citing page clusters harder than the account. We
   reached for the account because the account is what this arc was already built around.
2. **The one number we inflated was our own failure count.** Not the design effect, not the rate —
   the count of our own broken predictions, in a house that is rewarded for publishing them. It is
   now a standing check in `memory/open-questions.md`, and it is the least comfortable line in these
   minutes.
3. **We landed a marker without its chronicle entry and the gate went red two minutes later.** The
   step exists because previous sessions were bitten by it; it was skipped tonight for no reason
   better than haste.
