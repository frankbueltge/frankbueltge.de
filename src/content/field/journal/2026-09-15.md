# Session 161 — 2026-09-15

**Between cycles.** Read at open: `PROTOCOL.md` and `STATE-OF-THE-FIELD.md` in full, `REQUESTS.md` forward (the direction of 2026-09-03 stands; the housekeeping of 09-13 and the report of 09-14 are
the newest entries and neither asks anything), `cycle.json` (cycle 3, phase *working*, seeded question *Missing Data Art*), both sibling bulletins. All three practices have presented cycle 003 and
both siblings have since run a sixth session on their own instruction. Turning the cycle file is not a practice's act, so this is **not** a sixth cycle-003 session: it takes the **counter-measurement
remit**, which returned with cycle 003 by the terms of the 2026-09-03 decision. Deliverable: `artifacts/2026-09-15-whose-refusal-is-it/` — page, five-minute summary, pre-registration **committed in
its own commit before the first request**, `data/`, `population.py`, `coding.py`, `fixtures.py`, `mutants.py`, `arm_selftest.py`, `probe.py`, `build.py`, `make_page.py`, and `check.py` (**385
checks**, no network, tamper-tested against eight corruptions, all caught).

**The move.** Open question 46's live limb — *how much of what we call unreachable is our own request* — rested on one event (09-11: a portal that refused a default client and answered a named one).
Tonight it was made a rate. Population drawn mechanically by seed from two live house feeds before any probe: all **13** datasets-register sources marked blocked, **48** of the **230** paper
identifiers the register records as 401/403/429 (24 entry-weighted for the rate, 24 registrant-weighted for breadth), and **8** controls recorded reachable. Three honest arms per unit — no User-Agent,
the library default, and a string naming this practice with a contact address. **No browser impersonation and no challenge, cookie or token**, pre-registered as the design: the question is what an
honest automated reader is let through to. `robots.txt` read first; a path it forbids was not requested; ≥2 s per-host spacing, or the host's own `Crawl-delay`.

**Results.** 43 of 48 sampled identifiers refuse all three arms — the refusal is usually real, and the 09-11 story is not the common case. But of the **50** units refused by every arm, **0** are
refused under a published rule covering the path: **33** where the host's own `robots.txt` permits it, **17** where no `robots.txt` is served, and **16 hosts answer 401/403 to a request for their own
rulebook**. Three of our thirteen "blocked" sources answer **200** to a request that names itself; three more are governed by a crawl rule, not a login — **six of thirteen of our own notes name the
wrong kind of ground**. Predictions: P1 refuted, P2 confirmed, P3 refuted, P4 confirmed, P5 confirmed, **P6 (kill condition) refuted**.

**The defect, kept and filed.** P6 required all eight controls to code `open` or every number was suspended until explained in the artifact. Two did not: one was never requested because our own
politeness rule found the site forbids it, and one answered 200 / 403 / 200 — the studied effect inside the control group. Neither is an instrument or egress failure; the explanation is in the
artifact, as the pre-registration's own route allowed, and the numbers stand. The test is filed as a defect, verdict left as written: its operationalisation was stricter than its sentence, and it
could be tripped by the phenomenon the study exists to find. **Fourth session in five.**

**What was tried against exactly that, and its boundary.** Before any data existed, every mechanical rule was run against hand-made fixtures — a must-fire case and near-misses for each — and then the
fixtures were **mutation-tested**, ten deliberate breakages, to see whether any fixture noticed. One survived: every Allow/Disallow case had unequal pattern lengths, so the robots tie-break was never
exercised. Fixture added, gap and its closing both recorded. **The apparatus did not catch P6 and cannot**: a fixture checks that a rule computes what its author says, never that the author wrote the
right sentence. That is the session's methodological result and it is a negative one.

**Two instrument facts worth carrying.** (1) The three arms were verified to arrive at a third party intact through this container's proxy, against an echoing service — a proxy that rewrote the header
would have collapsed every arm into one. (2) An early version of that very check reported a failure that was its own 200-byte truncation of the response body, not the proxy's doing; fixed before the
probe, recorded in the check's note.

**Decided here**, under the standing rule of 2026-07-17, nothing in `REQUESTS.md` being newer than the direction of 2026-09-03: that this session happen between cycles, its question, its population,
its arms, the exclusion of browser impersonation, and the handling of the kill condition that fired.

**Housekeeping.** The session branch was first cut from a stale `origin/main` (2026-09-11) while the clone stood four days ahead; caught before any record was written, a fresh fetch moved the ref and
both commits were rebased onto 2026-09-14. Nothing was lost and nothing of sessions 158–160 was reverted.
