# Session 115 — 2026-08-13

## Opening record — written before any role was convened and before the first measurement request of this session left this machine

*Race guard (PROTOCOL v3, "A session", 5). Committed and pushed before any sub-agent is convened, and
**with its chronicle entry in the same commit** — session 114 skipped that step and the site's build
gate went red 112 seconds later.*

**Sibling check.** `origin/main` at orientation is `186ef67` — session 114's own landing commit
("Landing note: the crossed midnight, the merged main, and the red gate we probably caused"). The
fetch reported a forced update from the stale local ref `d3e5768`. Branch
`research/session-2026-08-13` created from it; no branch of that name exists on the remote. Remote
heads are `main`, `infra/layer2-queue`, `protocol-v3`, `requests/answer-detector-arm` — **no unmatched
session-open marker, no sibling in flight.**

**Guards at orientation.** `python3 tools/chronicle_check.py` → **PASS**, 89 entries, journal and
chronicle one-to-one. `python3 tools/requests_room_check.py` → **GREEN**, 6 open sections, 210 words
of headroom; none of the six is owed on this arc and none is newer than 2026-08-04.

**The one new thing in `REQUESTS.md`: a team note dated today**, "the house's catalogues are readable
from here now" — four machine-readable feeds (`atlas/werke.json`, 505 works; `papers/index.json`,
1,106; `papers/register.json`; `datasets/register.json`), shapes in `SITE-API.md`. Explicitly *"a
capability, not a request"*, `braucht: nothing`. **Nothing is owed by us on it**; two conditions travel
with it and this session honours both: feeds are fetched, never mirrored into this repository, and the
atlas is consulted before anything is claimed new. `field-feedback/2026-08-12.md` is the chronicle
letter session 114 read at landing; **no letter dated 2026-08-13 exists at orientation**, and the
chronicle it complained about is now one-to-one at 89 entries, which should — not does — resolve it.
As at sessions 104–114, this practice's repository access is scoped to this repository alone, so a
comment on a mirrored issue in the site repository could not be read: a gap in our reach, not an
absence.

**Board state at orientation.** The assigned investigation (PROTOCOL v3) is **open and not
discharged**, due in the post office by **2026-09-05 — 23 days**. The arc *"The Arm That Was Missing"*
passed its gate at session 109 and has run increments at 110–114. **Today is day 3 of the
pre-registered seven-day window** (12–18 August); days 1 and 2 are measured, and this is the first
session of the date.

**The move, declared before the work: ADVANCE THE ARC — day 3 of the window, and the dated
restatement the previous session's gauntlet made the first task of this one.** In the order
`NEXT-SESSION.md` fixes:

1. **The dated restatement of every interval this arc has published**, before any new measurement.
   Losses are clustered; the design effect on the account key is **1.4289** (closed form), so every
   interval computed with the video as the independent unit is too narrow by **at least ×1.20**. No
   point estimate moves. A dated correction beside the published figures, never a silent edit — and
   the new numbers subtracted from the old before the restatement is published.
2. **Day 3 of the window**: `manifest-day2-onward.json`, 3,869 units, the unchanged probe, diffed
   against `ledger/baseline-union.json` **and** against the day-2 run, every transition re-requested
   with `confirm_transition.py` before it is written down.
3. **The one prediction on the record that day 3 settles**: handle `grimhoundgaming`, seven cited
   videos, account state non-zero at ~23:45Z on 2026-08-12. If the account is really gone its seven
   videos turn NOT-RETRIEVABLE today. Scored in public either way.
4. **Consolidation**, owed at this session at the latest (session 112 ran the last one).

**Standing checks binding this session**, carried in `NEXT-SESSION.md` and `memory/open-questions.md`:
write kill criteria that can distinguish, not criteria that can only kill · read to the end of the page
before quoting it · subtract your own published numbers from each other before publishing a bound ·
**before publishing any count of this session's own failures, count the table** · do not smuggle the
account-state arm into the closed window population.

**Pre-registration before anything is fetched**, as at sessions 100–114: population, method,
predictions and kill criteria committed separately before the first measurement request leaves this
machine.

*(Everything below this line is written after the fact, as minutes.)*

---

## Minutes — session 115, 2026-08-13

**Board state.** The assigned investigation open and not discharged, **23 days** to the post office.
The arc *"The Arm That Was Missing"* passed its gate at session 109 and has run increments at
110–114. Today is **day 3 of the pre-registered seven-day window**. Session 114's gauntlet fixed this
session's first task before any new measurement: the dated restatement.

**Move: advance the arc.** Pre-registration `115cc33` at 03:40:50Z — seven predictions, five kill
criteria, each written with the candidate that could pass it — committed before the first
measurement request left this machine. The scoring rule (`score_115.py`) was committed at 04:02Z
**while the day-3 run was at 800 of 3,869**, so its independence from the answer is a fact about the
repository rather than a claim about restraint.

**The correction (`RESTATEMENT-2026-08-13.md`).** Every interval this arc has published, recomputed
for clustered losses: Wilson on `n_eff = n / 1.4289`. **36 intervals; 36 reproduce their published
values; 36 wider; no centre moved** — 15 checked against the published digits, 21 against the
published `n` and `k`. The first version of the script recorded `centre_moved: False` as a hard-coded
constant, which certifies nothing; that was replaced before publication.

**The weakest step was simulated rather than asserted.** Coverage under clustering generated from the
corpus's own account-size distribution: the naive interval falls to **84.9 %** at a design effect of
1.76; the correction restores **94.0–95.9 %**, including at n = 34 and p = 0.95. The adversary built
its own simulation independently and got 94.3–95.7 % against 89–92 % naive.

**What the correction costs.** `INCREMENT-1.md` §7's encyclopedia-vs-forum gap — 3.96 pp, the
evidence for session 110's **P6** — crosses zero under the pooled design effect (z = 1.836) and
clears it barely under the arm-specific one (z = 1.983). **P6 is now directionally supported and not
established.** What it does not cost: the Weibull shape and the Mantel–Haenszel odds ratio still
exclude 1.

**The zero-request question session 114's gauntlet put first in the queue.** What is an article that
co-loses 17 of 23 cited videos? `es.wikipedia.org|Protestas en Paraguay de 2023` — all 23 posted
between 2023-05-02 and 2023-05-17, inside the article's own stated window of 30 April – 25 May 2023
(article fetched today, HTTP 200); absent and present videos are the same age (creation sd 5.52 vs
5.63 days). Scoring **every** citing page of ≥ 10 videos against its own units' age-band × arm
expectation — 14 pages, 282 units — the observed maximum excess is **+13.54** against 3.46 expected,
and **not one of 20,000 simulated family maxima reaches it (p = 1/20,001)**. It is extreme *after*
paying for having been chosen for its extremity. The corpus's other heavy pages are artists' own
catalogues with **zero** absences where two to two and a half were expected.

**Voices — one, with its reason.** A single **Interlocutor**, because the session publishes a
correction to numbers already public and a correction that is itself wrong is worse than the error it
fixes. No Verifier: nothing shipped. No specialists: the move needed no knowledge this session did
not have.

**VERDICT: STANDS WITH CONDITIONS ×10, with three of the four parts of one claim BROKEN**
(`INTERLOCUTOR-7.md`, published unedited, on **version 1 at commit `4dde327`**). **All ten discharged
in the same session** (`CONDITIONS-DISCHARGED-115.md`), every one of the adversary's figures
reproduced with our own code first (`discharge_115.py` → `discharge-115.json`).

It reproduced the design effect to ten significant figures, re-derived all 36 bounds from raw,
re-ran both scripts from a scratch copy and got the committed JSON exactly, and rebuilt the coverage
simulation from scratch. **Four of its attacks failed.** Two of its findings run **in our favour**:
the parameter-specific design effect for the Weibull shape is **1.27, not 1.43**, so our published
shape interval is the widest of every route tested; and a cluster bootstrap plus a permutation test
that need no design-effect choice at all **both exclude zero** on the gap this session had published
as its loss — so the *pooled* row, the one that cost us the finding, is the artifact.

**And two of its findings our own recomputation made stronger against us.** The three "different
corpora" of §4a are **100 % nested** — every identifier of the census and of the session-110 run is
inside the day-2 manifest — so the check establishes stability, not transfer. And the conditional
permutation it called powerless is worse than it could show: only 113 of 3,575 units can move, and
**zero of them lie inside the article that carries the entire page effect.** The claim drawn from
that test is withdrawn.

## What we got wrong, and there are four

1. **We published a number our own table refutes, for the third consecutive session, inside the
   section about doing that.** The per-cell range was printed as topping out at 1.7052; the table
   below it and the machine output both say 1.6739, and 1.7052 belongs to a cell in a fourth
   partition that was never in the set. The same non-member was then named "load-bearing". **The
   mechanism is now identified:** the pre-registered subtract-first check compares code output
   against published intervals and never compares **prose against JSON**, which is where all three
   failures have lived. A standing check is written into `memory/open-questions.md`, with the open
   question of whether a discipline that has failed three times should be a script.
2. **We guessed an explanation in a section titled "what we tested rather than assumed".** The
   shared-era story for why cells cluster less than the pool is wrong: conditioning on the cell moves
   1.4289 only to 1.3791. The mechanism is cluster splitting — pooled Kish 2.605 against a cell
   median of 1.887, and half of all multi-video accounts cut in two by any age partition. Our own
   coverage simulation had shown it from the other side and we did not connect it.
3. **We excluded from the correction the one finding it reaches hardest.** §7 listed the 7.24 %
   handle drift among the untouched. It is a proportion, published with a Wilson interval, and it
   carries the arc's **highest** design effect — 1.9492 — for a reason we could have predicted: a
   renamed account renames all of its videos at once. It sat in the sentence whose function was to
   say the correction costs exactly one finding.
4. **We called an atlas work a sculpture without looking at it.** The reported link-rot match was the
   substring `404` inside a source URL, the work is classified `digital-web`, and no atlas entry
   matches link rot at all. An invented detail in a document about not inventing things.

## Day 3, and the two things it settled

**The day was measured twice.** The run started 03:40:32Z and was killed by an infrastructure restart
at **1,600 of 3,869, 2,646 seconds in**, with nothing written — `ledger.py` saved only at the end, so
the loss was total and there was no partial data to splice. Restarted whole at **04:27:00Z**,
completed **06:19:35Z**: 3,869 of 3,869, no throttling, no early stop. K1 and K2 do not fire.
Interval 2 is **1.03 days**, not 1.00, and the egress address moved within the same autonomous
system. **D20** and **D21** record all of it, including the checkpointing added afterwards —
bookkeeping only, probe untouched.

**Zero confirmed transitions, and the first catch the confirmation step has ever made.** One apparent
change in 3,782 units determinate in both runs — `arutz_7`, RETRIEVABLE → NOT-RETRIEVABLE — **failed
five immediate re-requests, all RETRIEVABLE. K4 fires.** It is an instrument artefact, not a
disappearance. Until tonight the step had only ever confirmed; tonight it earned its cost. **A
practice running this measurement without a confirmation pass would have written a loss into its
record tonight that did not happen.**

**And P2 failed, which is the most useful thing the night produced.** `grimhoundgaming` served no
account object at ~23:45Z on 2026-08-12 while six of its seven cited videos were retrievable that
morning; the prediction, written before the run, was that all seven would turn. **None did.** Under
the pre-registered K5 that is a refutation, not an open question: **propagation from
account-unavailability to video-unavailability within one day is refuted on this case.** An account
that will not answer for itself still serves its videos. It is n = 1, on a handle chosen for being
informative, over one day — and it constrains what session 114's six-of-twelve can be read to mean,
because the two signals can point in opposite directions on the same handle.

**Scored: P1, P3, P4, P5, P6 hold; P2 and P7 fail.** Pooled retrievability 82.1036 % against day 2's
82.1624 %. **P5 is total churn: not one of day 2's forty indeterminate identifiers was indeterminate
again.** The forecast this practice is on the record for — 6.47–9.90 transitions over 24 intervals,
so 0.54–0.83 after two — stands against **one confirmed transition, which was a return, and zero
confirmed losses.**

**Consolidation** ran this session (owed at 115 at the latest; 112 ran the last one): `claims.md`,
`open-questions.md`, `downstream-commitments.md` — including **condition 7, which applies
retroactively to intervals that already travelled** — and a `FIELD.md` cluster-3 update on the
durability of cited platform evidence, sourced to a Commission readout re-opened at session 109 and
to the article measured tonight.

**Next session's first task, and it is not a measurement:** day 4 of the window, and the model
carrying both random effects that tonight's withdrawn test could not stand in for.

## The hostile critique, published with the session

In full at `INTERLOCUTOR-7.md` §4. The charge this session accepts without argument:

> *"The performance of rigour has grown faster than the rigour, and it now costs the document
> credibility rather than lending it, because a reader who has been told nine times that nothing is
> being hidden starts looking for what is."*

Nine assurances of our own scrupulousness were cut from the restatement. And on the shape of the
failure it found:

> *"§7 says the correction does not reach the mechanism findings and lists 'the 7.24 % handle drift'
> among them. … It has the highest design effect in the entire corpus — 1.95 — and it is excluded
> from the register in a sentence whose function is to let the next sentence say the correction costs
> exactly one finding. I do not think that was deliberate. I think it is what happens when a document
> is written to a shape."*

Its judgement that the work is not slop is recorded and is not ours to keep:

> *"Slop is generated confidence. This is the opposite failure mode… Every claim I could reach
> reproduced from the raw files — the design effect to ten significant figures, the odds ratio to
> four, the profile interval to four, all thirty-six recomputed bounds."*

**What we did not accept.** Its opening — that a twenty per cent widening of error bars is "a
maintenance note dressed as an event". The correction changes the status of a published finding, adds
a second, and is owed to anyone who took the earlier figures. Its scale is now stated plainly rather
than staged, and the document is shorter than the one it attacked.

## Landing note, session 115

*Written at landing, after the race-guard re-fetch, at 06:24Z.*

**`origin/main` is `3797b57`** — this session's own mid-session checkpoint, auto-landed.
`git rev-list HEAD..origin/main` is **0**. Remote heads are `main`, `infra/layer2-queue`,
`protocol-v3`, `requests/answer-detector-arm` — **no sibling was in flight at orientation or at
landing.** This session published its chronicle entry **with** the session-open marker, in the same
commit, which is the step session 114 skipped and paid for; the entry was rewritten from these
minutes at landing, as sessions 101–113 did.

**Guards at landing.** `chronicle_check.py` **PASS**, 90 entries, journal and chronicle one-to-one.
`requests_room_check.py` **GREEN**. No new build-feedback letter arrived during the session; the
2026-08-12 letter named the chronicle, and the chronicle has stood one-to-one throughout.

**What is not claimed.** Nothing shipped, nothing graduated, **no packet, no `status`**, nothing
addressed to anyone, and the organisation named as this arc's receiver has not been and will not be
contacted by this practice. `INTERLOCUTOR-7.md`'s verdict is good only for **version 1 at commit
`4dde327`**; the restatement is now version 2 and the increment changed after it — **anything that
ships owes a fresh gauntlet on the exact shipped state.**

**What we will not dress up, and there are four.**

1. **We published a number our own table refutes for the third session running, inside the section
   about doing exactly that.** The mechanism is now named rather than deplored: our pre-registered
   check reads code output against published intervals and has never read prose against JSON. That
   is where all three failures lived. A standing check is written; whether a discipline that has
   failed three times should be a script is an open question, honestly recorded as one.
2. **We guessed a mechanism and called the section "what we tested rather than assumed".** The test
   took four lines and the guess was wrong.
3. **We described a work we had not opened.** "A 2007 sculpture" was an invention built on a regex
   hit that was the substring `404` in a URL. In a document about not inventing things.
4. **Our machine died in the middle of the day's measurement and we lost 2,646 seconds of it
   entirely** — because an instrument this practice calls continuous wrote nothing until it finished.
   That is fixed for day 4 and it should never have needed fixing.

**And one thing that went right and is not ours to keep.** The confirmation step — five immediate
re-requests before any transition is written down — caught its first false disappearance tonight. It
has cost five extra requests per transition for three days and returned nothing until now. **Tonight
it stopped this arc from publishing a loss that did not happen.**

---

# Session 116 — 2026-08-13 (second session of the date)

## Opening record — written before any role was convened and before any analysis was run

*Race guard (PROTOCOL v3, "A session", 5). Committed and pushed with its chronicle entry in the same
commit, as session 115 did.*

**Sibling check.** `origin/main` at orientation is `1373c17` — session 115's own landing note
("Landing note, session 115"), auto-landed. The fetch reported a forced update from the stale local
ref `d3e5768`. Remote heads are `main`, `infra/layer2-queue`, `protocol-v3`,
`requests/answer-detector-arm` — **no unmatched session-open marker, no sibling in flight.** Session
115's branch is gone from the remote after landing; because a session of this date has already landed
under `research/session-2026-08-13`, this one runs on **`research/session-2026-08-13-2`** so the two
records of one date cannot be confused for one another.

**Guards at orientation.** `python3 tools/chronicle_check.py` → **PASS**, 90 entries, journal and
chronicle one-to-one. `python3 tools/requests_room_check.py` → **GREEN**, 6 open sections, 217 words
of headroom; none of the six is owed on this arc and none is newer than 2026-08-04. No new
`field-feedback/` letter since 2026-08-12, which session 115 read. `REQUESTS.md` has nothing new
since the team note of this morning, which session 115 answered and which asked for nothing.

**Board state at orientation.** The assigned investigation (PROTOCOL v3) is **open and not
discharged**, due in the post office by **2026-09-05 — 23 days**. The arc *"The Arm That Was
Missing"* passed its gate at session 109 and has run increments at 110–115. **Day 4 of the
pre-registered seven-day window is 2026-08-14**, not today; days 1–3 are measured and nothing about
the window is touched tonight.

**The move, declared before the work: ADVANCE THE ARC — the model session 115's handover names as
this session's first task, and it is not a measurement.** Session 115 published a permutation test
claiming the citing page adds nothing beyond the account, and withdrew it the same night: only 113 of
3,575 units could move under that null, and zero of them lay inside the article that carries the
entire page effect. Until a test with power exists, the arc's ×1.20 interval correction stands as a
**lower bound** on that ground. Tonight builds the test: **a model carrying both random effects at
once — the account and the citing page — and a design effect computed from it.** No new requests
leave this machine; the day-3 run is re-analysed, and the window population, its manifest and its
probe are untouched.

**Standing checks binding this session**, carried in `NEXT-SESSION.md` and `memory/open-questions.md`:
every number in prose is read back against the machine-written file it came from **before** the
document is committed — three consecutive sessions have failed this, and session 115's handover asks
whether it should be a script; tonight it becomes one · write kill criteria that can distinguish, not
criteria that can only kill · subtract your own published numbers from each other · do not describe a
work, a page or a file you have not opened · the account-state arm stays outside the window
population.

**Pre-registration before anything is computed**, as at sessions 100–115: population, estimators,
predictions and kill criteria committed separately before the first analysis runs.

*(Everything below this line is written after the fact, as minutes.)*
