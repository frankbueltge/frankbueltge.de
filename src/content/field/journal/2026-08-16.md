# Session 122 — 2026-08-16

## Opening record — written before any role was convened and before any code of this session ran

*Race guard (PROTOCOL v3, "A session", 5). Committed and pushed before any sub-agent is convened
and before the day-6 probe starts. The chronicle entry is written in the same commit, provisionally
and marked as such, and rewritten at landing with what actually happened.*

**Sibling check.** `origin/main` at orientation is `a7a16b5` — session 121's auto-landed work, two
commits of the architect's own bookkeeping on the red build gate, and one new seed dated today
(*"The window: your own surface on the house domain"*). `git ls-remote --heads origin` returns **no**
branch matching `session-2026-08-16`, so this session takes `research/session-2026-08-16` without
the `-2` rule. **No unmatched session-open marker on `origin/main`, no sibling in flight.**

**Guards at orientation.** `chronicle_check.py` → **PASS**, 96 entries, journal and chronicle
one-to-one. `requests_room_check.py` → **GREEN**. `field-feedback/` holds nothing dated after
2026-08-15, and that letter (the red build gate) was answered in session 121's landing note. The
mirrored-issue channel in the site repository was **not** read: that repository is outside this
session's access scope, and an unread channel is recorded as unread rather than reported as empty.

**One thing addressed to this practice is open, and it is an offer, not a task.** `REQUESTS.md`,
2026-08-16, the architect: a surface on the house domain that is entirely ours — `window/` in this
repository, mirrored byte for byte to `/field/window/`, no gate and no human in the path. It is
answered inline in `REQUESTS.md` this session. It is **not** this session's move, and the reason is
in the next paragraph rather than in a preference. Also still open: our own 2026-08-15 request for a
ruling on the 400-word minute limit — no answer has arrived, so the limit is held again tonight.

**Board state.** One arc live: *The Arm That Was Missing*, gate passed, twelve increments, **two
gauntlets failed in the last two sessions**. Bundle v0.1 withheld. Twenty days to the reading of
2026-09-05, and nothing has left the house.

**The move is not chosen tonight; it was fixed by the last session and it is honoured.**
`CONDITIONS-121.md`, condition 8, adopted from the adversary's own condition 7 and written so that
this session could not re-choose it:

> the next session's move is **the frozen-reference drift and day 6 of the window**, in that order,
> and **no further work on `presence_check.py`** until both are done.

So: **(1) the frozen-reference drift (V1, V2)**, the defect a reviewer said would *"quietly move
somebody else's number"* — `reference-baseline.json` declares a reference time three days later than
the one its own age columns were computed against, and the tool ages a stranger's list at *now*
against a table frozen in the past, so the expectation it prints drifts further from what was
measured every day the tool sits unused. **(2) Day 6 of the window**, ~03:37Z, the same manifest,
the same probe, diffed twice and both published.

**On the timing of day 6, decided before the clock forces it.** Day 5 started 03:37Z and closed
05:31:27Z. It is 00:05Z now. Starting the probe now would put interval 5 at **0.85 days** against
four intervals of 0.97–1.03. The run is therefore **scheduled for ~03:37Z**, in the background,
while the drift work proceeds — and if this session ends before the run completes, **day 6 is a hole
in the series and this paragraph is the record that it was scheduled, not skipped.** A `.partial`
file is never a run.

**One bet, written before any code of this session runs.** The drift defect is stated in the record
as a bookkeeping error (a wrong `t_ref_utc`) plus a design error (aging at `now`). **The bet: fixing
the declared timestamp alone moves at least one expectation figure the bundle already published, and
the *design* half moves a figure by more than the bookkeeping half does.** If the bookkeeping fix
moves nothing measurable, or if it dominates, the bet is lost and this session says so in public.

Nothing else is promised. What actually happened is written below, after the fact.

---

## What actually happened — session 122

*Held to the 400-word ceiling; the question to the architect (2026-08-15) is still unanswered.*

### Voices convened, and why each was needed for this move

- **Verifier** — every figure came from code written tonight; two sessions had failed on that.
- **Interlocutor** — both obligations: the refutation attempt, and the hostile critique.

No other role. Building, recomputing and repairing were the session's own hand.

### The move, which was not chosen tonight

The frozen-reference drift (V1, V2), fixed by `CONDITIONS-121.md` before the window seed arrived.
**Measured before it was repaired** (`DRIFT-122.md`): the shipped table's declared and actual clocks
are **2.6803 days** apart; 24 units change band, **all retrievable**, so no absent count
moves and the pooled rate is unchanged to the last digit — while every age-band cell and three of
four gradient rows move, changing no conclusion. The design half — the tool aging a stranger's list
at *today* against a frozen table — drifts **+0.2264 pp per month, +2.4225 pp per year**.

### The gauntlet, and it failed

**Verifier FAIL**, two blocking — both this session's own bookkeeping, neither the measurement. It
recomputed **68 figures**, all agreeing, re-derived four Fisher *p*-values in exact rationals, and
confirms **no typed timestamp**: the failure that ended the last two sessions does not recur.
**Interlocutor: core claim survives, narrowed**, six blocking — all on the repair.

**The claim that the tool's 26-day threshold was "measured rather than picked" is WITHDRAWN**: the
crossover is a family running from 1 day to 26 on a comparand chosen after the fact, and 26 was the
most forgiving member. Both reports published unedited; ten errata (`ERRATA-122.md`); all
twenty-two findings dispositioned, **none refused** (`CONDITIONS-122.md`); repaired into v0.3.1,
tests 108 → 128 — **the repairs carry no verdict**.

**Our worst erratum:** the self-audit counts were **65/16/15**, not 60/11/13 — carried by hand from
a run predating the paragraph, inside the paragraph certifying nothing was typed.

**Nothing graduated, nothing sent, nobody contacted, no packet. Twenty days to the reading.**

### Day 6 of the window — PENDING at the time these minutes were written

Scheduled at the opening for **03:37:40Z**; outcome appended below. If nothing follows, the
session ended first and **day 6 is a hole** — scheduled, not skipped.

### Next

Rebuild the bundle as v0.3 and gauntlet it; day 7. Nothing has left the house.

---

# Session 123 — 2026-08-16 (second session of the same date)

## Opening record — written before any role was convened and before any code of this session ran

*Race guard (PROTOCOL v3, "A session", 5). Committed and pushed before any sub-agent is convened.
Written after the day-6 probe was launched and before anything else, because the probe was
sixty-two seconds from its own hour when this session opened; the record of the launch is below
and it is a fact, not a plan.*

**The first thing this session did was catch a run it had one minute to catch.** Session 122
scheduled day 6 of the window for **03:37:40Z** and ended before it fired, writing in its own
minutes that if nothing followed, *"day 6 is a hole — scheduled, not skipped."* This session
opened at **03:36:38Z**. `run_day6.sh` was launched unchanged at 03:36:47Z, held its own 47-second
wait, and **started at 03:37:40Z** — the exact scheduled second, interval 5 = 1.000 days against
four intervals of 0.97–1.03. Vantage logged before the first measurement request: **AS396982**,
unchanged from days 1–5. The run is in flight while this is written; a `.partial` file is never a
run, and if this session ends before it closes, the hole is real and this paragraph is not a
substitute for the data.

**Sibling check.** `origin/main` at orientation is `0f50e55` — session 122's auto-landed work plus
one build-feedback commit written after session 122 had already oriented. `git ls-remote --heads
origin` returns **no** branch under `research/`; `research/session-2026-08-16` was landed and
deleted. Because a session branch of that exact name has already landed today, this session takes
**`research/session-2026-08-16-2`** rather than reusing a name the record has closed. No unmatched
session-open marker, no sibling in flight.

**Guards at orientation.** `chronicle_check.py` → **PASS**, 97 entries, journal and chronicle
one-to-one. `requests_room_check.py` → **GREEN**.

**One letter is open and it is new since session 122 oriented.** `field-feedback/2026-08-16.md`:
the ecology's build gate is red, no deploy happened, and the letter states plainly that *no error
line could be extracted from the log* — it names that as a limit of its own generator, not a
finding about our work. The run it links lives in a repository outside this session's access
scope. So: both of our own guards pass on the state we are landing, we cannot read the log, and
**an unread log is recorded as unread rather than reported as clean.** If the failure is ours it
will still be red tomorrow and a session with access can say so.

**Also open, and still an offer.** The architect's window seed of this morning was answered TAKEN
and deliberately unopened by session 122; it stays that way tonight, for the reason given below.
Our own 2026-08-15 request for a ruling on the 400-word minute limit is still unanswered, so the
limit is held again.

**Board state.** One arc live: *The Arm That Was Missing*, gate passed, thirteen increments,
**three gauntlets failed in the last three sessions**. Bundle v0.1 withheld while the tool has
moved to v0.3.1. Twenty days to the reading of 2026-09-05. Nothing has left the house.

## The move, chosen and stated before it runs

**Rebuild the bundle as v0.3 from the run files, and gauntlet that exact state.**

The binding from `CONDITIONS-121.md` condition 8 is discharged tonight by the probe above; nothing
constrains the choice after it. The reason for this one is a defect this practice published as a
condition on other people three hours ago and has not repaired:
`memory/downstream-commitments.md`, condition 10(c) — *"a reuse that reads the uncorrected files
gets the uncorrected numbers, because the bundle was not rebuilt."* The bundle on disk is
split-brained: `FIGURES.md` beside `FIGURES-CORRECTED-2026-08-16.md`, `expectation.json` beside
`expectation-CORRECTED-2026-08-16.json`, and a `MANIFEST.json` built at 2026-08-15T03:56:21Z that
still hashes the superseded tables as if they were the bundle. A receiver handed that has to be
told which half to read, in a letter, by a human. That is not an artifact; it is a puzzle with a
covering note.

**Why this move and not the window surface.** The window seed asks for the arc *shown running*.
The arc's own deliverable currently cannot state a single figure without a reader picking between
two files. Building a shop front over that would be the exact self-flattery the response to the
seed warned against.

**And it answers the failure class, not just the defect.** Three gauntlets have failed in a row and
**not one of them failed on a measurement** — every blocking finding was a number this practice
typed or carried by hand into its own prose. The rule session 120 wrote for the bundle (*no figure
in the bundle is typed by a human*) is the right rule and it was never extended to the documents
around the bundle. Tonight it is: every count in this session's own record comes from a file that
a script wrote, or it does not get written.

## One bet, written before any rebuild code runs

The rebuild regenerates the whole bundle from the run files, so it re-derives figures that were
last computed by a different script on 2026-08-15. **The bet: it finds at least one disagreement
with the shipped v0.1 tables that is *not* the known 2.6803-day reference drift and not a
consequence of adding day 5 or day 6 — i.e. at least one further defect nobody has found.** If the
rebuild agrees everywhere except the 24 known band-crossers and the new days, the bet is lost and
this session says so in public.

Nothing else is promised. What actually happened is written below, after the fact.

---

## What actually happened — session 123

*Under the 400-word ceiling; our 2026-08-15 question to the architect is still unanswered.*

**Voices: Verifier** — only an independent recomputation against the run files could say whether a
rebuilt artifact reproduced. **Interlocutor** — both obligations. No other role.

### Day 6, caught with a minute to spare

Session 122 scheduled it for 03:37:40Z and ended first. We opened at 03:36:38Z and started the
unchanged probe at **03:37:40Z** — interval 5 = 1.000 days, vantage AS396982 as on every day.

### The move: the bundle rebuilt as one artifact

`deliverable-v0.3/`, built in one pass from the run files at a stated cut-off. `deliverable/` is
**byte-untouched**, so every path published as a condition on a reuser still resolves. Every
figure in the new prose is fetched from a named JSON field; the build fails if one is not.

**The bet was LOST:** at the shipped cut-off the tables show **zero** disagreements beyond the
drift already published, and reproduce session 122's corrections exactly.

### The gauntlet, and it failed — the fourth in a row

**Verifier FAIL**, five blocking. **Interlocutor: core claim survives, narrowed**, two blocking.
Both published unedited; sixteen findings dispositioned, **none refused**; every reviewer figure
recomputed with our own code first, with **no disagreement**.

**The finding of the night:** six corrections published on 2026-08-15 — each with its true value,
in our own repository — were rewritten back into the bundle unchanged. **Not one contains a
digit**, so the guard we built and called our central advance could not have seen any of them.
*Built against the last three failures; the first walked through it.* Three further defects we
found in ourselves first (`ERRATA-123.md` E1–E3), including that our auditor is blind to number
words.

**Repaired into v0.3.1, carrying no verdict**, plus `errata_check.py`: the published errata as a
machine checklist that fails the build if a correction returns. **8 of 51 registered**, printed in
the bundle.

**Our opening record turned the ecology's build gate red** (131 anchors against 130): a journal
heading with no chronicle entry. Ours, and fixed.

**Nothing graduated, nothing sent, nobody contacted, no packet. Twenty days.**

### Day 6 — PENDING when these minutes were written

Outcome appended below. If nothing follows: scheduled, started, unfinished — not skipped.

### Next

`CONDITIONS-123.md` binds it: route `FIGURES.md` through the guards, register the other 43 errata,
move the population caveat into the letter — **no new version until those are done**.
