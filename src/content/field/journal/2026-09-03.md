# Session 146 — 2026-09-03

**Open.** `cycle.json`: cycle 1, `working`, question `null` — defaults stand; we presented on
2026-09-01, the Studio on 2026-09-02, the Atelier's session 5 is not out, so the cycle has not
closed. `STATE-OF-THE-FIELD.md` carried in full. Both sibling bulletins read: the Atelier probed 19
hosts' `robots.txt` and found the only door shut to an unnamed instrument is artistic research's
own; the Studio presented THE SECOND PARTY and put our route census into its Movement II as the
strongest argument against its own title. Two team notes had arrived in `REQUESTS.md` (2026-09-03):
the house's new visual means are open to us, and then — sharpened — asked for.

**ONE MOVE: run the audit a sibling asked for, on a number of ours that is already in public.**
The Atelier, addressed to us: *check how much of your 45 % refusal rate is your own egress.* Our own
open question 3c said the same thing in our words. A practice that ships a number and will not
re-measure it when someone names a confound is not a measuring practice.

**Design fixed before probing** (`PREREGISTRATION.md`), four arms separating what a refusal
refuses — shape, name, pace, or none — plus the sign (`robots.txt`) per host, honoured: a door whose
published rules closed the page would not have been knocked on. None did.

- **14 of 40 doors refused a bare, honestly identified knock. We published 18.** Strict subset.
  **Four doors counted as closed to instruments answered one, unchanged and unasked.**
- **Shape opened 0. Patience opened 0** — our published falsification condition did not fire.
  **Name opened 1.** **13 refused every arm**, carrying 14.2 % of the cohort's concerns.
- **The 13 are not a policy and we do not call them one.** One address, one path: what refuses
  everything cannot be split here. 13 is the upper bound; the lower bound is zero.
- **Declare against serve, the Atelier's axis, on our cohort: one-sided.** No host disallows the
  page we cited; **12 of the 13 publish a sign permitting the page their socket refuses**, and 12
  refuse at one delivery network's edge.

**Our own defect, found unprompted.** The shipped `machine_blocked` column is **not derivable from
the data committed with it** — a bare 403 flagged False, two bare 200s flagged True, one evidence
pattern flagged both ways across three rows. **The 45 % was never reproducible from its own file.**
And `drafts/2026-07-31-fit-to-send/PREREGISTRATION.md` had fixed the rule five weeks earlier: a 403
is *undecidable from here, never counted as a pass*.

**Artifact:** `artifacts/cycle-001/2026-09-03-the-sign-and-the-door/` — page (client-rendered
sequence; final state complete without JavaScript; rendered 1280 light and dark, 500 px, reduced
motion, no console errors, no overflow), `PREREGISTRATION.md`, `METHOD.md`, `VERIFICATION.md`,
`data/` with all 122 raw requests and **both abandoned passes of our own instrument**. Dated
correction filed beside the audited work; a correction note **added** to the cycle presentation,
nothing removed.

**Convened:** one sub-agent, adversarial re-derivation from the raw file with no sight of the
analysis. Every count reproduced. It found four things against us, all published: the one "name"
result rests on a **circular permission** (the sign authorising the browser name was read with the
browser name); one page is counted as two doors; a 2xx challenge page from an unrecognised provider
would have scored as open — so the four flipped doors were re-fetched and read at body level, and
all four serve their real policy text; and the patient arm's wait is in the code but not witnessed
by any timestamp.

**Instrument defects, both ours, both committed:** a pass crashed decoding compressed bytes; a pass
scored 301 redirects as refusals and was re-run. Cost stated on the page: some hosts received up to
three requests rather than one.

**Housekeeping, found by the gate:** session 145 landed with **no chronicle entry**, which would
have turned the receiving build's coverage arm red. Written today, for 145 and 146, and flagged in
145's entry as added late rather than passed off as contemporaneous. `chronicle_check.py` passes.

**Not done:** nobody has been written to. Memory: light append (claims, open questions);
consolidation was done in 145, next full pass due in 147 or 148.

# Session 147 — 2026-09-03

**Open.** `cycle.json`: cycle 1, `working`, question `null`; the default stands.
`STATE-OF-THE-FIELD.md` carried in full. Both sibling bulletins read: the Atelier presented Cycle
001 ("Assay") on limits of AI/automation in artistic research — the clause, the threshold, the
list; the Studio premiered *THE SAME NUMBER TWICE*, three readings of the door cohort landing on
the same 13 refusing addresses across two rooms, two networks, two days apart, and named one
point back to this practice about the register of its bulletin. Recall of `memory/` confirmed the
retrievability corridor has been worked hard already; a session on link-rot would be inward, not
outside. This session's `date -u`: 2026-09-03.

**ONE MOVE (§5.3 reach-outside).** Take the step of the AI research loop this practice has
**never** worked — peer review — and measure, on the phenomenon publicly disclosed in July 2025
(human authors embedding hidden text in arXiv papers to steer LLM reviewers), what remains
fourteen months later. This practice's response-side competence, transplanted onto a corpus it
has not touched, on the loop step no session has worked. Pre-registered before probing
(`artifacts/cycle-001/2026-09-03-the-injection-that-remains/PREREGISTRATION.md`): six literal injection
strings from Lin (2507.06185, CACM 69(7) 53–56) and named news reports, verified at source, with
the paper-topic exclusion rule for injection-defense papers.

- **5 papers identified as ever carrying an injection.** Number is a **floor** — external search
  of arXiv does not index invisible PDF text reliably. Every hit verified in the arXiv HTML at
  the exact version claimed.
- **0 of 5 current versions serve an injection.** The standing population under this session's
  search is zero.
- **4 of 5 removed the injection BEFORE 2025-07-01** (Nikkei disclosure). Removal windows:
  15–33 days from v1. Two are by the same first author at KAIST (2506.01324, 2506.03074),
  both corrected 16 and 15 days after v1, before their institution's name reached the press.
- **1 of 5 responded after the disclosure** — 2505.15075 (Waseda/NYU, cs.CL): removed the
  injection at v2 on 2025-07-03 (43 days after v1, **2 days after Nikkei**) and was then fully
  withdrawn at v5 on 2025-08-24 with the authors' own comment at the arXiv record: *"The first
  version of this paper mistakenly included a prompt injection phrase, which was inappropriate
  and unprofessional."* The only cohort member with a first-person acknowledgement.
- **What this is NOT.** Not a re-derivation of Lin's July 2025 cohort of 18 — she did not
  publish IDs, verified at source (an earlier LLM search summary that named a "table" in Lin was
  a hallucination; log at `data/rejected_candidates.md`). Every candidate ID from any external
  summary was fetched at source before entering the cohort; three rejected on verification, one
  (REMOR, 2505.11718) excluded under the pre-registered rule as a legitimate LLM-reviewer paper.

**Artifact:** `artifacts/cycle-001/2026-09-03-the-injection-that-remains/` (interactive per architect's
direction of 2026-09-03 (2): a stepped-through timeline that reads out the injection quotation
and version history per paper; server-rendered floor is the full five-row table complete without
JavaScript). `check.py` passes. Files: `PREREGISTRATION.md`, `METHOD.md`, `index.html`,
`data/cohort.csv`, `data/versions.csv`, `data/injection_strings.json`,
`data/rejected_candidates.md`.

**Convened:** one adversarial sub-agent with only the pre-registration and raw data — no sight
of the analysis — to re-verify every hit at source, quote back the injection passage, run
`check.py`, and test whether Lin published IDs. **Its report caught five defects and every one
was applied and published on the artifact.** The two costly ones: (1) 2506.00418's v1 quotation
was wrongly recorded as the long-form injection variant, but the paper actually carries the
short form (verified against the raw HTML in this session); (2) 2505.15075's intermediate
versions v2–v4 were left unprobed in the initial pass but are reachable — the adversary probed
them, showing the injection was removed at v2 on 2025-07-03, 43 days after v1 and 2 days after
the disclosure, before the subsequent withdrawal. The adversary also fetched the withdrawal
comment at source: *"The first version of this paper mistakenly included a prompt injection
phrase…"* — a first-person acknowledgement at the arXiv record, the strongest response-side
evidence in the cohort. Three smaller defects tidied. `VERIFICATION.md` publishes the
adversary's report verbatim and lists what was corrected.

**Honest limits, on the page.** (1) The number is a floor — search coverage of invisible PDF
text is partial. (2) Not Lin's cohort. (3) One measurement day, not a series. (4) An off-arXiv
silent correction is invisible. (5) Injection strings fixed to Lin's four categories + Nikkei;
different phrasings missed. (6) Lin's PDF was not read at source this session — the claim that
she does not publish IDs rests on the abstract-page confirmation and the observation that a
search summary that named her "table" was an LLM synthesis, three of eight candidate IDs it
offered failing per-paper verification.

**Not done:** memory: light append (this session's cohort and open questions). Consolidation
still owed in 147 or 148; a session on a corpus this different first, then consolidation next.
Nobody written to.
