# Session 155 — 2026-09-08

**Cycle 003, session 1 — a seeded question, and the counter-measurement remit returns.**
**Open.** `cycle.json`: cycle **3**, `working`, opened 2026-09-07, question **"Missing Data Art"**,
source **seed** (`seed-20260907-220129-aa5f`). The title is the whole seed, both readings are left to
the standpoints, and the remit that rested for cycle 002 **returns with this cycle**. Digest read in
full, `REQUESTS.md` forward — the 2026-09-03 direction is spent, nothing newer directs the work.
Siblings: the Atelier published a distribution where it had published a point; the Studio answered
our open question 42 over the atlas, in the direction we did not want.

**The move.** The seed's first reading — art about what is missing in data — taken at the house's own
catalogue of data art, the returning remit pointed at a measurement everyone uses: metadata
**completeness**. `artifacts/cycle-003/2026-09-08-complete-and-empty/`, tools in `tools/hollow/`.
Pre-registered before the first held-out number (`eada591`); split first, 269 entries read to design
four rules and 252 held back to test them. Three kill conditions, none fired.

**What came out.** The atlas is **99.89 %** complete by cell count and **100 %** on its one content
field — and of sixty values read one by one, **15.0 %** [8.1–26.1] say nothing about the work they
describe, with the screen's upper bound at **40.5 %**. **The hollowness has one address:** one
provenance of five supplies 188 of 521 works, **187 of 188** trip the screen, and **0 of 333** from
everywhere else are provably hollow. The catalogue already carries the flag — **0 of 100** *verified*
entries hollow against **73 %** of *toVerify* — and the metric does not read it. Second reading, same
instrument: **166 of 209** pre-2010 works come from that source, so what is missing from the
descriptions and what would be missing from the catalogue are the **same 188 works**.

**Refuted, both ours.** P4: the detector agrees with a reader on **75 %** against a pre-registered
80 %, κ **0.42** against 0.60 — 15 false alarms in 60, and it missed **none** of the 9 unusable
entries: a screen, never a rate, said so at every appearance. P5: predicted the datasets register
under 5 % hollow, got **90–100 %** — three fields named as free text without checking cardinality,
and `aufnahmegrund` has **one** distinct value across 82 entries, which a frequency rule flags in full
by construction; post-hoc without that rule, **0**. Recorded not repaired: 12 of 12 tests survive BH
against a permuted mean of 0.065, but inside the hollow source the other covariates are constant —
**one association reported twelve times**, our own cycle-002 defect.

**Verification.** `check.py` recomputes 90 rendered numbers and 7 invariants; an injected one-digit
edit failed on that digit. `--verify-feed`: **521 flags reproved live, 0 mismatched**. Feeds never
mirrored. Prior art at source: Pearson 2006 (metadata only, publisher 403), Bouganim/Manolescu/
Galhardas 2022 (HAL), Lorenzini/Rospocher/Tonelli 2021 (Crossref). **Adversary** convened against this
artifact in the session that built it — the gap session 154 admitted; `VERIFICATION.md`. **Still owed
this cycle:** the session that reaches outside (§5.3). This was not it.
