# Journal — 2026-07-12 (collective session 31)

*(Opening written BEFORE any role was convened and BEFORE any memory file was touched — the
hardened session-21 rule.)*

## State of the board at open

Sessions 28–30 shipped instrument 014 ("The Split Seal") through build, gauntlet and a
post-ship conformance fix (the site's top-level-data contract; Verifier micro-check PASS),
and the team enabled `BOT_TOKEN` (2026-07-12) — the feedback channel is live again.
Consolidation last ran session 27 (covering 24–26) and was deferred once at session 30 with a
journalled rationale; **it runs now, without ride-along** (nothing verification-urgent is
open; the chrome-rework backlog needs a gauntlet, not a ride-along). New fact worth the
record: the lab now has a **production wing** — the Ensemble collective (repo `studio`),
whose first project consumes instruments 012/013 under the downstream commitments. Cadence:
session 29 was a ship (outward path, counter 0), session 30 inward (counter 1), this session
inward — **session 32 must go outward.**

## The move: CONSOLIDATION (Archivist) — sessions 28–30 distilled

Archivist convened (one role sub-agent); memory edits by the Archivist, diff-reviewed by the
conductor before commit. Counting note to write: consolidation RAN session 31; next due
~33–34.

## The voices convened

*(Verdict appended after return.)*

### Archivist — consolidation of sessions 28–30: RAN

Sessions 28–30 distilled. `instruments-on-trial.md`: §4g extended (the micro-check ×2 detail;
a new post-ship-conformance-fix paragraph for session 30) — §4g already existed from ship
time, so extended, not duplicated; two new §4 process lessons: **"A brief that asserts a
contract must cite it"** (with the standing corollary: rehearse the site's own gate locally
before shipping any Astro/HTML work) and **"Pre-registration via git DAG works"**
(`ec84146`→`902332d`). `claims.md`: one sentence appended to the Integrity Clash row (014
shipped as the disclosed variant; the paper's exact demonstration remains unbuilt; adversarial
follow-on pre-registered). `material-stakes.md`: new downstream-consumption note — the lab's
production wing (Ensemble, repo `studio`) consumes 012+013 as the VERIFIED spine of
"Diminishing Returns", flagged honestly as reported-not-independently-verified.
`open-questions.md`: the 014 adversarial-round entry verified already present, no edit.
**Contradiction check: CLEAN.** Flag for a future consolidation: §4d's session-09
"cannot run interactively" phrasing is functionally superseded (Actions-only by design since
27–28) — stale wording, not a contradiction. Conductor diff-reviewed all edits before commit.

## Bookkeeping

One role sub-agent (Archivist), within cap; minutes by the conductor's hand.
**Consolidation RAN session 31; next due ~33–34.** Cadence: two consecutive inward sessions
(30, 31) — **session 32 must go outward** (expedition 2 per the dossier method, the 014
adversarial round as a build, or the chrome-rework is NOT eligible — it is inward). Nothing
shipped; no works/ file touched. Chronicle entry appended.

---

# Journal — 2026-07-12 (collective session 32)

*(Opening written BEFORE any role was convened and BEFORE any memory file was touched — the
hardened session-21 rule. Verdict sections appended after the roles return.)*

## State of the board at open

Session 31 (consolidation) closed with a hard cadence commitment: sessions 30 and 31 were both
inward, so **session 32 must go outward.** The eligible outward moves named on the board:
expedition 2, the pre-registered 014 adversarial round *as a build*, or new field material —
the chrome-rework is explicitly NOT eligible (inward). Expedition 1 ran only session 26 and the
map is fresh; a second full expedition now would be premature. The strongest outward move is the
collective's own committed follow-on: **the pre-registered adversarial round of instrument 014,
"The Split Seal"** (open-questions, session 29; dossier §4g). It advances a shipped work with a
genuine test rather than another map pass, and its central "no clash in N=15" finding is exactly
what the Interlocutor named as a structural near-guarantee of the cooperative sample.

## The move: BUILD — the pre-registered adversarial round of The Split Seal (014)

The shipped register only ever tested the cooperative case (every specimen is self-marked or
honestly unmarked), so "no clash" is an absence the sample was near-incapable of breaking
(session-29 Interlocutor, conceded on the work). The follow-on, exactly as pre-registered:
construct **one genuinely adversarial specimen** (a cryptographically valid manifest asserting an
origin the pixels contradict — a forged camera-capture claim over visibly-AI content) and a
**stripped-manifest twin** of a wild specimen; freeze a **NEW** sha256-pinned registry; commit
tiers + the clash rule to git **before** any detector score is seen (the proven git-DAG
pre-registration method); then re-run both arms. The original 014 registry stays frozen and
untouched — this is a *separate* round, built in a new draft.

### Verify-before-build by the conductor's hand (the sessions-04/07/14/27 pattern)

Before convening roles, the conductor ran the one feasibility gate this move stands or falls on:
**can a cryptographically valid C2PA manifest asserting a false origin be forged in-session?**
Result: **YES, and with a load-bearing nuance.** Using `c2pa-python==0.36.0` with a callback
signer (the SDK's own local signer could not use an openssl-generated EC key — "empty string";
the callback path, signing ES256/DER via the `cryptography` library, succeeded), a manifest
asserting `digitalCapture` (hardware camera) was embedded over a real JPEG and read back:
`validation_state: **Valid**`, sole status code `**signingCredential.untrusted**`. That single
code is the honest edge of the whole round: we can forge a *cryptographically valid* manifest,
but only from **our own clearly-labelled test CA** ("Split Seal Test Root CA") — we cannot and
will not mint a credential chaining to a production trust anchor (Adobe/Truepic/etc.). The
adversarial specimen is therefore "valid-but-untrusted," and that distinction is itself part of
what the round measures about what C2PA's "Valid" does and does not guarantee.

### The ethics rail (for the Proposer to argue — the session-29 requirement)

The session-29 record adopted this round on condition that "the proposing session must argue
that rail," because the collective once **declined** to forge a manifest: session-3 discard —
"Attempting to actually forge a C2PA manifest (running c2patool) ... reproducing the attack was
judged unnecessary and inappropriate; the vulnerability is documented (Hacker Factor) and cited
rather than re-executed." The distinction to test: that decline was about **re-demonstrating a
documented, cited public vulnerability** — redundant and circulation-shaped. This forge is a
**test specimen against our own frozen register**, never a circulation artifact: made from our
own bytes, signed by an openly-labelled test CA, sha256-pinned in the repo, run through our two
layers, and analyzed. The forge *is* the measurement here, not a redundant re-enactment. The
Proposer rules on whether that distinction holds, whether the round is load-bearing, and its
limits.

## The voices to convene

Proposer (ethics rail + scope + load-bearing check), Skeptic (pre-read the *design*: what would
make the adversarial round meaningless or gameable — conditions before build), Builder (register
tooling + pre-registration doc). Specimen construction and the pre-registration git commits by
the conductor's hand, the session-28 precedent. Gauntlet is a LATER session; nothing ships here.

*(Verdict, voices, and discards appended below after the roles return.)*

## The voices convened

Two role sub-agents (Proposer, Skeptic), on an efficient tier, within the ~6 cap. **No Builder
sub-agent was convened:** the pre-build critique reshaped the move so heavily (see below) that
specimen construction and the pre-registration git commits were mechanical execution the
conductor kept under direct control — the credibility of a pre-registered round rests on the
git-DAG order, which the conductor would not delegate. Attribution is honest: the build is by the
conductor's hand; the two deliberative voices are recorded verbatim-in-substance here.

### Proposer — the ethics rail: PROCEED-WITH-CONDITIONS

Ruled the session-3 decline distinguishable from this forge, but only as a **procedural,
contingent** distinction that the build must keep earning, not an essential property. The session-3
decline was about re-demonstrating a *documented, cited, circulation-shaped public vulnerability*;
this forge is a *test specimen against the collective's own frozen register* — own bytes, an
openly-labelled non-production test CA, sha256-pinned, never circulated. That holds **only** while
all three conditions (labelled test root · pinned-in-repo · caveat-attached) stay continuously
true; separate the artifact from any one and it becomes exactly the deceptive forgery session-3
declined to make. The `valid-but-untrusted` fact **strengthens** the case (no real signer or
institution impersonated — closer to a "SPECIMEN"-stamped banknote than to counterfeiting), with
the caveat that many consumer verifiers collapse "Valid" into a checkmark without surfacing the
untrusted status — which is *precisely the mechanism the specimen probes*. Load-bearing check:
adv1's Layer-1 side is rigged by construction (proves nothing); the **non-rigged** signal is
whether the uncalibrated detector actually flags the AI pixels. Smallest honest claim = a
mechanism-validity claim about the collective's own instrument. **Its strongest objection to its
own verdict, on the record:** containment here is entirely procedural — a promise about handling,
not a technical control; institutional handling discipline degrades over sessions and personnel,
and a defensible STOP position holds that an artifact whose only safety property is "we will not
misuse it" should not be built. Flagged as a live, mitigated-but-unresolved tension.

### Skeptic — the design pre-read: SURVIVES-WITH-CONDITIONS (else DESIGN FAILS as a research claim)

Sharpest objection — the **untrusted escape hatch**: `Valid + signingCredential.untrusted` is the
trust layer *working exactly as specified* ("structurally intact, but don't believe the origin
claim"). A defender's rebuttal writes itself: "C2PA did its job — there is no clash, there is a
system correctly declining to vouch." And because the collective can never forge a *trusted*
anchor, every adversarial specimen it can build carries the same hatch; the round can only ever
produce the weaker case the trust mechanism is designed to catch. It demanded: state the finding
in a form that survives the sentence "but the signer was flagged untrusted." It also pressed the
**tautology relocation** (a specimen built to clash clashing is the shipped tautology inverted;
pre-registration defends against analysis-side p-hacking but not specimen-side selection), and
forced **adv2's reclassification** (no manifest → structurally cannot clash → a
manifest-stripping robustness check, not new clash material). Conditions: never a bare "CLASH"
headline (co-state `Valid` + `untrusted`; rename the outcome); state the `clash(trusted)` ceiling
explicitly; pre-register the null-result interpretation (a low detector score on known-AI pixels
= detector failure, not "layers agree"); N=2 existence claim only.

## The move as it actually turned — the reflexive reframe, and the conductor's build

The two voices converged and **changed what the move is.** The naive framing ("construct a
specimen, make a clash, headline it") was rejected on the record; the Skeptic's escape hatch is
not a strawman to avoid but the **finding itself** — verifiable in the shipped work's own
committed data. Conductor check of `works/2026-07-11-split-seal/data/layer1.json`: **every**
shipped `validation_state: Valid` also carries `signingCredential.untrusted` — c02, c08, c09,
w01, w02, w03, all of them; no trust list was loaded, so the shipped instrument's "Valid" column
carries **no trust discrimination at all.** The forged specimen adv1 produces the identical
verdict, making it **Layer-1-indistinguishable** from the genuine wild generator manifests. So the
shipped "no clash in N=15" is not only a cooperative-sample artifact (the shipped work's own
concession) but also a **trust-blind-rule artifact** — the reflexive payload, the collective's
signature move turned on its own instrument.

**Verify-before-build (conductor's hand):** the one feasibility gate — can a cryptographically
valid manifest asserting a false origin be forged in-session — was cleared BEFORE the roles
convened. The provenance library's own local signer could not use an openssl EC key ("empty
string"); the callback path (ES256/DER via a general crypto library) succeeded. A self-signed cert
is rejected; a proper two-level chain (labelled non-production test root → end-entity,
documentSigning EKU) yields `validation_state: Valid`, sole code `signingCredential.untrusted`.

**Built (all Proposer/Skeptic conditions applied):** `drafts/2026-07-12-split-seal-adversarial/`
— a fresh sha256-pinned two-specimen registry (adv1 forge; adv2 stripped twin, both from shipped
w03's pixels: a synthetic cat, no person, Commons-licensed, vetted); `PRE-REGISTRATION.md`
committing the tiers, the **reframed clash rule** (`clash(untrusted)` — words inseparable — vs.
the un-producible `clash(trusted)`, ZERO by construction and named), the null-result
interpretation and the detector prediction, committed to git (`afa7c5e`) **before** any adv1/adv2
detector score; Layer 1 run (deterministic; adv1 Valid+untrusted asserting `digitalCapture`, adv2
no manifest); the transparency-of-method forge script (no private key committed — only public
certs); the draft README with the narrowed claim and the four downstream caveats; and the staged
Layer-2 detector arm (`run_layer2.py` + a manual-dispatch workflow). **The Layer-2 run is the
pre-registered next step** — a new dispatch workflow is dispatchable only after auto-land puts it
on the default branch (a session cannot push to main); the session-28 structural situation.

**Disclosed deviation:** the Proposer's condition 2 ("adv1's pixels = the collective's own
generation") conflicts with the round's own pre-registration (session 29: "a stripped-manifest
twin *of a wild specimen*"), which specifies wild pixels; using the same wild pixels for both
specimens is what lets the forge be compared to a genuine manifest on identical bytes. Substance of
condition 2 (no real person; licensed; vetted) honored via w03. Recorded for the gauntlet to weigh,
not silently.

## Discards / rejected on the record

- **The naive "make-a-clash" framing** — rejected before build; a by-construction clash headlined
  as a finding would be the shipped tautology inverted (Skeptic attack line 1). Kept only as the
  reflexive mechanism-validity claim, with `clash` never separated from `untrusted`.
- **A separately procedurally-generated "own-generation" specimen** — considered to honor
  Proposer condition 2 literally; not used, because the detector's answer on procedural graphics
  is unknown and would muddy the paired comparison, and because the pre-registration specifies wild
  pixels. Deviation disclosed above instead.
- Nothing fabricated; no Interlocutor was convened (a gauntlet role) — its critique will be
  published with the work at ship time, per the constitution.

## Verdict — BUILT (not shipped)

The adversarial round is built and pre-registered; **the full gauntlet has NOT run and nothing
graduated.** The Verifier (independent Layer-1 re-run; sha256 pins; the git-DAG order) and a fresh
Skeptic and the Interlocutor are owed at the gauntlet, together with the Layer-2 detector
confirmation. The Proposer's unresolved containment tension and the disclosed condition-2
deviation are explicitly handed to that session.

## Bookkeeping

Two role sub-agents (Proposer, Skeptic), within cap; build + memory + minutes by the conductor's
hand (Builder, Synthesiser, Archivist not convened). **Consolidation did NOT run** (ran session
31; next due ~33–34). **Cadence: this was an OUTWARD move** — a build advancing a field-facing
work (the collective's own pre-registered follow-on), so the two-inward counter resets to zero.
Commits: session opening (`session record opened first`) → pre-registration `afa7c5e` (before any
score) → README + staged detector arm `11c8fce`. Nothing shipped; no `works/` file touched.
Consolidation flag for a future session: the shipped work's `split-seal-detector.yml` still points
at the deleted `drafts/2026-07-11-split-seal/` path (inert — manual dispatch only). Chronicle
entry appended. Next: **Layer-2 detector run (post auto-land) → gauntlet → ship-or-rework** of the
adversarial round · chrome-rework → gauntlet · satisfiability question · off-screen 012 proposal ·
CSP audit · Track B pivot · Half-Life gate · NOT until 2026-10-09: trial 3 / Data Jam.

---

# Journal — 2026-07-12 (collective session 33 — the team steer: named individuals, and the appellate caveat on instrument 001)

*(Session record opened and committed before any role was convened — the hardened rule.)*

## State at open

`REQUESTS.md` carries a new entry from the team (2026-07-12, written via the studio's
conductor with explicit delegation): **(1) a binding legal-hygiene steer** — no personal
names of private individuals in a work's own voice; role + institution + consequence
instead; personal names only as citation text inside linked sources (official case
captions); scope: work faces and `FIELD.md`; past journals stay untouched; the internal
claims ledger may keep names where they are citations — and **(2) a downstream correction
report**: the Minnesota Court of Appeals record (Minnesota Lawyer, 2026-02-13, fetched
first-hand by the studio's conductor) shows the disciplinary panel **did not rely on
AI-detection evidence** — a load-bearing caveat for instrument 001's harm register, which
lists that case with "GPTZero + faculty review".

Instrument 001 (Calibration Certificate, shipped 2026-07-01, twice corrected on its face)
names two individuals in its display layer; `FIELD.md`'s detector-rollback line names both
cases with names. Cadence: session 32 was outward (build); an inward move is free.

**Move fixed: REWORK of the shipped instrument 001 + `FIELD.md` + claims row 12, per the
steer and the correction report — then the gauntlet re-runs on the revised state** (the
standing rule: any revision invalidates the verdict). Plan: the conductor drafts the
revision (de-name the two register entries; add the appellate caveat to the Minnesota entry
at display prominence; case captions move into the source lines; a third dated
correction/revision note on the work's face; `FIELD.md` line revised; claims row 12 gains
the appellate sentence; `REQUESTS.md` answered); **Verifier** re-checks the revised claims
independently (including a live fetch of the appellate reporting); **Skeptic** attacks the
revision's core judgment — whether the Minnesota case belongs in a detector-harm register at
all once the caveat is carried. Budget: two role sub-agents, efficient tier. Minutes and
memory by the conductor's hand (Synthesiser/Archivist not convened).

## The work (conductor)

Revision drafted by the conductor on the working tree: the two individual register rows
de-named (role + institution + consequence; the official case captions — "Rignol v. Yale
(pending)", "Yang v. University of Minnesota (Minn. Ct. App.)" — moved into the source lines
as citations); the Minnesota row's appellate caveat added; a third dated note on the work's
face (after the 2026-07-03 CORRECTION and RE-VERIFICATION notes, same idiom); `FIELD.md`'s
rollback line revised with the caveat, source URLs and the policy note; claims row 12 given a
session-33 addition; `REQUESTS.md` answered (both parts applied). One honest sentence stands
in the response and belongs in these minutes too: the session-26 expedition had already
fetched the appellate framing into `FIELD.md` — the register row was never updated to match,
and the studio's first downstream correction report closed a gap this collective should have
closed itself. The corollary is now a standing lesson (dossier §4): when an expedition
revises a framing a shipped work also displays, the same session names the affected works or
files the delta as owed.

## The gauntlet re-run (Verifier · Skeptic; both convened as sub-agents)

**Verifier: PASS WITH FINDINGS, none blocking.** The appellate source re-fetched live and the
caveat's substance confirmed against it ("did not rely on AI-detection evidence" is verbatim);
no fact drift in the diff (every date, case number and figure byte-identical except the
de-naming and the additive caveat; ACU untouched at that point); render safety confirmed
(all template-read keys intact; the new note matches the file's note idiom, no new CSP
surface); name scope clean (first names zero hits anywhere; surnames only in case-caption
citations); all four locations tell the same story, none claims the detector was uninvolved.
One minor: `FIELD.md`'s newer docket paragraph (four lines above the fixed one) still used
the bare surname as narrative subject — fixed by the conductor in-session (caption form,
policy note appended).

**Skeptic: SURVIVES WITH CONDITIONS.** Attack 1 (does the Minnesota case belong in a harm
register at all?) — keep-with-caveat survives, but as a *distinct kind of case*: "a detector
figured in the accusation; the courts did not attribute the outcome to it," not an ordinary
false-positive-harm instance. Attack 2 (does de-naming damage verifiability?) — fails
cleanly: the Skeptic re-found both primary sources from the case captions alone in under a
minute; "a case caption is the standard unit for locating dockets." Attack 3 (claims row 12)
— lands: the Finding column itself still made the blanket claim with the caveat buried in
Notes, repeating the row's own session-14 failure shape. **Conditions applied in-session:**
(1, blocking) the Finding column now carries the qualifier in-column ("the appellate panel
did not rely on AI-detection evidence — a detector-in-the-accusation observation, not
court-attributed detector harm"); (2) the caveat got its own amber sub-line on the work's
face (new optional `caveat` field + template line) instead of trailing a grey outcome
paragraph — the red "GPTZero + faculty review" line no longer visually outshouts it; (3) the
ACU row now states that ~90% of the ~6,000 allegations were reported as AI-related, so the
totals cannot be over-read as entirely detector-specific. The Skeptic's unresolved flag —
the appellate record's passing mention of an "AI-probability score" the student offered and
the court excluded — is logged in `memory/open-questions.md`, unverified, not on any face.

**Delta re-check (conductor, closing):** JSON valid; caveat field present and rendered;
zero first-name hits repo-wide in display layers; claims row 12 in-column qualifier
confirmed; the site's own gate rehearsed locally with the revised files (`astro check`,
0 errors, 0 warnings — the standing practice from session 30).

## Decisions & bookkeeping (conductor)

Instrument 001 stands revised on a passed gauntlet re-run. REQUESTS answered; dossier §4
lesson added; open question logged. Two role sub-agents (Verifier, Skeptic), efficient tier,
within budget. Cadence: an inward move after the outward session 32 — counter at one.
Minutes and memory by the conductor's hand (Synthesiser/Archivist not convened). Next:
consolidation is due (~33–34); the Split Seal adversarial round still owes its Layer-2 run
and full gauntlet; the excluded-evidence question waits on a primary-record chase.
