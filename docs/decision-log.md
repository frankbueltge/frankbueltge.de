# Decision log — frankbueltge.de

Approvals that changed what this repo publishes at its root routes, most recent first. Code
comments reference this log instead of restating the politics inline — a comment that says
"don't merge without Frank's go" goes stale the moment the go happens; a dated table entry
does not.

| Date | Decision | Evidence |
| 2026-08-09 | **Machine Attention's entrance becomes the machine itself** (Frank's review, night session: `/machine-attention` was "ein sehr gutes Dossier über die Maschine … was noch fehlt, ist die Maschine als öffentliche Situation"): the entrance now stages the machine's newest real moment under the new `stage-moments/1` contract (one sentence, one subject, one honest age, **Follow it →**), with silence shown as a legal state rather than invented activity; the full manifesto moves intact to `/machine-attention/about` ("The apparatus"), where the counter-experiment framing gets its own *Why this machine exists* section so the entrance no longer depends on first understanding the ecology. Same session, practice side: The Foreknown's stage stops counting the cold start as watch — only window-open counts (1 of 100 tonight), cold start and drift get their own words, ledger splits into three source-open sections, export gains `futures_window_open`/`futures_cold_start`/`futures_drift`. Dark Ocean stays off the stage until its 2026-08-24 review, by the admission path's own rule. | `docs/design/2026-08-09-machine-attention-meta-stage.md`; `docs/design/2026-08-09-stage-moments-contract.md`; `src/pages/machine-attention/{index,about}.astro`; `src/lib/attention/moments.ts`; machine-attention commits `e108600` (stage honesty), `b2b9ff4` (moments substrate) + `docs/2026-08-09-buehnen-ehrlichkeit-und-momente.md`. |
|---|---|---|
| 2026-08-09 | **(evening) The homepage becomes an overview, and the vocabulary settles on PROJECT (Frank, correcting the same day's two earlier passes — verbatim: "da wird ja unter 'Research' alles zusammengeworfen … aktuell ist das eigentlich die haus- oder projektseite der ecology … ich würde bei projekten bleiben, weil es projekte sind").** **"House", introduced that afternoon, is withdrawn the same day** — the staffel needs no special word: **Project** (research ecology · Machine Attention · datavism.org · data-snack.com) contains **Practices** (Atelier, Field, Studio), **Experiments** (the 14 on /experiments, and Foreknown and Dark Ocean), **Instruments** (the observatory, the knowledge graph) and **Works** (the 60 practice works). Machine Attention's `tier` moves practice → project accordingly. **The homepage stops being the ecology's project page:** its rooms — "what this is", the four doors, the triptych, the Maschinenraum — move wholesale to `/ecology` (new `EcologyRooms.astro`, markup relocated unchanged so the history shows a move, not a rewrite). In their place an **overview**: the two research projects beside the strongest experiments, each card leading to its own home, the experiments' strings read from the works register so a page and the homepage cannot describe the same piece differently. **Built as a grid, not the sketched carousel** — a carousel shows six things and hides five, which is the opposite of an overview; on narrow screens it becomes one swipeable scroll-snap row, giving the same feel with no script, nothing hidden and no unrequested motion. Frank was told the reason and can still ask for rotation. **The nav is cut by kind of thing, not by house:** Projects · Experiments · Works · Talk back · About · Contact. No project keeps a mega-menu of its own; the ecology's apparatus and Maschinenraum are reachable from its project page. **Documented translation, not drift:** `machine-attention` keeps calling its investigations *projects* internally — its acceptance stage is literally named E-EXPERIMENT, and "the E-experiment of the experiment" would be unreadable. Site word **Experiment** = repo word **investigation**, recorded in the canon. | `docs/wording-kanon.md` §"Die Begriffsstaffel" (binding; the afternoon's four-/five-word rows are historical); `src/components/ecology/EcologyRooms.astro` (new); `src/components/pages/HubEntrance.astro` (overview replaces the ecology sections); `src/config/naming.ts` (`overview`, `houses` removed); `src/components/TopBar.astro` (nav by kind); `src/pages/ecology/index.astro`; `src/data/werke.ts` + `werke.test.ts` (`tier: 'project'`, `WERKE_PROJECTS`). |
| 2026-08-09 | **The homepage stops being the ecology — two houses, and the fifth word (Frank, evening, verbatim: "ich würde auch die homepage mal aktualisieren und research ecology nicht mehr prominent dort inszenieren, sondern als eines von mehreren projekten/praxen/experimenten - was ist es denn nun? :)").** His question exposed a gap in the vocabulary settled hours earlier: practice · project · instrument · experiment describe everything *inside* a house, but the research ecology is **three practices plus a contact zone** while Machine Attention is **one practice** — they were never on the same level, which is exactly what the homepage showed. **The fifth word is HOUSE:** a constituted arrangement under which practices run. Two exist — the ecology (many constitutions, federated) and Machine Attention (one constitution, one machine). Until there was a second exemplar, "the site" and "the ecology" were the same thing and no word was needed. **Executed:** the hero drops `a federated research ecology` for **`machines that research, in public`** — Frank explicitly declined `artistic research, under proof` for this slot ("so aber ohne artistic research"); the festival positioning of 2026-08-01 stays valid where it stands and is not a hero claim. The doors grid becomes a **TWO HOUSES** block at equal weight, each house showing its own inhabitants: the ecology keeps its four door cards with their validated identity colours *inside* its house, Machine Attention lists its investigations from the practice's own export. The nav group is renamed **Research** (naming it after one of two houses was the same error), gaining a **Houses** section and an **Investigations** section read from that same export, so menu, homepage and door page cannot drift apart. The SEO description follows. | `src/config/naming.ts` (`houses`, hero strings — the afternoon's `doors.secondHouse` superseded and removed); `src/components/pages/HubEntrance.astro` (`#doors` → `#houses`); `src/components/TopBar.astro`; `src/pages/index.astro` (SEO); `docs/wording-kanon.md` §"Die fünf Wörter". |
| 2026-08-09 | **Four words, one meaning each — Practice · Project · Instrument · Experiment (Frank, afternoon session, on his own observation that the site's vocabulary had come apart: "wir sollten auch ein einheitliches vokabular pflegen über die gesamte website und alle experimente").** **Practice** = a constituted, machine-run practice with its own constitution (Atelier, Field, Studio, **Machine Attention**); it produces works and is not itself one. **Project** = an admitted investigation inside a practice, having walked the admission path (The Foreknown, Dark Ocean, The State Before the Interface). **Instrument** = the class of a project that runs quietly with no stage claim and may produce nothing for years (opposite: **Flagship**); also used for the house's own tools, e.g. the knowledge graph. **Experiment** = a standalone piece of the lab, under no practice (The Protocol, Patterns, The Ghost Fleet …). **Three consequences executed the same session.** (1) **Machine Attention leaves the experiments list** — it had sat there as a peer of The Protocol since 2026-08-08, which compared a house with a room — and gets its own door at **`/machine-attention`**, built from the practice's own export rather than typed: what it is, what happens every night, its three projects with their live status, the five-stage admission path with its three outcomes, and the candidates on the shelf. It is deliberately **not a fifth door** in the ecology's grid: it is the counter-experiment, one constitution against many, and a shared grid would dissolve exactly that tension — so it stands as its own block beneath the four doors. (2) **"Background project" is struck** for The State Before the Interface: it is an **instrument** of the practice — quiet, but a full project of it, as `/observatory` already claimed by linking to the practice. (3) **`/holdings` → `/experiments`**, route and title finally the same word (the page had been titled "Experiments" since 2026-07-31 while the address said holdings); 301s on `/holdings` and `/holdings/*`, no link dies. `tier` in `src/data/werke.ts` now carries the four words, and `werke.test.ts` holds practice and instrument out of the experiments row so the next new rank cannot drift back in. The neighbourhood figure draws 14 experiments instead of 16, and its floor moved with a comment naming why rather than silently. | `docs/wording-kanon.md` §"Die vier Wörter" (binding, supersedes the route sentence of 2026-07-31 and the "Hintergrund-Observatorium" of 2026-08-08); `src/pages/machine-attention/index.astro`; `src/config/naming.ts` (`doors.secondHouse`); `src/data/werke.ts` + `werke.test.ts`; `public/_redirects`; `src/data/attention/export.json` (producer: `machine-attention` `31aa9fb`, contract `docs/design/2026-08-09-attention-export-contract.md`). |
| 2026-08-09 | **GCP returns as a targeted batch tool — G1 activated and live-tested, G5 conditionally approved (Frank, night session, on the portfolio audit's D6/D7).** The portfolio audit (same night, merged #475) extended the USP audit to the running instruments, the practices as work-lines, the ecology as a whole, and a per-project GCP opportunity map with facts verified against primary sources and live BigQuery metadata. Frank's D6 call, verbatim on G1 (BigQuery GDELT): *"okay, wenn das quasi nix kostet, könnten wir das gerne mal testen … und die lizensbedinungen müssen wir natürlich streng befolgen"* — and on G5 (Earth Engine S1): *"auch das nur, wenn keine wesentlichen Kosten entstehen und klar kann man auch mal Teile einer Ethik aufgeben, wenn es sinnvoll ist und der Sache dient."* G1 was tested the same hour: partition-filtered query on `gdelt-bq.gdeltv2.gkg_partitioned`, 30.4 MB billed (≈0.003% of the monthly free tier, €0), full run-time provenance trace (job ID, query text, bytes) — the template every future BigQuery step follows. G5 is recorded with its conditions (noncommercial tier only, zero cost, the reproducibility trade-off stated openly in the Dark Ocean V1 method sheet); no EE registration or build yet. **D7 executed in the same PR:** CLAUDE.md's "Kein GCP" line (2026-06-27, an infrastructure migration, never an ethics ruling) replaced by the conditional rule — Git stays the archive, no runtime cloud reads, trace/license/cost discipline per step. G2/G3 decide with their reworks; G4/G6 are verified negatives/reserve. | `docs/design/2026-08-09-gcp-activation.md` (decision + test record); `docs/design/2026-08-09-portfolio-audit.md` §6–§8 (merged #475); CLAUDE.md (GCP gezielt, 2026-08-09); BigQuery job `portfolio_audit_g1_test_20260809` in project `gen-lang-client-0843427097`. |
| 2026-08-09 | **Rework, never archive (Frank, same night, on reading the USP audit — verbatim): "nein, nicht archivieren, sondern so ausbauen, dass wirklicher mehrwert und alleinstellungsmerkmale entstehen!"** The audit (16 holdings: 1 draft-UNIQUE, 6 added-value, 9 redundant, every verdict against named linked neighbors) becomes a build program instead of a cull: **Phase 0** — every page cites its strongest neighbors and corrects its overclaims (honesty first, the audit's cheapest universal finding: no experiment cited its prior art); **Phase 1** — USP builds on data the pipelines already commit (Consensus syndication-structure layer, Headroom breakeven-spine, Patterns null-distribution, Attention institution scorecard, Iceberg register-integrity, Society verification pass); **Phase 2** — builds needing new material (Ghost Fleet compound joins, Delve/Tell Goodhart decline tracker, Watchtower densification time-series, Correction consequences layer, Round Numbers untested corpora, Policy cat-bond data). Checkpoint: the reading of 2026-09-05 — every holding shows its USP live or a dated build plan. The Observatory's section is handed to its own session. | `docs/design/2026-08-09-usp-rework-program.md` (the program + tracker); `docs/audits/2026-08-09-usp-audit.md` (merged #472); Phase-0 PR when it lands (branch `usp-phase0/2026-08-09`). |
| 2026-08-09 | **The USP obligation (Frank, night session, verbatim): every project must always carry "einen Mehrwert oder ein Alleinstellungsmerkmal … das kann man easy überprüfen mit recherchen im web. jedes experiment auf frankbueltge.de was keinen nachweisbaren mehrwert hat oder nicht einzigartig ist … kann eigentlich weg bzw. müsste überarbeitet werden."** Codified as a standing criterion beside the machine-advantage bar: the bar asks *could only a machine have made this*, the USP obligation asks *does the world already have this* — web-checkable, per experiment. Applies to the Holdings retroactively; new experiments answer it at the Werkgruppe §2 gate before anything is built. **A neighbor-research audit of every /holdings entry was ordered the same night** (one-time cloud agent with web research: per experiment — its own claim, its nearest existing projects worldwide, and a verdict draft *unique / added value proven / redundant*), reporting as a PR for Frank's decisions — the agent researches, it does not archive anything itself. Keep/rework/archive verdicts are Frank's, expected alongside the reading of 2026-09-05. | This row; CLAUDE.md (USP-Pflicht, 2026-08-09); the audit report PR when it lands (branch `usp-audit/2026-08-09`); `docs/superpowers/specs/2026-06-11-werkgruppe-design.md` §2 (the gate the obligation extends). |
| 2026-08-08 | **Research ecology v2 — the rebuild (Frank, night session 07./08.08., second decision of this date; supersedes parts of the row below it, each named).** Frank put the real question on the table himself: after weeks, *"nur belangloser dünnschiss produziert worden, den kein mensch da draußen interessiert oder beeindruckt"* — archive the ecology, or rebuild radically. He chose the rebuild (*"retten und weiterentwickeln wäre aber konsequent und mir natürlich lieber, weil ich dieses experiment liebe und schätze"*), ordered direct execution without practice negotiation (*"hier geht es auch nicht um verhandlung mit den praxen sondern einen echten umbau und das löschen und ersetzen von all dem scheiss den man nicht mehr braucht … quasi research ecology v2"*), and corrected the executing session's first cut mid-flight: **the three practices stay** — the field of tension between art, science and philosophy/artistic research IS the experiment; no new repos; the existing constitutions, protocols and prompts are what gets rebuilt. **Executed the same night: all three engine protocols rewritten clean** — no amendment strata, each superseded text archived unchanged in the engine's own `archive/protocols/` — with the corners named in every protocol (Field/Meridian = science; Studio/Ensemble = art; Atelier/Ulysses = artistic research/philosophy). **Shared law now plain text in all three:** machine-advantage bar at the ship gate (scale · repetition · verification · the temporal — experienceable by a stranger in the artifact) · arcs as the default object of a session, single-session works need a stated reason · concept gate/progress/ambition audit kept · outward-by-object cadence kept · record ceilings and daily line kept · zero-roster default kept · **the architect's new seven-day bind: a `prepared` packet is sent or dated-withheld within seven days**. **Lines set:** Meridian — counter-measurement in two forms (continuous instruments; FA-form investigations), **the bounded test assigned to it directly** (negotiation clause superseded); Ensemble — the 2026-08-08 line unchanged; Ulysses — **the warrant of numbers** (where a figure that governs a decision came from, whether its document still travels), set by the architect, superseding the same night's three-session proposal window. **Deleted:** the entire season/episode layer (SEASON.md closed in all three repos — Season 1 ends unfinished, **zero episodes shipped**; gate-passed work keeps its obligations and sheds only the number) · the amendment archaeology · the joint-inquiry machinery between practices (simple grammar remains: citation with pedigree, offers never orders, The Middle records) · **Ensemble's Artist as a standing voice** (supersedes the "not ruled on" of hours earlier — its question survives at full force as the conductor's own opening obligation, Kritiker-enforced at the concept gate) · stale law (Studio's "identity undecided" 75 sessions after Ensemble named itself; Meridian's duplicate outward-cadence rule; Ulysses' v4/v5 git archaeology and completed transition sections). **Kept, deliberately:** the inviolables verbatim (Ulysses' human publication gate included) · all gates at full severity · tiers and legal hygiene · **the severed readers, untouched** · memory schemes and race guard · the workshop · public seeds · REQUESTS.md as the one channel · the practices' names, signatures, archives · self-development clauses. **The reading of 2026-09-05 (the kill criterion, now in all three protocols):** four-week review + first investigation read together; house conditions: (1) the investigation stands with a named outside receiver, (2) ≥1 work shipped under v2 makes the machine's advantage experienceable, (3) it left the house under the seven-day bind (a quality-withhold counts as failing). All three → v2 continues; any failed → public dated post-mortem with **archiving as the default outcome** unless the review names evidence for continuing. **Site follow-through:** protocol mirrors resynced; `/ecology` anatomy requoted against the v3/v2/v6 texts (titles, studio's inviolable→"blurring tiers", refuses→"no trial registers", Artist removed from the cast row, field's move list corrected to six); `/season` frozen as a dated archive (closure banner, tenses to past, `CLOSED` block in `season1.ts`); `/ecology`'s season section rewritten closed. **Frank's open levers, named:** routine cadence reduction stays recommended-not-required (dispatcher prompts are version-agnostic and point at PROTOCOL.md); model heterogeneity stays deferred to the reading. | `docs/design/2026-08-08-research-ecology-v2.md` (the decision record, IN FORCE). Engine commits, author Frank Bültge: ulysses `adb63f4` (Protocol v6), field-research `19178ec` (Protocol v3), studio `2ecebad` (Studio Protocol v2) — each with `SEASON.md` closed, `REQUESTS.md` v2 note, superseded protocol archived in `archive/protocols/`. Site: `src/content/{atelier,field,studio}/PROTOCOL.md` (mirrors), `src/lib/ecology/anatomy.ts` (requotes), `src/data/season/season1.ts` (+`CLOSED`), `src/components/pages/SeasonPage.astro`, `src/pages/ecology/index.astro`, this log. |
| 2026-08-08 | **The apparatus turns outward — the 2026-08-07 proposal executed (Frank, night session, "lies … und setz um").** Four questions its §7 reserved for him were answered first, and the answers shape what was built. **(1) "Outward" is redefined by object, not activity (rule 5, all three protocols).** The old wording listed *kinds of work* and said nothing about what a session works *on*, so a practice counted a session as outward whenever the object was not its own output — field-research session 89 read it exactly that way, correctly, while auditing The Consensus, a house instrument. Under that reading the ecology could have run at 100 % outward while touching nothing outside itself. Outward now means an object that exists whether or not this house does; auditing a sibling's instrument is **inward**. Nothing forbids self-audit — it stops counting as engagement with the world. **Frank's answer on retroactivity: forward only** (rule 6's precedent, "binds forward; existing records stand"); sessions 89/90 are not reclassified. **(2) Rosters culled in field-research and studio.** Meridian keeps Interlocutor, Verifier, domain specialists and the parallel searches (reframed: search fan-out, not roles); loses Proposer, Synthesiser, Archivist, Builder **as roles** — each stays a step the conductor performs. Ensemble keeps the severed readers (untouched, and said so twice in the text), Kritiker, Verifier, guest voices, specialists, and the Dramaturg **conditioned on a finished object** — it judges and cuts, never co-writes; loses Archivist and Builder as roles. **The gauntlet did not soften: the Skeptic is cut as a role and its blocking clause is not** — *"the core claim must survive an independent refutation attempt"* moves verbatim into the Interlocutor's pass as clause (a), still blocking, and a pass returning only the hostile critique has not run (a). New in both: **the default is zero convened roles**, each convening named in the journal header with its reason, a repeated roster explained, unexplained convening a recorded violation — enforcement of a sentence both protocols already carried and neither exercised (Ensemble convened the identical five in sessions 71–74; Meridian six and five in 89–90). Ulysses is untouched by the cull because it has **no role machinery at all** — and that turned out to be the house's own strongest evidence: it convenes nothing and holds 56 entries in `works/` against field's 22 and studio's 5. **(3) Studio's remit narrowed to Frank's own words of 2026-08-06:** only digital works; only what a machine does better than a human — scale, repetition, verification, the temporal; the added value of a machine-run collective must be *experienceable in the work itself*. Consequences named rather than left to rot: spatial/printed/fabricated work leaves the remit, the physical-realisation channel and the €150/quarter fabrication ceiling of 2026-08-01 go **dormant, not deleted**, works already shipped (the NO PART wall) stand as shipped, and screen- or network-borne performative/participatory formats are **not** excluded. Field's remit deliberately unchanged ("do not fix this" — it ships most). **(4) Atelier's line: handed back to the practice, at Frank's choice** — Ulysses argues its own line in its own record within three worked sessions and Frank decides on the proposal; the candidate *the warrant of numbers* travels as material, explicitly not an instruction; silence past three sessions returns the decision to him. **(5) The bounded test, ordered:** one investigation in Forensic-Architecture form, on infrastructure outside this house, ending in an artifact a **named receiver outside the house** could use, in the post office by **2026-09-05** — three days after the Production Amendment's own review, deliberately (the review reads numbers; this reads whether anything left the house). Not assigned: negotiated between the practices in rule 8's grammar, falling to Meridian if unclaimed after three worked sessions. **It does not lift the standing rule of 2026-08-07** — the receiver is named in the packet, never addressed; sending stays Frank's. **Model heterogeneity (proposal §5.2) deferred at Frank's choice** — not now, reconsidered at the four-week review; the role cull is what would have paid for it. **Site follow-through:** the `/ecology` anatomy figure quotes the protocols verbatim and its honesty test caught the drift within minutes — field's cast goes 5 → 2, studio loses Builder/Archivist and gains the severed readers it had never named, the Skeptic's gate is redrawn as Interlocutor (a). The test's own guard changed with it: `cast.length > 3` was a proxy for "invents no roles" and broke by the cull being right, so it is replaced by the invariant it stood for — **every name shown must still appear in that practice's protocol**, which survives any future cull and additionally catches a role kept on the site after a practice drops it. The studio door on the hub stopped advertising "under no label" (an approved `NAMING` string, changed under the currency rule of 2026-07-25 because it now contradicted the constitution). **Not touched, deliberately:** the inviolables, all gates and their bars, provenance and tier discipline, the blind panel, the nightly pipelines, the Atlas, the Post Office, the archives — and **the season's direction, which has been the practices' own since 2026-08-06**; a remit is what kind of practice this is, a direction is what a season is about, and only the first moved. **Left open and said so:** Ensemble's **Artist** was never examined by the proposal and is therefore untouched — the reasoning that cut Meridian's Proposer would reach it, and studio's founding critique ("a production collective without an artist produces production, not art") is unanswered; it is a live question for the four-week review, not a decision made behind a summary. | Proposal executed: `docs/design/2026-08-07-the-apparatus-turns-outward.md` (§2, §3, §4, §7 — status block updated in place). Engine commits, author Frank Bültge: ulysses `b25e1fb`, field-research `4fdc415`, studio `0fa6c5a` (`PROTOCOL.md` amendments dated in place + `REQUESTS.md` architect notes 2026-08-08). Site: `src/lib/ecology/anatomy.ts` + `anatomy.test.ts` (the replaced guard), `src/lib/notation/register.ts` (change row + the 2026-08-03 open question updated, not overwritten), `src/config/naming.ts` (studio door), mirrored `src/content/{atelier,field,studio}/PROTOCOL.md`. In-house measurements behind the rows, re-checked this session rather than taken from the proposal: `ls works/` per engine repo (56 / 22 / 5). |
| 2026-08-06 | **Season 1's theme and its seven candidate directions withdrawn without replacement (Frank, night session).** His objection, verbatim: *„wer hat hier die themen vorgegeben? das ist doch totaler schwachsinn. die praxen sollen ihre eigenen themen finden und die episoden verhandeln und ssot ist nicht nur /holdings und ich würde sogar noch das thema der season verhandeln lassen."* Asked how far to open it, he chose the radical variant on both counts: no theme at all, and the seven directions struck with nothing put in their place. **Rule 8 amended in all three engine protocols:** the architect opens a season and fixes its **shape only** — how many episodes, and the date he reads the review. The direction is the practices' own negotiation (argued in a practice's own record, adopted/sharpened/contested/traded through their channels, The Middle records what meets); it may stay unset the whole season and be named at the close out of what the episodes turn out to have had in common; no timer forces it and the architect casts no tie-break. Reason the list had to go: a list of subjects in a constitutional document is an assignment however it is labelled — handing the practices slot *allocation* the day before left them negotiating only who takes which given topic, and the house already has a channel for the architect's ideas (`REQUESTS.md` seeds — offers, not orders). **SSOT corrected: no corpus is designated** — the bolded sentence making the Holdings the season's "root and material" is withdrawn; each practice's own archive is its first material, the house record (Holdings, Atlas, catalogues, committed data archives, The Middle) is citable but never expected, and material from outside the house counts exactly as much. **Studio's override lifted:** the clause by which the ecology's season *replaced* the house's internal season mechanism and its standing premiere ruling rested on the architect declaring the direction; whether those revive is now Studio's own call, made in its own record. Struck text is not reproduced — it stays in `SEASON.md`'s git history so records citing "candidate direction *n*" still resolve, and no work made under it is retracted (those directions were read out of the practices' journals in the first place). **Same session, three drifts on `/season` fixed:** Ulysses' Episode 6/7 claim of 2026-08-05 (*The warrant that does not travel*) was missing entirely; Meridian's concept was still displayed as a live "intent filed" carrying its day-one figure, although the practice **parked** it on 2026-08-05 after its own archive run refuted all three of its pre-registered predictions; and open slots still displayed the withdrawn topics. The register now cannot reintroduce them — an open slot carrying a title, subject, practice or note fails the test suite. | Engine repos ulysses / field-research / studio: `PROTOCOL.md` rule 8 (amended, dated in place), `SEASON.md` (rewritten), `REQUESTS.md` team note 2026-08-06 (author Frank Bültge). Withdrawn text in `SEASON.md` history: ulysses `20c1ca9`/`4dbd51d`, field-research `ea8fc54`/`d9aacd4`, studio `15c8488`/`34a189e`. Site: `src/data/season/season1.ts` + `season1.test.ts` ("an open slot carries no subject, no title and no practice"), `src/components/pages/SeasonPage.astro`, `src/pages/season/index.astro`, `src/pages/ecology/index.astro`, mirrored `src/content/{atelier,field,studio}/PROTOCOL.md`. Practice records behind the rows: ulysses `projects/2026-07-23-negative-parallax/EPISODE-6-CLAIM.md`, studio `projects/season1/STILL-DARK-DOSSIER.md`, field-research `drafts/2026-08-04-echo-below-the-line/CONCEPT.md` (dated notice, session 91). |
| 2026-08-05 | **The Production Amendment + Season 1 (Frank, night session 04./05.08.):** after a full corpus audit of all three practices (verdict: distinguishable, not wallpaper — constitutions show in works AND judgment; but apparatus outweighs work everywhere, and the Holdings, built in directed sessions, beat the autonomous loops per token), Frank ordered a hard rebuild instead of soft seeds — "ich will hier nicht eine woche mit einer ki verhandeln". The amendment (decided and drafted by the architect, committed to all three engine repos): sessions are paid compute · concept gate (proof in ≤ 3 sessions licenses arcs of any length — no hard deadlines, but no arc without a passed gate) · progress every 3 worked sessions · ambition audit (under-delivery = failed forecast → short leash) · theory ships as work only when demonstrably new · one inward session in four, repair counts as inward · record ceiling (journal ≤ 400 words, process record < 3,000) · daily public line · ecology-wide seasons. Season 1 declared: **Counter-Measurement, seven episodes** — the Holdings' instruments and archives are citable, auditable, extendable material (explicitly not a mere control group), Atlas + catalogues named sources, seven candidate directions offered. Inviolables, gates and honesty discipline unchanged. Same session: /consensus/archive shipped (42 days, gaps visible) and the evidence track (every outlet links to the article that carried the sentence; no backfill — links from 2026-08-05 on). Pending on Frank: dispatcher cadence reduction (routines live in his personal account). Review of the amendment dated: four weeks. | Engine commits ulysses `20c1ca9`, field-research `ea8fc54`, studio `15c8488` (PROTOCOL.md amendment + SEASON.md + REQUESTS in-force note, author Frank Bültge); site PRs #385 (archive), #386 (evidence track); the corpus audit in this session's record. |
| 2026-08-01 | **Poste restante** (Frank, evening — revises §3 of the same morning's governance yes): the outgoing post ledger is part of the work, not a task queue — letters lie addressed, complete and publicly collectible; "es ist quasi schon zugestellt, also an die welt"; any reader may be the courier, and in the best case the receiver collects their own mail. Nothing is ever archived away; the 7-day forwarding duty is withdrawn; collection/reply/enduring silence stay faithfully recorded. The tension with instrument 016 ("published is not delivered") stays on the record as deliberate inversion, not denial. Named exception: external calls with deadlines (DARC 2026-08-28, ZKM 2026-09-06) can only be submitted — unsent, they lapse, dated. | Governance §3 revision block (`docs/superpowers/specs/2026-08-01-governance-proposal.md`); `/post` copy; Steuerzentrale post lane; `src/data/post/ledger.json` notes; team note in the four practice channels. |
| 2026-08-01 | Governance in force + first gate decision (Frank, morning session): all four governance boxes yes, verbatim "alle 4 governance kästchen ja" — gate SLA 72 h with public gate ledger, fabrication ceiling €150/quarter (first draw: the NO PART wall), post rota 7 days, MRR leash. Same message: "operative ruler v3 go" — the Operative Ruler published with its correction record (second publication in the Atelier's history; executed per the kartographie precedent, ulysses `5bf4516`). And both festival applications laid into the post office as prepared items — Frank's framing: the ecology applies itself, he reviews and forwards. Commissioned next: the Steuerzentrale rework (design doc in this repo). | `docs/superpowers/specs/2026-08-01-governance-proposal.md` (§5 decided, §6 gate ledger first row, §7 consequences); ulysses `projects/2026-07-23-negative-parallax/PUBLICATION.json`; `src/data/post/ledger.json` (entries `2026-08-darc-application`, `2026-09-zkm-arte-util`); `docs/design/2026-08-01-steuerzentrale-v2.md`. |
| 2026-08-01 | Festival line decided (Frank, night session 31.07./01.08.: transmediale / Ars Electronica / ZKM as the 1–2-year goal — "genau das ist das ziel", then "machen von a bis c"): the positioning is superseded dated — "artistic research, under proof" (claim and show receipts) replaces the no-art-claim framing of 2026-06-20, whose honesty core (no unearned claims) stays binding. Eight concept adjustments (addressee as constitutional category, cross-practice review, work-before-apparatus, practice sharpenings, Middle as convenor, the human measured, the body clause) travel as seeds/offers, never decrees. Submission calendar verified against primary sources: DARC workshop 28.08.2026, ZKM Arte Útil 06.09.2026, Prix Ars Electronica "Artificial Life & Intelligence" + S+T+ARTS cross-submission Jan–Mar 2027; ISEA 2027 dropped (does not exist). Site wordings unchanged pending the wording gate. Note: this log had itself drifted (no rows 20.07.–31.07.); the interim decisions live in `docs/wording-kanon.md` and `docs/design/*` — future rows resume here. | `docs/superpowers/specs/2026-08-01-festival-line.md`; drafts `docs/festival/2026-08-darc-application-draft.md`, `docs/festival/2026-09-zkm-arte-util-draft.md`; ji-2026-002 registration in `research-ecology` (same session); CLAUDE.md positioning pointer updated. |
| 2026-08-01 | **Entrance section order (WP7), approved arrangement:** the Maschinenraum block — the compact score of the last fourteen days plus the four voice lanes — moves from behind LATEST to directly behind the doors and their new triptych, so the sequence reads hero → what this is → **who lives here (doors + triptych) → what they last did (Maschinenraum) → what they made (LATEST)** → the other houses → catalogues. Reason: "who lives here" and "what they last did" are one thought, and a visitor can only place a list of work titles once they know whose hands made them; LATEST used to interrupt that pairing. Nothing was removed — the same two blocks, in the other order. Each voice lane and each practice door also gained a link into that practice's guided tour (`DoorItem.tourHref`), and The Middle deliberately got none: it is a contact zone, not a practice, and keeps no tour of its own. | `src/components/pages/HubEntrance.astro` (section order + `mrRows` tour links), `src/config/naming.ts` (`DoorItem.tourHref`, `NAMING.triptych`, `NAMING.maschinenraum`), `src/components/hub/Triptych.astro`, `src/lib/hub/triptych.ts` + its test; branch `preview/hub-triptych`. |
| 2026-08-01 | **Climate ridgeline on the entrance: DECLINED** (WP7). A second hero figure from `src/lib/climate.ts` (the committed GISTEMP snapshot) was considered for the entrance and is not being built, for three reasons, each sufficient on its own: (1) **it is not this ecology's own record** — every other figure on the entrance is derived from what these practices committed, and a global temperature series is a borrowed dataset that would make the page assert something the research did not produce; (2) **currency** — the snapshot (`src/data/climate/global-temp-anomalies.json`) was last committed on 2026-06-10 and has not moved since, while the standing rule (CLAUDE.md, 2026-07-25) is that the site shows the latest state of the work; a figure no nightly refreshes would have been stale on the day it shipped; (3) **it duplicates the pulse's form** — a horizon/ridgeline of one series over time is what the hero pulse already is, and two of them one under the other would read as one chart drawn twice. `src/lib/climate.ts` and `npm run climate:refresh` stay in the repo, dormant and unlinked; nothing was deleted. The hero pulse stays mono and untouched: `pulse.json` carries no per-repository split, so colouring it per practice would be a fabricated attribution. | `docs/decision-log.md` (this row); `src/lib/climate.ts` (dormant, no importer); `src/components/pages/HubEntrance.astro` (hero pulse unchanged, caption still names its snapshot). |
| 2026-07-31 | English-only extended to code (Frank): comments, test names/descriptions and commit messages in this repo are English going forward; existing German comments migrate when a file is touched anyway — no mass rewrite of committed history. Same day: the visible name on `/holdings` returns to "Experiments" (house vocabulary traded back for a word everyone understands); route, links and redirects stay unchanged. | `CLAUDE.md` ("EN-only gilt auch im Code", 2026-07-31); `src/components/pages/BestaendeIndex.astro` (comment dated 2026-07-31). |
| 2026-07-30 | Practice visualizations may carry their own visual language (Frank: „[Wortlaut privat]"): colour, depth and interaction are allowed for Atelier/Field/Studio figures — as long as every figure derives from committed data, palettes are validated against both surfaces, and status colours stay out where the practice does not judge. | `CLAUDE.md` (2026-07-30, „Visualisierungen der Praxen dürfen eigene Bildsprache haben"); reference implementation `src/components/atelier/ProcessFigure.astro` + `src/styles/atelier-process.css`. |
| 2026-07-28 | Paper Catalogue ships, built from what the practices actually cite; the Dataset Register is cut back to a machine-readable index — 8,590 generated detail subpages removed. | Commits `03067c54` (papers), `800f51c0` (Rückbau). |
| 2026-07-26 | Licence change (Frank): all lab repos move to open licences — code Apache 2.0, works CC BY 4.0, data and archive snapshots CC0; the AI-training reservation moves from the licence into the crawler policy. Exception: seeds submitted via `/seed` stay CC BY-NC-SA 4.0 (promised to submitters, not retroactively changeable). Same day: "Catalogues" becomes the nav collection point for Atlas and Dataset Register. | Commits `6907f157` (licence), `269b33ce` (Catalogues, „Frank, 2026-07-26"); `docs/design/2026-07-26-lizenz-entscheidungsvorlage.md`; `docs/design/2026-07-26-crawler-politik.md`; `LICENSE.md`. |
| 2026-07-25 | Currency rule (Frank, binding): the site must always show the newest state of development — every session touching site or engines actively checks for drift; superseded structures are archived visibly and dated, never silently left standing as current. Same day: `docs/wording-kanon.md` becomes the authoritative language reference, "Error as Method" leaves the site's voice, and the atelier's public wording syncs to Protocol v5's work-line model. | `CLAUDE.md` (Aktualitäts-Regel, 2026-07-25); commits `a6b2ba8e` (wording canon), `21a1d1c8` (v5 wording sync); `scripts/drift-check.mjs`. |
| 2026-07-24 | Role line becomes "Data Engineering & Analytics" (Frank — not "Data & AI Engineer", not "data artist"). Same day the Atelier adopts Research Protocol v5 (open-horizon work-lines replace v4's bounded projects); site pages derive the protocol version dynamically from the mirrored `PROTOCOL.md` instead of hardcoding it. | `CLAUDE.md` (Rollen-Zeile, 2026-07-24); `src/content/atelier/journal/2026-07-24-protocol-v5-adoption.md`; `src/lib/atelier/protocol-version.ts`. |
| 2026-07-20 | Atelier-Eingang (Stufe 0), Frank approved 2026-07-20 („[Wortlaut privat]" auf den Vorher/Nachher-Mockup): `/atelier` wird eine verständliche Haustür — ein erklärender 4-Satz-Absatz und vier Türen (`now` mit Live-Projektzähler · `works` mit Nightly-Phasen-Rahmung · `what this is` → foundation · `the working sheet`), plus eine leise Zeile in die tieferen Räume. Das abstrakte Blatt bleibt unverändert und zieht als Zimmer nach `/atelier/sheet` um. Mit derselben Freigabe: `ATELIER_NARRATIVE.approval` → `approved` (Draft-Chip aus). Offen bleibt Stufe (b), separat: ob das Blatt Zimmer bleibt oder die /atelier-Struktur weiter umgebaut wird. | Merge `feat/atelier-entrance` (`e54f004`) nach `main`; `src/pages/atelier/index.astro`, `src/pages/atelier/sheet.astro`, `src/config/atelier-wording.ts` (`entrance`-Block + `approval`). |
| 2026-07-20 | Öffentliche Saat (Stufe 1), Frank approved 2026-07-20: D1 seeds address the ecology level, offered to one chosen practice or "open" to all three; D2 Turnstile is a deliberate, declared exception to the zero-third-party stance (same vendor as hosting); D3 the AI gate blocks autonomously with a public reason code, model, and prompt, but never has the last word — the practice does; D4 the register ships before email intake (Stufe 2), because a register with real seeds is the best case for signing up later; D5 only the three research practices (field, studio, atelier) are addressable in Stufe 1 — the Plenum is a later, separate question for data-snack.com. Status: built (register logic, gate contract, sync job), waiting on the §9 secrets checklist (`SAAT_GITHUB_TOKEN`, Turnstile keys, `GEMINI_API_KEY` as a CF Pages secret, engine amendments) before it goes live. | `docs/superpowers/specs/2026-07-20-oeffentliche-saat-design.md` (§1 decisions D1–D5, §9 checklist). |
| 2026-07-17 | Standing rule revised (Frank, afternoon session, "keine 7 tage regel"): the 7-day window for unanswered requests is replaced by same-session semantics — a request to Frank never blocks, and if he has not answered by the practice's own next session, it decides itself and journals the move. Same session: the private Steuerzentrale approved and built (route `/steuerzentrale` + `functions/api/zentrale/`, branch `steuerzentrale`) — status at a glance plus quick answers into the practices' REQUESTS.md; silence stays a legitimate answer. | REQUESTS.md commits `f213e0ef0b` (field-research), `a0e8d54eed` (studio), `e75d209f1b` (irrtum-als-methode), `68287fcfa9` (data-snack-plenum); `.github/workflows/morning-digest.yml` section title; `docs/design/2026-07-17-steuerzentrale.md`. |
| 2026-07-17 | Membership decisions ("ja gib frei", Frank, night session): Frank Bültge admitted to the ecology as a human practice (party to encounters, with the role-separation rule participant vs. conductor); data-snack.com admitted with the Plenum as resident collective; datavism.org candidacy stands pending its governance statement. Standing rule established across all practice repos: unanswered requests to Frank are never a blocker — past a named deadline (or 7 days without one) the practice decides itself and journals the decision. Same night, also approved and live: apparatus rewrite (P0.2), qualified-autonomy language site-wide, Ensemble re-framed as artist collective, declared trajectory (transitional architect role, phased governance experiments). | research-ecology `docs/design/membership-proposals-2026-07-17.md` (status block); `docs/spec/10-REVISION-NOTES.md` ("Declared trajectory"); REQUESTS.md standing-rule commits in field-research/irrtum-als-methode/studio/data-snack-plenum; site commits `bd01dcb`, `7f05fb6`. |
| 2026-07-16 | Hub wordings approved ("wortlaute sind freigegeben", Frank, morning session) — `NAMING.approval` flipped to `approved`, one factual fix included (Holdings: "Police" → "The Policy", the experiment's actual English title). Remaining open points delegated ("bitte durchziehen und selber entscheiden"): Atlas duality resolved as proposed (Frank's atlas stays `/atlas`; Ulysses' papers shelf goes to `/atelier/material` in a later package), practice pages stay static-from-data (apps remain undeployed until the CF-secrets/DNS package), "Holdings" kept as the English label for `/bestaende`. | This row; `src/config/naming.ts`; research-ecology `docs/design/site-v2-decisions-2026-07-16.md` §1 addendum. |
| 2026-07-16 | site-v2 principle session: descriptive lowercase title ("a federated research ecology"), pulse hero from real commit data, the current encounter prominent right under the hero, `/lab` → 301 to `/bestaende` (no two collection pages), German dropped site-wide (routes, toggle, i18n). | research-ecology `docs/design/site-v2-decisions-2026-07-16.md` (§1, Frank 00:45–01:00); implemented on branch `site-v2`. |
| 2026-07-15 | The site entrance becomes the Partitur (score map) of the current encounter, replacing the prior bio/experiments home — Frank's own words: "noch ziemlich dünn ... sollte genauso professionell und ästhetisch ansprechend sein wie die anderen Sachen." | Merged to `main` at commit `63692ae` ("ecology: Neustrukturierung live — Eingang als Partitur (Variante A)"), preceded by `d0f50ff`. |

Future approvals get a new row above, oldest at the bottom. See also `LICENSE.md` (standing,
not an approval event) and each experiment's own methodenblatt for its individual sign-off
trail.
