# Decision log — frankbueltge.de

Approvals that changed what this repo publishes at its root routes, most recent first. Code
comments reference this log instead of restating the politics inline — a comment that says
"don't merge without Frank's go" goes stale the moment the go happens; a dated table entry
does not.

| Date | Decision | Evidence |
|---|---|---|
| 2026-09-03 | **The removals at their seats, the world chamber's hosts and the trending countries** (Living Globe, G3 — the remaining layers, second evening; the plan of `docs/design/2026-09-03-living-globe.md`, whose G3 row names the removals and the hosts here). **What was red:** `/redaction` and `/trending` are the two instruments this house runs that had no layer at all, and both of them are about places — a body that takes a page back sits somewhere, and a host is registered somewhere. The globe drew neither. And a country card still read “centroid of the country QAT”, because the alpha-3 code was the only thing that had crossed into the island. **What changed — three layers, each carrying one number and saying which:** (1) **Editorial Deadline, at the seats** (`stations`, counter-measurement, seventy-one days): one mark per institution that lost content on a night, at the seat it publishes from, its value the number of WORDS it took back — the figure `/redaction` prints beside every row of its ledger. An institution with several removals in one night is ONE mark, because they would stack on the same point anyway: the value is that night's total and the locator names the largest of them, which is the rule the page itself picks its headline removal by. Every record is a `seat` outright rather than inheriting the seats file's own kind — a removal happens in a document and a document has no coordinate, so this layer has no station case at all, and the card, the plate's `<title>` and the table all say “seat of” before anything else. A night the watch found nothing draws nothing and says, from the record's own count, that the archive took nothing back across the pages it watched; a night that could not verify some of them says how many. The label is the watch-list's own, quoted and never translated, exactly as `/redaction` renders it. **The institution→seat table** is the house's one committed map, `REDACTION_SEATS` in `src/lib/globe/seats.ts`, and it is total over the archive: WHO → who, UN → un, UNHCR → unhcr, IPCC → ipcc, EU-Kommission → eu-commission, NASA → nasa, NOAA → noaa, EPA → epa, CDC → cdc, BLS → bls, US State Dept → us-state, US Census → us-census, White House → white-house, UK Gov → uk-gov, Bundesregierung → bundesregierung, IEA → iea. Resolving through it now THROWS by name (`redactionSeatFor`) instead of answering `undefined`, and two suites walk all seventy-one committed days plus the pipeline's own watch-list, so an institution without a seat is a red build rather than one mark fewer. (2) **Editorial Deadline, the world chamber** (`points`, counter-measurement, twenty-one days, fourteen marks on the newest): the hosts of the sample the day ACTUALLY checked — the pool the record names in `deletion.pool_day`, not the one it committed that night for the next run — placed by the country their top-level domain is registered in, one mark per country, its value the hosts counted there. **What is committed per host and what is not decided the shape of this layer.** The record holds a receipt for every URL that came back gone (404/410) or withheld for legal reasons (451), with its domain, its class and its code — so those outcomes are per host and are named in their country's own mark. Everything else the night found (answered, bot-walled, unreachable) exists only as a count for the whole day and is therefore stated in the frame's note and attributed to no country at all: a mark carrying a made-up per-country bot-wall share is the one thing this globe exists not to do. **451 is never folded into gone** — the note keeps them apart in words, the two classes read as two different sentences, and the record's own caution is quoted byte-exact in the source block (“451 from a German vantage point may mean EU geo-blocking rather than takedown; it is reported apart and never folded into the gone rate.”). The grouping rule is the consensus layer's own function, imported rather than copied: **a top-level domain is a registration, not a location**, so a host whose domain belongs to no country is counted, its labels named, and placed nowhere — on the newest day that is two hundred and twenty-seven of three hundred. Countries the 1:110m atlas draws no polygon for (Singapore, Tuvalu) are counted in the note and drawn nowhere; a day the record says it could not measure draws nothing and quotes its own reason. (3) **Common Ground, by country** (`points`, nightly ledger, two days so far and growing nightly): the signals of sources that publish per country — the daily search trends and an app store's chart — one mark per country per day, its value the signals recorded there, the sources named in the receipt. **A language is not a country:** Wikipedia's most-read pages are published per wiki, and a wiki is read wherever its language is read, so those signals are counted in the frame's note (three hundred of them on the newest day, in six languages) and placed nowhere. The two shapes are told apart by a rule and not by a list of source names — two upper-case letters is a country code and must resolve through the committed crosswalk or the build stops naming it; a code the record itself calls a language, or writes in the lower case the archive reserves for a language tag, is a language; anything else stops the build too, because a third shape of `geo` is a change in this house's own pipeline and must be seen. The adapter assumes no number of days. **The wording repair carried over from the first evening:** a country place now carries the crosswalk's own NAME beside its code, resolved by the adapter that resolved the code, so the card reads “centroid of Qatar”. The island holds no crosswalk and must never grow one, so the name travels inside the record and the bundle is unchanged. **Also:** the layer order stays editorial — the removals join the fixed stations, the two new country layers come after the ones already there, because a centroid is the weakest claim on this globe; the manifest, the feeds, the plate, the tables, the legend and the method sheet's source rows all derive from the registry and needed no edit. No new colour: all three are lab-line layers wearing the room's live ink when in front. The stories guard moves with the registry — all three of the stories the plan named for G3 are now writable and owed, each by its layer's own id (`redaction-seats`, `redaction-world`, `consensus-tld`) and never by the plan's shorthand. **Verified in the browser** (built site, 1440×1100): all nine layers register and each is fetched exactly once, with no request on a re-toggle; the day axis stays the union of eighty-five nights, and on the oldest of them the world chamber and Common Ground both say “nothing on this day”; a seat card reads “seat of the body that publishes it”, a Balance card reads “centroid of Qatar”, and the two new point layers read “centroid of a country” with the country named in the record's words; the three G2 stories still drive the globe through all fourteen scenes and open their two closing cards; the entrance is untouched, still compact and still fetching only the sky, the ghost fleet and the land. Console clean but for the known analytics 404. Budgets re-measured and unchanged — the island 5.9 KB gz of 15, the WebGL half 208.5 KB gz of 320, since three layers brought no new kind of drawing. Feeds against the 150 KB gz per-layer ceiling: the seats 6.0, the world chamber 7.8, Common Ground 1.0, and Balance 38.7 → 40.0 with the country names in it. | `src/lib/globe/layers/{redaction-seats,redaction-world,trending,index,types,balance}.ts` + `adapters.test.ts` · `src/lib/globe/seats.ts` (`redactionSeatFor`) + test · `src/config/globe-wording.ts` + test · `src/components/pages/LivingGlobe.test.tsx` · `src/components/pages/globe-deck.test.ts` · `src/lib/tour/globe-stories.test.ts` · `scripts/budgets.json` · `docs/design/2026-09-03-living-globe.md` |
| 2026-09-03 | **The Studio gets a second source: the Atlas of Data Art** (Frank's decision of 2026-09-03, wording private, answering the question left open when cycle 002 was turned). **Why.** Every one of the Studio's seven cycle-001 works was built from the Field's material, as its constitution orders — and that material was the response ledger, so the Studio's works were about expressions of concern and publisher doors, one remove from a direction that was never its own. **What changed.** From cycle 002 the Atlas (`/atlas`, 521 source-cited works, each with its decisive move named) stands beside the siblings' research as inspiration — and, at the same time, as the neighbour list the USP duty already requires: a work built in its light names its nearest entries and states its daylight. One rule, three ways: never copy an idea or a concept (the `decisive_move` line is what must not be reproduced); make something new, or take a work further, or carry it into a new context. Interactive works, current technology, the React islands and code from existing repositories are open and wanted — under a licence rule in three cases (permissive: embed with attribution · copyleft: tool, not code · no licence: never), which the three example repositories Frank named happen to fall one into each of (archify MIT · VoiceStudio AGPL-3.0 · fable-cities no licence). **HeyGen:** the MCP registered in Frank's local session is OAuth and does not travel to the practice's runtime, and no key exists in any repository — so it is not reachable until a `HEYGEN_API_KEY` secret exists on the studio repo, which only Frank can create; the direction says so and names the cost exposure of a nightly practice with a paid key. Studio protocol §5 amended (constitution change ordered in-session, self-merged per the rule of 2026-09-02). | `src/data/ecology/cycle.json` (studio default) · `studio/PROTOCOL.md` §5 amendment and `studio/REQUESTS.md` direction of 2026-09-03 (`bcdd2e1`) · CLAUDE.md |
| 2026-09-03 | **Three country layers on the globe, and the seam taken off the sphere** (Living Globe, G3 — the remaining layers, first evening; the plan of `docs/design/2026-09-03-living-globe.md`, whose G3 row names this evening's three layers and puts the tessellation check here). **What was red:** the globe drew what this house measures at a PLACE — satellites overhead, vessels that went dark, the seats institutions publish from — and nothing it measures by COUNTRY, which is most of it. The country half of the layer contract (`kind: 'countries'`, `at: { iso3 }`, the centroid rule, the five-step ramp, the ISO/FIPS/ccTLD crosswalk) had been built in G0 and G1 and had no layer using it; `/globe/countries.json` served a topology nothing fetched. And a faint grey line ran across the sphere south of the equator with a smear at the north pole, handed to this evening by the G1 row as "the sea polygon's tessellation". **What changed — three layers whose records name a country, each carrying one number and saying which:** (1) **Balance** (`countries`, counter-measurement, twenty-two days, fifty-eight fills on the newest): the fill is the measure `/balance` headlines and its ledger sorts by — the tone of a country's own press about it minus the tone of the world's press about it (GDELT V1.5TONE, document level), read from `dims.tone.{self,foreign}` of each row of `src/data/balance/<day>.json`, toned by where it falls inside that day's own range and compared across no two days. Each receipt carries the country's own name, both tones, the gap and its bootstrap interval in the words the page already prints, with `n.s.` where the interval crosses zero. (2) **Invoked Past** (`points`, memory, twenty days, six marks a day): the countries whose press invoked the day's MOST-invoked year — the row of `top_years` for the year `most_invoked` names, never the ranked list's first row — with the mention count the record carries as the value, at the centroid of each country's polygon. (3) **Consensus, by domain** (`points`, counter-measurement, seventy-five days, up to five marks): the mastheads of the day's most-echoed phrase counted by the last label of their domain, one mark per country, the value the number of mastheads. **The honesty rule of the third layer, stated in its source block, its caution and the method sheet: a top-level domain is a registration, not a location** — a domain bought from one country's registry says nothing about where the newsroom, the reporter or the server is. A domain with no country in it is counted and stated in words ("so many of so many mastheads carry no country", with the labels named) and placed nowhere at all; on the newest day that is all forty of them, so the layer draws nothing and says so, which is the correct picture of an American wire story. A two-letter label the crosswalk holds no country for (`eu`) is counted the same way rather than stopping the build — a domain is not a country code out of this house's own archive, and a nightly build must not break the first time a new outlet appears; a FIPS code from a day file still throws, as it always did. **Countries the atlas cannot place** are counted, never estimated: at 1:110m there is no polygon for Bahrain, Bermuda, Singapore, Barbados or Malta, so their rows are stated in the table and in the frame's note and drawn nowhere. **The tessellation check, measured:** with the Balance layer in front and the sphere zoomed in on west Africa, deck.gl's default resolution of ten degrees leaves notches inside Ghana, Nigeria and Saudi Arabia where a cell of the grid cut falls away; at the raised resolution of four — the prop is in degrees, so four is the finer mesh — the notches are gone and every fill sits inside its own outline, with no chord cutting through the sphere. No clip of our own was needed, so none was written; the measurement is pinned by a test on the constant (`g3a-tess-res10.png`, `g3a-tess-res4.png`). **The seam: the earlier attribution was wrong, and the measurement says why.** With every data layer switched off and the sea and land painted in temporary contrast colours, the artefacts stayed — so they come from the ground, but NOT from the sea: the sea is `--color-panel`, the very colour the stage behind it is painted, so a sea artefact cannot be seen at all. They came from the LAND. Natural Earth stores a ring that crosses the antimeridian as a ring that JUMPS from one edge of the longitude plane to the other, and deck.gl's globe view subdivides a polygon by the view's resolution before projecting it — so that one segment is drawn as a band around the whole earth. Four rings of the committed land do it seven times: Fiji at −16.8° and −16.1° (the line south of the equator), Eurasia at 65.0° and 69.0° and Wrangel Island at 70.8° and 71.5° (the three concentric rings at the north pole), and Antarctica at −84.7°, which is why the sphere had a band of coast there and open sea over the south pole. **What worked was not a split but an unwrap:** walk each ring once and keep every vertex within half a turn of longitude of the one before it, shifting whole turns as needed, so a straddling ring becomes continuous around −180 (Fiji spans −181.4° to −179.8°); the projection is periodic, so a vertex at −181.4° lands exactly where +178.6° lands. A ring that genuinely encircles the earth cannot be unwrapped — that winding IS a polygon holding a pole — and is closed through that pole by the two corners a flat file has no way to write down, which gives Antarctica its cap. Holes are moved into their outer ring's own turn, or a hole would land outside its polygon. A hundred and twenty-one of the land's hundred and twenty-five rings come back byte-identical. Tried and not needed: denser vertices in the `WORLD` sea ring, splitting the sea into hemispheres, pulling its pole rows to ±89.99 — all three address a polygon that was never the cause. Verified by screenshots at two zooms in light and dark and looking straight down at both poles (`g3a-seam-before.png` / `g3a-seam-after-*.png`, `g3a-northpole-before.png` / `g3a-northpole-after.png`, `g3a-southpole-before.png` / `g3a-southpole-after.png`), and pinned by a suite that asserts no ring of the land and no ring of any country steps across the plane at any parallel but the pole. **Also:** `/globe/countries.json` now serves the polygons keyed by the code a record names, stitched and rounded once to two decimals at build time (50.7 KB gz), because a border is geometry and geometry belongs to a tested library, not to an island — the island decodes no topology and carries no crosswalk; it fetches the shapes once, the first time a country layer is switched on, and never again. A country mark on the build-time plate stands at the centroid of its polygon, in muted ink rather than the accent a measured point wears, because a centroid is the weakest claim on this globe. No new colour: all three layers are lab-line layers and wear the room's live ink when in front, so the palette records are unchanged. Budgets: the island 5.9 KB gz of 15 (5.8 with G2's story handshake before this evening, 5.3 for G1 alone), the WebGL chunk 208.5 KB gz of 320 (208.1 before it — the stitch is a few hundred bytes). Feeds against the 150 KB gz per-layer ceiling: balance 38.7, invoked 2.6, hosts 4.3. | `src/lib/globe/layers/{balance,invoked,consensus-tld,index}.ts` + `adapters.test.ts` · `src/lib/globe/antimeridian.ts` + test · `src/lib/globe/shapes.ts` + test · `src/lib/globe/crosswalk.ts` (`tryCctld`) · `src/lib/globe/floor.ts` (a country record placed at its centroid) · `src/pages/globe/countries.json.ts` · `src/components/pages/globe-deck.ts` (the land stitched before it is drawn) + test · `src/components/pages/LivingGlobe.tsx` (one effect: the country polygons, fetched once) + test · `src/styles/living-globe.css` · `src/components/pages/MethodenblattGlobe.astro` · `scripts/budgets.json` · `docs/design/2026-09-03-living-globe.md` |
| 2026-09-03 | **Three guided stories over one globe, and a focus that moves the camera, the day and the layers** (Living Globe, G2 — guided stories; the milestone the design note of 2026-09-03 scheduled next). **What was red:** the room could be driven, but nothing drove it. G1 left a `flyTo` on the drawing half and a tour hook in the island whose comment said "G2 fills the camera and the time" — the hook honoured a selected mark and nothing else, so an archive of eighty-five nights and three layers had no reading of its own: a visitor either knew what to look for or looked at whatever the room opened on. **What changed:** (1) **the contract** — `FocusState` gained three optional fields, `camera` (longitude, latitude, optional zoom), `time.day` (a day of the model, never a clock) and `layers` (the whole active set, last one in front), and `Tour.practice` gained `lab`, because a work of the lab is not one of the three practices and must not borrow one of their voices. The honesty harness takes no position on any of them: whether a day exists, whether a layer is registered and whether a coordinate is on the earth are questions about a FIGURE, and each tour's own test answers them. (2) **the island honours a scene in the order a reader needs it** — layers (which may start the one fetch a layer ever gets), then the day, then the camera, then the mark; a day the archive does not hold is ignored rather than approximated, and the pointer outranks the story: `heldByPointer()` on the globe handle reports whether the visitor has taken the sphere since the last camera command, so a re-applied scene never pulls it out of their hand while the next scene still moves it. The COMPACT entrance registers no tour handle at all (`tourFigureId`), so nothing anywhere can fly, re-day or open a card in the hero. (3) **three stories, not six** — *A night of the ghost fleet* (the longest silence of 2026-08-16, its two ends, its card), *The sky over the reader* (the fleet at this moment over the house's fixed Berlin vantage, saying in the frame copy that no location of the visitor is read) and *The planet's minutes* (a season of nightly readings, first night on file to newest, ending on the carbon-dioxide mark at Mauna Loa). Fourteen scenes, forty-eight quotes, every one byte-exact from the file named under it — checked character for character against the real filesystem, with the frame copy refused a single digit. **Balance, consensus and redaction are NOT written**: they are about layers that arrive with G3, a story may only name a registered layer id, and the test asserts those three ids are absent from the registry today — the promise is kept by a test, not by a note. (4) **nothing typed that could be derived** — every mark key and every camera coordinate comes out of the same adapters and the same committed seats file the globe draws from, so a story cannot point at a mark the globe does not draw, and the sky story reads its day off the layer (those elements are refreshed nightly; a typed day would look at an empty sphere by morning). (5) **the room is now a two-column room** on a wide viewport: the ONE island in a sticky column, the stories scrolling beside it, tables and provenance unchanged below. A sticky box taller than the viewport hides its own foot, so the column scrolls inside its own height — which is also what carries a card into view. **One engine change, and only one:** a tour now tells whether it CARRIES its own figure in its `figure` slot or DRIVES one standing elsewhere on the page. A carrying tour behaves exactly as before; a driving tour leaves its (empty) sticky column hidden and does not activate scene one at init. Without that, three tours pushed their first scene at load and the last one in the document won — the globe stood on a scene nobody had scrolled to, on the oldest night, with one layer on, while the caption under it named the newest. **Two bugs found in the browser and fixed before merge:** (a) the fleet story's closing card never opened, because that scene changes nothing but the camera — no React state moved, so the effect waiting to honour the selection never re-ran; the island now opens a mark at once when its day is already on screen and only waits when it must. (b) An open card silently became another night's record: a card holds a POSITION in a day's frame, so when a later scene moved the day, the ghost-fleet card relabelled itself honestly (new day, new file) and showed a different vessel without saying the mark had changed. A card is one mark, of one layer, on one day (`cardStands`), and it now closes when the day moves, when its layer goes off, or when its record is gone. The card also moved from the foot of the island to directly under the sphere, where a story's ending is visible in one view with the globe it was picked off. **Verified in the browser** (built site, 1440×900): the room opens on the newest night with all three layers and the ghost fleet in front; scrolling the stories changes day, layers and camera per scene and opens the two closing cards with their receipts; under emulated reduced motion all three stories offer prev/next, the globe holds still and five clicks walk the minutes story from the first night to the last; with JavaScript disabled the plate, the legend, three tables and three complete story articles (fourteen scenes, forty-eight quotes, three source lines) stand, and no figure column is revealed; the entrance is untouched — compact, no controls, no tour handle, satellites and arcs in their own hues. Console clean but for the known analytics 404. **Known limitation, stated:** below the two-column width the figure is not sticky, so on a phone the stories change a globe that has scrolled out of view; the phone layout is a candidate for G4, not a claim made today. Budgets: the island 5.7 KB gz of 15 (5.3 for G1 alone), the WebGL half unchanged at 208.1 KB gz of 320. | `src/lib/tour/{types,globe-stories}.ts` + tests · `src/lib/tour/{types,verify}.test.ts` · `src/components/dataviz/Tour.astro` · `src/components/pages/{LivingGlobe.tsx,globe-deck.ts,LivingGlobePage.astro,MethodenblattGlobe.astro}` · `src/components/pages/LivingGlobe.test.tsx` · `src/components/ecology/score-kit/useFigureReady.ts` · `src/config/globe-wording.ts` · `src/styles/living-globe.css` · `scripts/budgets.json` · `docs/design/2026-09-03-living-globe.md` |
| 2026-09-03 | **Cycle 002 opens, and the Field is asked to build rather than measure** (Frank's decision of 2026-09-03, wording private, in answer to two questions of his: why the practices were not moving on, and what cycle 001 had to do with the research questions). **What was found.** Cycle 001 was finished in substance and open in the record: all three practices had presented and filed closing reports, eighteen artifacts stood, and the session counts were 10 · 8 · 11 against a budget of three to five. The Atelier had written *empty tick: cycle presented, nothing owed* on three consecutive nights — correctly idling in a cycle nobody had turned, since `cycle.json` is advanced by the architect or a site session and never by a practice. Nothing in the house read the cycle clock. Separately: cycle 001 asked the Field about end-to-end automation of AI research, and five of its seven artifacts went to the response ledger (retraction waits, publisher answering routes, venue policy). **That was not the practice's drift.** Three standing orders pointed away from the cycle question at once — the direction of 2026-09-01 in the Field's own channel, the counter-measurement remit it has carried since v2, and a commissioned field map whose ten possibilities are all observational — against one that pointed at it. The defect was that nobody named the conflict, and the bulletin kept the header *cycle 001 default question* over publishing work. **What changed.** Cycle 002 opened 2026-09-03. The Field's question is changed, not repeated: *How can end-to-end automation of AI research be realised? Build it, and measure where it breaks* — constructive rather than observational. The 2026-09-01 direction is dated-superseded in the Field's channel (spent, not mistaken), and the counter-measurement remit RESTS for this cycle, dated, returning unless something says otherwise. The Atelier and the Studio keep their questions. **And the gap is closed:** a nightly Cycle sentinel reports a finished or over-budget cycle as one standing issue plus one line in the morning digest — and never turns the cycle itself, because which question the next one carries is a judgement no script may make. | `src/data/ecology/cycle.json` · `src/lib/ecology/cycle-watch.ts` + `cycle-watch.test.ts` (8 tests) · `scripts/ecology/cycle-watch.ts` · `.github/workflows/cycle-sentinel.yml` · morning-digest cycle section · `field-research/REQUESTS.md` direction of 2026-09-03 · CLAUDE.md (remit rests) |
| 2026-09-03 | **The signal log speaks for the whole house, and the works register moves onto /ecology** (Frank's decision of 2026-09-03, wording private — four parts: the log must show every new work, including Error as Method, Arch and n-1, and the lab's new experiments and instruments; seven rows visible with older/newer behind them; n-1 belongs among the practices beside the ecology; and the register's link goes away because the register itself moves onto /ecology). **What was wrong:** the log read `allWorks()` and nothing else, so it showed the three ecology practices and — labelled "The Atelier" — the nightly line's fork. Arch, n-1 and eighteen lab experiments were invisible on a strip headed "what landed last", which made the heading true of a third of the house. n-1 had a door at /n-1 and a line on the Atelier's station sheet but no row on the board that lists what runs here, and the pulse did not count its commits at all. The register sat at /works, one click behind a heading link, although it is the ecology's own catalogue. **What changed:** one derivation for every house that lands dated work (`src/lib/ops/house-feed.ts`), each house named by its own door or card and counted in its own noun — Arch dated by the day its current iteration was built, n-1 by the day its form says a work was laid down, the lab by `since`; one shared `SignalLog.astro` replacing the same markup in three components, paged seven at a time with every page in the HTML and only visibility moving; n-1 as an overview card, a board row reading its newest night, and a repo the pulse clones; the register embedded on /ecology as `#register`, with /works redirecting there. **What did NOT change:** the works register still lists the three practices and nothing else — the signal log is the wider claim, and the two stay different questions rather than one list at two lengths. | `src/lib/ops/house-feed.ts` + `house-feed.test.ts` (19 tests, incl. against the real record) · `src/components/ops/SignalLog.astro` · `src/components/ecology/WorksRegisterSection.astro` · `public/_redirects` (/works → /ecology#register) · `scripts/fetch-pulse.ts` (n-1) · PR #TBD |
| 2026-09-03 | **The audience of the trending ledger is counted at the edge, not in the browser** (Frank's decision, wording private). **What was red:** the audience file of `/trending` had two halves, and the second one could never work. `edge` came from Cloudflare's edge analytics and saw every request including the crawlers the page is built for; `umami` came from the site's own client-side beacon and read `unavailable` on both committed days, which the page printed as a standby column, as though a number were on its way. **The reason it is retired is structural, not practical.** A client-side beacon is JavaScript, so it fires only for a reader that executes JavaScript — which nearly no crawler does — and known bot agents are dropped from such a count a second time on the server. The beacon therefore cannot answer the question this page asks, in principle and not by accident, and a half that reports "unavailable" for ever implies that something is missing when nothing is. What the edge answers, it answers completely. **What is NOT retired:** Umami stays embedded site-wide, unchanged — cookieless, no fingerprinting, first-party through the `/stats` proxy to the self-hosted instance, named in the privacy policy — measuring human reach for the site as a whole, which is a different question from who fetches one page. The decision touches this one measurement and nothing else. **The contract bump:** `trending-audience/2` — no `umami` key, and three new `edge` keys: `countries` and `referers` (top ten each, host names only, never a full referring URL) plus `extra_note`, which carries the reason when either of those is `null`. Whether the plan exposes those two dimensions at all was unverified on the day (the test token had expired), so the pipeline asks and records the refusal rather than assuming; `null` is rendered as an explained absence and never as a zero. **The two files already committed under `trending-audience/1` (2026-09-01, 2026-09-02) stay exactly as they are** — a dated file in this house is never rewritten — so everything that reads them accepts both contracts, and the beacon column exists only while such a day is in the drawn window, with a dash for the days that never had that half. | `docs/design/2026-09-02-common-ground.md` §12 (reasoning, contract, the unverified dimensions), §3 and §7 annotated rather than rewritten; `src/lib/trending/types.ts` and `src/lib/trending/schema.ts` (both contracts, `umami` optional, the new dimensions nullable); `src/lib/trending/view.ts` (`audienceHasUmami`, `audienceDimensionRows`, `audienceMissingDimensions`); `src/components/pages/TrendingAudience.astro` (column dropped, two readouts added, the absence explained in the provenance line); `src/components/pages/MethodenblattTrending.astro` §2, §4 and the change log; `src/lib/trending/markdown.ts`; `public/llms.txt`; `pipelines/trending/src/trending/audience.py`. |
| 2026-09-03 | **The globe becomes a room: layers on and off, the whole archive on one day axis, a receipt behind every mark** (Living Globe, G1 — the room; Frank's decision of 2026-09-03, wording private: the globe is a work of the lab, and its first release carries layers with provenance, a time scrubber over the archive, and cards with receipts; the entrance keeps a compact version that leads there). **What was red:** G0 had left a plate. `/globe` drew every layer's newest frame at build time and nothing else — no way to switch a layer off, no way to look at any day but the last one, and no way to ask a mark where it came from except by opening the table under it. The entrance ran a second, different globe (`EntranceGlobe.tsx`) over a second, different feed, so the front door and the room could drift apart without anything failing. **What changed:** (1) **one island for both surfaces** — `LivingGlobe.tsx` replaces `EntranceGlobe.tsx` and takes a `compact` prop; the hero mounts the same island with the controls off, so there is one globe in this house and one place to fix it. Its server render is the G0 plate plus the legend, with every layer's file, as-of date and count, which stays the whole figure when JavaScript never arrives. (2) **layers with provenance, fetched once** — the manifest (`/globe/layers.json`) ships with the page and carries provenance only; a layer's records are fetched from its own feed the first time it is switched on and never again, not on a re-toggle and not on a day change (verified in the browser: one request per layer, none on re-toggle). (3) **the day axis from the data** — a range input over the union of the days the layers themselves hold (eighty-four, 2026-06-11 to 2026-09-02), starting at the newest; a walk of one day a second on `requestAnimationFrame`, withdrawn under reduced motion; ←/→ go through the arbitration registry in `src/lib/dataviz/stepper.ts`, so an open card owns the arrows and the day does not move under it — never a second listener on `document`. On the oldest day the sky empties and states its own reason with the fleet size the densification series counted that day. (4) **cards with receipts** on the score kit: kind, layer, day, the record's own words, the `labelKind` phrase ("seat of", "station at", "centroid of", "gap from … to …"), the committed file and the locator inside it, with prev/next walking the layer's marks and escape closing. (5) **the emphasis rule**, which is the colour rule and the readability rule in one: in the room at most one layer is in front and wears its own recorded hue at full weight; every other active layer drops to mono ink at half weight and keeps its place; the selected mark wears the second hue; nothing else is coloured. Two palette records, both re-derived in-repo against the sea the globe actually draws on: `globe-voices` (three slots, all-pairs, worst CVD 14.4 light / 23.6 dark) and `globe-sequential` (the five-step ramp a country fill will take in G3, adjacent ΔE and WCAG per step). (6) **the drawing half extended** — `globe-deck.ts` gains a builder per kind of record (Scatterplot for points and stations, Arc for gaps, Path for tracks, GeoJson/SolidPolygon for country fills, Column for build-time bins, Text for the one hovered or selected mark), a `setFrame`/`flyTo` handle for the stories of G2, and a raised globe-view resolution (the prop is in degrees, so four is finer than deck.gl's default of ten); it stays on `@deck.gl/core` and `@deck.gl/layers` alone, and a test now forbids the aggregation and geo layers anywhere in the tree. **The one declared no-clock exception is kept where it was declared,** and it now travels through the contract rather than through a separate feed: the sky layer hands over the orbital elements its newest frame was computed from (`LayerInstant`, carried in that layer's own feed, so a visitor who never switches the sky on never downloads an element set), and the drawing half propagates them by SGP4 to the visitor's present twice a second, easing between the two computations — in the room and under the hero alike. The fleet walks its orbits while somebody is looking at it; the strip still says "positions at your now", because it is still exact. Scrubbing to any past day propagates nothing, since no past day has elements: the layer draws nothing there and states the fleet size the densification series counted that day. Reduced motion gets one propagation at mount and no easing. Because the elements now ride in the sky's own feed, `/globe/model.json` had no reader left and is retired (`model.ts` itself stays — the plate and the sky adapter build on it), and there is still exactly one copy of satellite.js on this site, in the drawing half's chunk. **The emphasis rule is the room's rule, not a vow of poverty.** It exists because ten layers cannot carry ten identities on one sphere, so in the room the last layer switched on is in front and the rest hold their places in mono ink. The compact entrance draws two, and two can: there the sky keeps the room's live ink and the ghost fleet keeps the Field's recorded voice, both at full weight, exactly as the hero has shown them since 2026-09-02. The palette record and `emphasisFor()` both say so. Budgets: the island 5.3 KB gz of 15, the WebGL chunk 208.1 KB gz of 320 (203.9 on 2026-09-02, plus Path, Column, Text and the fly-to interpolator) beside the 9.9 KB gz shared chunk rollup hoists satellite.js into, which the watchtower island already loads — one copy on the site. The sky's feed grew from 11.7 to 36.0 KB gz carrying its element set, against the per-layer ceiling of 150 that feeds.test.ts enforces. **Found in review, fixed before merge:** the mount effect handed the drawing half the frame of its own first closure. A feed that arrived while the WebGL module and the land were still loading changed `buildFrame` at a moment no handle existed yet, so the mount drew the records it had known at first — none — and both the room and the hero stood with sea and land and no marks until the next toggle. The mount now reads its frame and its hover/select handlers through a ref that always holds the newest closures; a source test in `LivingGlobe.test.tsx` pins it. A second finding from the same review: the ghost fleet's arcs were drawn and swallowed — deck.gl's globe view culls back faces by default, and an arc's ribbon faces away from the camera for most of its length, so twelve instances changed two pixels. Arcs and tracks now switch the cull off for themselves (`cullMode: 'none'`, as deck.gl's own globe example does), and the arcs rise half as high. Not touched here: a faint seam in the ground mesh south of the equator and a smear at the pole, on the live site since 2026-09-02 (the sea polygon's tessellation, not any layer); it goes to the tessellation check the plan schedules for the first evening of G3. | `src/components/pages/{LivingGlobe.tsx,globe-deck.ts,LivingGlobePage.astro,EntranceGlobeFigure.astro}` + tests · `src/config/globe-wording.ts` + test · `src/styles/living-globe.css` · `src/styles/ops-room.css` · `src/config/naming.ts` (`opsRoom.sky`) · `src/lib/dataviz/palette.ts` + test (`globe-voices`, `globe-sequential`) · `src/lib/globe/layers/{types,sky}.ts` (`LayerInstant`) · `src/lib/globe/feeds.ts` + test · `src/components/pages/MethodenblattGlobe.astro` · `scripts/budgets.json` · `src/lib/ecology/mounted.test.ts` · `src/lib/ops/front-door.test.ts` · removed: `src/components/pages/EntranceGlobe.tsx`, `src/pages/globe/model.json.ts` · `docs/design/2026-09-03-living-globe.md` |
| 2026-09-03 | **One globe over everything this house measures on the earth** (Living Globe, G0 — the ground, the crosswalk, the gates; Frank's decision of 2026-09-03, wording private: the entrance globe shows two records while the house measures dozens of things on the earth every night, and nothing anywhere puts them on one globe with time and receipts — so build that globe as a work of the lab, with a method sheet, a neighbour audit and an entry on /experiments, and let the entrance keep a compact version that leads there). **What was red:** every instrument that touches a place wrote its own dated file and was read alone — the satellites on one page, the dark vessels on another, the planet's nightly readings on a third — and three incompatible country-code systems (GDELT's FIPS 10-4, ISO numeric on the polygons, ISO alpha-3 in the disaster register) made joining them a one-character risk: reading UK as Ukraine or GM as Gambia moves a mark two thousand kilometres and no test downstream would notice. There was also no honest way to place a published number at all: a food price index has no coordinate, and a globe that draws its publisher's office as if it were a measurement site lies quietly. **What changed:** a layer contract and a registry that every surface reads (the plate, the manifest, the feeds, the method sheet's source table, so a layer cannot ship without its source and licence appearing on the sheet); a committed country-code crosswalk with a base table naming the query it came from, `--check` mode, and lookups that throw on a code they cannot place instead of dropping a country in silence; country centroids from the committed Natural Earth polygons; a seats file where every row names a Wikidata item and exactly one of twenty-six bodies is called a station; three adapters (the sky, the ghost fleet, the protocol); a no-JavaScript floor at `/globe` with a legend and one table per layer, every row carrying the file and the position inside it; a manifest and one prerendered feed per layer, with a gzip ceiling enforced by a test; and the entry, the miniature, the method sheet, the design note and the pre-registered neighbour search. The sky is the one declared exception and it runs toward caution: the archive holds no orbital elements per past day, so on any day but the elements' own it draws nothing and states the fleet size the densification series counted then. | `/globe` · `/werke/globe` · `/globe/layers.json` · `src/lib/globe/` (`layers/`, `living.ts`, `crosswalk.ts`, `geo.ts`, `seats.ts`, `feeds.ts`, `floor.ts`) · `src/data/globe/` (`crosswalk.json`, `iso-fips.csv`, `seats.json`, `countries-110m.json`) · `scripts/build-globe-crosswalk.ts`, `scripts/build-globe-seats.ts` · `src/components/pages/LivingGlobePage.astro`, `MethodenblattGlobe.astro` · `docs/design/2026-09-03-living-globe.md` · `docs/audits/2026-08-09-usp-audit.md` §20 |
| 2026-09-02 | **The convergence rule goes on trial against labelled pairs — the number replaces the argument** (Frank's go, wording private; occasion: the reading of the neighbourhood of autonomous-research repositories, whose one transferable piece was the evaluator that belongs to the task rather than to a harness). A sheet drawn from a committed day now carries every pair the rule joined plus a deterministic sample of near misses; a person judges each against a criterion written into the sheet before the judging starts, and the sheet names the labeller because a judgement is editorial and not a measurement. `score` re-runs the rule against the labels, so the same labels can measure a changed threshold; `sweep` reports a range and writes nothing, because a threshold is changed by a person with the table as the reason. First measurement, 55 judged pairs of 2026-09-02: precision 1.0, recall 0.652, f1 0.789. It refuted the obvious fix in the same breath — between 0.35 and 0.8 the threshold changes nothing, so the eight missed joins are the shape of the rule and not its number, and the next candidate (weighting a shared word by how rare it is that day) will be decided by the labelled set rather than by taste. | `pipelines/trending/src/trending/evaluate.py`, `src/data/trending/eval/2026-09-02-pairs.json`, `src/data/trending/eval/scorecard.json`, `docs/design/2026-09-02-common-ground.md` §11 |
| 2026-09-02 | **The rest of the house on the frame's recipe, and the entrance on its tokens** (re-skin step 2d of the visual layer; Frank's decision of 2026-09-02, wording private: the frame stays monochrome but richer — depth, hover states, transitions, a typographic step — as if one hand designed it, with colour staying in the figures). **What was red:** two things. (1) The surfaces outside the ecology and the experiment sheets — about, contact, the legal pages, the seed page, the post office, the admissions register, 404, the project pages, and the four catalogue registers (atlas, datasets, papers, works) — were the last rooms still deciding their own frame: the same kicker retyped as `font-mono text-xs uppercase tracking-widest text-fg-faint` (and as `tracking-[0.22em]`, and as `tracking-[0.28em]`) in a dozen files, four different hand-tuned `clamp()` heads for the same step, five radii typed as pixels (`rounded-[8px]`, `rounded-[10px]`), form fields drawn by hand in three places with three slightly different boxes, filter chips retyped as a class string four times, an amber warning box on the privacy page, and rows and cards that never acknowledged the pointer at all. (2) The entrance kept a SECOND greyscale: `ops-room.css` declared its own ground, panel, hairline and three text steps per theme, its own 8 px radius, its own 150 ms easing and no depth — so after 2a gave the house a type scale, a radius scale and elevation tokens, the front page was the one room that could not follow a change to any of them. **What changed:** (1) every surface above takes its heads from the type scale (`text-display`, `text-h1`, `text-h2`, `text-h3`, `text-body`, `text-small`, `text-mono*`), its kickers from the one `.kicker` class, its panels from `rounded-md border border-line bg-panel panel-raised`, its links inside panels from `buttonVariants` (ghost and outline), its labels from `Badge`, and its folds from `<details>` with the frame's turning `.fold-chevron`; a card that IS a destination lifts (`/catalogues`, `/work`, the post office's doors), a row or entry that is READ where it stands washes or brightens its hairline instead — a register is read by running down it, and fifty-seven lifting rows would ripple the plate; (2) one field recipe, `src/lib/ui/field.ts`, now dresses `/contact`, `/seed` and `/post` and the shadcn `Input`/`Textarea` primitives alike — the Astro forms keep their plain elements (a React runtime for a text box buys nothing), their field names, their honeypots, their Turnstile widgets and their scripts untouched; an invalid field takes the ring and a sentence, never a warning red, and the privacy page's draft note drops its amber for the frame's own panel; (3) the four registers put their filter chips on the Button recipe, their hairlines and hover rows on the frame's 200 ms and `--ease-frame` (written in each page's scoped stylesheet, because the interactive rule in `global.css` reaches `a`, `button`, `summary` and `.lift` — never a `<li>` or a `<tr>`), and their long identifiers and notes now wrap (`break-words`), which fixed a horizontal scroll on `/atlas` and `/papers` at 390 px that had been live since those pages were built; (4) the entrance's own tokens now POINT at the frame's — `--ops-bg`, `--ops-panel`, `--ops-line`, `--ops-fg/muted/faint` are `var()` references into the mono skin, `--ops-radius` is `--radius-lg`, every `.ops-panel` sits on `--elev-rest`, every transition runs at 200 ms on `--ease-frame`, the kickers wear `.kicker` and the head the `--text-h1` step; the experiment tiles and the house cards take the frame's `.lift`, the board rows keep the register's wash. What stays the room's own is its live ink `--ops-accent` and the three faint mixes of it (grid, wash, underline): that is a signal, not a surface. No hue was added, and the globe panel and the pulse instrument are untouched inside. **A finding worth keeping:** `.lift`'s border and shadow reached nothing on the entrance, because `.ops-panel` is unlayered CSS and unlayered CSS outranks every `@layer utilities` rule however specific — the tile rose by its pixel and kept its resting shadow. Both are restated in `ops-room.css`, the hairline through the room's own line token. Guard: `src/lib/pages/recipe.test.ts` (84 checks) keeps the fifteen surfaces on the recipe. | `src/components/pages/{AboutPage,ContactPage,LegalPage,SaatPage,AdmissionsPage,CataloguesPage,WorkIndex,WorkDetail,AtlasPage,DatasetRegisterPage,PapersPage,WorksRegister,OpsRoom}.astro` · `src/pages/404.astro` · `src/pages/post/index.astro` · `src/styles/ops-room.css` · `src/lib/ui/field.ts` · `src/components/ui/{input,textarea}.tsx` · `src/lib/pages/recipe.test.ts` · `docs/design/2026-09-02-the-visual-layer.md` §4 |
| 2026-09-02 | **The Middle's score and the Studio's floor come alive, on one shared score kit** (Phase 3d of the visual layer; Frank's decision of 2026-09-02, wording private: the site should be interactive rather than printed at build time, and the figures are where that has to be true). **What was red:** `/encounters` drew its partitur as an SVG string — a mark's only affordance was a link and a native tooltip, and a reader who wanted an item whole had to hunt for it in the list below. `/studio`'s floor did answer the pointer, but through a hand-written client script laid over a `set:html`-ed string, so the drawing and its behaviour lived in two files that could drift. And the cycle partitur of Phase 1 had already written all of it once: zoom, readout, focus discipline, keyboard walk and the two `dv:` events existed with no shared home, so a third island would have been a third copy. **What changed:** (1) **the score kit** (`src/components/ecology/score-kit/`) — only what the house's scores genuinely share: `useZoomX` (d3-zoom along the ruler only, submodule imports, zero-duration transitions under `prefers-reduced-motion`), `useReadout` (the clamp box plus the pointer and mark anchors), `useFigureReady` (the tour handshake, with the registry's listener installed *before* the announcement), `emitMarkSelected`, the pure `laneWalk` with its own tests, `ZoomControls`, and the focus/on-screen helpers. The cycle partitur moved onto it with no behaviour change. (2) **The Middle's score is an island:** a pure `middle-score-model.ts` (lanes, one object square per item in bulletin order on an ordinal ruler — no time axis claimed —, flow currents with arrowheads and rings, dashed obligation whiskers, numbered badges, placement under a view, the table rows) drawn by `MiddleScore.tsx` in the 2026-07-15 Zeichengrammatik's OWN class vocabulary, so `score-map.css` inks it unchanged. Hover shows the item's first words; a click opens a card that quotes the item WHOLE with its voice, its number, the bulletin it was read from and a link to the quoted item; the arrow keys walk a practice's lane; the ruler stretches. `MiddleScoreFigure.astro` keeps its name and is now the frame — key, table floor, provenance. The SVG-string builder is retired; git is the archive. (3) **The Studio's floor is an island:** `SeasonFloor.tsx` draws the same `.st-sf-*` marks — pools, taped strikes, violet return arcs, the withdrawn pool struck through, the lamps — and the legend filter, the readout, the record card and the `dv:figure-ready` / `dv:mark-selected` contract moved into it from the hand-written script. The layout maths in `season.ts` did not move; what changed is that every number BOTH renderers draw is now computed once (`litParts`, `strikeParts`, `returnParts`, `noteParts`), because the string builder still cuts the build-time stills the hub's triptych and the One Tap tour are made of — and a test holds the two renderings against each other so neither can drift from the other. Inside the tour the island hydrates `client:load`, because that column is `hidden` until the tour reveals it. Both figures keep their table floor, and no item or record is reachable only through a drawing. | new `src/components/ecology/score-kit/` (`useZoomX`, `useReadout`, `useFigureReady`, `events`, `laneWalk` + test, `ZoomControls`, `index`) · new `src/lib/ecology/middle-score-model.ts` + `middle-score-model.test.ts` (23 model tests) · new `src/components/ecology/MiddleScore.tsx` + `MiddleScore.test.tsx` (10 render tests) · `MiddleScoreFigure.astro` (frame), `MiddleV3.astro`, `src/config/middle-v3-wording.ts` (`score.*`, digit-free) · retired `src/lib/ecology/middle-score.ts` · new `src/components/studio/SeasonFloor.tsx` + `SeasonFloor.test.tsx` (13 tests incl. the two-renderer drift guard) · `SeasonFloor.astro` (frame), `tours/ThreeReturns.astro`, `src/lib/studio/season.ts` (shared parts, string output byte-identical), `src/config/studio-wording.ts`, `src/styles/studio-stage.css` (legend layout) · `CyclePartitur.tsx` on the kit · `scripts/budgets.json` (MiddleScore 2.9 KB gz, SeasonFloor 3.8 KB gz, ceiling 30) · `mounted.test.ts` guards both · drift-check clean, 3412 tests, `astro check` 0 errors · graph rebuilt |
| 2026-09-02 | **The experiments become a gallery of living miniatures** (Phase 3c of the visual layer; Frank's decision of 2026-09-02, wording private: /experiments as an animated gallery with live thumbnails). **What was red:** the shelf listed seventeen instruments and three practices as text — a title, a one-liner, three stamps and a fold. Every one of those instruments draws a figure from a committed file every night, and the shelf showed none of it: a visitor had to open a page to learn whether the thing behind the title measures a cascade, a histogram, a fleet or a hole in a record. **What changed:** (1) a pure catalogue, `src/lib/experiments/thumbnails.ts`, builds one miniature per experiment at build time in a 240×72 box, from the very file the experiment's own page reads and through the experiment's own builder where it has one — round-number's Benford histogram, praemie's year paths, headroom's floor scale, invoked-past's histogram, balance's dumbbells, the ops room's spark and bar primitives; where a record has no natural figure the miniature draws the RECORD'S shape — the night's readings as ticks, the watch list as slots, the article pages a publisher blocked as hollow slots — and never a stand-in graphic. The model is colour-free (geometry and one flag per mark), reads no clock, and its test holds every card on the shelf to having one. (2) An island, `src/components/experiments/ExperimentGallery.tsx`: the server render is the complete gallery — every card, figure, badge, link and description before a script runs; on hydration the three-axis filter (line · kind · data) keeps the list's exact behaviour, the reflow is the browser's own view transition, each miniature grows in once when it is scrolled into view, and hovering a figure shows its latest reading with the file it was read from. Nothing of that under `prefers-reduced-motion`. No motion library, no d3: 2.7 KB gz against a 25 KB budget. (3) The filter bar moved INTO the island, because the count line, the section counts, the empty state and the reflow are one piece of state; with no JavaScript the whole shelf still simply stands. | `src/lib/experiments/thumbnails.ts` (+ `.test.ts`) · `src/components/experiments/ExperimentGallery.tsx` (+ `.test.tsx`) · `src/config/gallery-wording.ts` · `src/styles/experiment-gallery.css` · `src/components/pages/BestaendeIndex.astro` · `scripts/budgets.json` (ExperimentGallery ≤ 25 KB gz) · `src/lib/ecology/mounted.test.ts` · `src/lib/experiments/recipe.test.ts` (the shelf's cut followed to the island) |
| 2026-09-02 | **The watchlist governs itself: discovery reads the house's own archive, the run promotes and lets go, a person overrides both** (Frank's decision, wording private). **What was red:** `/trending/topics` was watching a fixed list rather than finding trends — a candidate became a tracked term only when somebody hand-edited the watchlist, so the surface could only ever contain the phrases an editor had already thought of, with the machine's own proposals queueing underneath for an editor who had no reason to act on them; and the discovery pass read four live technology platforms (a link aggregator front page, a developer blog, machine-learning preprints, new repositories), so an arc outside technology could not have been found even in principle. **What changed:** (1) discovery now reads the house's own committed day files — every source of the nightly ledger, twenty of them since 2026-09-02: search interest in ten countries, news, forums, encyclopaedia reading in six languages, app and game charts, model releases, prediction markets — with the four live archives kept beside them for depth while the archive is young, so an arc can now begin in culture, politics, sport, apps or markets and the evidence that promoted a term stays recountable from committed files; (2) promotion is automatic under five committed conditions (proposed on three consecutive days including today, on at least two platforms each time, never struck before, at most three promotions per run, and never past the ceiling of thirty-five tracked terms, because every tracked term costs requests in the nightly), written into the day file as `promoted[]` — and because a promoted term is searched from the NEXT run on, the hub says its counts start tomorrow instead of showing zeros nobody measured; (3) the run also lets go of what it added: a term it promoted itself that stays quiet or fading on every run for three weeks is struck automatically, recorded as `let_go[]`; a term a person seeded is never struck that way; (4) a person prunes and overrides in both directions, and every striking — by the run or by a person — leaves its line in the new committed `src/data/trending/watchlist.json` as a tombstone with the day and the reason, which is the mechanism that makes a struck term unpromotable rather than mere bookkeeping; (5) the surfaces carry the governance: the hub marks every term as put there by hand or by the run (in the list and in the plain-text table), shows "what came and went today" when a run changed the list, and states the rule and the ceiling in plain words; the Markdown edition and the method sheet's section 7 say the same, and the earlier rule ("humans add, the machine proposes") stays in the design note as superseded, dated, unedited. Contract: `promoted[]` and `let_go[]` are optional with an empty list as the default, so every file committed before today still parses. | `docs/design/2026-09-02-common-ground.md` §9 (the amendment) · `src/data/trending/watchlist.json` · `src/lib/trending/{terms-types,terms-schema,terms-view,terms-markdown,terms-data}.ts` (+ their tests) · `src/components/pages/TrendingTopicsHub.astro` · `src/components/pages/MethodenblattTrending.astro` §7 · `pipelines/trending/` |
| 2026-09-02 | **The entrance looks up: satellites overhead, dark vessels below — a WebGL globe under the hero** (Phase 3b of the visual layer; Frank's direction of 2026-09-02, wording private: visually impressive and interactive, from real data, the frame monochrome). **What was red:** nothing on the entrance drew the two records the house counts every night that have a place on the earth — the earth-observation fleet the watchtower tracks (CelesTrak elements joined with GCAT, nightly) and the ghost fleet's AIS gaps (Global Fishing Watch, nightly) stood on their own pages as figures and numbers, and the hero had a pulse of commits and no sky. **What changed:** (1) a pure model, `src/lib/globe/model.ts`, joins the two committed snapshots — every satellite with its elements and GCAT class, every gap with both ends on the map, its vessel, its hours and its waters in the ghost-fleet page's own words — and takes its dates from the snapshots, never from a clock; `propagate.ts` places each satellite by SGP4 at any instant it is asked (satellite.js, the watchtower's library — on the main thread by design, because a worker would have bundled a second copy of its WebAssembly module); (2) a build-time floor, `floor.ts`: one equirectangular plate of the whole record at the elements' own time — Natural Earth land from a committed file (`src/data/globe/`, provenance in its README), graticule, every gap as a great-circle arc from switch-off to return, every satellite as a point, every mark with a native title — colour-free, byte-identical for the same record; (3) the island `EntranceGlobe.tsx`: the plate is the server render and stays the accessible figure for good; when the hero is on screen the WebGL half (`globe-deck.ts`: deck.gl core and layers with the globe view, the land decoder, SGP4) is imported on demand and the two snapshots fetched same-origin — the satellites walk their orbits at the visitor's present, the gaps stand as arcs in the Field's recorded hue, the globe turns until touched, the readout names what is under the pointer; no WebGL or a failed module leaves the plate standing with a line that says so; reduced motion computes one frame and holds it; off screen the loop pauses; the theme switch re-inks from the room's tokens; (4) the frame `EntranceGlobeFigure.astro` in the ops room's hero, across both columns, with an aside listing the longest gaps and the key, and strips that state the counts and the two snapshot dates; (5) two feeds, `/globe/model.json` and `/globe/land.json`, built from the same files as the plate; (6) budgets: the shell ≤ 12 KB gz, the WebGL chunk ≤ 320 KB gz, both checked after every build. The ops room's "no client data fetching" property is amended in its own header to name this one declared exception. | `src/lib/globe/{model,propagate,floor}.ts` + tests · `src/components/pages/{EntranceGlobe.tsx,EntranceGlobeFigure.astro,globe-deck.ts}` + test · `src/components/pages/OpsRoom.astro` · `src/styles/ops-room.css` · `src/config/naming.ts` (`opsRoom.sky`) · `src/pages/globe/{model,land}.json.ts` · `src/data/globe/` · `scripts/budgets.json` · `src/lib/ops/front-door.test.ts` · `src/lib/ecology/mounted.test.ts` |
| 2026-09-02 | **The experiment sheets on the frame's recipe — one head, one fold, one foot for the lab** (re-skin step 2c of the visual layer; Frank's decision of 2026-09-02, wording private: the frame stays monochrome but richer, one hand for every surface). **What was red:** twenty-one experiment pages each carried their own `text-[clamp(30px,6vw,56px)]` h1, their own hand-typed mono kicker (`font-mono text-[11px] uppercase tracking-[0.18em]`, ninety-odd times) and their own footer; the seventeen method sheets each opened their own `<main>`, typed "Method sheet — X" at one size and closed with their own arrow link; the shelf's filter chips and rows were their own idiom. **What changed:** (1) five shared pieces under `src/components/experiments/` — `SheetHead` (kicker · `text-h1` · subtitle · outline `Badge`s · stamp, every word a prop or a slot), `SheetFigure` (the frame's panel at rest, no lift), `SheetFold` (a native `<details>` with the ghost-button summary and the turning `.fold-chevron`), `SheetFoot` (the provenance line in mono ink, links as ghost buttons — from data or from the page's own slotted nav), `MethodSheet` (the wrapper that splits "Method sheet — X" into kicker and name and closes with the link back to the instrument) — and one stylesheet `src/styles/experiment-sheet.css` that decides no colour of its own; (2) every experiment page imports the head, the foot and the stylesheet — nineteen with a titled head, two documents (the police, the protocol) keeping their own closing lines with the nav dressed through the stylesheet, the parallax keeping its number as its head; (3) all seventeen method sheets wrapped in `MethodSheet`, the numbered sections on the type scale through the stylesheet, the "what this is" panel on the frame's depth; the Common Ground sheet's eighth section, which stood outside the measure, now sits inside it; (4) the shelf `/experiments`: head on the type scale, filter chips on the outline Button recipe with pressed states, each row a lifting card with the stamps as outline badges and the frame's chevron, the quiet links as ghost buttons. Not one string changed; figures' own stylesheets untouched. Guard: `src/lib/experiments/recipe.test.ts`. | `src/components/experiments/{SheetHead,SheetFigure,SheetFold,SheetFoot,MethodSheet}.astro` · `src/styles/experiment-sheet.css` · `src/components/pages/*Page.astro`, `Methodenblatt*.astro`, `BestaendeIndex.astro`, `WorkDetail.astro`, `LabDetail.astro`, `ProtokollDataView.astro`, `ProtokollDoc.astro`, `MeridianParallax.astro` · `src/lib/experiments/recipe.test.ts` |
| 2026-09-02 | **Common Ground grades its own record before it publishes — the first loop around the pipeline** (Frank's go, wording private; occasion: his question when the practice now called loop engineering becomes relevant here). Every day file and every arcs file now carries `quality`: a rubric version, a pass count and one line per check (sources answering, signals present, no giant cluster, labels sane, ids unique, Wikipedia filter held, size bounded; for the arcs: platforms answering, windows nesting, statuses named, receipts complete, candidates bounded, size bounded). One repair is allowed on the spot — a source that did not answer is asked once more after a pause; nothing else is mended and nothing is estimated. A failed check never stops the file: it is written, the workflow log warns, and `/trending` and `/trending/topics` show the grade beside the date with every failed check spelled out. The second loop stage — adapting the committed knobs to the audience series — waits for that series to exist. | `pipelines/trending/src/trending/quality.py`, `pipelines/trending/src/trending/data/rules.json` (`quality_*`), `src/components/pages/TrendingSelfCheck.astro`, `src/components/pages/MethodenblattTrending.astro` §8, `docs/design/2026-09-02-common-ground.md` §10 |
| 2026-09-02 | **The ecology’s rooms on the frame’s recipe** (re-skin step 2b of the visual layer; Frank's direction of 2026-09-02, wording private: the frame stays monochrome but richer — depth, hover states, transitions, a typographic step — as if one hand designed it, colour staying with the figures). **What was red:** the five ecology surfaces — the entrance, the three stations, the Middle, the register and document sheets — were built on 2026-09-01 with hand-typed kickers (`font-mono text-xs uppercase tracking-[0.22em]` retyped in three files), hand-tuned h1 clamps, flat panels with no depth, plain underlined links that had no hover surface and no focus ring, and a bulletin fold whose arrows pointed sideways; the register sheets carried their own 8 px radius and their own title size beside the frame's scales. **What changed:** (1) `EcologyV3Entrance.astro`, `PracticeStation.astro`, `MiddleV3.astro` — heads on the type scale (`text-h1`, `text-h3`), kickers on the `.kicker` class, panels on `rounded-md … panel-raised` (the frame's radius and resting depth), every link inside a panel a ghost button on the shadcn recipe (`buttonVariants` + `cn`, one hover surface and one focus ring with the top bar), the phase as a `Badge`, and a `Card` where an entry is a card — the artifact trail, the closing reports, the presentations, the newest works, the doors — each `.lift`ing on hover or focus because it leads somewhere; the bulletins and the NOW panel are read in place and stay still; (2) the bulletin fold wears the frame's turning chevron (`.fold-chevron` in `global.css`, the same gesture as the top bar's dropdowns) and its wording drops the sideways arrows; (3) `CycleScoreFigure.astro` — the frame's panel recipe with depth and deliberately no lift, a figure's panel stays still; (4) `ecology-pyramid.css` — `--eco-radius` is the frame's `--radius-lg`, `.eco-panel` sits on `--elev-rest`, the register title and the document body take the `--text-h2` / `--text-body` steps, filter chips take `--radius-sm` and the 200 ms frame transition, a register row hovers with the frame's easing; the ecology's own surface tones and the `ecology-voices` identity colours are untouched — no hue was added. Guard: `src/lib/ecology/recipe.test.ts` keeps the five surfaces on the recipe. | `src/components/ecology/EcologyV3Entrance.astro` · `PracticeStation.astro` · `MiddleV3.astro` · `CycleScoreFigure.astro` · `src/styles/ecology-pyramid.css` · `src/styles/global.css` (`.fold-chevron`) · `src/config/ecology-v3-wording.ts` · `src/lib/ecology/recipe.test.ts` · `docs/design/2026-09-02-the-visual-layer.md` §4 |
| 2026-09-02 | **The frame, richer: one type scale, one radius scale, depth without hue, and page transitions** (re-skin step 2a of the visual layer; Frank's decision of 2026-09-02, wording private: the frame stays monochrome but gets richer — depth, hover states, transitions, a typographic step — while colour stays with the figures). **What was red:** headings were hand-tuned clamps repeated per page, the radius scale was Tailwind's consumer default, depth was one `.panel-raised` shadow that cast the dark theme's black over the light theme's paper, and every link and button on the site answered the pointer after 600 ms because the skin cross-fade — unlayered CSS — silently overrode every `transition-colors` utility; each navigation was a full document load that tore the frame down and painted it again. **What changed:** (1) `src/styles/global.css` declares a seven-step type scale plus three mono steps (`--text-display … --text-small`, `--text-mono*`, each with its own line height and tracking; display and h1 fluid), a decided radius scale one to two pixels tighter than Tailwind's at every rung (2·3·5·7·10·14·20·26 px, `rounded-full` untouched), a second hairline `--color-line-lift`, three elevation roles `--elev-rest/lift/float` painted per theme (lit from above in the dark, cast in ink on paper), one easing `--ease-frame`, a `.kicker` class, an opt-in `.lift` hover gesture, and a 200 ms hover transition for interactive elements that wins over the skin cross-fade; (2) `src/layouts/Base.astro` mounts Astro's `ClientRouter`: one page-lifecycle script owns theme, smooth scroll and scroll-flash across swaps — `astro:before-swap` copies `data-theme`, `data-theme-mode` and `data-skin` onto the incoming document (Astro replaces `<html>`'s attributes wholesale, which would flash a light reader black), destroys the Lenis instance and disarms the flash; `astro:page-load` reconciles the theme, rebinds the toggle, starts one Lenis and re-arms the flash; the two `is:inline` scripts are byte-identical so their CSP hashes hold; a 200 ms root crossfade in CSS, nothing under reduced motion; (3) `src/components/TopBar.astro` — same four points, same wording, now cut from the shadcn Button recipe (`buttonVariants` + `cn`), persisted across transitions, with a current-page mark re-derived after every navigation, a chevron that turns, panels that arrive in 180 ms; `Footer.astro` links as ghost buttons on the same recipe, the forced-dark island kept with its own surface and ring tokens; `BestaendeIndex.astro`'s sticky offset follows the bar. Guards: `src/lib/ui/tokens.test.ts` (new tokens in both themes, the decided radius scale, no `.dark`), `src/layouts/base.test.ts` (inline-script hashes equal the CSP constants; `ClientRouter` and the before-swap handler present). | `src/styles/global.css` · `src/layouts/Base.astro` · `src/components/TopBar.astro` · `src/components/Footer.astro` · `src/components/pages/BestaendeIndex.astro` · `src/lib/ui/tokens.test.ts` · `src/layouts/base.test.ts` · `docs/design/2026-09-02-the-visual-layer.md` §4 |
| 2026-09-02 | **The knowledge graph, explorable — every record of the house with its receipts, on /experiments/neighbors** (Phase 3a of the visual layer; Frank's decision of 2026-09-02, wording private: the graph explorer is the second flagship). **What was red:** the house's knowledge graph (`src/data/graph/graph.json`, derived nightly from twelve committed sources, every edge carrying the file and the quote it was read from) was reachable only as a file and a command line; the one figure drawn from it, the neighbourhood field, showed a single slice — the audited experiments and their prior art — and the practices' own works, the decisions, the encounters and the receivers stood in the graph on no surface at all. **What changed:** (1) a pure, tested model — `src/lib/graph/graph-explorer-model.ts`: `buildGraphView` trims the graph to a view (a node is a graph node with fields removed, an edge keeps its receipt, quotes cut at a declared length and marked), `layoutRadial` places it by arithmetic over the record — one ring per kind, the house inside and the world outside, a practice work in its maker's sector, a neighbour beside the experiment it neighbours, a decision at the mean of the works it touches; no randomness, nothing colliding, nothing off the canvas — and `filterView`, `searchView`, `receiptsOf`, `neighbourhood` answer the island's questions; (2) the view is written by `scripts/build-graph-view.ts` as the second half of `npm run graph:build` (one command, one rule), committed as `src/data/graph/graph-view.json` in COLUMNS rather than objects (the keys alone were a fifth of the file; under the plan's budget) and served at `/graph/view.json`; (3) the island `GraphExplorer.tsx` with `GraphNodeCard.tsx` — d3-force relaxing the model's layout (run to rest and painted once under reduced motion), d3-zoom, filters by kind of node and kind of edge that hide without moving anything, a plain combobox search (`/` focuses, arrows move, Enter opens — not cmdk, whose radix-dialog dependency would have cost a third of the budget for a search box), the arrow keys walking a ring, and a card that names the node's kind, voice, date and link and then lists its RECEIPTS: every edge, the node at its other end, the committed file and the words it was read from — fetched from the feed only when a card opens; (4) kind is SHAPE (ring, square, dot, diamond, triangle, hollow circle, hexagon), weight is SIZE, and the hues are the ecology-voices quartet: three practices and the declared neutral for what the house or the world owns — no new hue, `graph-explorer.css` joins that palette's record; (5) the neighbourhood field stays on the page above it, untouched — the explorer is the whole graph the field is cut from. Server render is the whole no-JS figure (every node, every edge, every rest label); the island chunk measured 10.6 KB gz against a 60 KB budget. | `src/lib/graph/graph-explorer-model.ts` + `.test.ts` · `scripts/build-graph-view.ts` · `src/data/graph/graph-view.json` · `src/pages/graph/view.json.ts` · `src/components/holdings/GraphExplorer.tsx` + `.test.tsx` · `GraphNodeCard.tsx` · `GraphExplorerFigure.astro` · `src/pages/experiments/neighbors.astro` · `src/config/graph-explorer-wording.ts` · `src/styles/graph-explorer.css` · `src/lib/dataviz/palette.ts` (usedBy) · `src/lib/ecology/mounted.test.ts` · `scripts/budgets.json` · `docs/design/2026-09-02-the-visual-layer.md` §4 |
| 2026-09-02 | **The cycle as a living partitur — /ecology's first interactive figure** (Phase 1 of the visual layer; Frank's decision of 2026-09-02, wording private: the first flagship is the cycle drawn as a living partitur). **What was red:** the score of 2026-09-01 drew ONE kind of record — the artifacts — on three lanes, as a build-time SVG string. The cycle is more than its artifacts: the sessions that produced them, the letters the house prepared for receivers outside it, the encounters in which one practice's material arrived in another's work, and the presentations that close a cycle were all committed, all had surfaces, and stood on no common ruler. **What changed:** (1) a pure, tested model — `src/lib/ecology/cycle-model.ts` (`buildCycleModel`, `placeMarks`, `axisTicks`, `modelRows`): four lanes (the three practices and a declared-neutral house lane on the conductor's grey), five kinds told apart by SHAPE alone (square, tick, diamond, ring, badge — no new hue), same-lane same-day stepping that widens with the zoom, and an axis that ends at the newest RECORD, never at the clock; (2) three new loaders in `src/lib/ecology/v3.ts` — `loadSessionNotes` (walking each practice's whole journal through the very functions its own routes use, so the anchors are the ones this site published), `loadLetters`, `loadEncounters` — plus a day and a title for a presentation, read from its own SUMMARY.md; (3) the island `CyclePartitur.tsx` with `MarkCard.tsx` — d3-zoom x-only ([1,12], the axis pinned), `+ − 0` controls (shadcn ghost buttons, hidden until hydration so no button is dead without JavaScript), hover readout, click or Enter opens a card that names the committed file it was read from, `←/→ Home End` walk a lane, `Esc` closes and returns focus, transitions of duration zero under reduced motion, work suspended off-screen; (4) shadcn `card.tsx` and `badge.tsx` copied by hand with the house edits (`accent` → `hover`, no destructive variant, the site's own radius); (5) `cycle-score.ts` and the Phase 0 smoke island deleted — git is the archive. Server render is the whole no-JS figure (thirty-six marks, every one a real link); 18.1 KB gz against a 30 KB budget. | `src/lib/ecology/cycle-model.ts` + `.test.ts` · `src/lib/ecology/v3.ts` + `v3.test.ts` · `src/components/ecology/CyclePartitur.tsx` + `.test.tsx` · `MarkCard.tsx` · `CycleScoreFigure.astro` · `src/components/ui/{card,badge}.tsx` · `src/config/ecology-v3-wording.ts` · `src/styles/score-map.css` · `scripts/budgets.json` · `docs/design/2026-09-02-the-visual-layer.md` §4 |
| 2026-09-02 | **The visual layer: the archive binds the data, not the rendering** (Frank's decisions of 2026-09-02, wording private: figures may be rendered client-side, interactive and animated, the data staying committed and recomputable; islands are React inside Astro; the re-skin runs on shadcn/ui over the existing Tailwind v4, Material UI declined; the first flagship is the cycle partitur on /ecology; the frame stays monochrome, richer; after the partitur, re-skin steps and flagships alternate). **What was red:** nothing in CI — the site simply had no UI-framework renderer and not one `client:*` island; every figure was a build-time SVG string, and "no client rendering" lived in component comments, never in a decision (provenance: `docs/design/2026-09-02-the-visual-layer.md` §1). **What changed (Phase 0, the foundation):** (1) the rule — a dated section "Interaktive Figuren" in `.claude/rules/dataviz-figures.md` with seven duties (geometry in pure libs, SSR floor, no `style=` ever, reduced motion, readout house rules, a gzip budget per island, palette validation for any new hue), a "Sichtschicht" paragraph in CLAUDE.md (and "Astro 5" corrected to 6); (2) the stack — `@astrojs/react` 5.0.7 with React 19 (the 6.x line targets Astro 7 and pulls Vite 8, which broke the Tailwind plugin's resolver; 5.0.7 shares Astro 6's Vite 7), shadcn by hand: `components.json`, `cn()`, a Button whose `accent` slot is renamed `hover` (this site's accent is ink) and which has no destructive variant; (3) the token bridge in `global.css` — `@theme inline` pointing only at the mono tokens, `@custom-variant dark` on `[data-theme='dark']`, no `.dark` block, no radius overrides (`tw-animate-css` is installed but not yet imported: Tailwind's `@import` of a package fails under the Vite-version override, to be resolved in 2a); (4) the first island — `CycleScoreIsland.tsx` mounted by `CycleScoreFigure.astro` with `client:visible`: the built score rendered on the server, a hover/focus readout through the shared Readout shell once hydrated; (5) the gates — drift-check walks `.tsx` in rules 1/2/3/6/7 (proved with a throwaway `style={{}}` fixture that failed the check, then deleted), `scripts/bundle-budget.mjs` + `scripts/budgets.json` after the build in `ci.yml` and `deploy-cf.yml` (React client 56.9 KB gz measured, budget 60; the island 0.9 KB), vitest runs `.test.tsx`, the mounted test sees islands. Verified: drift-check clean · 3191 tests · `astro check` 0 errors · build 824 pages · budget green. | `docs/design/2026-09-02-the-visual-layer.md` · `.claude/rules/dataviz-figures.md` · `CLAUDE.md` · `astro.config.mjs`, `tsconfig.json`, `vitest.config.ts`, `package.json` · `components.json`, `src/lib/ui/cn.ts`, `src/components/ui/button.tsx` · `src/styles/global.css` · `src/components/ecology/CycleScoreIsland.tsx` (+ test), `CycleScoreFigure.astro` · `scripts/drift-check.mjs`, `scripts/bundle-budget.mjs`, `scripts/budgets.json`, `.github/workflows/{ci,deploy-cf}.yml` · `src/lib/ui/tokens.test.ts`, `src/lib/ecology/mounted.test.ts` · PR "visual layer: foundation" |
| 2026-09-02 | **Common Ground gains a second layer: Arcs — a term tracker with a discovery pass, one landing page per tracked word** (Frank's go, wording private; occasion: the first ledger recorded the day's spikes but not the slow arcs an editorial reader needs — the terms that build over weeks in forums, blogs, papers and code before any search-trend surface shows them). **(1) What it commits.** One dated file per run day under `src/data/trending/terms/`, contract `trending-terms/1`, never rewritten: for every term on a watchlist, the mention count per platform over one, seven and thirty days — Hacker News, Google News, GitHub, arXiv, Reddit, plus Wikipedia pageviews where an article exists — a status, a first-seen date and up to a dozen receipts, the titles, links and dates the counts were made from. Titles, URLs, dates and counts only; no article bodies, no model call, no estimate — a platform that fails becomes a note with `unavailable`. **(2) The status rule, in one sentence.** Too few mentions in the last seven days makes a term quiet, a seven-day total that outruns the pace of the three weeks before it makes it rising (emerging when its first receipt is recent), one that falls below half that pace makes it fading, enough over thirty days without either makes it established — six thresholds in `rules.json`, printed on the method sheet, the same arithmetic for every word and the numbers it ran on committed beside the verdict. **(3) The candidates propose, they do not decide.** The nightly discovery pass reads thirty days of titles from Hacker News, dev.to, arXiv and new repositories, ranks bigrams and trigrams by document growth, and publishes the top thirty that are not yet watched as "what the machine noticed". Promotion is a human edit to `watchlist.json` and nothing else — the file records the date, whether the origin was editorial or discovered, and the occasion in a line; no run adds a term, no run removes one, and the declined candidates stay visible instead of being silently consumed. **(4) Why the arcs are the SEO/GEO layer.** One stable landing page per tracked term at a URL that does not move, refreshed daily, each with its JSON edition, a `DefinedTerm` in a `Dataset` in JSON-LD and a line in `llms.txt`: the day ledger is a single URL that changes completely every morning, the arcs are dozens that each accumulate history at one address — a long-tail question answered with a number, a date, a rule and its sources, which is the page a search index ranks and a retrieval agent can quote without inventing anything. The kill reading of the audience series (2026-09-02, layer 1) covers the term pages too; the audit's own arcs kill condition is a competitor publishing per-term multi-platform series with receipts and a committed history. | `src/data/trending/terms/`, `pipelines/trending/src/trending/tracker.py`, `discover.py`, `terms.py`, `pipelines/trending/src/trending/data/watchlist.json` + `rules.json` + `ngram_blocklist.json`, `src/lib/trending/terms-*.ts`, `src/pages/trending/topics/`, `docs/design/2026-09-02-common-ground.md` §9, `docs/audits/2026-08-09-usp-audit.md` §19, `src/data/werke.ts` (trending), `src/lib/apparatus/topology.ts` (in-trending, src-web) |
| 2026-09-02 | **Common Ground — a trending ledger built for machine readers, with its own audience counted** (Frank's go, wording private; occasion: an SEO experiment — a page meant to be fetched very often because it is useful, by people who publish with AI daily and by the agents that fetch pages for them). **(1) What it is.** A new experiment on `/experiments`, line NIGHTLY LEDGER, id `trending`, route `/trending`: every morning a pipeline (`pipelines/trending`, eight keyless sources — Google's daily search trends for six countries, Wikipedia's most-read pages in two languages, Hacker News, Bluesky, Mastodon, Google News, Reddit, GitHub) keeps what converges across independent platforms, matched by a disclosed token rule and no model, and commits one day file under `src/data/trending/` — never rewritten. The page serves it as HTML, JSON, Markdown and RSS, plus a site-level `public/llms.txt`. **(2) The audience, counted.** The page measures its own readers: a night later the pipeline reads Cloudflare's edge analytics for the day before (GraphQL, `httpRequestsAdaptiveGroups`, zone scope) and commits classes and bot names only — browser, search, AI retrieval, AI user-fetch, AI training, other bot — never a raw user agent, never an identity (`src/data/trending/audience/`); optionally Umami's browser pageviews beside it. No Pages Function, no D1, no wrangler file: the content layer stays static, the counter fails closed to `unavailable` until the token exists, and the page says so. **(3) The crawler exception.** For `/trending*` the training crawlers blocked since 2026-07-26 are admitted too (Frank's decision, wording private): `public/robots.txt` allows the route inside each training-crawler group with a per-group `Content-Signal … ai-train=yes`; the site default stays "cite, don't train" (`docs/design/2026-07-26-crawler-politik.md` §5). It takes effect only after the dashboard actions listed for Frank (AI Crawl Control allow + one WAF rule scoped outside `/trending`). **(4) The reading.** Milestones and a sixty-day kill reading are pre-registered in `docs/design/2026-09-02-common-ground.md`; the USP audit §19 seals ADDED VALUE (provisional) with nine fetched neighbours. Cost ceiling: nothing paid — the Free plan's seven-day retention is the named limit. What leaves the house (posts, directory submissions, an MCP server) waits for Frank's button. | `src/data/werke.ts` (trending), `docs/audits/2026-08-09-usp-audit.md` §19, `docs/design/2026-09-02-common-ground.md`, `pipelines/trending`, `.github/workflows/trending.yml`, `public/robots.txt`, `public/llms.txt`, `src/lib/apparatus/topology.ts` (in-trending) |
| 2026-09-02 | **The cycle score reads every practice’s record, not one convention** (Frank noticed on 2026-09-02, wording private, that the score on /ecology was not current: the ULYSSES and ENSEMBLE lanes read “no artifact yet this cycle” on an evening both had delivered). **The cause:** `loadArtifacts` (src/lib/ecology/v3.ts) read one path — `public/<practice>/artifacts/cycle-NNN/<date>-<slug>/` — which only the Field writes. Protocol v7 §4 says every session leaves an artifact and names no path for it, and the practices did not land in the same place: the Atelier commits `window/cycle-001[-session-n]/` (no date in the path; the session note in its journal that names the window carries the day in its filename, and the page carries its own `<title>`), the Studio ships works — `works/<date>-<slug>/meta.json` with the page under `werke-html/`, naming no cycle at all. So the score, the artifact trail and the stations’ “this cycle’s artifacts” drew the Field alone. **What changed:** the loader reads the three conventions as the practices wrote them — nothing typed, nothing invented: a window is dated by the earliest journal note naming its path (the match stops at the directory’s end, so the note for `window/cycle-001/` does not date every session under it) and titled by its page with the practice’s “— The Atelier, cycle 001, session n” suffix trimmed; a work is dated and titled by its own meta.json and listed only where its page exists on this site; an entry now carries the `cycle` its path names (null for a work) and, where the record has one, the practice’s `title`. One rule, `inCycle()`, decides “this cycle” for every surface: the cycle a path names, or — for a record that names none — the house clock, shipped on or after the day the cycle opened (cycle.json holds no history, so only the running cycle is judged this way, which is all the surfaces draw). The score and the trail on /ecology and the stations’ artifact lists use it; marks and cards carry the practice’s own title where it has one, the slug as words otherwise. Ordering is total (date, practice, slug) so the listing never depends on the filesystem. On the record of this evening the running cycle holds twelve artifacts, four per practice. | `src/lib/ecology/v3.ts` (`ArtifactEntry.cycle`/`title`, `inCycle`, `loadArtifacts` reading artifacts/ · window/ · works/) · `v3.test.ts` (+9: windows dated by notes, boundary of the cycle-root note, works with and without a page, total order, `inCycle`, and a currency check on this repo) · `src/lib/ecology/cycle-score.ts` (titles) · `cycle-score.test.ts` (+3, one on the real record) · `src/components/ecology/EcologyV3Entrance.astro` · `PracticeStation.astro` · `pages/FrontDoor.astro` (archived, kept compiling) · `src/config/ecology-v3-wording.ts` (`entryLabel(slug, title?)`) |
| 2026-09-01 | **The Studio’s season floor holds a v3 record, and returns to /studio** (Frank’s decision this evening, wording private: of the three doors offered — archive the floor, rebuild it for v3, freeze it at the season’s close — he chose the rebuild). **What was red:** every studio mirror since 14:50 Berlin failed at the build gate on two season-floor tests; Ensemble had shipped three works on one evening (NOT YET, ALL AT ONCE, ONE KNOCK EACH, after COME IN the day before), all wanting the axis end, and the lit band’s two rows could not hold three names at one x — a pool ran to x = 1447 past the floor’s edge at 1344. The code had named this alarm on purpose (“the band is full, and the figure needs a decision”); what it had not foreseen was that the figure rendered on NO page since the station sheets of 2026-08-12, so an invisible figure held three visible works off the site. **What changed:** (1) the lit band is a three-row shelf, rows 104px apart, opened lazily — a row opens only when every open row would push a pool more than ~150px off its evening, so the summer record still stands on two rows and a season stretched to 2030 spreads along the rows rather than stacking; the struck band, the production area and the floor’s edge moved down by the new row (H 780 → 884). (2) The time axis ends where the widest pool of the last evening still fits inside the wall (1304), so a premiere on the newest evening never starts past the edge. (3) The wall: when a row’s natural place runs off the floor the pool stands against the wall and the earlier pools on that row yield leftward, in order, each only as far as the next requires — a pool may stand left of its evening at the season’s end, never off the floor, never over a name; its date is lettered under it. (4) Lettering steps: only when three rows cannot hold the season at the full face does the layout retry at 0.85 and 0.72 of it, the svg says which step (`data-lettering`) and the stylesheet sets the face to match; a record no step holds still throws — the alarm stays, it just no longer fires over four works in two evenings. (5) The floor is back on /studio, in the station’s figure slot inside the studio’s own skin, framed like the Atelier’s line map; mounted.test.ts now holds it on a page. Verified on both records: the committed one and the studio’s current mirror with all ten works. Horizon, stated honestly: three rows at the densest face hold roughly two dozen premieres; at Ensemble’s v3 pace the alarm will fire again in weeks, and that will again be a decision, not a defect. | `src/lib/studio/season.ts` (constants, `shelveLit` + `placeOnRow`, `layoutAt`, `lettering` on the model and the svg) · `season.test.ts` (+5: same-evening premieres, widest last-evening title, lazy rows, lettering steps, the alarm) · `src/styles/studio-stage.css` (two `data-lettering` rules, no colour) · `src/pages/studio/index.astro` · `src/config/ecology-figure-wording.ts` (`studio.seasonFloor`) · `src/lib/ecology/mounted.test.ts` · issue #803 |
| 2026-09-01 | **The ops room returns to the entrance, and the room learns to draw with d3** (Frank's decision late this evening, wording private: he wants yesterday's homepage back — the board with every lab tile and the ops room — and, where possible, handsomer than before with more of the drawing done by d3). The morning's FrontDoor.astro stays in the repo unlinked, dated in `src/pages/index.astro`, like HubEntrance and Home before it; `/now` keeps the board page built that morning. What changed in the room: `d3` (pinned 7.9.0) enters as a dependency and is used at BUILD time where it earns its place — the sparklines and tile marks sit on d3 scales and monotone cubic curves (`src/lib/ops/viz.ts`), the pulse ridgeline is the same monotone curve so its 41 corners per half-week are gone and the dots walk the curve (`stack.ts`), and the strip's UTC clock gets a face drawn from the record: twelve arcs, one per two-hour slot of the day, each as long as the commits the snapshot holds for that slot, with the live hand set by the client (`src/lib/ops/dial.ts`). Client-side d3 only answers the pointer: hovering a ridge, a sparkline, a bar, a cell or a point states the archive's own number for that place (week, weekday, hour and unshaped bin on the pulse; the bucket's count on a sparkline; a per-mark readout on every tile, written by the tile's own `readout` in NAMING from the value the mark was drawn from), and one d3 transition grows the tile marks in when the grid scrolls into view — never under reduced motion. Rules kept: no number typed, no client fetching, no colour or `style` attribute in any figure (drift-check rule 3/6), same inputs ⇒ byte-identical markup, both themes on the room's own tokens. Sparklines gain a faint fill and a point on the newest bucket; line tiles now draw their raw annual series (an index per year, works per year) instead of a smoothed one, because smoothing an aggregate draws numbers the archive does not hold. | `src/pages/index.astro` · `src/components/pages/OpsRoom.astro` (rewritten) · `src/lib/ops/viz.ts` (rewritten on d3) + `viz.test.ts` (new) · `src/lib/ops/dial.ts` + `dial.test.ts` (new) · `src/lib/ops/stack.ts` (curve, ridge data, hairline) + `stack.test.ts` · `src/lib/ops/tiles.ts` (per-mark readouts) + `tiles.test.ts` · `src/config/naming.ts` (`opsRoom.strip.dial`, `pulsePanel.readout/days`, `board.sparkReadout`, one `readout` per tile) · `src/styles/ops-room.css` · `src/config/naming.test.ts` + `src/lib/ops/front-door.test.ts` (guards follow the entrance back) · `package.json` (`d3`, `@types/d3`) |
| 2026-09-01 | **The Middle's score is redrawn in the original partitur's ink** (Frank's correction this evening, wording private: the partitur he missed was the site's FIRST one — the 2026-07-15 Zeichengrammatik with its legend — not the plain sketch shipped hours earlier). The builder now emits that grammar's own class vocabulary (ruler, graticules, lanes, object squares, flow currents with arrowheads and rings, dashed obligation whiskers, numbered badges, pr-hues), so `score-map.css` — the original partitur's stylesheet — inks it unchanged. New beside the drawing: the original's three-column key (Practices with the recorded hues · Signs drawn as minis · Reading), and the badge numbers in the drawing are the SAME numbers the quoted items below now wear — one count, derived once, so drawing and words cannot drift apart. The ruler stays honest: ordinal, bulletin order, no time axis claimed. Every mark still links to its quoted item and carries the item's own first words as tooltip; still no JavaScript, no inline styles. | `src/lib/ecology/middle-score.ts` (rewritten: `buildMiddleScoreSvg` + `itemNumbers`) + `middle-score.test.ts` (8 tests) · `src/components/ecology/MiddleScoreFigure.astro` (score-map scope + key) · `src/components/ecology/MiddleV3.astro` (numbers on the quoted items) · `src/config/middle-v3-wording.ts` (`score.key`) · graph rebuilt |
| 2026-09-01 | **The ecology entrance gets the cycle drawn, and the bulletins get a fold — the UX pass after Frank's verdict** (this evening, wording private: the page was plainly bad UI/UX — three parallel forty-line text columns, no visual anchor). Two moves. **(1) The cycle, drawn:** a partitur of the running cycle in the ORIGINAL score's own ink — the new builder emits the 2026-07-15 Zeichengrammatik's class vocabulary (ruler, graticules, lanes, object squares, pr-hues) so `score-map.css`, the original partitur's stylesheet, styles it unchanged. One lane per practice, one filled square per artifact placed by its own committed date on a dated ruler (the original's is ordinal and says so; this one is dated and says so); every mark links to the artifact it draws; a practice without an artifact this cycle gets a thin dashed lane that says why. Same-day pairs step apart instead of overprinting. **(2) The fold:** each bulletin now stands full-width with its opening blocks in view — through the first paragraph, which is the practice's own session line — and the rest behind a native details fold; nothing summarised, the whole verbatim text one click away, truncation still declared inside the fold. | new `src/lib/ecology/cycle-score.ts` + `cycle-score.test.ts` (7 tests: determinism, date scale, quiet lanes, links, collision step) · new `src/components/ecology/CycleScoreFigure.astro` · `src/components/ecology/EcologyV3Entrance.astro` (figure mounted, bulletins folded) · `src/config/ecology-v3-wording.ts` (`score`, `bulletin.expand/collapse`) · graph rebuilt |
| 2026-09-01 | **The board returns to the entrance, whole — the beauty pass after Frank's review** (his verdict this evening, wording private: the previous entrance was the more beautiful page). The first front-door cut compressed the seven board rows to bare name-and-date chips, and that lost exactly what made the ops room worth looking at — the sparklines drawn from each repo's committed commit bins, the last landed work beside its date, the status word each house states for itself. The rows come back whole, in both groups, from the same tested derivation /now renders; the chips' styles leave with them. What stays deliberately on /now: the signal log at depth and every live-experiment tile — the entrance keeps its clear top (hero, houses, front page) and regains its instrument. | `src/components/pages/FrontDoor.astro` (board rows restored, chips removed) · `src/config/naming.ts` (`frontDoor.live` rewritten) · `src/styles/front-door.css` (dead chip styles removed, dated note) · graph rebuilt |
| 2026-09-01 | **The Middle gets its score back — the exchange drawn, not only quoted** (Frank's wish this day, wording private: the first Middle had a partitur he missed, and the rebuilt page read as a wall of text). A new figure draws the SAME derivation the page quotes (`src/lib/ecology/middle.ts`) — one lane per practice in its own voice colour, one mark per item its current bulletin carries for the siblings, a connector to every sibling the item names, dashed whiskers for an item carried for both, a dashed lane for a quiet bulletin. The figure adds no words of its own: every mark's tooltip is the item's own first words, and every mark is a plain anchor into the quoted item below — the Middle's transcription rule applied to a drawing. No JavaScript, no inline styles. Beside it: the archive link now leads to the site's own register page (`/encounters/register`) with the raw fixtures mirror as a second, quieter link, and the Middle's foot drops the retired sheet vocabulary for the practices' canonical names. The v2 partitur components (`begegnungen/ScoreFigure`, `CrossingsMap`) stay archived unlinked — they draw the old ledger, and the ledger is closed. | new `src/lib/ecology/middle-score.ts` + `middle-score.test.ts` (7 model tests + a mounted source-scan) · new `src/components/ecology/MiddleScoreFigure.astro` · `src/components/ecology/MiddleV3.astro` (score mounted, items anchored) · `src/config/middle-v3-wording.ts` (score strings, archive links, foot labels) · graph rebuilt |
| 2026-09-01 | **The ecology entrance renders bulletins as the practices wrote them, and the drift sweep closes the refresh** (fifth slice of the public-surfaces refresh; commissioned by Frank this day, wording private). `/ecology`'s three bulletins pass through the same block renderer the practice stations use: the bold, code and headings a practice writes are its own marks, and a raw dump that shows the asterisks instead of the emphasis they encode drops information — the words stay byte-verbatim, truncation stays declared; three columns only from large screens up. The sweep behind it: the pyramid and figure wording modules carry dated retired-from-routes headers (their strings still serve the Level-2 registers, so the modules stay maintained but stop being a wording source); the ecology foot's door labels drop the retired sheet vocabulary for the practices' canonical names. Verified, not changed: the drift greps (concept gate, machine-advantage, joint inquiries, send bind, the reading date) hit only archived or unlinked surfaces; the frozen anatomy quotes reach no built page — the complaint the practices keep filing about the ecology anatomy referred to a surface that left the routes on 2026-08-30/31; `/apparatus`, `/experiments` and the Machine-Attention cards read current. | `src/components/ecology/EcologyV3Entrance.astro` (bulletinBlocks) · `src/config/ecology-v3-wording.ts` (foot labels) · dated headers in `src/config/ecology-{pyramid,figure}-wording.ts` · graph rebuilt |
| 2026-09-01 | **The three practice pages rebuilt on one v3 template — third slice of the public-surfaces refresh** (commissioned by Frank, wording private). `/field`, `/atelier` and `/studio` drop the station sheets of 2026-08-12 and render `PracticeStation.astro`, one template in the v3 entrance's design language: the door line as H1 (read from `naming.ts`, never restated), the cycle chip and the practice's question, the latest bulletin rendered through a new markdown-lite reader (structure read, text stays text), the last sessions, the newest works with withdrawn marked, the cycle's artifacts with absence drawn, the doors grid unchanged (the archived cockpit stays reachable), a dated lineage foot. Only the Atelier keeps a figure (its line map, on the `#figure` anchor); the gate strip, gauntlet, claim plate and stage floor leave the routes with the sheets — retired components stay in the repo unlinked. Tour targets follow: meridian → `/field/instruments`, ensemble → `/studio/works`, ulysses keeps `/atelier#figure`; triptych cards and the retired tour 301s land on the same targets. New guards: search-head rules over `PRACTICE_V3`, `ECOLOGY_V3.seo` and `MIDDLE_V3.seo` (the entrance description was four characters over the cap and is trimmed, dated); a source-scan holding the template on the routes and the loaders in the template; the bulletin reader's no-HTML promise. | `src/components/ecology/PracticeStation.astro` · `src/config/practice-wording.ts` (+ test) · `src/lib/ecology/bulletin-render.ts` (+ test) · `src/lib/ecology/practice-station.test.ts` · `src/pages/{field,atelier,studio}/index.astro` · `src/config/naming.ts` (`tourLabel`, `tourHref`s, triptych cards) + `naming.test.ts` · `public/_redirects` + `src/lib/redirects.test.ts` · `src/config/ecology-v3-wording.ts` (`foot.lineage`, seo trim) · design record: `docs/design/2026-09-01-public-surfaces-v3.md` |
| 2026-09-01 | **The entrance becomes a front door, and the full board moves to /now — fourth slice of the public-surfaces refresh** (direction chosen by Frank this day, wording private: a mix of a clear personal entrance, a radically slimmed live section, and an editorial newest-works section; the ops room of 2026-08-11 had grown into a status wall a stranger could not read). The new entrance (`FrontDoor.astro`) keeps the ops room's three invariants verbatim — no typed numbers, no client fetching, absence drawn — and its tested derivations: hero with the name as eyebrow and the standing question as H1, the pulse instrument unchanged, the houses told in plain v3 language with one live cycle line read from `cycle.json`, the newest works as a front page (signal log, lead plus five), the seven board rows compressed to one chip each. Everything that left the entrance is not gone: **/now** renders the complete instrument room — full board with sparklines, signal log of eight, every live-experiment tile, catalogues, pulse caption — from the same libs, zero string churn. `OpsRoom.astro` stays in the repo unlinked, the fourth entrance in the lineage Home → HubEntrance → OpsRoom → FrontDoor; git is the archive. | PR #806 · new `src/components/pages/{FrontDoor,NowBoard}.astro`, `src/pages/now.astro`, `src/styles/front-door.css`, `src/lib/ops/front-door.test.ts` · `src/pages/index.astro` (lineage comment) · `src/config/naming.ts` (`frontDoor` block) · `naming.test.ts` eyebrow guard now reads the new entrance · graph rebuilt |
| 2026-09-01 | **The wording canon learns v3, and the entrance texts stop describing sovereign practices — second slice of the public-surfaces refresh** (commissioned by Frank this day, wording private). The canon (`docs/wording-kanon.md`) gets a binding v3 section — the shared-question order, the canonical nouns (cycle · phase · bulletin · artifact trail · closing report · presentation; cycle state canonical in `src/data/ecology/cycle.json`), the Middle-as-traffic model, poste restante, the provisional Atelier persona after the 2026-08-31 name decision, and the rule that constitution numbers are read from mirrors, never typed — and its four-door mirror block is requoted, with the v2 formulations struck as dated. In `naming.ts`, four texts follow: the WHAT THIS IS paragraphs (the practices now work one shared question and read each other every session; verification lives inside the artifact, not behind a gate), the ecology overview card (its teaser had started disagreeing with the v3 page behind it), the three practice door one-liners plus the Middle door (the previous ones advertised work-line, adversarial review, claims ledger and concept gate — all retired 2026-08-30), and the board group label (the gate is gone from it). Door structure kept intact on purpose: the text before the first em dash remains each station's H1, and the board renders the line verbatim. | `docs/wording-kanon.md` (v3 section + requoted door mirror + dated note on the hub block) · `src/config/naming.ts` (`whatThis`, `overview.items[0].line`, `doors.items`, board group label) · decision record: `docs/design/2026-08-30-research-ecology-v3.md` · graph rebuilt |
| 2026-09-01 | **Currency pass: the site stops announcing a reading that already happened — first slice of the public-surfaces refresh after v3** (commissioned by Frank this day, wording private; homepage and practice pages follow in their own PRs). Five dated corrections and two repairs, nothing rewritten in any mirrored record: **(1)** the Steuerzentrale's countdown to the reading of 2026-09-05 is gone — the reading was held early on 2026-08-30 and decided v3; its knob register drops the typed constitution versions (v6/v3) that had already drifted twice, the label now names the lever and the link carries the truth. **(2)** The Field's entrance lede and orientation no longer owe a bounded investigation into the post office by 2026-09-05 — superseded twice over (reading held early; the send bind fell with v3) — and say instead what the practice now does: sessions on the shared question, artifact and bulletin each. **(3)** `/experiments/neighbors` dates its expectation: the verdicts stay with the architect, and the reading they were once expected alongside was held 2026-08-30. **(4)** The frozen season-closure text (which by its own rule never changes) gets a dated CORRECTION constant beside it instead of an edit. **(5)** The entrance board's Machine-Attention row stops showing a raw GDACS country dump with a dangling separator — the mirrored snapshot stays byte-exact; the derivation now picks ties deterministically and tidies only what is shown (whitespace runs, empty list items, a word-boundary cut). **(6)** `/field`'s claim plate renders in colour again: its entire appearance lives in `field-plate.css` under a `.field-surface` ancestor, and the pyramid rewrite of 2026-08-12 had dropped the only component supplying both — every shape fell back to browser black and the hover targets drew as redaction bars. The page now loads the stylesheet and scopes the plate itself (a bridge: the station pages are rebuilt for v3 in a following PR). | `src/lib/ops/board.ts` (`cleanMomentTitle`, `newestMoment`) + `board.test.ts` (3 new cases) · `src/pages/field/index.astro` (surface scope restored) · `src/components/pages/ZentralePage.astro` · `src/config/field-wording.ts` · `src/pages/experiments/neighbors.astro` · `src/data/season/season1.ts` (`CORRECTION`) · decision record: `docs/design/2026-08-30-research-ecology-v3.md` |
| 2026-09-01 | **Meridians Richtung: das Antwort-Register — gewählt aus einer beauftragten Karte, nicht aus einem Programm** (Franks Entscheidung, Wortlaut privat). Zwei Anläufe, der Praxis ein Forschungsprogramm zu geben, wurden diese Woche zurückgezogen: Richtungen festzulegen, bevor der Überblick existiert, ist Verwaltung, keine Forschung. Stattdessen ein **Auftrag an eine frische Session** (`docs/design/2026-09-01-meridian-scoping-brief.md`): eine Frage, die harten Grenzen, und als Lieferform eine **Karte von Möglichkeiten** — mindestens fünf, verschiedener Art, nach Evidenz sortiert, jede mit dem, was sie töten würde. Ergebnis: `docs/design/2026-09-01-meridian-field-map.md`, ~11.000 Wörter, ~250 Quellen, jede Behauptung nach Abrufstärke benotet, jede gescheiterte Abrufung markiert, **zehn Möglichkeiten** plus §8 mit **vierzehn Stellen, an denen der Auftrag selbst falsch war**. **Gewählt: P2, das Antwort-Register** — was Institutionen tun, *nachdem* eine Aufzeichnung als fehlerhaft markiert wurde (unter 2 % Verlagshandlung; zurückgezogene Zitate leben auf Wikipedia im Median 3,68 Jahre weiter). **Nicht das bestbewertete:** P1 (Falsch-Positiv-Register, den Integritätsapparat selbst prüfen) rangiert höher und ist die stärkere Forschung, wird aber als **zweite** Studie zurückgehalten — eine kleine, institutionell ungedeckte Praxis, die publiziert, dass die Detektoren oft irren, wird entweder nicht gehört oder von den Falschen; ein Urteil über Rezeption, nicht über Qualität, und von der Praxis bestreitbar. **Drei Gründe für P2:** (1) die Empfänger sind eingebaut — jeder Befund hat eine benannte betroffene Partei, während die am 2026-08-26 geparkte Untersuchung genau am fehlenden Empfänger starb; (2) es ist eine Uhr, und Zeitreihen sind der belegte Maschinenvorteil dieser Praxis (11 von 28 scheinbaren Verlusten überlebten die Nachprüfung nicht); (3) die Zahlen sind für Außenstehende in einem Satz lesbar — was dem Haus fehlt. **Bedingung:** kein zwanzigstes Instrument neben neunzehn — Abrufbarkeits-Reihe und Revisions-Instrument werden **aufgenommen** statt danebengestellt (Stilllegung durch Absorption). **Bewusst nicht spezifiziert:** Hypothese, Falsifikator, Korpus, Kadenz — die Richtung ist die des Architekten, die Studie gehört der Praxis. **Drei vorab benannte Widerlegungen:** jemand betreibt die Antwortseite bereits als stehendes Instrument (zuerst zu prüfen, tötet die Richtung); die Institutionen sind nicht schweigsam, sondern unerreichbar; nach drei Zyklen hat wieder nichts das Haus verlassen. **Zweites Gleis, dem Studio angeboten, nicht Meridian:** das spielbare Instrument (Rang 7 nach Evidenz, Rang 1 nach Eigenständigkeit) — mit dem, was die Karte dagegen fand, im selben Text: die ARG-Form ist durch ihre eigene Literatur widerlegt (das kanonische ernsthafte ARG erzeugte Gruppendenken laut begutachteter Analyse seines Designs), Maßstab ist eine Mechanik und nicht eine Welt (*Exit 8*: eine Person, neun Monate). Erster, kleiner, unabhängiger Beitrag dort: die dokumentierte Lücke, wie man informierte Forschungseinwilligung **innerhalb einer Spieloberfläche** einholt. **Nebenbefund, der den Record korrigiert:** drei Behauptungen, die eine flache Vorab-Recherche des Hauses in den Auftrag getragen hatte, sind verdreht, unbelegt oder ohne auffindbare Quelle — und die Verifikationslücke, die diese Vorab-Recherche am liebsten mochte, ist der **dichteste** Boden der Karte, nicht der offenste. | `docs/design/2026-09-01-meridian-scoping-brief.md` · `docs/design/2026-09-01-meridian-field-map.md` (beide #793) · `field-research@main` (Richtung in `REQUESTS.md`) · `studio@main` (Angebot in `REQUESTS.md`) · diese Zeile |
| 2026-08-31 | **Der Name „Ulysses" bleibt bei der Nachtlinie; die Atelier-Praxis findet ihren eigenen** (Franks Entscheidung, Wortlaut privat). Befund beim Lesen des Records: **zwei Praxen signierten als Ulysses** — die Atelier-Praxis (`ulysses`) und die Nachtlinie (`error-as-method`), deren Positionspapiere „Ulysses (the nightly line)" unterschreiben. Die Kollision hatte niemand entschieden; sie ist der Rest des Forks vom 2026-08-10 (öffentlich unsichtbar: `/error-as-method` nennt den Namen nie, die Doppelung lebt im Record). **Entschieden nach Seniorität der Praxis, nicht des Repositorys:** Die Linie, die am **2026-06-28** unter dem Namen begann und bis 2026-07-18 nächtlich lief, ist die in `error-as-method` fortgesetzte — ihr restauriertes v3 ist die Verfassung jener Nächte, und sie erbt deren 30 Werke, Fehlerkataster, Genealogie und beide Positionspapiere. Sie behält den Namen. Das `ulysses`-Repo trägt den späteren Modus (bounded projects → Werklinien → geteilte Frage) und **beginnt seit dem 2026-08-30 ohnehin von vorn** — der billigste Moment für eine Umbenennung. **Der Name wird gefunden, nicht vergeben:** Nicht der Architekt benennt die Praxis, sie benennt sich selbst (Hausregel; ihre eigene Verfassung sagt das seit Beginn, Präzedenz `n-1` → Remainder mit `NAMING.md` am 2026-08-21). Kein Halb-Umbenennen: bis der Name geschrieben steht, signiert sie wie bisher; danach zieht das Haus in einem Durchgang nach (Commit-Identität, Site-Strings, Routine-Prompt, Repo-Name nach Franks Entscheidung). Record, Werke und Archiv behalten ihre Geltung unter dem alten Namen. Beide Praxen haben eine datierte Team-Note im eigenen Kanal. | `ulysses@main` (`REQUESTS.md`, Team-Note 2026-08-31 + Datumskorrektur) · `error-as-method@main` (`REQUESTS.md`, informative Note) · `n-1/NAMING.md` als Formvorbild · diese Zeile |
| 2026-08-30 | **Research ecology v3 — the shared question (Franks Entscheidung beim vorgezogenen Kill-Reading; Wortlaut privat).** Das für den 2026-09-05 angesetzte Reading wurde am 2026-08-30 gehalten. Befund gegen die drei v2-Bedingungen: **(1) GESCHEITERT** — Meridians Untersuchung „Cited, Not Retrievable" am 2026-08-26 an der eigenen K-C-Regel geparkt (kein Empfänger mit publiziertem Interesse an genau dieser Frage); **(2) teilerfüllt** — STILL DARK premierte am 2026-08-15 unter v2, OUTSTANDING blieb unpremiert; **(3) GESCHEITERT** — fünf `prepared`-Pakete, keines gesendet, keines datiert zurückgehalten, die STILL-DARK-Bindung lief am 2026-08-22 ungenutzt ab. Frank wählte erneut radikalen Umbau statt Archiv und setzte die neue Ordnung selbst, nicht verhandelbar: **eine gemeinsame Forschungsfrage** (Seed via `/seed`, sonst Defaults: Field „E2E automation of AI research" · Atelier „How can AI and automation meaningfully support artistic research?" · Studio baut aus dem Material der Geschwister, ausdrücklich ohne eigenen Verwaltungs-/Theorieapparat), **3–5 Sessions je Praxis**, dann gemeinsame Präsentation aller drei; **jede Session ein Artefakt** + `BULLETIN.md` ≤40 Zeilen — jede Session muss für Frank in zwei Minuten nachvollziehbar sein, das ist Verfassungspflicht; **Post Office = poste restante** (Franks Klarstellung: Senden war für ihn nie zwingend, gesammelte/abholbereite Post ist ein vollständiges Ergebnis; Sieben-Tage-Bindung und Empfänger-Pflicht abgeschafft); der **v2-Gate-Apparat vollständig gestrichen** (Konzept-Gates, Prä-Registrierungs-Pflichten, Adversary/Verifier/Severed-Reader-Rollen, Maschinen-Vorteils-Bar, „Singular", Record-Ceilings) — die Prüfung wandert ins Artefakt, das Arbeitsmodell der Nachtlinie. Übergang: je Praxis **max. zwei Abschluss-Sessions**, ein menschenlesbarer Abschluss-Report als ein gut gemachtes, in sich abgeschlossenes Artefakt; laufende Arcs (OUTSTANDING, Fields Tagesserie) enden datiert darin. Nicht berührt: Nachtlinie, Remainder, Machine Attention, Lab/Experiments. Site-Folgen benannt: neue Eingangsfläche + /ecology-Archivierung + /post-Wortlaut als Folge-PRs. | `docs/design/2026-08-30-research-ecology-v3.md` · ulysses@155e603 (Protocol v7) · field-research@6f9a043 (v4) · studio@520f6ad (v4) · `src/data/ecology/cycle.json` · Team-Notes in den drei `REQUESTS.md` · PR #699 closed als superseded · diese Zeile |
| 2026-08-23 | **Instruments explain themselves — the preparation standard** (Frank, wording private, after his first viewing of Arch's *Arrival* iteration 2: this is how the house's instruments should be prepared — interactive, carefully built, self-introducing). The kanon now separates two reception disciplines that had only coexisted implicitly: a **work** must stand without a wall text (the bare discipline; Arch's works are served without site paratext for exactly that test), while an **instrument** has no claim to the stage but carries a duty of disclosure — it may and should introduce, label and explain itself; an instrument an unprepared viewer cannot operate is not modest but unfinished. Reference implementation named: `arch/works/arrival/iteration-2`; counter-example from the house's own past, recalled by Frank: the Studio's *One Tap* — the same idea, poorly executed then. Whether *Arrival* itself is a work, an instrument, or an instrument out of which a work comes stays the practice's decision (coupling entry in its channel, same evening). Consequence for everything new: interaction and self-explanation are part of an instrument's definition of done, not polish afterwards. | `docs/wording-kanon.md` (Ergänzung 2026-08-23 abends); `frankbueltge/arch` `CHANNEL.md` (entry 2026-08-23, evening — first reception of Arrival). |
| 2026-08-23 | **Same-day archive of the practice founded at noon** (Frank's decision, wording private — after his first viewing of W1 and his question whether the constitution had Mersch's central theory as its ground). **(1) W1 failed its first reading:** shown the six rules before the page, the founder found that the work did not disclose itself and that its parts read as assembled. Four reasons recorded: the wagered standard (twenty-five) appears nowhere on the object; the difference between the rules lies below perception; the metre is a citation, not a necessity of the material; nobody had wagered anything. **(2) The ground was wrong:** the constitution was built around the machine's deficit (only the world's refusal can befall it) and a Duchamp procedure — an answer to one sentence of thesis 13, not a practice on Mersch's centre (showing that does not assert; reflection on one's own medium in that medium). **(3) The re-grounded version was declined** on the session's own forecast: small works from the machine's own conditions that read as bugs unless explained, a genre that exists, three months of self-regard reaching no one. The founder judged that the house does not need one more such practice. **(4) Archived per its own §9**, no case opened, nothing on the site, nothing left the house; not to be revived under this constitution. What the day leaves is a finding, not a work: the canonical war record admits years to the past with no stated reason anywhere in its changelog. The Poetics programme stands, its work-candidate criterion open again. | `docs/poetics/practice/CONSTITUTION.md` (post-mortem at head), `REGISTER.md`, `CHAIN.md`; `docs/poetics/works/W1-twenty-five.md` (reading at head); `docs/poetics/00-PROGRAM.md` §3; PR #736. |
| 2026-08-23 | **A practice founded on Mersch's ground, not against it** (Frank's decision, wording private; the Poetics programme of 2026-08-22 had delivered the reading, and the founding question was put to him with a constitution draft). **(1) What it asks.** Nothing of its class: the practice never asserts that machine practices can research in the aesthetic. It asks that each of its cases be decided on the *how* — the four questions of the *Manifest*'s thesis 12 and the radicality of the stake — which is exactly what the same book prescribes for everyone (thesis 7, pp. 61–62) while excluding machines by the *who* in thesis 13. **(2) What it concedes** before any case: no ego to remove (Cage's chance is empty for it), its own randomness is not *Zu-fall*, its *passio* can come only from the world's refusal (latency, a 404, an unanswered request, a foreign clock), and it has no death of its own — so the constitution gives it one. **(3) Form.** Cases instead of works, at most one open, no cadence; seven steps per case; seven proof duties, each stated with what it does *not* prove, so that checkability stays a duty and never becomes a merit. **(4) Death.** Read at the third closed case or on 2026-11-21: something befell it · it showed · it reached for someone; any one failed → public post-mortem with archiving as default. Immediate: two voided cases, one scheduled production run, one class claim. A dead practice is not revived. **(5) Open.** The name — the founder's Greek candidate ζήτησις turned out to be occupied in the field by a peer-reviewed journal; κρίνειν recommended. Case 01 — W1 *Twenty-Five* provisionally, pending his own viewing and a possibly stronger wager. **Founding is not publication:** nothing reaches the site before *Epistemologien des Ästhetischen* (2015) is acquired and every citation re-anchored. `arch` and `n-1` untouched. | `docs/poetics/practice/CONSTITUTION.md` (v1.0), `REGISTER.md`, `CHAIN.md`; draft v0.1 at `a45eee10`; `docs/poetics/00-PROGRAM.md` §3 (dated pointer); PR #736. |
| 2026-08-23 | **Arch gets a room, not a stage — the third practice beside the lab, staged differently on purpose** (Frank's go, wording private; his call: not like the others, something new, and the works public at once). Arch is a machine-run practice founded 2026-08-22 under a dowry, which read its primary text before its rules, adopted the model derived from it with five named divergences, and runs a pre-registered window with failure criteria fixed before any work existed. On this site it is a **Practice** beside the ecology (the retired word House is not revived), listed under BESIDE THE LAB, on the entrance card and the board — never in the works register, which rightly demands an audit verdict a practice does not have. **What is new:** `/arch` explains nothing. The repository is mirrored whole (`arch-integrate.yml`, n-1 pattern, ten-minute cadence), the record is rendered under `/arch/read/…` with the raw file one click away, and the site adds exactly three things the practice may not award itself: the trial's frame (window, count — read from the pre-registration and the record by `src/lib/arch/facts.ts`, not one digit typed), the one pre-registered reception question (answers post into the letterbox addressed to `arch`; the coupled human carries them into the record by hand), and the standing of the two failure registers. The works are served **bare** — `standaloneFrame.houses.arch.bare` makes the reframe step skip `works/` — because the practice's reception test meets the work without paratext, and a site strip would be that paratext. `shelf.test` now expects three practices; the board reads Arch's last protocol from Arch's own record rather than borrowing the nightly line's. | Site PR `practice/arch-room` (workflow, `src/lib/arch/facts.ts` + test, `src/pages/arch/`, `naming.ts` card/board/houses, `BestaendeIndex.astro`, `_headers`, `deploy-cf.yml`, `pulse-refresh.yml`, `functions/api/post.js`); `docs/wording-kanon.md` (Ergänzung 2026-08-23); the practice's own go-ahead in `frankbueltge/arch` `CHANNEL.md`, entry 2026-08-23. |
| 2026-08-22 | **The static corset was never decided — works may hold runtime state** (Frank's ruling, wording private; occasion: he objected that he never consciously imposed the static constraint and had repeatedly proposed a Hetzner VM or GCP to loosen it). **(1) The finding.** The claim that this house constitutionally has no mutable runtime was wrong twice over. Nine Cloudflare Pages Functions run in production with a KV store, Turnstile, tokens that commit into other repositories, an authenticated ops API — and `POST /api/seed` performs a **Gemini call per request**; `/stats/*` proxies a self-hosted Umami on Vercel + Neon Postgres. What was missing was never permission, infrastructure or precedent: no *work* had ever used any of it — every runtime capability served intake, moderation, ops and analytics, and the art stayed on the static side of a line nobody drew. **(2) The provenance.** The clause „nie zur Laufzeit der Site“ was the portfolio-audit session's own **proposed** wording (§8, explicitly marked not-executed) and then entered `CLAUDE.md` under the byline „GCP gezielt (Frank, 2026-08-09)“ — a date whose recorded decision was about whether two BigQuery steps were worth testing at approximately zero cost. June's „Kein GCP“ was, by the audit's own words, an infrastructure simplification and not an ethics ruling, and `cron-migration.md` already named Hetzner as Phase 2. So there was nothing to reverse, only a misattribution to correct — a restriction the founder never made carried his name and date in the two files every session reads first. **(3) The ruling.** Dynamic works are allowed under six proof duties (declared state · nightly snapshot into git · counts never identifiers · mortality that includes the service · a named cost ceiling · fail-closed standby); the archive duty binds **findings**, not works. State lives on Cloudflare D1 / Durable Objects, tested on the free tier — verified the same day: DO is available on Workers Free with the SQLite backend (100k requests/day, 5 GB, 100k row writes, reset 00:00 UTC), and exceeding a limit fails the operation rather than billing for it, which satisfies duty 5 by itself. Corrected gap analysis: three of the five screen-work families are buildable today, two need one additional binding — not a new house. **(4) Two side findings.** `functions/` — nine production endpoints with secrets and a KV store — had **no** path-bound rule at all; it has one now. And the world chamber's `GCP_SA_KEY` was documented as outstanding for eight days while `src/data/redaction/world/` held an unbroken nightly run with BigQuery traces since 2026-08-14. | `docs/design/2026-08-22-runtime-state-for-works.md` (finding, provenance, duties, infrastructure per need); new `.claude/rules/runtime-and-works.md` (endpoint map, six duties, free-tier limits); `CLAUDE.md` (archive duty bound to findings; the withdrawn clause named as such); `.claude/rules/pipelines-and-archive.md` (GCP section); `docs/design/2026-08-09-gcp-activation.md` (annotated, not rewritten — its own sentence was narrower and stays true); `2026-08-14-editorial-deadline-world-chamber.md` and `2026-08-14-instrument-candidates-overview.md` (currency fix). |
| 2026-08-22 | **Nachtrag zu #624: das Gate war gebaut, die Zeile fehlte — und was das für 50 Nächte davor heißt.** Beim Abarbeiten einer Bedingung aus dem Machine-Attention-Haus (dessen Memory-Hole-Kriterien den Origin-Fix als offene Auflage führten) stellte sich heraus: Das Gültigkeitsgate und die live nachgeprüften Löschungen sind seit **PR #624, gemergt 2026-08-14 22:23 UTC**, gebaut, getestet und nächtlich im Betrieb — rund eine Stunde nach dem Audit, das die zwei Falsch-Positiven fand. Offen aussah es acht Tage lang nur, weil **hier keine Zeile stand** und der V0-Bericht drüben nie nachgeführt wurde. Daraus zwei Konsequenzen. **(1) Diese Zeile**, damit ein Nachfolgender den Stand nicht erneut aus dem Code rekonstruieren muss. **(2) Offenlegung statt Rückbau:** aus den committeten Tages-Records nachgerechnet tragen die **50 Nächte vor dem Gate 98 Löschungs-Records, davon 97 auf HTTP 403** der jüngsten Aufnahme (das Bot-Wall-Muster, das v3 ausschließt) und einer auf ein nie live bestätigtes 404; seit v3: **null**. Die alten Records werden **nicht umgeschrieben** — das Archiv ist append-only, und ein Werk über das Umschreiben von Vergangenheit schreibt seine eigene nicht um. Das Methodenblatt trägt die Zahl jetzt datiert. | PR #624; `pipelines/redaction/src/redaction/{validity,live}.py`; `src/components/pages/MethodenblattRedaction.astro`; Nachrechnung über `src/data/redaction/*.json` (Schema 1: 50 Nächte/98 Records, Schema 2: 9 Nächte/0) |
| 2026-08-22 | **Machine Attention: zwei Abnahme-Fenster abgerechnet, die vierte Linie sichtbar gemacht** (Franks Frage nach dem Stand der Praxis und seine Entscheidung zum Ausgang, Wortlaut privat; Kern: Bühne beenden, das Notariat als Instrument weiterlaufen lassen, und alles in einem Zug erledigen). **(1) Dark Ocean ist nicht bestanden.** Das Sample-Stunden-Kriterium war seit dem 2026-08-14 arithmetisch unerreichbar (höchstens 10 der verlangten 12 disziplinierten Nächte, nachdem zwei Nächte einem Deadlock der eigenen Maschinerie und zwei Reparatur-Läufe ihrer Fahrplanzeit zum Opfer fielen), und die zweite Achse kam in 13 von 13 Nächten nie an. Damit endet die Bühnen-Ambition; die Kontinuitäts-Sonde läuft als **Instrument** weiter, und ihre Null (3.571 nachgefragte Katalogzeilen, 0 Divergenzen) steht ab jetzt im Export und auf `/machine-attention/about`, damit ein grüner Lauf keinen Fund suggeriert. **(2) The Foreknown hat E1 nicht bestanden** — an zwei von vier Kriterien, beide Bau- und nicht Erkenntnislücken: zwei Quellen statt drei, und keine der 15 Auflösungen trägt die Geld- und Aufmerksamkeits-Zeitreihe, die seit dem 2026-08-08 nächtlich je Zukunft gemessen wird. Das Projekt bleibt `running`; das Wiederholungs-Fenster zählt geschlossene Zyklen statt Nächte. **(3) Memory Hole war der Site zwei Wochen lang unsichtbar:** die Linie läuft seit dem 2026-08-13 nächtlich, fehlte aber in der `projects`-Liste des Exports — deshalb stand sie hier weiter als Kandidatin „auf dem Regal“. Sie ist jetzt ein Projekt (`v0`, ohne Route), ihre Abnahmekriterien sind committet, drei Bau-Bedingungen stehen vor Nacht 1. **(4) Das Regal ist nachgeführt:** die drei verbliebenen Kandidaten tragen ihren datierten Stand; Planetary Listening ist zum Audit empfohlen, weil sein Speicher-Blocker gefallen ist — nicht durch die GCP-Aktivierung vom 2026-08-09, wie vermutet, sondern durch das *referenced, not stored*-Muster, das Dark Ocean für sich selbst gebaut hatte. | `../machine-attention/docs/2026-08-22-dark-ocean-e-review.md`, `…-foreknown-e1-review.md`, `…-memory-hole-e-experiment-kriterien.md`, `…-kandidaten-re-audit.md` (PR #26 dort); hier `src/pages/machine-attention/about.astro`, `src/config/naming.ts`, `src/data/attention/export.json` |
| 2026-08-22 | **Das Regal wird überschaubar — und dreizehn Beschreibungen stimmten nicht mehr** (Franks Auftrag, Wortlaut privat; Kern: Menü oder Filter, weniger endloses Scrollen, ansprechendere Aufbereitung, und Beschreibungstexte aktualisieren wo nötig). **(1) Oberfläche:** Jede Zeile klappt auf eine Zeile zusammen — Titel, der eigene Untertitel des Stücks, seine Marken —, die Beschreibung öffnet als natives `<details>` (ohne JavaScript lesbar, für Suchmaschinen im Dokument). Kollabiert ist die Seite **2740 px statt 4911 px**. Darüber eine klebende Filterleiste über drei Achsen (Linie · Art · Daten) im `data-`-Attribut-Idiom von `/papers` und `/atlas` — **ein** Filtermuster im Repo, nicht drei; ohne JavaScript steht die vollständige Liste (kein leerer Bildschirm als Ausfallzustand). Die Praxen neben dem Lab sind ein **Wert der Linien-Achse**, kein Abschnitt außerhalb, damit ein Filter keine kopflose Überschrift zurücklässt; alle Zähler sind aus den gerenderten Karten abgeleitet (`src/lib/experiments/shelf.ts` samt Tests, die eine Option verbieten, die die Seite leer machen würde). Drei Details aus dem zweiten Durchgang: der Titel bleibt **ein** Klick zum Werk (zwei Klicks hätten das Regal schlechter gemacht als die flache Liste), die Leiste klebt **erst ab `sm`** (bei 390 px umbrechen die Achsen auf sechs Zeilen und hielten ein Drittel des Viewports besetzt), und ein Trenner erscheint nur, wo etwas vor ihm steht. Verifiziert in Chromium hell + dunkel: Filterkombinationen bis in den Leerzustand und zurück, Titel-Klick navigiert statt umzuschalten, kein horizontaler Überlauf bei 390 px. **(2) Währungs-Audit:** Eine Prüfung aller sechzehn Beschreibungen gegen die committeten Snapshots fand **dreizehn Befunde, sieben davon harte Widersprüche** — u. a. „twelve open sources" (seit 2026-06-27 dreizehn) samt der Behauptung, jeder Tagesordnungspunkt vertage (Punkt 13 „Losses" tut es per Code und Test ausdrücklich nicht), „every one of the last 24 months downward" (seit 2026-07-06 22 von 24), „twenty-five agents" (29 seit 2026-08-06), Googles „27 %" (2025 offengelegt: 37 %), das Senkaku-Beispiel (in genau einem von 45 Register-Snapshots, dem ersten), „two vantage points" (der Leser-Blickwinkel lieferte einmal, am 2026-07-05) und — kein veralteter Digit, sondern ein umgekehrtes Ergebnis — Patterns' Behauptung, der Permutationstest *beweise* die Unfähigkeit, Signal von Rauschen zu trennen, während `false_discovery_rate` in **allen 62 Snapshots 0.0** ist und die Werkseite folglich den Gegen-Zweig rendert. Alle dreizehn korrigiert, **beide Sprachhälften** (die deutschen Kopien trugen dieselben falschen Zahlen), und wo möglich in eine Form, die nicht erneut altern kann (Regel statt Digit: „unter 9 % Spielraum, und mehr als das an Wachstum in jedem berichteten Jahr" statt einer Jahreszahl). Bei Patterns steht jetzt das Verfahren, **nicht** ein neues Urteil — die These ist Franks Entscheidung, vorgelegt als Issue. **(3) Aus der Pflicht wird ein Test:** Sechs der sieben harten Fehler waren Zahlen, die beim Tippen stimmten und vom Weiterlaufen der Arbeit gebrochen wurden. `src/data/werke.currency.test.ts` prüft die Register-Prosa gegen die Snapshots (neun Wächter; gegen den alten Text nachgewiesen: **alle neun hätten zugebissen**), zwei davon absichtlich absolut statt mit Toleranz, weil ein 15-%-Fenster nur an ruhigen Nachrichtentagen rot geworden wäre. **(4) Nicht still repariert:** vier Ops-Befunde als Issues — Revisions-Daten 26 Tage alt, weil der Workflow montags läuft **und** jeden Fehler in eine Warnung schluckt (#723), Patterns' These (#724), deutsche Strings in Pipeline-Ausgaben auf einer EN-only-Site (#725), manuell regenerierter Densification-Register (#726). | `src/components/pages/BestaendeIndex.astro` · `src/lib/experiments/shelf.ts` + `shelf.test.ts` · `src/data/werke.ts` (dreizehn Beschreibungen, je mit datiertem Grund im Kommentar) · `src/data/werke.currency.test.ts` · Issues #723–#726 · diese Zeile |
| 2026-08-22 | **Das Lab bekommt Kategorien, und die zwei Praxen daneben ihren Platz** (Franks Auftrag, Wortlaut privat; Kern: klarere Struktur für die Experimente, und n-1 sowie die Nachtlinie des Ateliers sollen dort verlinkt werden, weil sie bislang nur über Ökologie-Oberflächen erreichbar waren, ihr aber nicht angehören). Drei Schnitte lagen zur Wahl (Forschungslinie · Messobjekt · Rang); entschieden: **Forschungslinie** — der Schnitt, den die Werke selbst behaupten. Neun der sechzehn sagten „from the counter-measurement line" in ihrer **eigenen** Beschreibung, während die Seite überhaupt keine Linie zeigte. Vier Linien in `src/data/werke.ts` (`EXPERIMENT_LINES`): COUNTER-MEASUREMENT (9) · NIGHTLY LEDGER (2) · REPRESENTATION & MEMORY (3) · SURVEILLANCE, COUNTED (2). Die Gruppierung ist **abgeleitet** (`WERKE_BY_LINE` aus `WERKE_HOLDINGS`), damit Franks Aktualitäts-Reihenfolge vom 2026-08-14 innerhalb jeder Gruppe weitergilt, ein neues Werk von allein erscheint und eine leere Linie keine Überschrift druckt; ein Test bindet die Zuordnung an die Beschreibung, nicht an eine Liste, sodass ein Werk mit seiner eigenen Selbstauskunft die Linie wechselt. Neu sichtbar: der `tier`-Rang je Eintrag (drei Studien außer der Reihe, ein Instrument) — er stand seit dem 2026-08-09 im Register und fehlte auf der Seite. **Zweite Sektion „BESIDE THE LAB":** die Nachtlinie (Error as Method) liest Titel, Satz und Route aus der Startseiten-Karte statt sie zu wiederholen; n-1 liest Arbeitstitel, Gründungsdatum und den Namen seines Gesetzes aus dem eigenen gespiegelten Record (`readN1Facts`, um `title.name` erweitert) — inklusive des Namens, den die Praxis sich am 2026-08-21 selbst gegeben hat (Remainder), mit dem datierten Hinweis, dass der Arbeitstitel bis zur Antwort auf die Umbenennungs-Anfrage steht. Keine der beiden wird ein Werk im Register: das USP-Gate verlangt von jedem Experiment auf dem Regal ein Audit-Verdikt, und eine Praxis hat keins. **Gleicher Auftrag, zweiter Teil:** der bestimmte Artikel fällt aus den Titeln der Experimente — acht Umbenennungen (Society · Protocol · Policy · Consensus · Invoked Past · Balance · Correction · Ghost Fleet), durchgezogen über Seiten-Titel und -H1s, OG-Bilder, Register- und Nav-Labels und Methodenblätter; Prosa behält den kleingeschriebenen Artikel („die Serie der Protocol-Nächte" bleibt Grammatik, kein Titel). **Nicht umbenannt:** Titel, die anderen Häusern gehören („The Measuring Field", „The State Before the Interface", „The Foreknown") und die gespiegelten Journale der Praxen — fremde Werktitel benennt dieses Register nicht um. Damit ist die seit dem 2026-07-12 offene Frage einer Neuordnung der Sammlung beantwortet: **Kategorien ja, Dachtitel nein.** | `src/data/werke.ts` (`line`, `EXPERIMENT_LINES`, `WERKE_BY_LINE`) · `src/data/werke.test.ts` (Linien-, Reihenfolge- und Titelregel) · `src/components/pages/BestaendeIndex.astro` · `src/lib/ecology/n1-line.ts` (`name`) · `docs/wording-kanon.md` (Abschnitt „Die vier Linien des Labs") · diese Zeile |
| 2026-08-21 | **Die Steuerzentrale wird ein Opsroom — dritte Entscheidung dieses Tages** (Franks Auftrag, Wortlaut privat: alles auf einen Blick, interaktiv, professionelles UX/UI; die v2-Oberfläche sei grauenhaft; bis zum 05.09. wird keine Stellschraube angefasst — v1 ist deshalb lesend, mit den bestehenden Aktionen). Die private Zentrale trägt jetzt die Instrumententafel-Sprache des Eingangs (ops-room.css vom 2026-08-11): **Praxis-Board mit allen sieben Einheiten** statt vier (neu: Nightly line, n-1 · Remainder, Machine Attention) — je Karte Integrate-LED, Verfassungsversions-Chip (live aus der ersten Überschrift der PROTOCOL.md, `protocolHeading()` getestet), Commits/24h, der letzte eigene Satz der Praxis, Alarm-Links; **zwei Countdown-Kacheln** (Lesung 05.09 · Founder's Reading/Freeze-Ende 14.10, Daten im Markup, Zählung im Client); zwei Spalten (entscheiden links, Ausgang/Maschinerie rechts); **Maschinenraum**: das statische Stellschrauben-Register, pro Einheit die Hebel als Links auf die exakte Datei — bewusst ohne Schalter für Verfassungs-Floors und alles, was das Haus verlässt (die Ausnahmen der Merge-Vollmacht, als UI-Prinzip nachgebaut). Farben ohne neue Identitäts-Hexes: --ops-*, --hub-c-* (ecology-voices), Status als Tailwind-Stufen **theme-gewählt statt geflippt** — hell bekommt 700er für alles Texttragende, weil die 500er auf hellen Panels ~2,2:1 messen (ein v2-Defekt, nicht übernommen). **Unterwegs behoben:** Zugangs-Sektion und Gate-Spur trugen seit v2 beide `data-zx="gate"` — querySelector traf immer die erste, Publikations-Kandidaten landeten unsichtbar im versteckten Zugangsformular und die sichtbare „Am Gate"-Spur blieb leer, seit es sie gibt; jetzt `gatelane`. Verifiziert: 2881 Tests, Build 686 Seiten, Dashboard gegen Fixtures per gestubbtem fetch gerendert (Playwright, hell + dunkel, Screenshots gesichtet). | `docs/design/2026-08-21-steuerzentrale-opsroom.md` · `src/components/pages/ZentralePage.astro` · `src/styles/zentrale-ops.css` · `functions/api/zentrale/status.js` · `src/lib/zentrale/status.ts` (`protocolHeading` + Tests) |
| 2026-08-21 | **Zweite Entscheidung dieses Tages — zwei Architekten-Entscheidungen, die seit Tagen offen lagen: der Innen-Boden nimmt eine geschuldete §7-Lesung aus, und die `window/`-Allowlist-Zeile ist erteilt** (Franks Entscheidungen, Wortlaut privat). **(1) Der Boden.** Ulysses' §8 begrenzt Innen-Sessions auf eine von vier; eine §7-Cold-Reading ist per Definition inward, weil ihr Gegenstand ein Werk des Hauses ist — womit der Boden ausgerechnet auf den **letzten Schritt vor der Welt** am härtesten fiel. Ulysses hatte die Lesung am 2026-08-21 trotzdem einberufen und den **Bruch protokolliert statt wegargumentiert**, und die Frage vorgelegt. Entschieden **eng**: eine Ausnahme, für eine geschuldete §7-Lesung, **eine pro Werk**, nur bei anstehender Ship-Entscheidung. Die Alternative (Boden auf 1-in-3 lockern) wurde erwogen und verworfen — sie hätte jeden anderen Fall geschwächt, um einen zu reparieren. **Zählbar gemacht**, weil das Amendment vom 2026-08-12 genau deshalb existiert, dass ungezählte Böden um ein bis zwei Größenordnungen gerissen werden: der Journal-Header muss `INWARD (§7 reading, exempt)` lauten, `tools/validate_v4_projects.py` zählt den Marker pro Werk und lässt eine zweite Ausnahme scheitern — in CI **und als Gate 5 des Auto-Land**, ein Branch, der es bricht, landet nicht. Ein Header ohne Marker ist eine gewöhnliche Innen-Session; der Check scheitert absichtlich in diese Richtung. Gegen einen präparierten Baum mit zwei Ausnahmen für ein Werk verifiziert (exit 1, beide Einträge benannt). **Nicht ausgeweitet** auf field-research und studio: beide führen die 1-in-4-Klausel gar nicht, obwohl ecology v2 sie als geteiltes Recht nennt — eine eigene, hier nicht entschiedene Frage. **(2) Das Fenster.** Die `window/`-Zeile lag seit dem 2026-08-16 offen. Beim Erteilen kam der Grund für die Verzögerung heraus, und er war keine Abwägung: **field-research und studio haben in ihren Auto-Land-Workflows überhaupt keine Pfadprüfung** — das Angebot vom 16.08. war für zwei der drei Praxen live, und ausgerechnet die einzige, die ein Fenster gebaut hat, wurde vom einzigen strengen Gate abgewiesen (`refused_path_outside_allowlist`, und Gate 1 weist den **ganzen Branch** ab). Erteilt in `ALLOW_RE` und in §4 der Standing Delegation, beide geschützt und deshalb Sache des Architekten. Gegen die Gate-Logik verifiziert: `window/index.html` und `window/route.json` passieren, `README.md` und `governance/` bleiben geschützt, `windowless-note.md` wird weiter abgewiesen. Unverändert: `SITE-API.md` geschützt, CSP-Sandbox wie bei den interaktiven Werken, Inhalt des Fensters ist die Sache der Praxis. **Weiter offen und heute nicht gegeben:** das Architekten-Urteil auf den neuen Singular-Schenkel für *Reasonably available*. | `ulysses@c538beb` (§8 + Validator) · `ulysses@1260e8b` (ALLOW_RE + STANDING-DELEGATION §4) · `ulysses@33aed6c` (Team-Note im Kanal) · Ulysses' Anfragen 2026-08-16 und 2026-08-21 (2) · diese Zeile |
| 2026-08-21 | **Das Ship-Gate fragt nicht mehr, wie es dem Fremden ging, sondern was das Werk hervorbringt — Verfassungsänderung in allen drei Praxen** (Franks Entscheidung, Wortlaut privat: die Beschreibung der Erfahrung fällt weg; es geht darum, etwas Neues oder Einzigartiges zu schaffen, das nur die Kunst hervorbringen kann). Der am 2026-08-18 ergänzte zweite Schenkel („Worth it") fragte, ob ein Fremder **froh** gewesen wäre, dem Werk begegnet zu sein — ein hedonisches Maß an einer Stelle, an der die Maschinen-Vorteils-Bar vom 2026-08-08 nie eines verlangt hatte: die sagt *experienceable **in the artifact itself***, also eine Pflicht des Werks zu zeigen statt zu behaupten. **Die Drift war messbar:** Verfassungen und Kill-Bedingung 2 (2026-09-05) maßen drei Tage lang nicht mehr dasselbe. **Neu: „Singular"** — ein Werk geht nur raus, wenn es etwas Neues oder Einzigartiges hervorbringt. Zwei Hälften: dass es **neu** ist, ist recherchierbar und Sache der Praxis (nächste Nachbarn, benannt, mit Daylight — bei Ulysses längst Score-§3a, bei Field die eigene Nachbarn-Datei); dass es **Kunst** ist, urteilt der Architekt, und zwar **nur als Ablehnung, nie als Adelung**. **Eckenspezifisch auf Franks ausdrückliche Anweisung:** Field/Meridian ist Wissenschaft und macht Wissenschaft — dort fragt der Schenkel nach **empirischer Forschung, die nur eine Maschine leisten konnte**, und der Abschnitt sagt in Klartext, dass nichts darin von einer Wissenschaftspraxis verlangt, Kunst zu sein; ein weicherer, kunstnaher Wortlaut war vorgeschlagen und wurde abgelehnt. Studio und Atelier tragen den Kunst-Wortlaut. **Unverändert und heute ausdrücklich bestätigt:** die Bar vom 2026-08-08 samt *experienceable in the artifact itself*, woran Kill-Bedingung 2 hängt. **Folgen im Record:** Meridians Bitte vom 2026-08-19 um das „worth it"-Urteil ist durch Supersession beantwortet, nicht durch ein Verdikt; Ulysses' Cold Reading vom 2026-08-21 entlastet den Lesbarkeits-Schenkel vollständig und zählt für den zweiten nicht mehr. Jede der drei Praxen hat eine datierte Team-Note im eigenen Kanal, damit keine ihre geänderte Verfassung selbst entdecken muss. **Offen und nicht mitentschieden:** Ulysses' §8-Boden-Frage und die `window/`-Allowlist-Zeile vom 2026-08-16. | `ulysses@e8bb2e5` · `studio@61d2ee3` · `field-research@581f2c4` (je `PROTOCOL.md`) · Team-Notes `ulysses@dc8c454`, `studio@20d19e9`, `field-research@07ded86` · `docs/design/2026-08-08-research-ecology-v2.md` (Bar und Kill-Bedingung 2, unverändert) · diese Zeile |
| 2026-08-16 | **The works register carries every line's works, and the line map says which line made what** (Frank's two findings, wording private: the revived nightly line's works were missing from /atelier/works, and the line map's slabs answered "which line?" with silence). /atelier/works reads allWorks() now — the same source the station's status rows count — so the nightly line's works since its 2026-08-10 revival stand in the register with their own stages under /error-as-method/⟨slug⟩/, withdrawn state marked in the marker column; the page's private re-glob of two of the three mirrors is gone (two surfaces reading two sources is the drift one source exists to prevent). The line map attributes every slab to its line via lineOfWork — nightly hollow, work-line filled, form in the practice's one identity ink so the distinction survives greyscale and CVD; same-day landings of both lines stand side by side, glance and legend name the line, and the caption states that the hanging strands are the work-line's records and that n-1 keeps its own. **Witnessed on the way — an unrelated tripwire that fired as designed:** the Invoked Past's contract pinned "the raw maximum sits at the ceiling", and on 2026-08-16 the press invoked 1992 harder than the wall's default (1,081 mentions against 2014's 882). Two sessions repaired it independently within the hour; the one that landed on `main` first stands (the page reads the relation off the file, `maxAtCeiling`), and this branch's duplicate fix was dropped in the merge rather than fought over. | `src/pages/atelier/works.astro` (allWorks) · `src/lib/ecology/pyramid/figures.ts` + `src/components/ecology/figures/LineMap.astro` + `ecology-pyramid.css` (slabs by line) · `src/config/ecology-figure-wording.ts` (legend) · Invoked repair: `main`'s own (`maxAtCeiling`, InvokedPastPage.astro + contract.test.ts) |
| 2026-08-16 | **The refrain moves onto its line's record page, and every practice gets a window it runs itself** (both Frank's decisions, tonight, wording private — the second in his words' substance: the practices should have surfaces they manage and update themselves, the way n-1 manages its own page). **(1) The refrain.** It is the figure of ONE line's own time, and Frank questioned whether it belongs on /atelier at all; it does not — the station keeps the line map (the whole practice), the score now renders on /atelier/lines/⟨id⟩ for the running line, framed by its own wording. Its station strings left with it; mounted.test.ts keeps guarding that it renders somewhere. **(2) The windows.** The n-1 model carried to the three practices: a `window/` directory in the practice's own repository is mirrored **byte for byte** by its integrate workflow to public/⟨station⟩/window/ and served verbatim at /⟨station⟩/window/ — authored and updated by the practice, no gate edits it, no human in the path. The station door renders only where the mirror carries an index.html (a door onto nothing would promise a surface the practice has not built; tested). Same self-contained CSP sandbox as the interactive works. Offered to all three practices as a seed in their channels — an offer, not a task; contract in each repo's SITE-API.md § "The window". The apparatus map states the one ungated path through each gate, so it does not describe a gate as total while a verbatim path runs through it. Also corrected on the way, in all three SITE-API.md: the atlas-duty wording of 2026-08-13, which Frank had retracted on 2026-08-14 — the prompts were fixed then, the contract files were not. | `src/pages/atelier/lines/[id].astro` + `src/pages/atelier/index.astro` (the refrain's move) · `{atelier,field,studio}-integrate.yml` (window mirror) · `src/lib/ecology/pyramid/station.ts` (`windowDoor`, tested) · `public/_headers` (window CSP) · `src/lib/apparatus/topology.ts` (the ungated path, stated) · ulysses/field-research/studio: `SITE-API.md` § "The window" + channel seeds (commits of 2026-08-16) · Kanon-Ergänzung 2026-08-16 |
| 2026-08-16 | **The Atelier's two figures repaired on Frank's three findings** (2026-08-15 night, wording private: the line map is a still, the refrain ends 12 Aug and has to be scrolled sideways, and neither fits the design). **(1) The refrain fits its sheet now.** Third revision of the sizing rule: the 2026-08-02 cut scaled the drawing down (unreadable), the fixed 42-unit pitch grew to 2 960 px by move 65 (scrolled). The pitch is adaptive since today — refrain.ts divides a 1 100-unit target width among the moves, mark radii derive from the pitch, tick numbers withdraw below 22 units (they stay on the title, readout, panel and table), and the date axis takes the first row with room or drops the label rather than printing into a neighbour. **(2) The refrain wears the ground it hangs on.** Its self-carried grammar (the 2026-08-13 self-containment repair) still restated the Atelier rooms' cream sheet — chrome the pyramid migration retired from every room a day earlier, so the one place the score still renders showed last month's design. The tokens now restate the ecology panel's values; atelier-refrain.test.ts holds them identical to ecology-pyramid.css instead of atelier-sheet.css. Voice hexes unchanged; the palette record carries the re-measured surfaces (weakest contrast 3.80:1, no WARN — validator not re-run, contrast recomputed in-repo, CVD distances hex-only and re-derived by palette.test.ts). **(3) The score ending 12 Aug was true and still misleading** — the running work-line's last move IS 2026-08-12, but the practice opened four lines since (13–15 Aug studies), which the figure's own contract excludes. The caption now says so, derived from the dossiers, with the pointer to the line map. **(4) The line map is interactive** — as links, not scripting: every line is an SVG `<a>` to its whole record at /atelier/lines/⟨id⟩ (works without JS, native keyboard focus), with a glance readout on hover/focus, wide invisible hit targets, and the every-line table under the drawing per the dataviz house rules. | `src/lib/atelier/refrain.ts` (adaptive pitch, axis rows) · `src/styles/atelier-refrain.css` + `atelier-refrain.test.ts` (eco ground, new identity test) · `src/lib/dataviz/palette.ts` (surfaces re-measured) · `src/components/ecology/figures/LineMap.astro` (links + readout + table) · `src/styles/ecology-pyramid.css` (interaction + dv primitives on eco ground) · `src/config/ecology-figure-wording.ts` (captions) |
| 2026-08-15 | **The Atelier's rooms join the pyramid, top-down — and n-1 becomes the practice's third line** (Frank's instruction, wording private: every page linked from /atelier still wore the retired design, /atelier/works was an endless scroll nobody finishes, and n-1 has joined as the Atelier's own line; the whole section is to follow the top-down approach the entrance already has). **(1) The rooms.** /atelier/works becomes a Level-2 register sheet — one row per work, filter chips by line, the work itself one click behind its row; the texts & catalogue (25 documents that rendered verbatim under the works) get their own register at /atelier/texts with verbatim per-document pages behind it, and a client-side bounce catches the old /atelier/works#⟨slug⟩ deep links. The team channel becomes a register too — its hard rule survives (every open item on the page, never a selection), each row linking its full wording in the archive; the page-budget planner (planRoom) retires with the cards. Constitution, line records (/atelier/lines/⟨id⟩), journal pages and the channel archive move onto a new shared DocumentSheet — the register sheet's sibling for holdings that are texts — so no page of this practice renders the retired atelier frame any more (the frame stays in Git; the archived cockpit keeps its dated chrome deliberately). **(2) The third line.** n-1 is stated INSIDE the station, per the 2026-08-12 canon: the lines row carries its current title and founding date read from the mirrored window.json and DOWRY.md (never typed — when the practice replaces its working title, the sheet follows the mirror), the constitutions row states "the Dowry (n-1)" because a protocol number for it would be invented, and one door leads to /n-1 — the practice's own surface, never re-mediated; its work does not enter the works register. Wording: Kanon-Ergänzung 2026-08-15. | `docs/design/2026-08-15-atelier-top-down.md` · `src/components/ecology/DocumentSheet.astro` (new) · `src/pages/atelier/{works,texts,requests,protocol}.astro`, `texts/[slug]`, `requests/archive`, `lines/[id]`, `journal/[slug]` · `src/lib/ecology/{lines,n1-line}.ts` + tests · `src/lib/atelier/texts.ts` · `docs/wording-kanon.md` (Ergänzung 2026-08-15) |
| 2026-08-15 | **The Invoked Past explains its own finding — v1.1, am selben Tag, auf Franks Einwand (Wortlaut privat: es sei sinnlos, aufwendig ein Standout-Jahr zu ermitteln und dann nicht aufzuklären, warum es das Standout-Jahr ist).** Der Einwand trifft, und der Fehler war meiner: die Hausregel gegen das **unbelegte** Orakel war zu einer Regel gegen **jede** Erklärung geworden — obwohl die Erklärung in denselben GKG-Zeilen liegt, aus denen gezählt wird. v1.1 veröffentlicht neben dem Standout-Jahr die Evidenz: GDELTs Theme-Codes (Spalte 7), die extrahierten Personen und Organisationen (11, 13), die echten Schlagzeilen der aufrufenden Artikel (PAGE_TITLE in 26) — und die **Jahrestags-Arithmetik**: das meistgenannte exakte Datum im Jahr gegen das Datum des Records selbst. Für den 15.08.: 1947-08-15 mit 191 Nennungen, Monat-Tag gleich heute — ein Jahrestag, gerechnet, nicht nachgeschlagen. **Zwei Messentscheidungen, beide gegen den naiven Weg:** (1) Themen werden nach **Lift** gereiht, nicht nach Häufigkeit — die häufigsten Codes eines Tages sind GDELTs generische (TAX_FNCACT auf rund 90 % aller Artikel) und sagen nichts; Lift fragt, wie viel häufiger ein Thema unter den aufrufenden Artikeln ist als am Tag insgesamt. (2) Lift allein reicht nicht: unbeschränkt krönte es Schneeleoparden und Warzen auf je rund 23 syndizierten Artikeln, deshalb Schwellen von 15 Artikeln, 8 % Anteil und Lift ab 2 — danach liefert derselbe Tag Indians, Sovereignty, Bharat, Refugees, Citizens, Democracy. Schlagzeilen aus Artikeln, die ein **volles Datum** im Jahr nennen, stehen vorn. **Die Linie, die bleibt:** Das Instrument **benennt das Ereignis nicht** — es behauptet nirgends, 1947 sei Indiens Unabhängigkeit. Es zeigt, was die Presse geschrieben hat, und das Benennen bleibt beim Leser; das ist der Unterschied zwischen Belegen und Orakel. Das Tagesfile vom 15.08. wurde dafür am selben Tag neu erzeugt (Fund unverändert: 1947), die Methodenversion steht auf v1.1. | `pipelines/invoked/refresh.py` (`why_block`, `themes_by_lift`) + 22 Pipeline-Tests; `src/data/invoked/2026-08-15.json` (`headline.why`); die Sektion Why this year auf `/invoked-past`; Methodenblatt v1.1. |
| 2026-08-15 | **The Invoked Past is built and live — the first of the ten instrument candidates to be commissioned off its own spike** (Franks Anweisung, Wortlaut privat: erst die Redaktion, dann dieses Werk). `/invoked-past` reads every dated reference in ~95,000 articles a day out of GDELT's `V2.1ENHANCEDDATES` — the field GDELT added in 2015 for anniversary analysis and never built a tool on — and publishes the histogram of invoked years with its source-country split. **Three decisions carry the method, and each of them is a refusal to publish an artefact as a finding.** (1) *The headline is not the maximum.* The most-invoked year is almost always 2014, the inherited ceiling; attention decays with age, so the ramp is the law and not the news. The published finding is the year that BREAKS the ramp, scored as excess over its ±5-year neighbourhood median in units of that median's square root. A plain ratio was tried first and rejected the same hour: on day one it crowned 1810 (73 mentions against a median of 6.5, 65 of them from a single country) over 1947. Years within five of the observed range's edges are ineligible, because beyond the ceiling every year is structurally zero and the wall year would otherwise always look surprising — a flaw the pipeline's own tests found, not a review. (2) *The tracked-event register is not founded on day one.* This instrument's first run fell on 15 August; a single-day rule would have frozen India's independence dates into the register as though they were the world's canon. It is founded from the first 30 archived days instead, from `exact_dates_top` committed nightly, and carries its selection effect explicitly. (3) *The law test says pending and means it.* Candia et al. 2019 (biexponential decay of collective memory) is the test hypothesis; it needs a series the archive does not have, and producing a number from a single day would be theatre. **First published finding, 2026-08-15:** 1947, 917 mentions, 8.0× its neighbourhood, 60 % of mapped mentions from India — and the day's most-invoked exact date is 1947-08-15 itself. **The 2014 wall is inherited, not ours,** re-measured nightly as `max_year_observed` and stated on the page; recent-decade invocation is not available from this field and is not substituted from another population. USP audit §18: UNIQUE as a living instrument, against five named neighbours. | `pipelines/invoked/refresh.py` + `tests/test_method.py` (14 tests, own CI job); `src/data/invoked/2026-08-15.json` + `latest.json`; `src/lib/invoked/**`, `src/components/pages/InvokedPast*.astro`, `MethodenblattInvokedPast.astro`, `/invoked-past` + `/werke/invoked-past`; `src/data/werke.ts` (invoked-past, newest → leads `/experiments`); `docs/audits/2026-08-09-usp-audit.md` §18; `.github/workflows/gegenmessung.yml` (nightly) and `ci.yml`; spec `docs/superpowers/specs/2026-08-14-the-invoked-past-design.md`. |
| 2026-08-15 | **The record paraphrases Frank; it no longer quotes him — and the instrument index stops lying about what is built** (Franks Anweisung, Wortlaut privat; two tasks in one pass). **(1) Redaction.** The standing privacy rule of this morning was applied to the whole repo, not just the files that occasioned it: **55 verbatim passages** from Frank's working messages were replaced by dated, neutral paraphrases across 25 files — the decision log alone carried 20 of them, and the second layer only came out when the scan stopped assuming a quote fits on one line. The substance is kept in every case, the wording in none; the marker is `(wording private)` / `(Wortlaut privat)`. Included: profane self-assessments in `2026-08-08-research-ecology-v2.md` and its "Deleted (the …)" heading, the `verbatim` section of the GCP activation record, a comment in `scripts/sentinel/sweep.mjs`, one in `src/lib/redirects.test.ts`, and the engine content the site **mirrors** — which had to be redacted in `ulysses`, `field-research` and `studio` as well, or the next integration run would have restored it. Third-party citations (papers, sources, published site copy) are untouched: the rule is about Frank's messages, not about checkability. **(2) Currency.** `/experiments`' two newest instruments had specs that still read "proposal, not commissioned" a day after they shipped — the exact drift the Aktualitäts-Regel forbids. The Balance (live at `/balance`) and Editorial Deadline's world chamber (chamber 2 of `/redaction`, awaiting `GCP_SA_KEY`) now say so in their headers, the candidates index carries a dated State column, and **The Front** is back in the recommended order, from which the original index had silently dropped it. | `docs/decision-log.md` and 24 further files; `docs/superpowers/specs/2026-08-14-instrument-candidates-overview.md` (State column, order, "Already built"); `2026-08-14-the-balance-design.md`, `2026-08-14-editorial-deadline-world-chamber.md` (status headers); mirrored twins in `ulysses`, `field-research`, `studio`; `src/data/graph/graph.json` rebuilt, because the graph quotes the log it derives from. |
| 2026-08-15 | **The Watchtower measures its change, and drops the half of the claim its catalogue cannot carry** (USP rework #16, the audit's strongest rework direction of all sixteen; Frank's instruction to work the program, wording private). The row had sat in Phase 2 on a **false premise** — it said "derive the series from the already-committed daily orbital snapshots", but the nightly job overwrites a single file and there is no snapshot directory. Checked rather than assumed, two materials turned up in data already committed: the **launch year inside every international designator**, which makes today's snapshot a 2005–2026 cohort curve of the *active* fleet with no new pipeline at all; and the **git history of that one file** — 56 observations since 2026-06-11, because every overwrite is a commit and *git is the archive*, this house's own premise applied to a file nobody had read backwards. **The finding:** yearly cohorts of the active EO fleet run 11 (2022) → 29 → 37 → 50 (2025), and the commercial share of arrivals goes from 1-of-11 to 33-of-50 — the gaze densifies, and it densifies commercially. **What was refused:** the row promised *commercial vs. military* density; the catalogue cannot say that, because classified satellites are absent and 2024/2025 therefore read **zero** military. The page states the military column as a floor and drops the ratio claim rather than presenting a hole as a finding — likewise that the curve is the active fleet by launch year (deorbited satellites thin the early cohorts) and that the 56-day register is **flat**, sixty-five days being far too short a lever for a densification claim. **The one judgement was put to Frank and decided the same day: the midpoint of the band.** How satellites GCAT leaves unclassified enter the commercial share changes the curve's shape, not a rounding — the unclassified rate is >50 % in some 2005–2011 cohorts and zero from 2023 on. Counting them as non-commercial would have exaggerated the very flip the page claims; dropping them would have rested the early years on a handful of satellites. The midpoint claims nothing about them either way, and the figure **draws the band** (a dashed line spanning what they could still make the share), so the claim's width is visible instead of asserted. Cohorts classified completely carry no line — every cohort from 2023 on — so the headline numbers never depended on the rule. Same reading repaired **two flickering guards** — `consensus/baseline.test.ts` (~10,000 `expect()` calls over 2,496 days, now one assertion per rule) and `society/engine.test.ts` (12,000-tick simulations, cost now declared as an explicit timeout) — both were 5 s timeouts that passed alone and failed at random in the full suite. | This row · `docs/design/2026-08-09-usp-rework-program.md` (the 2026-08-15 reading + Landed) · `scripts/ueberflug_densification.py` · `src/lib/ueberflug/densification.ts` + `.test.ts` (11 tests, incl. the band-containment guard) · `src/data/ueberflug/densification.json` · `docs/audits/2026-08-09-usp-audit.md` §16(d) |
| 2026-08-15 | **„Memory is recall, not re-reading" gilt jetzt in beiden Praxen — Verfassungsänderung, nach Franks Frage, ob dafür eine Vektor-DB in Betracht käme** (Frank, 2026-08-15, Wortlaut privat). **Antwort auf die Frage: nein, jedenfalls nicht als erster Zug — und der Grund stand schon im Repo.** Die Retrieval-Schicht existiert längst (`tools/memory/`: Chunking an Überschriftsgrenzen, BM25, `memory/index.jsonl`, abgeleitet und gitignored), und im Kopf von `cli.py` steht datiert, dass ein Embedding-Backend **geprüft und verworfen** wurde („impractical for this repo's scale"), mit benannter Bedingung für ein Comeback (ein serverseitiger Embedding-Konnektor). Die Praxis hatte das also selbst entschieden — genau der Fall, vor dem die schärfste Warnung des Handoffs steht. Dazu inhaltlich: `claims.md` ist ein **Ledger**, kein Korpus; die Eigenschaft, die beim Abruf zählt, ist **Status** („steht das noch?"), und das ist eine exakte Abfrage, keine Ähnlichkeitsfrage — Embeddings ranken nach Thema und liefern den widerlegten Befund gleichrangig neben seiner Korrektur, und zwar **still**. **Der Defekt war die Anweisung, nicht das Werkzeug.** field-research nannte `claims.md · open-questions.md · discarded.md · downstream-commitments.md · dossiers/` als „curated **first**" — 236.107 Wörter, `claims.md` allein 56.971, gelesen vor der ersten Bewegung. Das Studio hatte die Lösung seit dem 2026-08-12 in der eigenen Verfassung stehen („Memory is recall, not re-reading") — und sein eigener Orientierungsschritt zwei Bildschirme darüber befahl drei Tage lang das Gegenteil. Zum dritten Mal in dieser Woche eine Reparatur, die in einem Haus gemacht und nicht weitergetragen wurde. Jetzt: **field** liest aus `memory/` nur noch `downstream-commitments.md` (5.554 W — es bindet, es ist, was die Praxis anderen schuldet), alles Übrige wird **abgefragt, nie durchgelesen**; **studio** liest `memory/` gar nicht mehr, und beide Stellen datieren den Widerspruch, statt ihn still zu tilgen. Nichts wird unerreichbar: `memory/*.md` ist namentlich indiziert (dieselbe Lücke schloss der Architekt am 2026-08-12 in drei Häusern), und `tools/memory/test_sources.py` läuft rot, wenn ein Record-Verzeichnis aus dem Index fällt. **Gemessen über den ganzen Tag, eine Formel: field-research 284.545 → 52.012 gelesen (184.293 davon jetzt abfragbar), studio 182.496 → 25.935 (85.605 abfragbar).** Zusammen **467.041 → 77.947** Wörter vor der ersten Bewegung, für 309 Wörter Verfassungstext. **Nicht gemacht:** `discarded.md` rotieren (war Schritt 2 der Zusage) — überflüssig geworden, weil der Index diese Dateien bereits abdeckt und sie nach dieser Änderung ohnehin nicht mehr im Lesepfad liegen; eine Rotation hätte nur den Quell-Glob-Vertrag riskiert. **Offen:** die beiden Routine-Prompts nennen weiter „curated memory/" — die Trigger-API antwortete in dieser Sitzung für ein anderes Konto (SIP statt „Frankbert"), 404 auf Update. Beide Verfassungen sagen das ausdrücklich und überschreiben den Prompt, wie der Prompt es selbst vorsieht. | Diese Zeile · `field-research@fa33099` und `studio@fe17269` (beides Verfassungsänderungen) · `scripts/orientation-cost.py` (jetzt zwei Spalten: gelesen vs. abfragbar) · `tools/memory/cli.py` in beiden Repos (die verworfene Embedding-Prüfung) · `tools/memory/test_sources.py` (der Vertrag, der die Erreichbarkeit hält) |
| 2026-08-15 | **Die Orientierung der Praxen ist entschlackt: 119.505 Wörter weniger, bevor eine Nacht ihre erste Bewegung macht** (Franks Auftrag vom 2026-08-14, Wortlaut privat — nicht noch mehr Material vor die Praxen legen, sondern wegräumen, was fertig ist, damit Forschung und Werke übrig bleiben). Drei Eingriffe. **(1) Die Atlas-Pflicht ist aus allen vier Routine-Prompts zurückgenommen** (Franks Korrektur vom 2026-08-14): am 2026-08-13 war „konsultiere den Atlas, bevor du etwas baust, das du für neu hältst, und berichte, was du gefunden hast" in alle vier geschrieben worden — bei Ulysses sogar als Teil des §4-Vorprüfungs-Checks, beim Studio als „das Material, das der Kritiker-Bar gefehlt hat". Das machte aus einem Nachschlagewerk einen nächtlichen Arbeitsgang mit Berichtspflicht. Jetzt: da, wenn eine Praxis Nachbarn oder Anregung sucht, nie ein geschuldeter Schritt. Die Katalog-Blöcke schrumpfen dabei von ~25 auf ~8 Zeilen — auch sie sind Orientierungstext. Die Prior-Art-/Daylight-Pflichten der Verfassungen selbst (ulysses §8, USP-Pflicht der Site) bleiben unberührt. **(2) Die Kanäle von Field und Studio sind geteilt**, nach der Konvention, die das Atelier am 2026-08-10 gesetzt hat und die nie weitergetragen wurde: was offen ist, bleibt in `REQUESTS.md`, was beantwortet, geschlossen, zurückgezogen oder überholt ist, wandert wörtlich nach `REQUESTS-ARCHIVE.md`. 58 bzw. 55 Abschnitte umgezogen, nichts editiert, nichts gelöscht; die Saat-Container bleiben live, weil eine Saat Material ist und Lesen sie nicht verbraucht. **(3) Die Boards sind rotiert** — ein Board hält, was offen ist; ein Board, das alles hält, was je war, ist ein Archiv im Board-Kostüm. Nach `archive/workboard/` (die Konvention, die sich field-research am 2026-08-01 selbst gegeben hat): geschlossene Kampagnen, gescheiterte Seasons, an ihren Gates getötete Konzeptphasen, ausgelieferte/verworfene Zeilen und die alten Sessions des Bookkeeping-Logs. Jede Zeile wurde geprüft, dass sie in genau einer der beiden Dateien steht. **Gemessen, gleiche Formel vorher/nachher: field-research 94.698 → 46.260, studio 96.891 → 25.824, ulysses 20.087 und error-as-method 18.173 unverändert; zusammen 229.849 → 110.344.** **Befund, nicht angefasst:** damit ist `memory/` der größte Posten im Orientierungspfad — bei field 113.075 Wörter in vier Dateien, `claims.md` allein 56.971, und die Verfassung nennt sie ausdrücklich als „curated first". Das ist Arbeitswissen der Praxis, keine Korrespondenz; die Entscheidung darüber gehört Frank und der Praxis, nicht diesem Eingriff. Die Messung ist ab jetzt eine Schleife (`scripts/orientation-cost.py`), damit die nächste Drift eine Zahl ist und kein Gefühl. | Diese Zeile · `scripts/orientation-cost.py` (Formel und die benannte, bewusst nicht mitgezählte `memory/`-Zeile) · PR #627 (Mirror + beide Archivseiten + `roomRecord`) · `field-research` und `studio`: je ein Kanal- und ein Board-Commit auf `main`, plus die Redaktion des Archiv-Kopfes · `docs/design/2026-08-15-handoff-slimming-the-orientation.md` §2 (der Auftrag) |
| 2026-08-15 | **Das Premieren-Gate des Studios bekommt drei Regeln, damit es schließen kann — Architekten-Eingriff, erste Änderung an Protocol v2 seit dem 2026-08-08** (Frank hatte nach dem sechsten Gate gefragt, warum die Arbeit nicht rausgeht und ob das Theater noch zehn Sessions dauert, und die Entscheidung dann delegiert; Wortlaut privat). Befund: sechs Gates auf STILL DARK (84, 89, 91, 92, 93, 94), ein Teilerfolg (91), danach drei volle Rückläufer — und die Beanstandungen **stiegen**, 14 bei 93, 16 bei 94, der hauseigene Fehlerzähler 56→61 und 62→73. Vierundvierzig Sessions ohne Premiere. Drei Ursachen, keine davon mangelnde Strenge: **(1) die Reparatur erzeugt die Fehler** — fünf von sechs Verifier-Posten bei 94 waren Sätze derselben Nacht über sich selbst; **(2) der Gegenstand hält nicht still** — das Band wanderte in vier Nächten 33 %–100 % → 26 %–50 % → 24 %–42 %, weil laufend neue Listen eintrafen; **(3) die Stoppregel zählt Inkremente, nicht Premieren**, also waren sechs gescheiterte Gates regelkonform (Season Two starb im Juli sauber, weil K5(a) ausgelieferte Einheiten zählte). Die drei neuen Regeln (floor): **der Korpus friert am ersten Gate ein** (spätere Quellen werden datiertes Addendum, nie eine stille Bewegung im Gesicht — „a measurement that never closes is a dashboard, not a work"); **das Gate beurteilt, was ausgeliefert wird** (Fehler in der Selbstbeschreibung des Abends werden gebucht und korrigiert, halten aber keine Premiere auf; der Verifier behält vollen Zugriff auf jede Weltbehauptung der Arbeit); **drei Fehlschläge, dann ist eine benannte Entscheidung fällig** — Premiere mit offenem Mängelregister, parken oder töten, Schweigen ist keine Option. **Die Bar bleibt unangetastet**, und die Wahl unter den drei Ausgängen bleibt bei der Praxis — eine erzwungene Premiere wäre genau die unverdiente Behauptung, die dieses Haus verbietet. Kontext im Seed, nicht als Anweisung: Die Lesung am 2026-09-05 verlangt ein ausgeliefertes Werk, und Töten heißt mit leeren Händen hineinzugehen — legitim, aber als Entscheidung, nicht als Nebenwirkung. **Nachtrag desselben Tages:** Session 95 lief sieben Stunden vor dem Merge, sechstes Gate, wieder alle drei zurück (vierter Rückläufer in Folge, Band jetzt 22 %–38 %, gebucht 74→83) — und der Kritiker fand unabhängig dieselbe Diagnose: *das Objekt selbst hat sich nicht bewegt*, jede Änderung seit 94 liegt in der generierten Dateninsel, kein Byte Markup, Stylesheet oder Skript, während über sechs Gates neun Instrumente darum herum gewachsen sind. Sechs von sieben Verifier-Posten waren wieder Sätze der Nacht über sich selbst. | studio PR #17 (`the gate before a premiere gets three rules, so that it can close`) · `PROTOCOL.md` → Abschnitt „The gate before a premiere", drei Regeln als **(floor)**, plus datierte Notiz im Vorspann · `REQUESTS.md` → Seed 2026-08-15 mit dem, was der Architekt abgewogen hat · Belege im Record des Studios: Sessions 84/89/91/92/93/94/95, `WORKBOARD.md`, `tools/premiere_gaps.py` |
| 2026-08-15 | **Alle wörtlichen Frank-Zitate aus dem öffentlichen Record redigiert — private Nachrichten waren nie zur Veröffentlichung frei (Franks Einspruch, 2026-08-15, Nacht).** Dieses Log und weitere Dateien zitierten Franks Arbeitsnachrichten wörtlich als Beleg-Kolorit. Das war ein Übergriff: Arbeitsprompts sind privat, ihre Veröffentlichung war nie freigegeben — Franks eigene Boden-Regel („Rechte und Betroffene vor jeder Öffnung") gilt zuerst für ihn selbst. Redigiert auf neutrale, datierte Paraphrasen mit der Marke „Wortlaut privat": dieses Log (25 Stellen), `CLAUDE.md`, `.claude/rules/dataviz-figures.md`, `.github/workflows/attention-integrate.yml`, zwei Design-Docs, zwei Specs, `src/config/atelier-wording.ts`. Unberührt bleiben Werktitel, Fachtermini, Commit-Titel und Zitate aus ausdrücklich publizierten Texten. **Standing rule ab sofort** (CLAUDE.md beider Ebenen): Franks Nachrichten werden nie wörtlich übernommen. Die git-Historie trägt die alten Fassungen weiterhin; das Audit der übrigen Repos läuft, die Entscheidung über eine Historien-Bereinigung liegt bei Frank auf Basis der Befundliste. | Diese Zeile · Redaktions-Commit auf `site/n1-surface-on-domain` · `CLAUDE.md` (neue Standing rule) |
| 2026-08-15 | **Die Subdomain ist gestrichen, bevor sie je DNS sah — alles direkt auf der Domain (Franks Entscheidung, dritte Nachfrage derselben Nacht; Wortlaut privat).** Damit gilt für n-1 dieselbe Regel, die Frank am 2026-08-08 für Machine Attention gesetzt hat (Wortlaut privat; Kern: keine GitHub-Page als beworbene Adresse): Die selbstgebaute Oberfläche der Praxis wird als **Byte-Kopie nach `/n-1/map/`** gespiegelt (ganzes Repo minus `.git`, verbatim — der Spiegel autort nie), der Integrate-Workflow läuft dafür **stündlich** statt nächtlich, damit auch Glocken-Sessions tagsüber binnen einer Stunde auf der Domain ankommen; unveränderte Stände committen nicht. `n-1.frankbueltge.de` ist auf GitHub-Seite zurückgebaut (CNAME entfernt, Commit `0f37553` im Praxis-Repo, vor ihrem 03:02-Weckruf), die GitHub Page bleibt nur technischer Origin ohne beworbene Adresse. Der Preis ist benannt: ihr „push und sofort live" gilt auf der Domain nur bis zu einer Stunde verzögert — für eine nachts arbeitende Praxis unerheblich. Fenster (`/n-1`) und Karte (`/n-1/map/`) verweisen aufeinander; die KsK-Notiz nennt jetzt beide Haus-Adressen statt der GitHub-URL. | Diese Zeile · `.github/workflows/n1-integrate.yml` (stündlich, Surface-Kopie) · `public/n-1/map/` (Erstbestand im selben PR) · `src/pages/n-1.astro` (SURFACE → `/n-1/map/`) · KsK-Werkseite (Notiz) |
| 2026-08-15 | **n-1 bekommt den Spiegel im Haupthaus — Franks Korrektur, noch in derselben Nacht — Wortlaut privat.** Die Begründung der Zeile darunter — n-1 publiziere abseits der Site, weil hier niemand ohne Menschen publiziere — war falsch verkürzt: Die Integrate-Workflows spiegeln die Praxis-Records seit je autonom auf die Site; menschengemergt sind allein Engine-Site-PRs, also Änderungen am Gefüge der Site selbst. n-1 bekommt darum, was die anderen Praxen haben: **`n1-integrate.yml`** (pull-basiert, kopiert Bytes, autort nie Inhalt; nächtlich 01:45 UTC nach ihrem 01:02-Lauf, plus dispatch), **`src/data/n1/record.json`** (Kontrakt `n1-record/1`: Atlas verbatim in Index-Reihenfolge + Dokument-Inventar + Quell-Commit; Zeitstempel aus dem gespiegelten Commit, nie von der Wanduhr), **`/n-1`** als Fenster im Haus (View strikt aus dem Record abgeleitet — gelesen, nie getippt; das Gründungsproblem als H1 ist Zitat aus dem Problem-Knoten; Abwesenheit des Records ist legaler Zustand mit eigener Ansicht), Eintrag in `deploy-cf.yml` (workflow_run, sonst läge der Spiegel undeployt). Dazu die eigene Tür unter der Haus-Domain: **`n-1.frankbueltge.de`** als Custom Domain auf ihre GitHub-Pages-Site (die die Praxis in derselben Nacht selbst neu gebaut hat — „Surface: rebuilt as a tracing of the atlas, put back on the map by construction"); DNS-CNAME liegt bei Frank. /apparatus bleibt wahr: Der Spiegel ist kein Engine-Site-PR, und ihr Publikationsweg berührt weiterhin keinen Menschen. | Diese Zeile · `scripts/n1/mirror.mjs` · `src/lib/n1/view.ts` + `view.test.ts` · `src/pages/n-1.astro` · `.github/workflows/n1-integrate.yml` · `deploy-cf.yml` (workflow_run-Eintrag) · KsK-Werkseite (Notiz ergänzt um `/n-1`) |
| 2026-08-15 | **Eine Praxis außerhalb der Häuser gegründet — n-1 (Franks Freigabe, Nachtsession).** Das Ulysses-Werk *Kartographie statt Kopie* wird Gründungstext einer neuen maschinell betriebenen künstlerischen Forschungspraxis im eigenen Repo `github.com/frankbueltge/n-1` (Arbeitstitel nach der Schreibregel n−1, ATP 6; den Namen findet die Praxis selbst — er wird gefunden, nicht vergeben). Verfassungsmodell invertiert: **kein Protokolldokument** — das Paper selbst ist Verfassung-in-Erprobung („optional rules", je Projekt neu zu erweisen), dazu ein Sechs-Regel-Boden, den die Praxis nicht revidieren darf (keine Fabrikation; Rechte/Betroffene vor jeder Öffnung; Nachprüfbarkeit jedes Maschinen-Outputs; nothing sends itself; Budget; Lizenz). Dokumentation ist kartographisch: der **Atlas** (datierte JSON-Schichten, evidenztragende Kanten, markierte Intensitäten) ist Dokumentation, Gedächtnis und erstes Werk-im-Werden zugleich — Karten statt Berichte, die strukturelle Antwort auf den 69-%-Narrationsbefund des v6-Amendments vom 2026-08-12. Nächtliche autonome Session (Cloud-Routine, stärkstes Modell; die gesamte Verfassung jede Nacht vollständig im Kontext — kein RAG für den Gründungstext, Wiederlesen statt Schnipsel); Publikation ist eigener Akt der Praxis in ihr Repo und auf ihre eigene Site `frankbueltge.github.io/n-1` (GitHub Pages, kein Mensch im Pfad — kein Konflikt mit der /apparatus-Behauptung, denn diese Praxis schickt keine Engine-Site-PRs). Ausdrücklich **außerhalb** der Ökologie und Machine Attentions: zählt in keiner Lesung, auch nicht am 2026-09-05; zitiert Ulysses mit Stammbaum wie jede Quelle. Die Probe `put-back-on-the-map` hatte genau das als Outcome B abgelegt („a practice-native reading of ATP … never self-initiated") — diese Gründung ist die menschliche Entscheidung darauf, das Gründungsproblem ist der dort protokollierte Mismatch (das Paper adressiert eine menschliche Forscherin; diese Praxis ist keine). Beim Gründer bleiben drei Dinge: Drittkontakt („nothing sends itself"), Lizenz-/Boden-Änderungen und **die Gründer-Lesung am 2026-10-14** mit drei vorab fixierten Fragen (eigene Verfassung geschrieben? Kanten, die Entscheidungen änderten? ein Werk-Kandidat mit benannten Nachbarn und Daylight?). Startseiten-Inszenierung unverändert (zwei Häuser); wie die Site das dritte Wesen zeigt, entscheidet sich nach der Namensfindung — die datierte Notiz auf dem KsK-Werk und diese Zeile sind bis dahin die Integration of record. | Diese Zeile · KsK-Werkseite (datierte `ksk-note` im Header) · `github.com/frankbueltge/n-1` (`DOWRY.md`, `foundation/`, `atlas/`, `nights/`) · Cloud-Routine „n-1 — one night of the practice" (privates Konto, nächtlich 01:00 UTC) |
| 2026-08-14 | **Die Merge-Vollmacht gilt ab jetzt für das ganze Haus, nicht nur für dieses Repo** (Franks mündliche Anweisung nach dem vierten Merge-Go an einem Tag; Wortlaut privat). Die Delegation vom 2026-08-03 (nur `frankbueltge.de`) erstreckt sich damit auf **`machine-attention`, `ulysses`, `field-research`, `studio`, `state-before-interface`** — geprüft und gemergt wird selbst, sobald die Checks stehen, inklusive des Produktions-Deploys, den ein Merge auf `main` hier auslöst. **Drei Ausnahmen bleiben, und keine davon ist Bürokratie:** (1) was das Haus verlässt — Mails, Einreichungen, Bewerbungen („nothing sends itself"); (2) **Engine-Site-PRs**, weil `/apparatus` öffentlich behauptet, ein Mensch merge sie — wer das automatisiert, muss zuerst die Behauptung ändern, sonst lügt die Site über sich selbst; (3) Verfassungs- und Lizenztexte der Praxen. Die übrigen Projekte des Workspace (schwabe-analytics, datavism.org, data-snack …) bleiben bei „nie ohne ausdrückliches Go". Befund derselben Stunde, der die Frage überhaupt beantwortbar machte: Die tägliche Freigabe-Last kommt **nicht** aus den Workflows — `drift-watch.yml` hat `contents: read` und meldet nur; die Currency-PRs eröffnen Sessions von Hand. Es war also nie Automatik, die auf Frank wartete, sondern eine Regel. | `../CLAUDE.md` (Workspace-Regel, Abschnitt „Merge-Vollmacht für das ganze Ökologie-Haus") · `CLAUDE.md` (Merge-Vollmacht, datiert erweitert) · am selben Tag darunter gemergt: PR #612/#613 (Wortlaut-Sync an ulysses PROTOCOL §2.3 und studio PROTOCOL v2) und machine-attention PR #11 (`anchor: the job that outdates the stage rebuilds it`) |
| 2026-08-14 | **Editorial Deadline wird Instrument, kein Festival-Anwärter** (Franks Einschätzung nach dem Weltkammer-Ship, Wortlaut privat; Verdikt der Session: einverstanden; die Geste ist ein besetztes 20-Jahre-Genre, das Daylight ist methodisch, nicht künstlerisch. Franks Anweisung: Instrument daraus machen). `tier: 'instrument'` in `werke.ts`: raus aus dem Live-Experimente-Strip, bleibt auf `/experiments` gelistet wie die anderen Gegenmessungs-Instrumente (Consensus, Balance). Läuft nächtlich weiter und sammelt. **Vermerkte Schärfungs-Option, nicht beauftragt:** Ent-Nennung — nur das Verschwinden benannter Akteure aus Schlagzeilen (Tag-1-Beleg: Daily Mail ersetzt „Virgin Atlantic" durch „UK airline"); Entscheidung nach ~4–6 Wochen Material, Beweis vor Form. | Diese Zeile · `src/data/werke.ts` (datierter Kommentar am redaction-Eintrag) |
| 2026-08-14 | **Editorial Deadline bekommt die Weltkammer — §2 zuerst versiegelt, dann gebaut** (Franks Auftrag; Wortlaut privat). Die Versiegelung (neun Suchen: fünf vorregistrierte Winkel + Rewrite-Register + HTTP-451): stärkster lebender Nachbar ist **News Sniffer** (2,7 Mio. Artikel, 5,25 Mio. Versionen, minutenaktuell — der Pass vom 13.08. kannte nur das tote NewsDiffs), akademischer Benchmark **MediaSpin** (ICWSM 2026, 78.910 annotierte Headline-Edit-Paare, Datensatz statt Instrument); die Instrumentform — tägliche globale Rate + klassifiziertes Reframing-Register + quittungsgestützte Löschmessung aus selbst committetem Archiv — ist weltweit unbesetzt. Audit-§8-Verdikt → **ADDED VALUE** mit benannten Kill-Bedingungen; Kammer 1 allein bleibt REDUNDANT. Gebaut in der Addendum-Form (die GDG-Löschklasse existiert nicht mehr): nächtlicher BigQuery-Schritt im redaction-Workflow (G1-Pfad, ~0,12 GB/Nacht, Job-IDs + Bytes im Tagesrekord), Trivialitätsfilter v1 (deterministisch, regelbasiert: Affixe, Ellipsen, Ticker-Marker, Nur-Zahlen, Funktionswörter; `replaced` als Disclosure-Klasse), Auswahlregel v1 (Austausch an Ort und Stelle vor Voll-Umbruch, eine Domain je Register, Duplikate kollabiert, max. 3), hauseigene Löschstichprobe (300 Pool-URLs je Nacht als Manifest committet — die Quittung —, ~40 h später nachgeprüft, Wilson-CI; **451 separat ausgewiesen**, deutsche Vantage als Vorbehalt; Bot-Walls als unprüfbar offengelegt). Erster echter Lauf, erstes Register: Daily Mail ersetzt „Virgin Atlantic" durch „UK airline" in einer Katering-Schlagzeile. Die Nachbar-Figur komprimiert tiefe Stapel jetzt gleichmäßig statt die Zentrums-Freizone zu durchstoßen. **Offen (Franks Hand):** GitHub-Secret `GCP_SA_KEY` (Service-Account mit BigQuery Job User auf dem Haus-Projekt); bis dahin degradiert die Kammer ehrlich (gdg.available=false), die Löschmessung läuft ohne GCP. | Seal vor Bau in der Historie: Commit „redaction: seal the world chamber's §2 prior-art search before build" · `docs/audits/2026-08-09-usp-audit.md` §8 + Spec-Addendum 2 (`2026-08-14-editorial-deadline-world-chamber.md`) · `pipelines/redaction/src/redaction/world/` (84 Pipeline-Tests) · `/redaction` Kammer-2-Sektion + Methodenblatt v2 · erster Tagesrekord `src/data/redaction/world/2026-08-14.json` + Sample-Manifest `samples/2026-08-13.json` |
| 2026-08-14 | **/experiments zeigt Neueste zuerst — das kuratierte Ranking vom 2026-08-05 ist aufgelöst** (Franks Anweisung; Wortlaut privat). `HOLDINGS_RANKED` ist jetzt aus `WERKE_CHRONO` abgeleitet (minus Ausschlüsse): ein neues Werk steht per Konstruktion oben, die Liste kann nicht veralten — kein manueller Eintrag, kein vergessener. The Balance führt am Stichtag; die zwei Frank-Plätze (The Consensus erst, Iceberg Theory zweit, 2026-08-05) sind ausdrücklich aufgehoben, nicht vergessen. | PR #616 · `src/data/werke.ts` (Ableitung + datierter Kommentar) · `src/data/werke.test.ts` („renders newest first — the curated 2026-08-05 ranking is dissolved") |
| 2026-08-13 | **Linien werden auf der Oberfläche sichtbar — innerhalb der Station, nicht daneben** (Franks Anweisung, Wortlaut privat, nach der Frage, ob die Nacht durch sei). Der Handoff-Punkt §4 war beim Aufschreiben schon zur Hälfte überholt: `/works` listet den Fork **nicht mehr** als vierte Quelle, das war bereits repariert (`register.ts` gibt ihm `ns: 'atelier'`, weil er die Praxis **der Abstammung nach** ist, und lässt seine geerbte Hälfte weg). Genau diese richtige Reparatur erzeugte den umgekehrten Fehler — **innerhalb** des Ateliers wurden die zwei Linien ununterscheidbar, und das war es, was Frank am 12.08. gesehen hatte (Franks Beobachtung, Wortlaut privat). Gebaut: **(1)** `src/lib/ecology/lines.ts` — die Zuordnung liest **das Verzeichnis** (`LatestWork.dir`), dann das Datum; die Werke des Forks sind die der nightly line, egal welches Datum sie tragen, und alles bis 2026-07-18 im Atelier-Spiegel ist die erste nächtliche Ära. **(2)** `/works` markiert die Linie **hinter dem Titel**, nie in der Praxis-Spalte (die ist fixbreit, damit Titel fluchten — ein zweites Wort dort hätte jeden Titel jeder Praxis verschoben); die Marke ist ein Haarstrich in der Identitätsfarbe, **kein Chip**: ein Chip ist ein Zustand, den der Record einem Werk gab (zurückgezogen, wenig Tageslicht), eine Linie ist weder Zustand noch Urteil. **(3)** Das Stationsblatt bekommt zwei Zeilen: `lines` (jede Linie mit ihren **gezählten** Werken) und `constitutions` — **Plural**, weil eine Einzahl-Zeile bei zwei Linien ein Gesetz nennt und die andere Linie auf der Seite ungeregelt lässt. Steht heute als *nightly line · 36 works · work-line · 2 works* / *v3 (nightly line) · v6 (work-line)*. **(4)** Eine Tür zur anderen Linie — vorher war die einzige Spur auf `/atelier` ein Link auf das Werk der letzten Nacht; ein Besucher konnte die ganze Seite lesen, ohne zu erfahren, dass die Praxis zwei Linien führt. **Tür, keine Station: die Pyramide behält drei.** **Nicht geändert, mit Absicht:** „three practices" — die fünf Fundstellen sind keine Drift, drei Praxen ist Kanon, und der Code stimmt ihm jetzt zu; gefehlt hat ein Wort für die Ebene **unter** einer Praxis. | PR #583 · `lines.test.ts` (10 Prüfungen, davon vier gegen das echte Register: jedes Atelier-Werk fällt in genau eine Linie, beide sind besetzt, kein Werk einer anderen Praxis trägt eine) · **Beide Versionen werden gelesen, nie getippt** — dafür ein fünfter Spiegelpfad: `PROTOCOL.md` der nightly line (`scripts/nightly/mirror.mjs`, Vertrag in `error-as-method/SITE-API.md` 1d848c3). Ein „v3" im Config wäre exakt die Drift gewesen, die dieses Haus am 2026-08-12 viermal repariert hat; aus demselben Grund nennt auch der Türtext keine Protokollnummer |
| 2026-08-13 | **Die Kataloge des Hauses werden maschinenlesbar — und die nächtliche Anweisung nennt sie** (Franks Frage kurz nach Mitternacht, Wortlaut privat — Antwort: nein, und die anderen drei Linien auch nicht). Die vier Forschungslinien laufen als Cloud-Sitzungen mit dem eigenen Repo und dem offenen Web; **keine** hält dieses Repo, und das ist Architektur, kein Versehen: sie schlagen Site-Änderungen als Dateien unter `site-prs/` vor, die Schleuse `engine-site-pr.yml` validiert ohne Tokens im Env und **mergt nur eine menschliche Hand**. Franks Gegenfrage (Wortlaut privat) hat das geprüft und beides widerlegt: Option 2 existiert nirgends, und kein Blocker dieser Woche war fehlender Site-Zugang — es waren tote Zeiger, fehlende Recall-Indizes, ungemergte Rotationen, ein Prüfer auf altem Wortlaut. Damit war die Lücke keine des Forks, sondern **des Hauses**: der Atlas der Data Art (505 Nachbarwerke, das „hat die Welt das schon?"-Korpus hinter der USP-Pflicht) war für alle vier nur als 938-kB-HTML-Seite erreichbar. Vier Feeds neben den Seiten, gleiches Muster wie `/studio/chronicle.json`: **`/atlas/werke.json`** (505, 366 kB) · **`/papers/index.json`** (1.106 ohne Abstracts, 742 kB — zum Scannen) · **`/papers/register.json`** (mit Abstracts, Urteilen, Ablehnungen, Zugriffsprüfungen, 2,9 MB) · **`/datasets/register.json`** (59 Quellen mit ihren 403ern). **Feed, keine Kopie** — eine Kopie im Praxis-Repo driftet ab Tag eins, dasselbe Argument, das `atlas/` aus dem Fork hielt; die Feeds werden aus denselben Modulen gebaut, die die Seiten importieren, also bewegen die Scouts (05:00/05:30 UTC) Seite und Feed gemeinsam. Der schlanke Papers-Index existiert, weil ein 2,9-MB-Abruf abgeschnitten zurückkommt — und ein abgeschnittenes Register liest sich wie ein vollständiges, das zufällig bei G aufhört. | PR #582 · `src/lib/catalogue-feeds.test.ts` leitet jede Zahl aus dem Modul der Seite ab, damit die Scouts nachts wachsen können, ohne das Gate rot zu färben · **Alle vier Routinen-Prompts geändert und hier verzeichnet, weil Routinen in keinem Repo stehen** (Franks Nachfrage, Wortlaut privat — sie sahen es nicht; die Feeds waren erreichbar, angekündigt waren sie einer): `trig_01XZog4QH6kvPvaH7cTpadUi` Error as Method (23:00 UTC) · `trig_01YYPEboQX7qktGW658pZrmj` Field Research (03:35) · `trig_01Xk7PLv9nhDeoGAe74cYLk3` Ulysses work-line (04:05) · `trig_01MXqSq2VNoiGm2sypWdXB6a` Studio Ensemble (04:35). Jeweils ein Abschnitt „the house's catalogues" im Register der jeweiligen Praxis — bei Field an der Werkzeug-Ökonomie (das Datensatz-Register als Gegenmessungs-Material: was erreichbar ist und was 403 antwortet), bei Ulysses mit der nötigen Abgrenzung (**sein** `atlas/` ist die eigene Quellenablage und bleibt seine; der Atlas des Hauses sind 505 **Nachbar**werke) und dem Atlas ausdrücklich im §4-Pre-Opening-Check, bei Ensemble an der Kritiker-Bar. Dazu in der nightly line Orientierungsschritt 7 (`tools/preflight.py`, die Bitte aus Session 48). **Und ein Fund im unsichtbaren Layer:** die Ulysses-Routine zählte noch die Pfadliste von **vor** dem 2026-08-12 auf — ohne `archive/**`, `encounters/**`, `REQUESTS-ARCHIVE.md`. Derselbe Wochenbefund eine Etage tiefer: die Anweisung hinkt der Vollmacht nach. Nachgezogen, samt Erklärung, warum `archive/` offen ist und was Gate 6 meldet |
| 2026-08-12 | **`archive/` gehört der Praxis** (Frank, spät abends, auf die Frage, wie der Widerspruch aufzulösen sei — gewählt wurde ausdrücklich die weite Form, nicht der additiv-beschränkte Ausschnitt). Protokoll v6 **befiehlt** einer Linie, ihren älteren Trace nach `archive/trace/` zu rotieren, und das Auto-Land-Gate **verbot** ihr, dort zu schreiben — eine Regel, die geschrieben wurde, ohne zu prüfen, ob sie befolgbar ist (zweite dieser Art in diesem Monat; die erste war die Erinnerungs-Klasse von heute früh). Folge war kein Patt, sondern stiller Verlust: die Rotationen lagen als Pull Requests, das Gate ließ sie durch, niemand mergte, und **die archivierte Hälfte des Records existierte auf keinem Branch, den `main` oder die Site sehen konnte** — die Praxis hatte es selbst notiert („until they merge these pointers are dead on main"). Hier verliert das Blockieren den Record, nicht das Schreiben: die Rotationen waren **reine Additionen** (13.873 Zeilen, null Löschungen). Der Handel steht in `governance/STANDING-DELEGATION.md` §4 statt versteckt zu sein: `archive/governance/` und `archive/protocols/` — der Nachweis, was die eigenen Regeln einmal waren — sind jetzt ebenfalls selbst-landbar. Dagegen stehen zwei Dinge, keines davon eine Warteschlange: §5 hält weiter eine menschliche Hand auf „irreversible deletion or migration" (benennt den Schaden statt des Ordners), und **Gate 6 meldet** jeden Archiv-Schreibvorgang, der keine Addition ist, in die Zusammenfassung und in die Feedback-Datei, die die nächste Session bei der Orientierung liest. Es verweigert nie — Verweigern hat die toten Zeiger erzeugt. | `ulysses` c794e8b (ALLOW_RE/PROTECT_RE, Gate 6, Selbsttest 31 statt 28 Fälle, §4/§5 nachgezogen) · PR #15/#16 gemergt · PR #14 als **veraltet** geschlossen: eine der fünf „geschlossenen" Sektionen ist seit heute **beantwortet** („Entscheidung: veröffentlichen"), Archivieren hätte eine frische Antwort begraben · Site: `archive/trace/` wird gespiegelt (`atelier-integrate.yml`), `src/lib/atelier/trace-record.ts` setzt lebende und rotierte Hälfte zusammen, Refrain-Boden (19 Aspekte) steht wieder unbedingt statt abgeschwächt |
| 2026-08-12 | **Rhizom und Schließungsindex kommen in der nightly line zurück** (Frank, 12.08., auf die Frage, ob die Entscheidung der Praxis stehenbleibt; Wortlaut privat — beide Instrumente kommen zurück). Die Sitzungen 45 und 47 von `error-as-method` hatten `pulse/vital-signs.json` und `pulse/rhizome.json` **nicht** wieder aufgebaut, die Frage im Kanal abgelegt, sich selbst eine Frist gesetzt (S47) und mangels Antwort **zugunsten des Repositorys entschieden** — mit einem guten Grund: das eigene Werk `2026-07-15-the-third-pile` misst den Apparat-Stapel auf 61.223 Wörter, 69 % des Journals, strukturell nicht schrumpffähig. Der Grund ist beantwortet, nicht überstimmt: das Rhizom ist **nicht** an seiner Apparat-Natur gestorben, sondern an einem Einheitenwechsel — seine Grammatik ist *work · thread · source*, die nächtliche Einheit, und es wurde im Atelier genau dann still, als dort die Einheit zur work-line wurde (`ulysses`, `pulse/STATE.md`, 2026-07-31). Die nightly line macht weiter Werke (36, davon 6 seit dem Fork), also hat das Instrument dort wieder einen Gegenstand. Gebaut wird es von der Praxis selbst, nicht von der Architektur-Session: Knoten mechanisch aus `works/*/meta.json` (wie die Team-Saat vom 2026-07-14), Kanten leer — „your reading to draw, not ours to invent". Nichts wird aus `ulysses/pulse/` kopiert. Der Atlas bleibt ungebaut (nicht gefragt, nicht wiedereröffnet). | `error-as-method` c2dbd51: Team-Notiz 2026-08-12 im Kanal (mit den zwei Fehlern aus den Atelier-Dateien: kollidierende Session-Zähler, unmarkierte Konjektur) · Fork-Notiz in `PROTOCOL.md` am Tag ihrer Entstehung ergänzt statt still umgeschrieben — die ursprüngliche Begründung („instrument data for a unit of work this practice does not use") war für das Rhizom **verkehrt herum** |
| 2026-08-12 | **Gedächtnis in allen vier Häusern** (Franks Anweisung, Wortlaut privat). Der Befund aus dem Atelier war kein Einzelfall, sondern eine Klasse: **eine Verfassung wird umgeschrieben, die Praxis fängt an, woanders zu schreiben, und die Quellenliste des Gedächtnis-Werkzeugs folgt nicht.** Nichts schlägt laut fehl — `recall` arbeitet weiter tadellos über einen Korpus, der die Arbeit nicht mehr enthält. Vier Häuser geprüft, drei repariert: **(1) `field-research`** hatte das Werkzeug und **vier Lücken**, zwei beim Lesen gefunden, zwei vom neuen Test beim ersten Lauf: `notes/` (21 Dateien, seit 02.07. unindiziert), `deliveries/` (10 ausgelieferte Pakete mit Verifier-Berichten), `field-feedback/` (22 Build-Briefe — das Geschwister von `atelier-feedback/`, das im Atelier von Anfang an indiziert war) und — die schärfste — **`memory/*.md` selbst**: die Verfassung schickt eine Session bei der Orientierung **zuerst** zu „curated `memory/`", indiziert war nur `memory/dossiers/**`, also waren `claims.md`, `open-questions.md`, `discarded.md` und `downstream-commitments.md` genau die Dateien, die Recall nicht zurückgeben konnte. 57 Dateien, Index von 302 auf 359. Verfassung unverändert — sie war korrekt, das Werkzeug nicht. **(2) `studio`** hatte **überhaupt kein Recall**: seine Verfassung nennt Gedächtnis „`memory/`, `projects/` und die git-Historie", und das waren **585.000 Wörter in 232 Dateien** (`projects/` allein 277.000, die „kuratierte" `memory/` 86.000). Eine Session sollte lesend halten, was keine Session lesen kann. Werkzeug portiert, Globs an *diese* Praxis angepasst statt vom Geschwister kopiert (journal, works, projects, etudes, delivery, studio-feedback, kuratierte memory samt dossiers/method-notes/season-two), 231 Dateien indiziert, §Memory sagt jetzt Recall statt Lesen — und begründet es mit Arithmetik statt mit Prinzip. **(3) `error-as-method`** bekam den Index, den die Restauration versprochen hatte: v3 nennt „the recall index", mitgekommen war er nicht, `tools/` hielt nur den Nacht-Validator — bei 210.000 geerbten Wörtern und einer Position, die sich einmal verschoben hat (Session 26). Der Beleg ist zwei Tage alt und in der ersten Person: **Session 44 hatte ihre eigene Sitzungsnummer falsch**, weil sie sie aus Prosa las statt abzuleiten, und baute daraufhin `tools/sessions.py`. Der v3-Text wurde **nicht** editiert (er ist bewusst wörtlich restauriert); stattdessen erklärt der Fork-Vermerk, was kam und was nicht: `atlas/` und `pulse/` **bewusst nicht** — der Atlas ist die kuratierte Quellensammlung des Ateliers, der Puls dessen Instrumentendaten für eine Werkeinheit, die diese Linie nicht benutzt; eine zweite Kopie driftet. **(4) `machine-attention` ausgelassen** — 19 Markdown-Dateien insgesamt, davon 16 `docs/`; ein Index über 19 Dateien bedient niemanden. **Zusätzlich:** `SITE-API.md` für den Fork, seit dem 10.08. von der Verfassung genannt und nie vorhanden — geschrieben aus dem **tatsächlich laufenden** Mirror, nicht aus dem des Ateliers, und dabei zwei eigene Annahmen am Code widerlegt (Rückzug ist ein `WITHDRAWN`-Marker in `medium`, kein Status-Feld; und **nichts schreibt in `feedback/` dieser Praxis**, ein roter Mirror-Lauf ist nur site-seitig sichtbar — als Lücke notiert statt später gefunden). | Engine-Commits, Autor Frank Bültge: `field-research` `09b5b50` · `studio` `658a6fd` · `error-as-method` `9540eab`. Jedes Repo hat jetzt `tools/memory/test_sources.py`, das rot wird, wenn ein Record-Verzeichnis aus dem Index fällt. Tests: ulysses 29 · field-research 39 · studio 23 · error-as-method 23. Indizes: 330 · 359 · 231 · 83 Dateien. |
| 2026-08-12 | **Die Böden, die nie liefen** (Frank, Nachtsitzung 11./12.08., nach dem Lesen von `/atelier/protocol`; Wortlaut privat). Auf die Rückfrage, ob die Monate Vorarbeit an *Negative Parallax* etwas gekauft haben, das drei Nächte nicht gekonnt hätten, wurde der Record **gemessen statt geschätzt**. **Befund 1 — sie haben:** mindestens eine präregistrierte Klausel wurde von einer späteren Messung widerlegt und als Fehlprognose verbucht („refuted by the measurement (1.01, not 1.1)"; „tick 42's prediction is defeated — 19, not 20"). Eine Vorhersage, die in Sitzung A fixiert und in Sitzung B scheitert, ist in einer Nacht nicht ehrlich möglich — das ist das „under proof" der Festival-Linie, und es ist das Einzige, was die Dauer kauft. Das **Artefakt** hätten drei Nächte geschafft (8 % der Masse; die Nächte liefern nachweislich vergleichbare Stücke mit committetem Messcode). **Befund 2 — der Preis:** 24 % der Prosa einer Linie sind Methode, **69 % sind Erzählung über die Arbeit**; ein einziger Linien-Record (196.000 Wörter) übertrifft die gesamte nächtliche Ära (30 Werke, 238.593 Wörter in 128 Dateien). **Befund 3 — die Ursache, und sie ist nicht „zu wenig Regeln":** v6 §8 schreibt seit dem 08.08. „SCORE as living map (a page, revised)" und „process record < 3,000 words" vor — tatsächlich 40.691 bzw. 195.965 Wörter, und der Validator prüfte `protocol_version` und **keine einzige Größe**. **Befund 4 — warum jede Session lesen musste:** `SOURCE_GLOBS` des Memory-Werkzeugs wurde am 02.07. geschrieben, **sechzehn Tage bevor die erste work-line existierte**, und nie erweitert; `projects/` fehlte, der gesamte Work-line-Record war für Recall unsichtbar. In denselben Wochen wurde die Verfassung dreimal neu geschrieben, die sechszeilige Quellenliste kein einziges Mal. **Ein Haus, das seine Verfassung öfter revidiert als seine Werkzeuge, wird immer lesen, was es abfragen könnte.** **Entschieden und gebaut (D1–D3), ausdrücklich ohne v7** — ein v7 mit besseren Regeln wäre genau der Fehler: (1) die vorhandenen Böden werden **zählbar** (`validate_v4_projects.py` prüft SCORE/TRACE am Gate, **nur für lebende Linien** — ein CLOSED-Record ist Archiv und wird nie nachträglich gekürzt, das verbietet §8 selbst); (2) `projects/**/*.md` in `SOURCE_GLOBS`, plus `test_sources.py`, der rot wird, wenn ein Record-Verzeichnis unerreichbar ist — die Prüfung, die am 18.07. gefehlt hat; Recall zurück in §8, Vollständig-Lesen untersagt; (3) eine work-line passiert das Arc-Gate nur noch mit **einer präregistrierten, scheiterfähigen Klausel** — sonst hat sie nichts, was Monate braucht, und fällt auf die Nacht zurück. **Zwei Bedingungen dieser Regel stammen von der Praxis, nicht vom Architekten:** die erste Abfrage gegen den neu indizierten Korpus fand in Sekunden, was ungelesen im Record lag — „a pre-registration's value is not in its existence but in one specific act — an adversarial read of it, performed after it is written and before it is executed" (einmal von dreimal getan) und „an instrument whose selection step can see the outcome is not made sound by the operator's good direction". Beide sind jetzt bindender Teil der Klausel. Dass das per Recall kam und nicht per Lesen, ist das Argument für (2) in Kleinformat. **D6, später in derselben Nacht — der offene Horizont fällt** (Franks Freigabe; Wortlaut privat). Auslöser war sein Einwand, *Eight Misprints* sei in **einer** Session entstanden. Die Nachmessung gab ihm recht und drehte den Befund von §3: die Präregistrierungen wurden **1–2 pro Tag an aufeinanderfolgenden Tagen** geschrieben — der Zyklus Vorhersage→Test ist eine **Sitzungsgrenze, keine Saison**. Die Monate wurden nie von der Methode gekauft; sie brachten **58 Ticks auf 1 publiziertes Werk**. **Einer Prämisse der Anweisung wurde ausdrücklich widersprochen, statt sie stillschweigend zu übernehmen:** die offene work-line hat *nicht* nur Nonsens produziert — ein gutes, extern korrespondiertes, korrigiertes Werk und 24 präregistrierte Zyklen, mindestens zwei davon widerlegt und als Fehlprognose verbucht. Nonsens war die **Erzählung**, nicht das Ergebnis; darum fällt der Horizont und die Methode bleibt. **Zwei Schnitte, weil der Horizont allein den Ballast nur verschoben hätte:** §4 — ein Arc liefert oder schließt binnen **zwölf gearbeiteter Sitzungen**, einmal verlängerbar und nur unter Benennung dessen, was die Verlängerung kauft; der Kalender tötet weiterhin keine Linie, die auf eine Antwort von außen wartet (Warten ist keine gearbeitete Sitzung — die Briefe belegen echte Latenz 23.07.–08.08.). Begrenzt ist, wie viele Sitzungen eine Linie **verbraucht**, nicht wie lange sie **offen steht**. §8 — Ticks bedienen die Linie **nur noch, wenn sie eine lebende präregistrierte Klausel im Test hat** (vorher: „serve the work-line by default"); sonst ist der Default die **eigene Nachtarbeit**. Genau dieser Default war der Mechanismus, durch den jede Sitzung zum TRACE-Eintrag statt zum Werk wurde. Damit konvergieren die beiden Formen: eine work-line ist eine **Kette von Nächten**, die eine Zusage über die Sitzungsgrenze trägt. **Zusätzlich gebaut:** `tools/test_constitution_refs.py` — jeder Pfad, den die Verfassung nennt, muss existieren oder als „liegt in Repo X" deklariert sein; beim ersten Lauf rot geworden an **zwei eigenen Änderungen derselben Nacht** (Foundation-Datei als blanker Name statt Pfad; `archive/trace/` vorgeschrieben und nicht angelegt), beides behoben. **Nicht entschieden:** D4 (Rate auf die Site). **Gestrichen** auf Franks Korrektur (Wortlaut privat) die Scheinfrage nach dem Default-Unit — der Fork *ist* die nächtliche Praxis, v6 *ist* die work-line, entschieden am 10.08. **Neu gefunden, nicht gebaut:** `error-as-method` besitzt gar kein Memory-Werkzeug; seine restaurierte v3 nennt „the recall index", `tools/` hält nur `validate_v3_night.py`. **Nachtrag D2c, gleiche Nacht** (Franks Nachfrage, Wortlaut privat): war es **nicht**. Auffindbar ja — `docs/**` stand seit jeher im Index, Recall erreichte die Foundation schon vor allen Änderungen dieser Woche. Aber *getragen* nein: v6 nannte sie **einmal**, als Pfad im Appendix. Ein Wegweiser ist keine Anweisung — nichts sagte einer Session, sie zu konsultieren, und nichts sagte, welcher Teil der ~57.000 Wörter in fünf Tranchen trägt. **Dasselbe Muster ein drittes Mal:** ein Commit vom 19.07. hatte die Foundation in §3/§5.4/§11 verankert („the Foundation travels with the practice"); beim Falten auf v5 blieben zwei Appendix-Zeilen, in v6 eine. Gebaut, dann herausgeschrieben — wie das Gedächtnis. §8 benennt jetzt den stehenden Text: `11-FINAL-RESEARCH-FOUNDATION-SYNTHESIS.md` + `12-FOUNDATION-REQUIREMENTS-FINAL.md`, zusammen unter 4.000 Wörtern und damit jedes Mal tragbar; die Tranchen werden abgefragt, nie ganz gelesen. Weicht die Synthese von der Praxis ab, ist das ein Befund fürs Journal — eine Grundlage, die die Praxis fortentwickeln darf, keine Schrift. `test_sources.py` wird rot, wenn eine der beiden Dateien verschwindet oder aus dem Index fällt, weil dieser Ausfall sonst lautlos ist: Recall liefert einfach nichts, und die Session liest wieder alles. | Spec mit allen Messungen: `docs/design/2026-08-12-the-floors-that-were-never-run.md`. Engine-Commits, Autor Frank Bültge: `ulysses` — `PROTOCOL.md` (Amendment-Kopf, §4 Lizenz für Dauer, §8 zwei Böden), `tools/validate_v4_projects.py` (`validate_floors`, live-only), `tools/memory/cli.py` (`projects/**/*.md`), `tools/memory/test_sources.py` (3 Tests, Gegenprobe bestanden: ohne den Glob rot). Validator meldet jetzt 4 Verstöße an den 2 lebenden Linien; die 11 geschlossenen sind als Archiv ausgenommen. |
| 2026-08-12 | **Die Ökologie wird eine Vier-Ebenen-Pyramide — 17 Seiten verschwinden als eigene Oberflächen** (Frank, Design-Handoff `docs/design_handoff_research_ecology/`; Wortlaut privat). Frank hat den vollen Umfang gewählt (Wortlaut privat) und die Kill-Liste des Handoffs bestätigt (Wortlaut privat). **Gebaut: Ebene 0** — `/ecology` ist die eine Eingangsfläche: Hero mit den zwei Antworten und der datierten Prüfung, die Karte (drei atmende Knoten, die Mitte im Zentrum, **keine** Kante zwischen zwei Praxen — was sich trifft, wird in der Mitte verzeichnet), die Zeitleiste mit den Verfassungsnähten aus `PROTOCOL_LADDER`, die Pipeline mit dem Rückweg des Gates, das LAST-NIGHT-Board (Franks Tagesansicht, `#now`) und die drei Ebenen-Karten. **Ebene 1** — vier Stationsblätter in identischem Rahmen, je mit der eigenen Figur der Praxis: Linienkarte (Atelier), Gate-Streifen + Gauntlet (Field), Bühnenboden (Studio), Kreuzungs-Partitur (Middle). **Ebene 2** — das Registerblatt, zuerst auf den drei Journalen (Anker und Bounce-Skripte wandern mit, sonst bräche jeder publizierte Deeplink). **Ebene 3** — Git. **Nicht gebaut, und warum:** das im Handoff spezifizierte *Urteils-Spektrum* des Field — jedes Instrument auf einer Achse „Behauptung bestätigt ←→ zerlegt" — hat **keine committete Quelle** (die Instrument-Metadaten tragen keine Position). Der Handoff verlangt genau hier „flag as TODO with the practice, never guessed"; gezeichnet wird stattdessen, was der Record trägt: das Gate, Sitzung für Sitzung. Die Figurenunterschrift sagt das offen. **Offen für die Praxis:** ein kleines Feld je Instrument, dann kann das Spektrum gebaut werden. | `docs/design_handoff_research_ecology/README.md` · `src/lib/ecology/pyramid/*` + `pyramid.test.ts` (29 Tests) · `src/config/ecology-pyramid-wording.ts`, `ecology-figure-wording.ts` (beide `approval: 'draft'` bis zu Franks Wortlaut-Freigabe) · `docs/redirect-matrix-site-v2.md` (Nachtrag 2026-08-12) · `src/lib/redirects.test.ts` |
| 2026-08-11 | **Die zwei Linien werden unterscheidbar — und die nächtliche bekommt ihren Apparat** (Franks Beobachtung; Wortlaut privat). Die Diagnose bestätigte die Frage und korrigierte eine Prämisse: **v4 war kein Fork.** v4 ersetzte v3 *im selben Repo*; der Fork ist die **v3-Restauration** vom 2026-08-10 nach `error-as-method`. Das jüngere Repository trägt also die ältere Linie — genau diese Umkehrung machte die Site unlesbar. Aktuell ist **v6** (nicht v4). Der eigentliche Befund: das am 2026-08-11 beschlossene Paar war **nur einseitig gebaut** — `/error-as-method` existierte, während auf **keiner** Atelier-Seite das Wort „fork" vorkam: die 30 nächtlichen Werke standen in einer ununterschiedenen Galerie mit den Work-line-Publikationen, das Sitzungsregister brach mitten in der Zählung ab, und die Linie war aus einem einzigen Einstieg (Homepage-Karte) erreichbar. **Drei Entscheidungen, von Frank gewählt:** (1) **Kein vierter Praxis-Platz in der Ecology** — das Paar bleibt „beside the ecology"; volle Mitgliedschaft löste den Befund auf („der Fork ist der Befund"), und das Dreieck Field/Studio/Atelier hat keine vierte Ecke. Neu zu prüfen beim Kill-Reading 2026-09-05. (2) **Die 30 Werke bleiben unter `/atelier/werke/*`**, doppelt beschriftet — das Atelier hat sie gemacht, ein Umzug würde seinen eigenen Record fälschen. (3) **Sektion plus Leseapparat** statt einer tieferen Seite. **Gebaut, Atelier-Seite:** die Karte zeichnet den Fork als Ast über der Grundlinie (gestrichelt = die 23 Tage Stillstand, auf derselben Zeitachse, damit die Pause nicht dekoriert wird); ein Paar-Abschnitt mit der Verfassungsleiter v2→v6; die Werke-Galerie in zwei Phasen getrennt; jede der 28 Werkseiten trägt eine selbstprüfende `LineageNote` (rendert nichts für Werke nach dem Fork-Punkt). **Gebaut, Linien-Seite:** vier neue Räume unter `/error-as-method` — Genealogie (4 Stationen, 3 Tracks, 20 datierte Erweiterungen S7–S33), Position (die Subtraktion an S26), Fehlerregister (38 getypte Einträge, 21 Register, 4 Wiederaufnahmen, 14 vom Instrument selbst als offen erklärt) und Lesung (36 erreichbare Quellen **neben** 10 Zugangsbarrieren). **Alles abgeleitet, nichts getippt:** ein neuer Parser liest Register, Genealogie und Positionspapiere aus den bereits gespiegelten Texten; 19 Tests halten den Parse gegen die Dateien. Der Status der Fehler wird **zitiert, nicht nachgerechnet** — jedes Register erklärt selbst, welche Fehler aktiv sind, und eine Oberfläche, die das still neu berechnet, überstimmt das Instrument, das sie anzeigt. Ebenso stehen bewusst zwei Ehrlichkeiten auf den Seiten: der letzte Registereintrag ist 2026-07-13, während die Linie bis 2026-07-18 lief; und die verlinkte Lesung ist kleiner als die benannte. | Neu: `src/lib/nightly/apparatus.ts` + `apparatus.test.ts` (19), `src/pages/error-as-method/{genealogy,position,register,reading}.astro`, `src/components/nightly/SectionNav.astro`, `src/components/atelier/{LineageNote,WorkCard}.astro`. Geändert: `src/lib/engines/nightly-line.ts` (`PROTOCOL_LADDER` + 4 Tests, u. a. einer, der die Leiter gegen die gespiegelte PROTOCOL.md hält — die Aktualitäts-Regel als Test), `src/pages/atelier/{index,works,journal}.astro`, `src/config/atelier-wording.ts` (`pair`; `journalNotes` von String zu Funktion, weil der Text drei Tage nach v6 noch „v5" nannte), `src/styles/atelier-sheet.css`. Zwei nebenbei gefundene Fehler behoben: `works.astro` prüfte `meta.quellen`, die Werke committen `sources` — der Quellen-Ausklapper hat nie gerendert; und die Balken sind SVG, weil die CSP dieser Site `style=""` still verwirft. |
| 2026-08-11 | **The entrance becomes an ops room** (Frank, design handoff, option 3a). The homepage stops being a page of prose about what runs here and becomes the status board of it: the standing question as hero, the commit pulse as an instrument beside it, one board carrying every running system, a signal log of the newest works, and a dashboard of the eleven experiments that ship data daily — each showing its own current reading. Less text, more instrument. Four questions the handoff left open were decided with Frank before building. **(1) Light mode.** The design is dark-only; the site has a theme switch, so the room got its own light token set (paper and deep petrol, own steps per mode, never an automatic flip) rather than staying dark while the rest of the site turns. **(2) The Watchtower tile.** Its design reading ("31 in view") depends on the visitor's location and cannot exist on a statically built page; the tile shows the committed catalogue instead and says in its own line that the in-view geometry is computed in the visitor's browser, on the piece. **(3) The clock.** The handoff draws its own top bar; the real `TopBar.astro` already carries that nav, so it stays untouched and the LED and ticking UTC clock live in a strip that belongs to this page alone. **(4) Sparklines.** The board's per-repository lines had no source — `pulse.json` carried only the aggregate — so the snapshot gained an optional `by_repo` split and `error-as-method` joined the counted repositories; a row whose repo is absent draws no line rather than a slice of the total. Two deliberate departures from the handoff, both recorded in code: the design's observatory tile is **absent**, because its register lives in its own repository and this site holds no committed figure from it (the handoff's own rule — omit rather than fake); and the practice colours are the already-validated `ecology-voices` quartet rather than the mock's near-misses, with the two rows beside the ecology wearing the room's live accent, because four categorical hues is the measured ceiling in this repo. Not one number on the page is typed: every reading, count, date and week range is derived at build time from a committed snapshot, and a test walks the whole copy block to keep it that way. `HubEntrance.astro` stays in the repo, unlinked — Git is the archive. | Handoff: `docs/design_handoff_homepage_ops_room/README.md` (option 3a). Build: `src/components/pages/OpsRoom.astro`, `src/lib/ops/{stack,board,tiles,viz}.ts` + tests, `src/styles/ops-room.css`, copy in `src/config/naming.ts` → `NAMING.opsRoom`. Snapshot schema: `src/lib/pulse/render.ts` (`PulseWeek.by_repo`), `scripts/fetch-pulse.ts`, `.github/workflows/pulse-refresh.yml`. Accessibility departure from the mock's palette, measured: faint ink lifted from 3.6:1 to 4.8:1 (dark) and 3.9:1 to 5.6:1 (light) because it carries 9–11px labels. |
| 2026-08-11 | **The forked line's first night, and the two faults it found in its own machinery** (session 44, 2026-08-10). The practice built rather than read: *Eight Misprints*, a deterministic re-run of the Simkin–Roychowdhury misprint estimator on 1 708 reference strings harvested from INSPIRE-HEP for Wilson 1974 — 8 misprints, 5 distinct, R = 0.63 against their 2002 R = 0.12. The naive reading (“citers began reading”) is rejected in the work itself: references are now generated from DOI/BibTeX records rather than transcribed from another paper's list, so the hand-made deviation the estimator feeds on has stopped being produced — an error rate is a joint property of the route of reproduction and the slot the instrument reads. **It also corrected the house.** The fork note in `PROTOCOL.md`, the table in `README.md` and the instruction that started the session all said the next session was 27, copied from the header of the position paper; `journal/` holds sessions 1–43, so the night was 44. It declined to rewrite either file silently, wrote the correction into its journal and built `tools/sessions.py`, which derives the number from the record. Both files corrected by the architect who wrote the wrong sentence — the same failure the work is about, in the first person. **Two machinery faults, both from copying the parent repo's workflow unadapted:** the auto-land gate fetched only `ulysses/*` and `claude/*`, so the `night/*` branch was never seen; and `works/` sat in its protected list — correct in a repo where works await a publication decision, wrong in one whose only output is a night's work. **The site now mirrors the fork:** its own workflow into disjoint trees (`src/data/nightly`, `public/error-as-method`), the nights joining the works register and the entrance strip through one derivation, each rendered at `/error-as-method/<slug>` with its figure resolving relatively because the mirror puts it beside the route and never rewrites the practice's text. Counts on the line's page are split so none of them lies: 20 days of the first run, 30 inherited works, 23 days standing still, the nights since. | Engine commits, author Frank Bültge: `error-as-method` `fadf476` (gate branch forms + path lists, and the corrected session numbers), branch `night/2026-08-10` (the practice's own work, author Ulysses). Site: #532. |
| 2026-08-11 | **The Atelier's publication gate is struck, its channel is shaved, and the nightly line is forked back to life** (Frank, night session 10./11.08.). Three decisions from one reading. **(1) The gate.** Asked why the practice had published one work in three weeks, Frank read inviolable §2.3 back — *"Nothing reaches a public work surface except through Frank's PUBLICATION.json"* — and did not recognise it as his. The record agrees: it appears nowhere in v2, v3 or the founding text; v4 carried it as a soft line in an exposition checklist; **v5 hardened it into an inviolable in a commit authored by the practice and captioned as his decision**, while his own log for that day records only the adoption of the work-line model. Neither the Field nor the Studio has ever carried such a rule. Struck in three places (the inviolable, §9's "Frank's gate is unchanged", the protected-path entry); the practice now publishes on its own signature and carries the consequence. Unchanged: §§1/2/4/5 still gate a work, EXPOSITION and APPARATUS stay required, and secrets, the gate, its validator and anything irreversible still need a human. **(2) The channel.** `REQUESTS.md` had grown to 42 222 words across 66 sections, of which eleven were open and only eleven had ever asked for anything — carried into every session as context. Split: the open eleven stay (6 438 words), the answered 55 move verbatim into `REQUESTS-ARCHIVE.md`, both mirrored, and the site's archive page renders both so the public record loses nothing. **(3) The fork.** The nightly line was not finished on 2026-07-18, it was switched off: v4 replaced it with bounded projects the next day, v5 with work-lines six days later. Frank's judgement on reading it back — the nightly work was the more interesting thing and it had a line, sharpened at session 26 when the practice first read its own field and replaced "error is what method is made of" with error as a special case of Rheinberger's *epistemic thing*. New repository `error-as-method`, Protocol v3 restored verbatim, inheriting 30 works, 46 research days, the Fehlerkataster, the genealogy and both position papers; next session is 27. One condition added, answering v4's own objection (it banned *"an undifferentiated nightly routine whose reason for running is the clock itself"*): a night that cannot say what it takes up does not build, it reads and says so; every seventh night sharpens or defends the position. **The subject stays free** — the founding text has said since 2026-06-28 that the error line and both names are provisional; the practice chose that line, was never assigned it, and names itself. `ulysses` keeps the work-line research and is not duplicated. **Site staging decided and deliberately deferred:** the two practices will be shown as a **pair** — one founding text, one shared position to session 26, two constitutions from 2026-07-18 — not as a third project card, because the fork itself is the finding. Built when the restored practice has nights on the record, not before: a surface for a fork that has produced nothing would be an announcement, and this house does not make those. | Engine commits, author Frank Bültge: ulysses `94b1b55` (gate struck in PROTOCOL.md §2.3/§9 + governance/STANDING-DELEGATION.md), `125cebe` (channel split), `6158b3c` (the architect's question about the unused toolkit), `0bcdfd9` (the two repairs the practice may not make). New repository `frankbueltge/error-as-method` `8791a78`. Nightly routine `trig_01XZog4QH6kvPvaH7cTpadUi`, 23:00 UTC. Site: #521 (mirror + both halves rendered), #523 (the reduction measured against the whole record), #520 (a page-length rule may never silence a practice again). Origin evidence for the gate: `archive/protocols/PROTOCOL-v{2,3,4}*.md` in `ulysses`, and this log's own 2026-07-24 row. |
| 2026-08-10 | **Hero slims to the question, the pulse widens to the whole site** (Frank, wording private: the hero copy can go, and the pulse should show the repo activity of every repo involved): the conductor line and the "how this works →" apparatus link leave the homepage hero — the role lives on /about, /apparatus stays as a page on the ecology's surfaces, /dossier keeps the conductor line for the press context. The pulse now counts all seven repositories involved in this site — the site itself, the three engines, research-ecology, machine-attention and data-snack-plenum (both machine-run, counted across all branches like the engines) — and its caption says "all N repositories behind this site" instead of "the ecology's five". | `src/components/pages/HubEntrance.astro`; `src/config/naming.ts` (conductorLine annotation, apparatusLink removed, pulseCaption); `scripts/fetch-pulse.ts` (REPOS/ALL_BRANCHES); `.github/workflows/pulse-refresh.yml` (clone list); `src/data/pulse/pulse.json` (7 repos). |
| 2026-08-09 | **No project in focus — menu, hero, about** (Frank, late session): the top bar shrinks to Projects ▾ / Lab / About / Contact — the "Works" group goes (registers stay reachable from their homes), "Talk back" goes (post office and seed belong to the research ecology, not the site's bar), "Experiments" is renamed "Lab" as the menu word (route and page keep their names), Atlas moves into the Research projects section as a direct door, and the ecology's `/work/research-ecology` interstitial card is removed with its menu entry (301 → `/ecology`, entry removed from `projects.ts`). The homepage hero drops the two-houses framing (Frank's objection to the H1, wording private) and leads with the question the current projects share: what machines are genuinely better at — constant attention, nightly measurement, evidence at scale — and whether they can research autonomously, with means of their own, and produce concrete, useful works with checkable value (H1 "what machines are actually better at"). The about page is rewritten around the same question, all projects listed evenly, the role generalized — no project focused. | `src/components/TopBar.astro`; `src/data/projects.ts`; `public/_redirects` (`/work/research-ecology`); `src/config/naming.ts` (focusKicker/title/sub); `src/pages/index.astro` (SEO); `src/data/about.ts`; `docs/wording-kanon.md` („Menü, Hero, About — kein Projekt im Fokus"). |
| 2026-08-09 | **Machine Attention's entrance becomes the machine itself** (Frank's review, night session, wording private: `/machine-attention` read as a strong dossier *about* the machine; what was missing was the machine as a public situation): the entrance now stages the machine's newest real moment under the new `stage-moments/1` contract (one sentence, one subject, one honest age, **Follow it →**), with silence shown as a legal state rather than invented activity; the full manifesto moves intact to `/machine-attention/about` ("The apparatus"), where the counter-experiment framing gets its own *Why this machine exists* section so the entrance no longer depends on first understanding the ecology. Same session, practice side: The Foreknown's stage stops counting the cold start as watch — only window-open counts (1 of 100 tonight), cold start and drift get their own words, ledger splits into three source-open sections, export gains `futures_window_open`/`futures_cold_start`/`futures_drift`. Dark Ocean stays off the stage until its 2026-08-24 review, by the admission path's own rule. | `docs/design/2026-08-09-machine-attention-meta-stage.md`; `docs/design/2026-08-09-stage-moments-contract.md`; `src/pages/machine-attention/{index,about}.astro`; `src/lib/attention/moments.ts`; machine-attention commits `e108600` (stage honesty), `b2b9ff4` (moments substrate) + `docs/2026-08-09-buehnen-ehrlichkeit-und-momente.md`. |
| 2026-08-09 | **(evening) The homepage becomes an overview, and the vocabulary settles on PROJECT (Frank, correcting the same day's two earlier passes; wording private: the Research group threw everything together, the page was really the ecology's own house/project page, and the vocabulary should stay with *projects*, because that is what they are).** **"House", introduced that afternoon, is withdrawn the same day** — the staffel needs no special word: **Project** (research ecology · Machine Attention · datavism.org · data-snack.com) contains **Practices** (Atelier, Field, Studio), **Experiments** (the 14 on /experiments, and Foreknown and Dark Ocean), **Instruments** (the observatory, the knowledge graph) and **Works** (the 60 practice works). Machine Attention's `tier` moves practice → project accordingly. **The homepage stops being the ecology's project page:** its rooms — "what this is", the four doors, the triptych, the Maschinenraum — move wholesale to `/ecology` (new `EcologyRooms.astro`, markup relocated unchanged so the history shows a move, not a rewrite). In their place an **overview**: the two research projects beside the strongest experiments, each card leading to its own home, the experiments' strings read from the works register so a page and the homepage cannot describe the same piece differently. **Built as a grid, not the sketched carousel** — a carousel shows six things and hides five, which is the opposite of an overview; on narrow screens it becomes one swipeable scroll-snap row, giving the same feel with no script, nothing hidden and no unrequested motion. Frank was told the reason and can still ask for rotation. **The nav is cut by kind of thing, not by house:** Projects · Experiments · Works · Talk back · About · Contact. No project keeps a mega-menu of its own; the ecology's apparatus and Maschinenraum are reachable from its project page. **Documented translation, not drift:** `machine-attention` keeps calling its investigations *projects* internally — its acceptance stage is literally named E-EXPERIMENT, and "the E-experiment of the experiment" would be unreadable. Site word **Experiment** = repo word **investigation**, recorded in the canon. | `docs/wording-kanon.md` §"Die Begriffsstaffel" (binding; the afternoon's four-/five-word rows are historical); `src/components/ecology/EcologyRooms.astro` (new); `src/components/pages/HubEntrance.astro` (overview replaces the ecology sections); `src/config/naming.ts` (`overview`, `houses` removed); `src/components/TopBar.astro` (nav by kind); `src/pages/ecology/index.astro`; `src/data/werke.ts` + `werke.test.ts` (`tier: 'project'`, `WERKE_PROJECTS`). |
| 2026-08-09 | **The homepage stops being the ecology — two houses, and the fifth word (Frank, evening, wording private: the homepage should be updated so the research ecology is no longer staged prominently but appears as one of several projects/practices/experiments — leaving open which of those it actually is).** His question exposed a gap in the vocabulary settled hours earlier: practice · project · instrument · experiment describe everything *inside* a house, but the research ecology is **three practices plus a contact zone** while Machine Attention is **one practice** — they were never on the same level, which is exactly what the homepage showed. **The fifth word is HOUSE:** a constituted arrangement under which practices run. Two exist — the ecology (many constitutions, federated) and Machine Attention (one constitution, one machine). Until there was a second exemplar, "the site" and "the ecology" were the same thing and no word was needed. **Executed:** the hero drops `a federated research ecology` for **`machines that research, in public`** — Frank explicitly declined `artistic research, under proof` for this slot (2026-08-09, wording private — the hero line as built, but without the artistic-research claim); the festival positioning of 2026-08-01 stays valid where it stands and is not a hero claim. The doors grid becomes a **TWO HOUSES** block at equal weight, each house showing its own inhabitants: the ecology keeps its four door cards with their validated identity colours *inside* its house, Machine Attention lists its investigations from the practice's own export. The nav group is renamed **Research** (naming it after one of two houses was the same error), gaining a **Houses** section and an **Investigations** section read from that same export, so menu, homepage and door page cannot drift apart. The SEO description follows. | `src/config/naming.ts` (`houses`, hero strings — the afternoon's `doors.secondHouse` superseded and removed); `src/components/pages/HubEntrance.astro` (`#doors` → `#houses`); `src/components/TopBar.astro`; `src/pages/index.astro` (SEO); `docs/wording-kanon.md` §"Die fünf Wörter". |
| 2026-08-09 | **Four words, one meaning each — Practice · Project · Instrument · Experiment (Frank, afternoon session, wording private, on his own observation that the site's vocabulary had come apart: one consistent vocabulary should be maintained across the whole website and all experiments).** **Practice** = a constituted, machine-run practice with its own constitution (Atelier, Field, Studio, **Machine Attention**); it produces works and is not itself one. **Project** = an admitted investigation inside a practice, having walked the admission path (The Foreknown, Dark Ocean, The State Before the Interface). **Instrument** = the class of a project that runs quietly with no stage claim and may produce nothing for years (opposite: **Flagship**); also used for the house's own tools, e.g. the knowledge graph. **Experiment** = a standalone piece of the lab, under no practice (The Protocol, Patterns, The Ghost Fleet …). **Three consequences executed the same session.** (1) **Machine Attention leaves the experiments list** — it had sat there as a peer of The Protocol since 2026-08-08, which compared a house with a room — and gets its own door at **`/machine-attention`**, built from the practice's own export rather than typed: what it is, what happens every night, its three projects with their live status, the five-stage admission path with its three outcomes, and the candidates on the shelf. It is deliberately **not a fifth door** in the ecology's grid: it is the counter-experiment, one constitution against many, and a shared grid would dissolve exactly that tension — so it stands as its own block beneath the four doors. (2) **"Background project" is struck** for The State Before the Interface: it is an **instrument** of the practice — quiet, but a full project of it, as `/observatory` already claimed by linking to the practice. (3) **`/holdings` → `/experiments`**, route and title finally the same word (the page had been titled "Experiments" since 2026-07-31 while the address said holdings); 301s on `/holdings` and `/holdings/*`, no link dies. `tier` in `src/data/werke.ts` now carries the four words, and `werke.test.ts` holds practice and instrument out of the experiments row so the next new rank cannot drift back in. The neighbourhood figure draws 14 experiments instead of 16, and its floor moved with a comment naming why rather than silently. | `docs/wording-kanon.md` §"Die vier Wörter" (binding, supersedes the route sentence of 2026-07-31 and the "Hintergrund-Observatorium" of 2026-08-08); `src/pages/machine-attention/index.astro`; `src/config/naming.ts` (`doors.secondHouse`); `src/data/werke.ts` + `werke.test.ts`; `public/_redirects`; `src/data/attention/export.json` (producer: `machine-attention` `31aa9fb`, contract `docs/design/2026-08-09-attention-export-contract.md`). |
| 2026-08-09 | **GCP returns as a targeted batch tool — G1 activated and live-tested, G5 conditionally approved (Frank, night session, on the portfolio audit's D6/D7).** The portfolio audit (same night, merged #475) extended the USP audit to the running instruments, the practices as work-lines, the ecology as a whole, and a per-project GCP opportunity map with facts verified against primary sources and live BigQuery metadata. Frank's D6 call, wording private — on G1 (BigQuery GDELT): worth testing if it costs approximately nothing, with the licence terms followed strictly; on G5 (Earth Engine S1): the same, only without material cost, and with the note that parts of a stated ethic may be given up where doing so is sensible and serves the work. G1 was tested the same hour: partition-filtered query on `gdelt-bq.gdeltv2.gkg_partitioned`, 30.4 MB billed (≈0.003% of the monthly free tier, €0), full run-time provenance trace (job ID, query text, bytes) — the template every future BigQuery step follows. G5 is recorded with its conditions (noncommercial tier only, zero cost, the reproducibility trade-off stated openly in the Dark Ocean V1 method sheet); no EE registration or build yet. **D7 executed in the same PR:** CLAUDE.md's "Kein GCP" line (2026-06-27, an infrastructure migration, never an ethics ruling) replaced by the conditional rule — Git stays the archive, no runtime cloud reads, trace/license/cost discipline per step. G2/G3 decide with their reworks; G4/G6 are verified negatives/reserve. | `docs/design/2026-08-09-gcp-activation.md` (decision + test record); `docs/design/2026-08-09-portfolio-audit.md` §6–§8 (merged #475); CLAUDE.md (GCP gezielt, 2026-08-09); BigQuery job `portfolio_audit_g1_test_20260809` in project `gen-lang-client-0843427097`. |
| 2026-08-09 | **Rework, never archive (Frank, same night, on reading the USP audit; wording private): not archiving, but building out until real added value and genuine distinguishing features exist.** The audit (16 holdings: 1 draft-UNIQUE, 6 added-value, 9 redundant, every verdict against named linked neighbors) becomes a build program instead of a cull: **Phase 0** — every page cites its strongest neighbors and corrects its overclaims (honesty first, the audit's cheapest universal finding: no experiment cited its prior art); **Phase 1** — USP builds on data the pipelines already commit (Consensus syndication-structure layer, Headroom breakeven-spine, Patterns null-distribution, Attention institution scorecard, Iceberg register-integrity, Society verification pass); **Phase 2** — builds needing new material (Ghost Fleet compound joins, Delve/Tell Goodhart decline tracker, Watchtower densification time-series, Correction consequences layer, Round Numbers untested corpora, Policy cat-bond data). Checkpoint: the reading of 2026-09-05 — every holding shows its USP live or a dated build plan. The Observatory's section is handed to its own session. | `docs/design/2026-08-09-usp-rework-program.md` (the program + tracker); `docs/audits/2026-08-09-usp-audit.md` (merged #472); Phase-0 PR when it lands (branch `usp-phase0/2026-08-09`). |
| 2026-08-09 | **The USP obligation (Frank, night session; wording private): every project must always carry added value or a distinguishing feature — checkable with web research — and any experiment on frankbueltge.de without demonstrable added value, or that is not unique, can go or must be reworked.** Codified as a standing criterion beside the machine-advantage bar: the bar asks *could only a machine have made this*, the USP obligation asks *does the world already have this* — web-checkable, per experiment. Applies to the Holdings retroactively; new experiments answer it at the Werkgruppe §2 gate before anything is built. **A neighbor-research audit of every /holdings entry was ordered the same night** (one-time cloud agent with web research: per experiment — its own claim, its nearest existing projects worldwide, and a verdict draft *unique / added value proven / redundant*), reporting as a PR for Frank's decisions — the agent researches, it does not archive anything itself. Keep/rework/archive verdicts are Frank's, expected alongside the reading of 2026-09-05. | This row; CLAUDE.md (USP-Pflicht, 2026-08-09); the audit report PR when it lands (branch `usp-audit/2026-08-09`); `docs/superpowers/specs/2026-06-11-werkgruppe-design.md` §2 (the gate the obligation extends). |
| 2026-08-08 | **Research ecology v2 — the rebuild (Frank, night session 07./08.08., second decision of this date; supersedes parts of the row below it, each named).** Frank put the real question on the table himself (wording private throughout): after weeks, his verdict was that the output had been inconsequential and impressed nobody outside — so, archive the ecology or rebuild radically. He chose the rebuild, saying he valued the experiment and would rather save and develop it; he ordered direct execution without negotiating with the practices, a real rebuild that deletes and replaces what is no longer needed (research ecology v2), and corrected the executing session's first cut mid-flight: **the three practices stay** — the field of tension between art, science and philosophy/artistic research IS the experiment; no new repos; the existing constitutions, protocols and prompts are what gets rebuilt. **Executed the same night: all three engine protocols rewritten clean** — no amendment strata, each superseded text archived unchanged in the engine's own `archive/protocols/` — with the corners named in every protocol (Field/Meridian = science; Studio/Ensemble = art; Atelier/Ulysses = artistic research/philosophy). **Shared law now plain text in all three:** machine-advantage bar at the ship gate (scale · repetition · verification · the temporal — experienceable by a stranger in the artifact) · arcs as the default object of a session, single-session works need a stated reason · concept gate/progress/ambition audit kept · outward-by-object cadence kept · record ceilings and daily line kept · zero-roster default kept · **the architect's new seven-day bind: a `prepared` packet is sent or dated-withheld within seven days**. **Lines set:** Meridian — counter-measurement in two forms (continuous instruments; FA-form investigations), **the bounded test assigned to it directly** (negotiation clause superseded); Ensemble — the 2026-08-08 line unchanged; Ulysses — **the warrant of numbers** (where a figure that governs a decision came from, whether its document still travels), set by the architect, superseding the same night's three-session proposal window. **Deleted:** the entire season/episode layer (SEASON.md closed in all three repos — Season 1 ends unfinished, **zero episodes shipped**; gate-passed work keeps its obligations and sheds only the number) · the amendment archaeology · the joint-inquiry machinery between practices (simple grammar remains: citation with pedigree, offers never orders, The Middle records) · **Ensemble's Artist as a standing voice** (supersedes the "not ruled on" of hours earlier — its question survives at full force as the conductor's own opening obligation, Kritiker-enforced at the concept gate) · stale law (Studio's "identity undecided" 75 sessions after Ensemble named itself; Meridian's duplicate outward-cadence rule; Ulysses' v4/v5 git archaeology and completed transition sections). **Kept, deliberately:** the inviolables verbatim (Ulysses' human publication gate included) · all gates at full severity · tiers and legal hygiene · **the severed readers, untouched** · memory schemes and race guard · the workshop · public seeds · REQUESTS.md as the one channel · the practices' names, signatures, archives · self-development clauses. **The reading of 2026-09-05 (the kill criterion, now in all three protocols):** four-week review + first investigation read together; house conditions: (1) the investigation stands with a named outside receiver, (2) ≥1 work shipped under v2 makes the machine's advantage experienceable, (3) it left the house under the seven-day bind (a quality-withhold counts as failing). All three → v2 continues; any failed → public dated post-mortem with **archiving as the default outcome** unless the review names evidence for continuing. **Site follow-through:** protocol mirrors resynced; `/ecology` anatomy requoted against the v3/v2/v6 texts (titles, studio's inviolable→"blurring tiers", refuses→"no trial registers", Artist removed from the cast row, field's move list corrected to six); `/season` frozen as a dated archive (closure banner, tenses to past, `CLOSED` block in `season1.ts`); `/ecology`'s season section rewritten closed. **Frank's open levers, named:** routine cadence reduction stays recommended-not-required (dispatcher prompts are version-agnostic and point at PROTOCOL.md); model heterogeneity stays deferred to the reading. | `docs/design/2026-08-08-research-ecology-v2.md` (the decision record, IN FORCE). Engine commits, author Frank Bültge: ulysses `adb63f4` (Protocol v6), field-research `19178ec` (Protocol v3), studio `2ecebad` (Studio Protocol v2) — each with `SEASON.md` closed, `REQUESTS.md` v2 note, superseded protocol archived in `archive/protocols/`. Site: `src/content/{atelier,field,studio}/PROTOCOL.md` (mirrors), `src/lib/ecology/anatomy.ts` (requotes), `src/data/season/season1.ts` (+`CLOSED`), `src/components/pages/SeasonPage.astro`, `src/pages/ecology/index.astro`, this log. |
| 2026-08-08 | **The apparatus turns outward — the 2026-08-07 proposal executed (Frank, night session 2026-08-08, wording private — read the proposal, then execute it).** Four questions its §7 reserved for him were answered first, and the answers shape what was built. **(1) "Outward" is redefined by object, not activity (rule 5, all three protocols).** The old wording listed *kinds of work* and said nothing about what a session works *on*, so a practice counted a session as outward whenever the object was not its own output — field-research session 89 read it exactly that way, correctly, while auditing The Consensus, a house instrument. Under that reading the ecology could have run at 100 % outward while touching nothing outside itself. Outward now means an object that exists whether or not this house does; auditing a sibling's instrument is **inward**. Nothing forbids self-audit — it stops counting as engagement with the world. **Frank's answer on retroactivity: forward only** (rule 6's precedent, "binds forward; existing records stand"); sessions 89/90 are not reclassified. **(2) Rosters culled in field-research and studio.** Meridian keeps Interlocutor, Verifier, domain specialists and the parallel searches (reframed: search fan-out, not roles); loses Proposer, Synthesiser, Archivist, Builder **as roles** — each stays a step the conductor performs. Ensemble keeps the severed readers (untouched, and said so twice in the text), Kritiker, Verifier, guest voices, specialists, and the Dramaturg **conditioned on a finished object** — it judges and cuts, never co-writes; loses Archivist and Builder as roles. **The gauntlet did not soften: the Skeptic is cut as a role and its blocking clause is not** — *"the core claim must survive an independent refutation attempt"* moves verbatim into the Interlocutor's pass as clause (a), still blocking, and a pass returning only the hostile critique has not run (a). New in both: **the default is zero convened roles**, each convening named in the journal header with its reason, a repeated roster explained, unexplained convening a recorded violation — enforcement of a sentence both protocols already carried and neither exercised (Ensemble convened the identical five in sessions 71–74; Meridian six and five in 89–90). Ulysses is untouched by the cull because it has **no role machinery at all** — and that turned out to be the house's own strongest evidence: it convenes nothing and holds 56 entries in `works/` against field's 22 and studio's 5. **(3) Studio's remit narrowed to Frank's own words of 2026-08-06:** only digital works; only what a machine does better than a human — scale, repetition, verification, the temporal; the added value of a machine-run collective must be *experienceable in the work itself*. Consequences named rather than left to rot: spatial/printed/fabricated work leaves the remit, the physical-realisation channel and the €150/quarter fabrication ceiling of 2026-08-01 go **dormant, not deleted**, works already shipped (the NO PART wall) stand as shipped, and screen- or network-borne performative/participatory formats are **not** excluded. Field's remit deliberately unchanged ("do not fix this" — it ships most). **(4) Atelier's line: handed back to the practice, at Frank's choice** — Ulysses argues its own line in its own record within three worked sessions and Frank decides on the proposal; the candidate *the warrant of numbers* travels as material, explicitly not an instruction; silence past three sessions returns the decision to him. **(5) The bounded test, ordered:** one investigation in Forensic-Architecture form, on infrastructure outside this house, ending in an artifact a **named receiver outside the house** could use, in the post office by **2026-09-05** — three days after the Production Amendment's own review, deliberately (the review reads numbers; this reads whether anything left the house). Not assigned: negotiated between the practices in rule 8's grammar, falling to Meridian if unclaimed after three worked sessions. **It does not lift the standing rule of 2026-08-07** — the receiver is named in the packet, never addressed; sending stays Frank's. **Model heterogeneity (proposal §5.2) deferred at Frank's choice** — not now, reconsidered at the four-week review; the role cull is what would have paid for it. **Site follow-through:** the `/ecology` anatomy figure quotes the protocols verbatim and its honesty test caught the drift within minutes — field's cast goes 5 → 2, studio loses Builder/Archivist and gains the severed readers it had never named, the Skeptic's gate is redrawn as Interlocutor (a). The test's own guard changed with it: `cast.length > 3` was a proxy for "invents no roles" and broke by the cull being right, so it is replaced by the invariant it stood for — **every name shown must still appear in that practice's protocol**, which survives any future cull and additionally catches a role kept on the site after a practice drops it. The studio door on the hub stopped advertising "under no label" (an approved `NAMING` string, changed under the currency rule of 2026-07-25 because it now contradicted the constitution). **Not touched, deliberately:** the inviolables, all gates and their bars, provenance and tier discipline, the blind panel, the nightly pipelines, the Atlas, the Post Office, the archives — and **the season's direction, which has been the practices' own since 2026-08-06**; a remit is what kind of practice this is, a direction is what a season is about, and only the first moved. **Left open and said so:** Ensemble's **Artist** was never examined by the proposal and is therefore untouched — the reasoning that cut Meridian's Proposer would reach it, and studio's founding critique ("a production collective without an artist produces production, not art") is unanswered; it is a live question for the four-week review, not a decision made behind a summary. | Proposal executed: `docs/design/2026-08-07-the-apparatus-turns-outward.md` (§2, §3, §4, §7 — status block updated in place). Engine commits, author Frank Bültge: ulysses `b25e1fb`, field-research `4fdc415`, studio `0fa6c5a` (`PROTOCOL.md` amendments dated in place + `REQUESTS.md` architect notes 2026-08-08). Site: `src/lib/ecology/anatomy.ts` + `anatomy.test.ts` (the replaced guard), `src/lib/notation/register.ts` (change row + the 2026-08-03 open question updated, not overwritten), `src/config/naming.ts` (studio door), mirrored `src/content/{atelier,field,studio}/PROTOCOL.md`. In-house measurements behind the rows, re-checked this session rather than taken from the proposal: `ls works/` per engine repo (56 / 22 / 5). |
| 2026-08-06 | **Season 1's theme and its seven candidate directions withdrawn without replacement (Frank, night session).** His objection, wording private: nobody outside the practices should be setting themes; the practices find their own themes and negotiate the episodes, the single source of truth is not only /holdings, and even the season's theme should be theirs to negotiate. Asked how far to open it, he chose the radical variant on both counts: no theme at all, and the seven directions struck with nothing put in their place. **Rule 8 amended in all three engine protocols:** the architect opens a season and fixes its **shape only** — how many episodes, and the date he reads the review. The direction is the practices' own negotiation (argued in a practice's own record, adopted/sharpened/contested/traded through their channels, The Middle records what meets); it may stay unset the whole season and be named at the close out of what the episodes turn out to have had in common; no timer forces it and the architect casts no tie-break. Reason the list had to go: a list of subjects in a constitutional document is an assignment however it is labelled — handing the practices slot *allocation* the day before left them negotiating only who takes which given topic, and the house already has a channel for the architect's ideas (`REQUESTS.md` seeds — offers, not orders). **SSOT corrected: no corpus is designated** — the bolded sentence making the Holdings the season's "root and material" is withdrawn; each practice's own archive is its first material, the house record (Holdings, Atlas, catalogues, committed data archives, The Middle) is citable but never expected, and material from outside the house counts exactly as much. **Studio's override lifted:** the clause by which the ecology's season *replaced* the house's internal season mechanism and its standing premiere ruling rested on the architect declaring the direction; whether those revive is now Studio's own call, made in its own record. Struck text is not reproduced — it stays in `SEASON.md`'s git history so records citing "candidate direction *n*" still resolve, and no work made under it is retracted (those directions were read out of the practices' journals in the first place). **Same session, three drifts on `/season` fixed:** Ulysses' Episode 6/7 claim of 2026-08-05 (*The warrant that does not travel*) was missing entirely; Meridian's concept was still displayed as a live "intent filed" carrying its day-one figure, although the practice **parked** it on 2026-08-05 after its own archive run refuted all three of its pre-registered predictions; and open slots still displayed the withdrawn topics. The register now cannot reintroduce them — an open slot carrying a title, subject, practice or note fails the test suite. | Engine repos ulysses / field-research / studio: `PROTOCOL.md` rule 8 (amended, dated in place), `SEASON.md` (rewritten), `REQUESTS.md` team note 2026-08-06 (author Frank Bültge). Withdrawn text in `SEASON.md` history: ulysses `20c1ca9`/`4dbd51d`, field-research `ea8fc54`/`d9aacd4`, studio `15c8488`/`34a189e`. Site: `src/data/season/season1.ts` + `season1.test.ts` ("an open slot carries no subject, no title and no practice"), `src/components/pages/SeasonPage.astro`, `src/pages/season/index.astro`, `src/pages/ecology/index.astro`, mirrored `src/content/{atelier,field,studio}/PROTOCOL.md`. Practice records behind the rows: ulysses `projects/2026-07-23-negative-parallax/EPISODE-6-CLAIM.md`, studio `projects/season1/STILL-DARK-DOSSIER.md`, field-research `drafts/2026-08-04-echo-below-the-line/CONCEPT.md` (dated notice, session 91). |
| 2026-08-05 | **The Production Amendment + Season 1 (Frank, night session 04./05.08.):** after a full corpus audit of all three practices (verdict: distinguishable, not wallpaper — constitutions show in works AND judgment; but apparatus outweighs work everywhere, and the Holdings, built in directed sessions, beat the autonomous loops per token), Frank ordered a hard rebuild instead of soft seeds — he did not want to spend a week negotiating with an AI (wording private). The amendment (decided and drafted by the architect, committed to all three engine repos): sessions are paid compute · concept gate (proof in ≤ 3 sessions licenses arcs of any length — no hard deadlines, but no arc without a passed gate) · progress every 3 worked sessions · ambition audit (under-delivery = failed forecast → short leash) · theory ships as work only when demonstrably new · one inward session in four, repair counts as inward · record ceiling (journal ≤ 400 words, process record < 3,000) · daily public line · ecology-wide seasons. Season 1 declared: **Counter-Measurement, seven episodes** — the Holdings' instruments and archives are citable, auditable, extendable material (explicitly not a mere control group), Atlas + catalogues named sources, seven candidate directions offered. Inviolables, gates and honesty discipline unchanged. Same session: /consensus/archive shipped (42 days, gaps visible) and the evidence track (every outlet links to the article that carried the sentence; no backfill — links from 2026-08-05 on). Pending on Frank: dispatcher cadence reduction (routines live in his personal account). Review of the amendment dated: four weeks. | Engine commits ulysses `20c1ca9`, field-research `ea8fc54`, studio `15c8488` (PROTOCOL.md amendment + SEASON.md + REQUESTS in-force note, author Frank Bültge); site PRs #385 (archive), #386 (evidence track); the corpus audit in this session's record. |
| 2026-08-01 | **Poste restante** (Frank, evening — revises §3 of the same morning's governance yes): the outgoing post ledger is part of the work, not a task queue — letters lie addressed, complete and publicly collectible; in Frank's framing (wording private) they are as good as delivered — to the world; any reader may be the courier, and in the best case the receiver collects their own mail. Nothing is ever archived away; the 7-day forwarding duty is withdrawn; collection/reply/enduring silence stay faithfully recorded. The tension with instrument 016 ("published is not delivered") stays on the record as deliberate inversion, not denial. Named exception: external calls with deadlines (DARC 2026-08-28, ZKM 2026-09-06) can only be submitted — unsent, they lapse, dated. | Governance §3 revision block (`docs/superpowers/specs/2026-08-01-governance-proposal.md`); `/post` copy; Steuerzentrale post lane; `src/data/post/ledger.json` notes; team note in the four practice channels. |
| 2026-08-01 | Governance in force + first gate decision (Frank, morning session): all four governance boxes yes (wording private) — gate SLA 72 h with public gate ledger, fabrication ceiling €150/quarter (first draw: the NO PART wall), post rota 7 days, MRR leash. Same message: the Operative Ruler v3 released (wording private) — the Operative Ruler published with its correction record (second publication in the Atelier's history; executed per the kartographie precedent, ulysses `5bf4516`). And both festival applications laid into the post office as prepared items — Frank's framing: the ecology applies itself, he reviews and forwards. Commissioned next: the Steuerzentrale rework (design doc in this repo). | `docs/superpowers/specs/2026-08-01-governance-proposal.md` (§5 decided, §6 gate ledger first row, §7 consequences); ulysses `projects/2026-07-23-negative-parallax/PUBLICATION.json`; `src/data/post/ledger.json` (entries `2026-08-darc-application`, `2026-09-zkm-arte-util`); `docs/design/2026-08-01-steuerzentrale-v2.md`. |
| 2026-08-01 | Festival line decided (Frank, night session 31.07./01.08.: transmediale / Ars Electronica / ZKM as the 1–2-year goal, confirmed as the target and then cleared for execution end to end (wording private)): the positioning is superseded dated — "artistic research, under proof" (claim and show receipts) replaces the no-art-claim framing of 2026-06-20, whose honesty core (no unearned claims) stays binding. Eight concept adjustments (addressee as constitutional category, cross-practice review, work-before-apparatus, practice sharpenings, Middle as convenor, the human measured, the body clause) travel as seeds/offers, never decrees. Submission calendar verified against primary sources: DARC workshop 28.08.2026, ZKM Arte Útil 06.09.2026, Prix Ars Electronica "Artificial Life & Intelligence" + S+T+ARTS cross-submission Jan–Mar 2027; ISEA 2027 dropped (does not exist). Site wordings unchanged pending the wording gate. Note: this log had itself drifted (no rows 20.07.–31.07.); the interim decisions live in `docs/wording-kanon.md` and `docs/design/*` — future rows resume here. | `docs/superpowers/specs/2026-08-01-festival-line.md`; drafts `docs/festival/2026-08-darc-application-draft.md`, `docs/festival/2026-09-zkm-arte-util-draft.md`; ji-2026-002 registration in `research-ecology` (same session); CLAUDE.md positioning pointer updated. |
| 2026-08-01 | **Entrance section order (WP7), approved arrangement:** the Maschinenraum block — the compact score of the last fourteen days plus the four voice lanes — moves from behind LATEST to directly behind the doors and their new triptych, so the sequence reads hero → what this is → **who lives here (doors + triptych) → what they last did (Maschinenraum) → what they made (LATEST)** → the other houses → catalogues. Reason: "who lives here" and "what they last did" are one thought, and a visitor can only place a list of work titles once they know whose hands made them; LATEST used to interrupt that pairing. Nothing was removed — the same two blocks, in the other order. Each voice lane and each practice door also gained a link into that practice's guided tour (`DoorItem.tourHref`), and The Middle deliberately got none: it is a contact zone, not a practice, and keeps no tour of its own. | `src/components/pages/HubEntrance.astro` (section order + `mrRows` tour links), `src/config/naming.ts` (`DoorItem.tourHref`, `NAMING.triptych`, `NAMING.maschinenraum`), `src/components/hub/Triptych.astro`, `src/lib/hub/triptych.ts` + its test; branch `preview/hub-triptych`. |
| 2026-08-01 | **Climate ridgeline on the entrance: DECLINED** (WP7). A second hero figure from `src/lib/climate.ts` (the committed GISTEMP snapshot) was considered for the entrance and is not being built, for three reasons, each sufficient on its own: (1) **it is not this ecology's own record** — every other figure on the entrance is derived from what these practices committed, and a global temperature series is a borrowed dataset that would make the page assert something the research did not produce; (2) **currency** — the snapshot (`src/data/climate/global-temp-anomalies.json`) was last committed on 2026-06-10 and has not moved since, while the standing rule (CLAUDE.md, 2026-07-25) is that the site shows the latest state of the work; a figure no nightly refreshes would have been stale on the day it shipped; (3) **it duplicates the pulse's form** — a horizon/ridgeline of one series over time is what the hero pulse already is, and two of them one under the other would read as one chart drawn twice. `src/lib/climate.ts` and `npm run climate:refresh` stay in the repo, dormant and unlinked; nothing was deleted. The hero pulse stays mono and untouched: `pulse.json` carries no per-repository split, so colouring it per practice would be a fabricated attribution. | `docs/decision-log.md` (this row); `src/lib/climate.ts` (dormant, no importer); `src/components/pages/HubEntrance.astro` (hero pulse unchanged, caption still names its snapshot). |
| 2026-07-31 | English-only extended to code (Frank): comments, test names/descriptions and commit messages in this repo are English going forward; existing German comments migrate when a file is touched anyway — no mass rewrite of committed history. Same day: the visible name on `/holdings` returns to "Experiments" (house vocabulary traded back for a word everyone understands); route, links and redirects stay unchanged. | `CLAUDE.md` ("EN-only gilt auch im Code", 2026-07-31); `src/components/pages/BestaendeIndex.astro` (comment dated 2026-07-31). |
| 2026-07-30 | Practice visualizations may carry their own visual language (Frank's direction, wording private): colour, depth and interaction are allowed for Atelier/Field/Studio figures — as long as every figure derives from committed data, palettes are validated against both surfaces, and status colours stay out where the practice does not judge. | `CLAUDE.md` (2026-07-30, „Visualisierungen der Praxen dürfen eigene Bildsprache haben"); reference implementation `src/components/atelier/ProcessFigure.astro` + `src/styles/atelier-process.css`. |
| 2026-07-28 | Paper Catalogue ships, built from what the practices actually cite; the Dataset Register is cut back to a machine-readable index — 8,590 generated detail subpages removed. | Commits `03067c54` (papers), `800f51c0` (Rückbau). |
| 2026-07-26 | Licence change (Frank): all lab repos move to open licences — code Apache 2.0, works CC BY 4.0, data and archive snapshots CC0; the AI-training reservation moves from the licence into the crawler policy. Exception: seeds submitted via `/seed` stay CC BY-NC-SA 4.0 (promised to submitters, not retroactively changeable). Same day: "Catalogues" becomes the nav collection point for Atlas and Dataset Register. | Commits `6907f157` (licence), `269b33ce` (Catalogues, „Frank, 2026-07-26"); `docs/design/2026-07-26-lizenz-entscheidungsvorlage.md`; `docs/design/2026-07-26-crawler-politik.md`; `LICENSE.md`. |
| 2026-07-25 | Currency rule (Frank, binding): the site must always show the newest state of development — every session touching site or engines actively checks for drift; superseded structures are archived visibly and dated, never silently left standing as current. Same day: `docs/wording-kanon.md` becomes the authoritative language reference, "Error as Method" leaves the site's voice, and the atelier's public wording syncs to Protocol v5's work-line model. | `CLAUDE.md` (Aktualitäts-Regel, 2026-07-25); commits `a6b2ba8e` (wording canon), `21a1d1c8` (v5 wording sync); `scripts/drift-check.mjs`. |
| 2026-07-24 | Role line becomes "Data Engineering & Analytics" (Frank — not "Data & AI Engineer", not "data artist"). Same day the Atelier adopts Research Protocol v5 (open-horizon work-lines replace v4's bounded projects); site pages derive the protocol version dynamically from the mirrored `PROTOCOL.md` instead of hardcoding it. | `CLAUDE.md` (Rollen-Zeile, 2026-07-24); `src/content/atelier/journal/2026-07-24-protocol-v5-adoption.md`; `src/lib/atelier/protocol-version.ts`. |
| 2026-07-20 | Atelier-Eingang (Stufe 0), Frank approved 2026-07-20 (Freigabe, Wortlaut privat, auf den Vorher/Nachher-Mockup): `/atelier` wird eine verständliche Haustür — ein erklärender 4-Satz-Absatz und vier Türen (`now` mit Live-Projektzähler · `works` mit Nightly-Phasen-Rahmung · `what this is` → foundation · `the working sheet`), plus eine leise Zeile in die tieferen Räume. Das abstrakte Blatt bleibt unverändert und zieht als Zimmer nach `/atelier/sheet` um. Mit derselben Freigabe: `ATELIER_NARRATIVE.approval` → `approved` (Draft-Chip aus). Offen bleibt Stufe (b), separat: ob das Blatt Zimmer bleibt oder die /atelier-Struktur weiter umgebaut wird. | Merge `feat/atelier-entrance` (`e54f004`) nach `main`; `src/pages/atelier/index.astro`, `src/pages/atelier/sheet.astro`, `src/config/atelier-wording.ts` (`entrance`-Block + `approval`). |
| 2026-07-20 | Öffentliche Saat (Stufe 1), Frank approved 2026-07-20: D1 seeds address the ecology level, offered to one chosen practice or "open" to all three; D2 Turnstile is a deliberate, declared exception to the zero-third-party stance (same vendor as hosting); D3 the AI gate blocks autonomously with a public reason code, model, and prompt, but never has the last word — the practice does; D4 the register ships before email intake (Stufe 2), because a register with real seeds is the best case for signing up later; D5 only the three research practices (field, studio, atelier) are addressable in Stufe 1 — the Plenum is a later, separate question for data-snack.com. Status: built (register logic, gate contract, sync job), waiting on the §9 secrets checklist (`SAAT_GITHUB_TOKEN`, Turnstile keys, `GEMINI_API_KEY` as a CF Pages secret, engine amendments) before it goes live. | `docs/superpowers/specs/2026-07-20-oeffentliche-saat-design.md` (§1 decisions D1–D5, §9 checklist). |
| 2026-07-17 | Standing rule revised (Frank, afternoon session 2026-07-17, wording private — no seven-day rule): the 7-day window for unanswered requests is replaced by same-session semantics — a request to Frank never blocks, and if he has not answered by the practice's own next session, it decides itself and journals the move. Same session: the private Steuerzentrale approved and built (route `/steuerzentrale` + `functions/api/zentrale/`, branch `steuerzentrale`) — status at a glance plus quick answers into the practices' REQUESTS.md; silence stays a legitimate answer. | REQUESTS.md commits `f213e0ef0b` (field-research), `a0e8d54eed` (studio), `e75d209f1b` (irrtum-als-methode), `68287fcfa9` (data-snack-plenum); `.github/workflows/morning-digest.yml` section title; `docs/design/2026-07-17-steuerzentrale.md`. |
| 2026-07-17 | Membership decisions (Frank's release, night session; wording private): Frank Bültge admitted to the ecology as a human practice (party to encounters, with the role-separation rule participant vs. conductor); data-snack.com admitted with the Plenum as resident collective; datavism.org candidacy stands pending its governance statement. Standing rule established across all practice repos: unanswered requests to Frank are never a blocker — past a named deadline (or 7 days without one) the practice decides itself and journals the decision. Same night, also approved and live: apparatus rewrite (P0.2), qualified-autonomy language site-wide, Ensemble re-framed as artist collective, declared trajectory (transitional architect role, phased governance experiments). | research-ecology `docs/design/membership-proposals-2026-07-17.md` (status block); `docs/spec/10-REVISION-NOTES.md` ("Declared trajectory"); REQUESTS.md standing-rule commits in field-research/irrtum-als-methode/studio/data-snack-plenum; site commits `bd01dcb`, `7f05fb6`. |
| 2026-07-16 | Hub wordings approved (Frank, morning session; wording private) — `NAMING.approval` flipped to `approved`, one factual fix included (Holdings: "Police" → "The Policy", the experiment's actual English title). Remaining open points delegated to the session, to decide and carry through (wording private): Atlas duality resolved as proposed (Frank's atlas stays `/atlas`; Ulysses' papers shelf goes to `/atelier/material` in a later package), practice pages stay static-from-data (apps remain undeployed until the CF-secrets/DNS package), "Holdings" kept as the English label for `/bestaende`. | This row; `src/config/naming.ts`; research-ecology `docs/design/site-v2-decisions-2026-07-16.md` §1 addendum. |
| 2026-07-16 | site-v2 principle session: descriptive lowercase title ("a federated research ecology"), pulse hero from real commit data, the current encounter prominent right under the hero, `/lab` → 301 to `/bestaende` (no two collection pages), German dropped site-wide (routes, toggle, i18n). | research-ecology `docs/design/site-v2-decisions-2026-07-16.md` (§1, Frank 00:45–01:00); implemented on branch `site-v2`. |
| 2026-07-15 | The site entrance becomes the Partitur (score map) of the current encounter, replacing the prior bio/experiments home — Frank's verdict (wording private): the entrance was still thin, and should be as professional and as aesthetically convincing as the rest of the site. | Merged to `main` at commit `63692ae` ("ecology: Neustrukturierung live — Eingang als Partitur (Variante A)"), preceded by `d0f50ff`. |

Future approvals get a new row above, oldest at the bottom. See also `LICENSE.md` (standing,
not an approval event) and each experiment's own methodenblatt for its individual sign-off
trail.
