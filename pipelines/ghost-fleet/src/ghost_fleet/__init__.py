"""The Ghost Fleet — nightly Gegenmessung pipeline.

Counts deliberate AIS disabling (vessels "going dark") from the GFW Events API,
foregrounding gaps in protected/contested waters. Git is the archive.
"""
PIPELINE_VERSION = "0.1.0"
SCHEMA_VERSION = "1"
